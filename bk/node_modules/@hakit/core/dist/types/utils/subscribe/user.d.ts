import { Connection } from 'home-assistant-js-websocket';
export interface MFAModule {
    id: string;
    name: string;
    enabled: boolean;
}
export interface CurrentUser {
    id: string;
    is_owner: boolean;
    is_admin: boolean;
    name: string;
    credentials: Credential[];
    mfa_modules: MFAModule[];
}
export interface AuthUser {
    id: string;
    name: string;
    is_active: boolean;
    is_owner: boolean;
    credentials: {
        type: string;
    }[];
    group_ids: string[];
    system_generated?: boolean;
    username?: string;
}
export interface FetchUsersArgs {
    includeSystemGenerated?: boolean;
    includeInactiveUsers?: boolean;
}
/**
 * Subscribe to (fetch once) the list of users. Returns an unsubscribe no-op.
 * If you need periodic refresh, wrap this in a polling interval externally.
 */
export declare const subscribeUsers: (conn: Connection, onChange: (users: AuthUser[]) => void, args?: FetchUsersArgs) => (() => void);
export declare const userCollection: (conn: Connection) => import('home-assistant-js-websocket').Collection<CurrentUser>;
export declare const subscribeUser: (conn: Connection, onChange: (user: CurrentUser) => void) => import('home-assistant-js-websocket').UnsubscribeFunc;
//# sourceMappingURL=user.d.ts.map