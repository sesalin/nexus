import { HassConfig } from 'home-assistant-js-websocket';
import { FrontendLocaleData } from './subscribe/frontend_data';
export declare const LOCAL_TIME_ZONE: string;
export declare const resolveTimeZone: (option: TimeZone, serverTimeZone: string) => string;
export declare enum TimeFormat {
    language = "language",
    system = "system",
    am_pm = "12",
    twenty_four = "24"
}
export declare enum TimeZone {
    local = "local",
    server = "server"
}
export declare const shouldUseAmPm: (locale: FrontendLocaleData) => boolean;
export declare const UNIT_TO_MILLISECOND_CONVERT: {
    ms: number;
    s: number;
    min: number;
    h: number;
    d: number;
};
export declare const formatDuration: (duration: string, units: keyof typeof UNIT_TO_MILLISECOND_CONVERT) => string;
export declare function millisecondsToDuration(d: number): string | null;
export declare const isTimestamp: (input: string) => boolean;
export declare const isDate: (input: string, allowCharsAfterDate?: boolean) => boolean;
export declare function checkValidDate(date?: Date): boolean;
/** Format a date (long month) e.g. "August 9, 2021" */
export declare const formatDate: (dateObj: Date, config: HassConfig, locale: FrontendLocaleData) => string;
/** Format a time respecting 12/24 preference e.g. "8:23 AM" or "08:23" */
export declare const formatTime: (dateObj: Date, config: HassConfig, locale: FrontendLocaleData) => string;
/** Format a time forcing 24h cycle (HH:MM) without an AM/PM suffix, ignoring user 12h preference but honoring timezone preference. */
export declare const formatTimeWithoutAmPm: (dateObj: Date, config: HassConfig, locale: FrontendLocaleData) => string;
/** Hour numeric only respecting 12/24 preference (no suffix). e.g. "5" or "17" */
export declare const formatHour: (dateObj: Date, config: HassConfig, locale: FrontendLocaleData) => string;
/** Minute numeric only (zero-padded, e.g. "07") */
export declare const formatMinute: (dateObj: Date, config: HassConfig, locale: FrontendLocaleData) => string;
/** Seconds numeric only (zero-padded, e.g. "09") */
export declare const formatSeconds: (dateObj: Date, config: HassConfig, locale: FrontendLocaleData) => string;
/** Long date & time without seconds e.g. "August 9, 2021, 8:23 AM" */
export declare const formatDateTime: (dateObj: Date, config: HassConfig, locale: FrontendLocaleData) => string;
/** Long date & time with seconds e.g. "August 9, 2021, 8:23:15 AM" */
export declare const formatDateTimeWithSeconds: (dateObj: Date, locale: FrontendLocaleData, config: HassConfig) => string;
/** Short date/time without year if same year e.g. "Aug 9, 8:23 AM" */
export declare const formatShortDateTime: (dateObj: Date, locale: FrontendLocaleData, config: HassConfig) => string;
/** Short date/time with year e.g. "Aug 9, 2021, 8:23 AM" */
export declare const formatShortDateTimeWithYear: (dateObj: Date, locale: FrontendLocaleData, config: HassConfig) => string;
/** Conditionally include year (current year omitted) */
export declare const formatShortDateTimeWithConditionalYear: (dateObj: Date, locale: FrontendLocaleData, config: HassConfig) => string;
/** Browser default locale (useful for fallback) */
export declare const formatDateTimeWithBrowserDefaults: (dateObj: Date) => string;
/** Numeric date/time variant (delegates to existing helpers if present) */
export declare const formatDateTimeNumeric: (dateObj: Date, locale: FrontendLocaleData, config: HassConfig) => string;
/** Weekday + Month + Day (e.g. "Tuesday, August 10") */
export declare const formatDateWeekdayDay: (dateObj: Date, locale: FrontendLocaleData, config: HassConfig) => string;
/** Short date (e.g. "Aug 10, 2021") */
export declare const formatDateShort: (dateObj: Date, locale: FrontendLocaleData, config: HassConfig) => string;
/** Very short date (e.g. "Aug 10") */
export declare const formatDateVeryShort: (dateObj: Date, locale: FrontendLocaleData, config: HassConfig) => string;
/** Month + Year (e.g. "August 2021") */
export declare const formatDateMonthYear: (dateObj: Date, locale: FrontendLocaleData, config: HassConfig) => string;
/** Month name (e.g. "August") */
export declare const formatDateMonth: (dateObj: Date, locale: FrontendLocaleData, config: HassConfig) => string;
/** Year (e.g. "2021") */
export declare const formatDateYear: (dateObj: Date, locale: FrontendLocaleData, config: HassConfig) => string;
/** Weekday long (e.g. "Monday") */
export declare const formatDateWeekday: (dateObj: Date, locale: FrontendLocaleData, config: HassConfig) => string;
/** Weekday short (e.g. "Mon") */
export declare const formatDateWeekdayShort: (dateObj: Date, locale: FrontendLocaleData, config: HassConfig) => string;
/** Numeric date honoring user ordering preference (e.g. DMY -> 10/08/2021) */
export declare const formatDateNumeric: (dateObj: Date, locale: FrontendLocaleData, config: HassConfig) => string;
/**
 * Return a localized day period (AM/PM or locale equivalent) for the given date.
 * This intentionally ignores the user's 24h preference so callers can always access the suffix if desired.
 * Only timezone and language are considered (not time_format). Falls back to simple "AM"/"PM" on failure.
 */
export declare const formatAmPmSuffix: (dateObj: Date, locale: FrontendLocaleData, config: HassConfig) => string;
//# sourceMappingURL=date.d.ts.map