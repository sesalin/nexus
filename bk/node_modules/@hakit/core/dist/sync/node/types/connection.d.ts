import { Auth, HassEntity, HassServices } from 'home-assistant-js-websocket';
import { default as WebSocket } from 'ws';
interface HaWebSocket extends WebSocket {
    haVersion: string;
}
export declare function createSocket(auth: Auth): Promise<HaWebSocket>;
export declare function connect(url: string, token: string): Promise<{
    services: HassServices;
    states: HassEntity[];
}>;
export {};
