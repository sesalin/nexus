import { Options } from 'prettier';
export interface TypeSyncOptions {
    url: string;
    token: string;
    outDir?: string;
    filename?: string;
    /** this is used internally to generate the default supported services, you will most definitely need to leave this as true */
    custom?: boolean;
    domainWhitelist?: string[];
    domainBlacklist?: string[];
    serviceWhitelist?: string[];
    serviceBlacklist?: string[];
    prettier?: {
        options: Options;
        disable: boolean;
    };
}
export declare function typeSync({ url, token, outDir: _outDir, filename, domainWhitelist, domainBlacklist, serviceWhitelist, serviceBlacklist, custom, prettier, }: TypeSyncOptions): Promise<void>;
