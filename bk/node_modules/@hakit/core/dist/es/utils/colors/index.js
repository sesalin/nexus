import "lodash";
import "../../../index-1tDtVGr2.js";
import "react";
import "../../hooks/useLocale/locales/index.js";
import "home-assistant-js-websocket";
import "@iconify/react";
import "use-debounce";
import "../date.js";
import { ON as o } from "../../data/entity.js";
const c = (t) => {
  if (t && t.attributes && t.attributes.brightness && !t.entity_id.startsWith("plant")) {
    const r = t.attributes.brightness, s = t.state === o ? Math.max(Math.round((t.attributes.brightness ?? 0) * 100 / 255), 1) : 0, i = Number(Number((r + 245) / 5).toFixed(0));
    return {
      raw: s,
      css: `brightness(${i}%)`
    };
  }
  return {
    css: "brightness(100%)",
    raw: 100
  };
};
export {
  c as stateColorBrightness
};
//# sourceMappingURL=index.js.map
