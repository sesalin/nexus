import { useMemo as c } from "react";
import "lodash";
import { W as l, aa as p } from "../../../index-1tDtVGr2.js";
import "../useLocale/locales/index.js";
import "home-assistant-js-websocket";
import "@iconify/react";
import "use-debounce";
import "../../utils/date.js";
const d = (t) => t.attributes.unit_of_measurement === "%" && t.attributes.device_class === "battery", g = ({ blacklist: t = [], whitelist: s = [], min: o = 0, max: i = 20 } = {}) => {
  const m = l(
    p((e) => Object.values(e.entities).filter(d))
  );
  return c(
    () => m.filter((e) => {
      const u = Number(e.state) <= i && Number(e.state) >= o, a = t.some((r) => e.entity_id.includes(r)), n = s.length === 0 || s.some((r) => e.entity_id.includes(r));
      return u && n && !a;
    }),
    [t, m, i, o, s]
  );
};
export {
  g as useLowDevices
};
//# sourceMappingURL=index.js.map
