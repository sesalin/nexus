import { EntityName, FilterByDomain } from '@hakit/core';
import { VideoState } from './';
export interface WebRTCPlayerProps {
    /** The name of your entity */
    entity: FilterByDomain<EntityName, "camera">;
    controls?: boolean;
    muted?: boolean;
    autoPlay?: boolean;
    playsInline?: boolean;
    posterUrl?: string;
    onStateChange?: (state: VideoState) => void;
}
export type WebRtcOfferEvent = WebRtcId | WebRtcAnswer | WebRtcCandidate | WebRtcError;
export interface WebRtcId {
    type: "session";
    session_id: string;
}
export interface WebRtcAnswer {
    type: "answer";
    answer: string;
}
export interface WebRtcCandidate {
    type: "candidate";
    candidate: RTCIceCandidateInit;
}
export interface WebRtcError {
    type: "error";
    code: string;
    message: string;
}
export interface WebRTCClientConfiguration {
    configuration: RTCConfiguration;
    dataChannel?: string;
    getCandidatesUpfront: boolean;
}
/**
 * A WebRTC stream is established by first sending an offer through a signal
 * path via an integration. An answer is returned, then the rest of the stream
 * is handled entirely client side.
 */
export declare function WebRTCPlayer({ entity, controls, muted, autoPlay, playsInline, posterUrl, onStateChange }: WebRTCPlayerProps): import("@emotion/react/jsx-runtime").JSX.Element;
//# sourceMappingURL=webrtc.d.ts.map