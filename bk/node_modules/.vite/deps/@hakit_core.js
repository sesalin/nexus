import {
  require_jsx_runtime
} from "./chunk-LBJFE7KM.js";
import {
  require_react
} from "./chunk-5HXT6MYD.js";
import {
  __commonJS,
  __toESM
} from "./chunk-5WRI5ZAA.js";

// node_modules/lodash/lodash.js
var require_lodash = __commonJS({
  "node_modules/lodash/lodash.js"(exports, module) {
    (function() {
      var undefined2;
      var VERSION = "4.17.21";
      var LARGE_ARRAY_SIZE = 200;
      var CORE_ERROR_TEXT = "Unsupported core-js use. Try https://npms.io/search?q=ponyfill.", FUNC_ERROR_TEXT = "Expected a function", INVALID_TEMPL_VAR_ERROR_TEXT = "Invalid `variable` option passed into `_.template`";
      var HASH_UNDEFINED = "__lodash_hash_undefined__";
      var MAX_MEMOIZE_SIZE = 500;
      var PLACEHOLDER = "__lodash_placeholder__";
      var CLONE_DEEP_FLAG = 1, CLONE_FLAT_FLAG = 2, CLONE_SYMBOLS_FLAG = 4;
      var COMPARE_PARTIAL_FLAG = 1, COMPARE_UNORDERED_FLAG = 2;
      var WRAP_BIND_FLAG = 1, WRAP_BIND_KEY_FLAG = 2, WRAP_CURRY_BOUND_FLAG = 4, WRAP_CURRY_FLAG = 8, WRAP_CURRY_RIGHT_FLAG = 16, WRAP_PARTIAL_FLAG = 32, WRAP_PARTIAL_RIGHT_FLAG = 64, WRAP_ARY_FLAG = 128, WRAP_REARG_FLAG = 256, WRAP_FLIP_FLAG = 512;
      var DEFAULT_TRUNC_LENGTH = 30, DEFAULT_TRUNC_OMISSION = "...";
      var HOT_COUNT = 800, HOT_SPAN = 16;
      var LAZY_FILTER_FLAG = 1, LAZY_MAP_FLAG = 2, LAZY_WHILE_FLAG = 3;
      var INFINITY = 1 / 0, MAX_SAFE_INTEGER = 9007199254740991, MAX_INTEGER = 17976931348623157e292, NAN = 0 / 0;
      var MAX_ARRAY_LENGTH = 4294967295, MAX_ARRAY_INDEX = MAX_ARRAY_LENGTH - 1, HALF_MAX_ARRAY_LENGTH = MAX_ARRAY_LENGTH >>> 1;
      var wrapFlags = [
        ["ary", WRAP_ARY_FLAG],
        ["bind", WRAP_BIND_FLAG],
        ["bindKey", WRAP_BIND_KEY_FLAG],
        ["curry", WRAP_CURRY_FLAG],
        ["curryRight", WRAP_CURRY_RIGHT_FLAG],
        ["flip", WRAP_FLIP_FLAG],
        ["partial", WRAP_PARTIAL_FLAG],
        ["partialRight", WRAP_PARTIAL_RIGHT_FLAG],
        ["rearg", WRAP_REARG_FLAG]
      ];
      var argsTag = "[object Arguments]", arrayTag = "[object Array]", asyncTag = "[object AsyncFunction]", boolTag = "[object Boolean]", dateTag = "[object Date]", domExcTag = "[object DOMException]", errorTag = "[object Error]", funcTag = "[object Function]", genTag = "[object GeneratorFunction]", mapTag = "[object Map]", numberTag = "[object Number]", nullTag = "[object Null]", objectTag = "[object Object]", promiseTag = "[object Promise]", proxyTag = "[object Proxy]", regexpTag = "[object RegExp]", setTag = "[object Set]", stringTag = "[object String]", symbolTag = "[object Symbol]", undefinedTag = "[object Undefined]", weakMapTag = "[object WeakMap]", weakSetTag = "[object WeakSet]";
      var arrayBufferTag = "[object ArrayBuffer]", dataViewTag = "[object DataView]", float32Tag = "[object Float32Array]", float64Tag = "[object Float64Array]", int8Tag = "[object Int8Array]", int16Tag = "[object Int16Array]", int32Tag = "[object Int32Array]", uint8Tag = "[object Uint8Array]", uint8ClampedTag = "[object Uint8ClampedArray]", uint16Tag = "[object Uint16Array]", uint32Tag = "[object Uint32Array]";
      var reEmptyStringLeading = /\b__p \+= '';/g, reEmptyStringMiddle = /\b(__p \+=) '' \+/g, reEmptyStringTrailing = /(__e\(.*?\)|\b__t\)) \+\n'';/g;
      var reEscapedHtml = /&(?:amp|lt|gt|quot|#39);/g, reUnescapedHtml = /[&<>"']/g, reHasEscapedHtml = RegExp(reEscapedHtml.source), reHasUnescapedHtml = RegExp(reUnescapedHtml.source);
      var reEscape = /<%-([\s\S]+?)%>/g, reEvaluate = /<%([\s\S]+?)%>/g, reInterpolate = /<%=([\s\S]+?)%>/g;
      var reIsDeepProp = /\.|\[(?:[^[\]]*|(["'])(?:(?!\1)[^\\]|\\.)*?\1)\]/, reIsPlainProp = /^\w*$/, rePropName = /[^.[\]]+|\[(?:(-?\d+(?:\.\d+)?)|(["'])((?:(?!\2)[^\\]|\\.)*?)\2)\]|(?=(?:\.|\[\])(?:\.|\[\]|$))/g;
      var reRegExpChar = /[\\^$.*+?()[\]{}|]/g, reHasRegExpChar = RegExp(reRegExpChar.source);
      var reTrimStart = /^\s+/;
      var reWhitespace = /\s/;
      var reWrapComment = /\{(?:\n\/\* \[wrapped with .+\] \*\/)?\n?/, reWrapDetails = /\{\n\/\* \[wrapped with (.+)\] \*/, reSplitDetails = /,? & /;
      var reAsciiWord = /[^\x00-\x2f\x3a-\x40\x5b-\x60\x7b-\x7f]+/g;
      var reForbiddenIdentifierChars = /[()=,{}\[\]\/\s]/;
      var reEscapeChar = /\\(\\)?/g;
      var reEsTemplate = /\$\{([^\\}]*(?:\\.[^\\}]*)*)\}/g;
      var reFlags = /\w*$/;
      var reIsBadHex = /^[-+]0x[0-9a-f]+$/i;
      var reIsBinary = /^0b[01]+$/i;
      var reIsHostCtor = /^\[object .+?Constructor\]$/;
      var reIsOctal = /^0o[0-7]+$/i;
      var reIsUint = /^(?:0|[1-9]\d*)$/;
      var reLatin = /[\xc0-\xd6\xd8-\xf6\xf8-\xff\u0100-\u017f]/g;
      var reNoMatch = /($^)/;
      var reUnescapedString = /['\n\r\u2028\u2029\\]/g;
      var rsAstralRange = "\\ud800-\\udfff", rsComboMarksRange = "\\u0300-\\u036f", reComboHalfMarksRange = "\\ufe20-\\ufe2f", rsComboSymbolsRange = "\\u20d0-\\u20ff", rsComboRange = rsComboMarksRange + reComboHalfMarksRange + rsComboSymbolsRange, rsDingbatRange = "\\u2700-\\u27bf", rsLowerRange = "a-z\\xdf-\\xf6\\xf8-\\xff", rsMathOpRange = "\\xac\\xb1\\xd7\\xf7", rsNonCharRange = "\\x00-\\x2f\\x3a-\\x40\\x5b-\\x60\\x7b-\\xbf", rsPunctuationRange = "\\u2000-\\u206f", rsSpaceRange = " \\t\\x0b\\f\\xa0\\ufeff\\n\\r\\u2028\\u2029\\u1680\\u180e\\u2000\\u2001\\u2002\\u2003\\u2004\\u2005\\u2006\\u2007\\u2008\\u2009\\u200a\\u202f\\u205f\\u3000", rsUpperRange = "A-Z\\xc0-\\xd6\\xd8-\\xde", rsVarRange = "\\ufe0e\\ufe0f", rsBreakRange = rsMathOpRange + rsNonCharRange + rsPunctuationRange + rsSpaceRange;
      var rsApos = "['’]", rsAstral = "[" + rsAstralRange + "]", rsBreak = "[" + rsBreakRange + "]", rsCombo = "[" + rsComboRange + "]", rsDigits = "\\d+", rsDingbat = "[" + rsDingbatRange + "]", rsLower = "[" + rsLowerRange + "]", rsMisc = "[^" + rsAstralRange + rsBreakRange + rsDigits + rsDingbatRange + rsLowerRange + rsUpperRange + "]", rsFitz = "\\ud83c[\\udffb-\\udfff]", rsModifier = "(?:" + rsCombo + "|" + rsFitz + ")", rsNonAstral = "[^" + rsAstralRange + "]", rsRegional = "(?:\\ud83c[\\udde6-\\uddff]){2}", rsSurrPair = "[\\ud800-\\udbff][\\udc00-\\udfff]", rsUpper = "[" + rsUpperRange + "]", rsZWJ = "\\u200d";
      var rsMiscLower = "(?:" + rsLower + "|" + rsMisc + ")", rsMiscUpper = "(?:" + rsUpper + "|" + rsMisc + ")", rsOptContrLower = "(?:" + rsApos + "(?:d|ll|m|re|s|t|ve))?", rsOptContrUpper = "(?:" + rsApos + "(?:D|LL|M|RE|S|T|VE))?", reOptMod = rsModifier + "?", rsOptVar = "[" + rsVarRange + "]?", rsOptJoin = "(?:" + rsZWJ + "(?:" + [rsNonAstral, rsRegional, rsSurrPair].join("|") + ")" + rsOptVar + reOptMod + ")*", rsOrdLower = "\\d*(?:1st|2nd|3rd|(?![123])\\dth)(?=\\b|[A-Z_])", rsOrdUpper = "\\d*(?:1ST|2ND|3RD|(?![123])\\dTH)(?=\\b|[a-z_])", rsSeq = rsOptVar + reOptMod + rsOptJoin, rsEmoji = "(?:" + [rsDingbat, rsRegional, rsSurrPair].join("|") + ")" + rsSeq, rsSymbol = "(?:" + [rsNonAstral + rsCombo + "?", rsCombo, rsRegional, rsSurrPair, rsAstral].join("|") + ")";
      var reApos = RegExp(rsApos, "g");
      var reComboMark = RegExp(rsCombo, "g");
      var reUnicode = RegExp(rsFitz + "(?=" + rsFitz + ")|" + rsSymbol + rsSeq, "g");
      var reUnicodeWord = RegExp([
        rsUpper + "?" + rsLower + "+" + rsOptContrLower + "(?=" + [rsBreak, rsUpper, "$"].join("|") + ")",
        rsMiscUpper + "+" + rsOptContrUpper + "(?=" + [rsBreak, rsUpper + rsMiscLower, "$"].join("|") + ")",
        rsUpper + "?" + rsMiscLower + "+" + rsOptContrLower,
        rsUpper + "+" + rsOptContrUpper,
        rsOrdUpper,
        rsOrdLower,
        rsDigits,
        rsEmoji
      ].join("|"), "g");
      var reHasUnicode = RegExp("[" + rsZWJ + rsAstralRange + rsComboRange + rsVarRange + "]");
      var reHasUnicodeWord = /[a-z][A-Z]|[A-Z]{2}[a-z]|[0-9][a-zA-Z]|[a-zA-Z][0-9]|[^a-zA-Z0-9 ]/;
      var contextProps = [
        "Array",
        "Buffer",
        "DataView",
        "Date",
        "Error",
        "Float32Array",
        "Float64Array",
        "Function",
        "Int8Array",
        "Int16Array",
        "Int32Array",
        "Map",
        "Math",
        "Object",
        "Promise",
        "RegExp",
        "Set",
        "String",
        "Symbol",
        "TypeError",
        "Uint8Array",
        "Uint8ClampedArray",
        "Uint16Array",
        "Uint32Array",
        "WeakMap",
        "_",
        "clearTimeout",
        "isFinite",
        "parseInt",
        "setTimeout"
      ];
      var templateCounter = -1;
      var typedArrayTags = {};
      typedArrayTags[float32Tag] = typedArrayTags[float64Tag] = typedArrayTags[int8Tag] = typedArrayTags[int16Tag] = typedArrayTags[int32Tag] = typedArrayTags[uint8Tag] = typedArrayTags[uint8ClampedTag] = typedArrayTags[uint16Tag] = typedArrayTags[uint32Tag] = true;
      typedArrayTags[argsTag] = typedArrayTags[arrayTag] = typedArrayTags[arrayBufferTag] = typedArrayTags[boolTag] = typedArrayTags[dataViewTag] = typedArrayTags[dateTag] = typedArrayTags[errorTag] = typedArrayTags[funcTag] = typedArrayTags[mapTag] = typedArrayTags[numberTag] = typedArrayTags[objectTag] = typedArrayTags[regexpTag] = typedArrayTags[setTag] = typedArrayTags[stringTag] = typedArrayTags[weakMapTag] = false;
      var cloneableTags = {};
      cloneableTags[argsTag] = cloneableTags[arrayTag] = cloneableTags[arrayBufferTag] = cloneableTags[dataViewTag] = cloneableTags[boolTag] = cloneableTags[dateTag] = cloneableTags[float32Tag] = cloneableTags[float64Tag] = cloneableTags[int8Tag] = cloneableTags[int16Tag] = cloneableTags[int32Tag] = cloneableTags[mapTag] = cloneableTags[numberTag] = cloneableTags[objectTag] = cloneableTags[regexpTag] = cloneableTags[setTag] = cloneableTags[stringTag] = cloneableTags[symbolTag] = cloneableTags[uint8Tag] = cloneableTags[uint8ClampedTag] = cloneableTags[uint16Tag] = cloneableTags[uint32Tag] = true;
      cloneableTags[errorTag] = cloneableTags[funcTag] = cloneableTags[weakMapTag] = false;
      var deburredLetters = {
        // Latin-1 Supplement block.
        "À": "A",
        "Á": "A",
        "Â": "A",
        "Ã": "A",
        "Ä": "A",
        "Å": "A",
        "à": "a",
        "á": "a",
        "â": "a",
        "ã": "a",
        "ä": "a",
        "å": "a",
        "Ç": "C",
        "ç": "c",
        "Ð": "D",
        "ð": "d",
        "È": "E",
        "É": "E",
        "Ê": "E",
        "Ë": "E",
        "è": "e",
        "é": "e",
        "ê": "e",
        "ë": "e",
        "Ì": "I",
        "Í": "I",
        "Î": "I",
        "Ï": "I",
        "ì": "i",
        "í": "i",
        "î": "i",
        "ï": "i",
        "Ñ": "N",
        "ñ": "n",
        "Ò": "O",
        "Ó": "O",
        "Ô": "O",
        "Õ": "O",
        "Ö": "O",
        "Ø": "O",
        "ò": "o",
        "ó": "o",
        "ô": "o",
        "õ": "o",
        "ö": "o",
        "ø": "o",
        "Ù": "U",
        "Ú": "U",
        "Û": "U",
        "Ü": "U",
        "ù": "u",
        "ú": "u",
        "û": "u",
        "ü": "u",
        "Ý": "Y",
        "ý": "y",
        "ÿ": "y",
        "Æ": "Ae",
        "æ": "ae",
        "Þ": "Th",
        "þ": "th",
        "ß": "ss",
        // Latin Extended-A block.
        "Ā": "A",
        "Ă": "A",
        "Ą": "A",
        "ā": "a",
        "ă": "a",
        "ą": "a",
        "Ć": "C",
        "Ĉ": "C",
        "Ċ": "C",
        "Č": "C",
        "ć": "c",
        "ĉ": "c",
        "ċ": "c",
        "č": "c",
        "Ď": "D",
        "Đ": "D",
        "ď": "d",
        "đ": "d",
        "Ē": "E",
        "Ĕ": "E",
        "Ė": "E",
        "Ę": "E",
        "Ě": "E",
        "ē": "e",
        "ĕ": "e",
        "ė": "e",
        "ę": "e",
        "ě": "e",
        "Ĝ": "G",
        "Ğ": "G",
        "Ġ": "G",
        "Ģ": "G",
        "ĝ": "g",
        "ğ": "g",
        "ġ": "g",
        "ģ": "g",
        "Ĥ": "H",
        "Ħ": "H",
        "ĥ": "h",
        "ħ": "h",
        "Ĩ": "I",
        "Ī": "I",
        "Ĭ": "I",
        "Į": "I",
        "İ": "I",
        "ĩ": "i",
        "ī": "i",
        "ĭ": "i",
        "į": "i",
        "ı": "i",
        "Ĵ": "J",
        "ĵ": "j",
        "Ķ": "K",
        "ķ": "k",
        "ĸ": "k",
        "Ĺ": "L",
        "Ļ": "L",
        "Ľ": "L",
        "Ŀ": "L",
        "Ł": "L",
        "ĺ": "l",
        "ļ": "l",
        "ľ": "l",
        "ŀ": "l",
        "ł": "l",
        "Ń": "N",
        "Ņ": "N",
        "Ň": "N",
        "Ŋ": "N",
        "ń": "n",
        "ņ": "n",
        "ň": "n",
        "ŋ": "n",
        "Ō": "O",
        "Ŏ": "O",
        "Ő": "O",
        "ō": "o",
        "ŏ": "o",
        "ő": "o",
        "Ŕ": "R",
        "Ŗ": "R",
        "Ř": "R",
        "ŕ": "r",
        "ŗ": "r",
        "ř": "r",
        "Ś": "S",
        "Ŝ": "S",
        "Ş": "S",
        "Š": "S",
        "ś": "s",
        "ŝ": "s",
        "ş": "s",
        "š": "s",
        "Ţ": "T",
        "Ť": "T",
        "Ŧ": "T",
        "ţ": "t",
        "ť": "t",
        "ŧ": "t",
        "Ũ": "U",
        "Ū": "U",
        "Ŭ": "U",
        "Ů": "U",
        "Ű": "U",
        "Ų": "U",
        "ũ": "u",
        "ū": "u",
        "ŭ": "u",
        "ů": "u",
        "ű": "u",
        "ų": "u",
        "Ŵ": "W",
        "ŵ": "w",
        "Ŷ": "Y",
        "ŷ": "y",
        "Ÿ": "Y",
        "Ź": "Z",
        "Ż": "Z",
        "Ž": "Z",
        "ź": "z",
        "ż": "z",
        "ž": "z",
        "Ĳ": "IJ",
        "ĳ": "ij",
        "Œ": "Oe",
        "œ": "oe",
        "ŉ": "'n",
        "ſ": "s"
      };
      var htmlEscapes = {
        "&": "&amp;",
        "<": "&lt;",
        ">": "&gt;",
        '"': "&quot;",
        "'": "&#39;"
      };
      var htmlUnescapes = {
        "&amp;": "&",
        "&lt;": "<",
        "&gt;": ">",
        "&quot;": '"',
        "&#39;": "'"
      };
      var stringEscapes = {
        "\\": "\\",
        "'": "'",
        "\n": "n",
        "\r": "r",
        "\u2028": "u2028",
        "\u2029": "u2029"
      };
      var freeParseFloat = parseFloat, freeParseInt = parseInt;
      var freeGlobal = typeof global == "object" && global && global.Object === Object && global;
      var freeSelf = typeof self == "object" && self && self.Object === Object && self;
      var root = freeGlobal || freeSelf || Function("return this")();
      var freeExports = typeof exports == "object" && exports && !exports.nodeType && exports;
      var freeModule = freeExports && typeof module == "object" && module && !module.nodeType && module;
      var moduleExports = freeModule && freeModule.exports === freeExports;
      var freeProcess = moduleExports && freeGlobal.process;
      var nodeUtil = (function() {
        try {
          var types = freeModule && freeModule.require && freeModule.require("util").types;
          if (types) {
            return types;
          }
          return freeProcess && freeProcess.binding && freeProcess.binding("util");
        } catch (e6) {
        }
      })();
      var nodeIsArrayBuffer = nodeUtil && nodeUtil.isArrayBuffer, nodeIsDate = nodeUtil && nodeUtil.isDate, nodeIsMap = nodeUtil && nodeUtil.isMap, nodeIsRegExp = nodeUtil && nodeUtil.isRegExp, nodeIsSet = nodeUtil && nodeUtil.isSet, nodeIsTypedArray = nodeUtil && nodeUtil.isTypedArray;
      function apply(func, thisArg, args) {
        switch (args.length) {
          case 0:
            return func.call(thisArg);
          case 1:
            return func.call(thisArg, args[0]);
          case 2:
            return func.call(thisArg, args[0], args[1]);
          case 3:
            return func.call(thisArg, args[0], args[1], args[2]);
        }
        return func.apply(thisArg, args);
      }
      function arrayAggregator(array, setter, iteratee, accumulator) {
        var index = -1, length2 = array == null ? 0 : array.length;
        while (++index < length2) {
          var value = array[index];
          setter(accumulator, value, iteratee(value), array);
        }
        return accumulator;
      }
      function arrayEach(array, iteratee) {
        var index = -1, length2 = array == null ? 0 : array.length;
        while (++index < length2) {
          if (iteratee(array[index], index, array) === false) {
            break;
          }
        }
        return array;
      }
      function arrayEachRight(array, iteratee) {
        var length2 = array == null ? 0 : array.length;
        while (length2--) {
          if (iteratee(array[length2], length2, array) === false) {
            break;
          }
        }
        return array;
      }
      function arrayEvery(array, predicate) {
        var index = -1, length2 = array == null ? 0 : array.length;
        while (++index < length2) {
          if (!predicate(array[index], index, array)) {
            return false;
          }
        }
        return true;
      }
      function arrayFilter(array, predicate) {
        var index = -1, length2 = array == null ? 0 : array.length, resIndex = 0, result = [];
        while (++index < length2) {
          var value = array[index];
          if (predicate(value, index, array)) {
            result[resIndex++] = value;
          }
        }
        return result;
      }
      function arrayIncludes(array, value) {
        var length2 = array == null ? 0 : array.length;
        return !!length2 && baseIndexOf(array, value, 0) > -1;
      }
      function arrayIncludesWith(array, value, comparator) {
        var index = -1, length2 = array == null ? 0 : array.length;
        while (++index < length2) {
          if (comparator(value, array[index])) {
            return true;
          }
        }
        return false;
      }
      function arrayMap(array, iteratee) {
        var index = -1, length2 = array == null ? 0 : array.length, result = Array(length2);
        while (++index < length2) {
          result[index] = iteratee(array[index], index, array);
        }
        return result;
      }
      function arrayPush(array, values) {
        var index = -1, length2 = values.length, offset = array.length;
        while (++index < length2) {
          array[offset + index] = values[index];
        }
        return array;
      }
      function arrayReduce(array, iteratee, accumulator, initAccum) {
        var index = -1, length2 = array == null ? 0 : array.length;
        if (initAccum && length2) {
          accumulator = array[++index];
        }
        while (++index < length2) {
          accumulator = iteratee(accumulator, array[index], index, array);
        }
        return accumulator;
      }
      function arrayReduceRight(array, iteratee, accumulator, initAccum) {
        var length2 = array == null ? 0 : array.length;
        if (initAccum && length2) {
          accumulator = array[--length2];
        }
        while (length2--) {
          accumulator = iteratee(accumulator, array[length2], length2, array);
        }
        return accumulator;
      }
      function arraySome(array, predicate) {
        var index = -1, length2 = array == null ? 0 : array.length;
        while (++index < length2) {
          if (predicate(array[index], index, array)) {
            return true;
          }
        }
        return false;
      }
      var asciiSize = baseProperty("length");
      function asciiToArray(string) {
        return string.split("");
      }
      function asciiWords(string) {
        return string.match(reAsciiWord) || [];
      }
      function baseFindKey(collection, predicate, eachFunc) {
        var result;
        eachFunc(collection, function(value, key, collection2) {
          if (predicate(value, key, collection2)) {
            result = key;
            return false;
          }
        });
        return result;
      }
      function baseFindIndex(array, predicate, fromIndex, fromRight) {
        var length2 = array.length, index = fromIndex + (fromRight ? 1 : -1);
        while (fromRight ? index-- : ++index < length2) {
          if (predicate(array[index], index, array)) {
            return index;
          }
        }
        return -1;
      }
      function baseIndexOf(array, value, fromIndex) {
        return value === value ? strictIndexOf(array, value, fromIndex) : baseFindIndex(array, baseIsNaN, fromIndex);
      }
      function baseIndexOfWith(array, value, fromIndex, comparator) {
        var index = fromIndex - 1, length2 = array.length;
        while (++index < length2) {
          if (comparator(array[index], value)) {
            return index;
          }
        }
        return -1;
      }
      function baseIsNaN(value) {
        return value !== value;
      }
      function baseMean(array, iteratee) {
        var length2 = array == null ? 0 : array.length;
        return length2 ? baseSum(array, iteratee) / length2 : NAN;
      }
      function baseProperty(key) {
        return function(object) {
          return object == null ? undefined2 : object[key];
        };
      }
      function basePropertyOf(object) {
        return function(key) {
          return object == null ? undefined2 : object[key];
        };
      }
      function baseReduce(collection, iteratee, accumulator, initAccum, eachFunc) {
        eachFunc(collection, function(value, index, collection2) {
          accumulator = initAccum ? (initAccum = false, value) : iteratee(accumulator, value, index, collection2);
        });
        return accumulator;
      }
      function baseSortBy(array, comparer) {
        var length2 = array.length;
        array.sort(comparer);
        while (length2--) {
          array[length2] = array[length2].value;
        }
        return array;
      }
      function baseSum(array, iteratee) {
        var result, index = -1, length2 = array.length;
        while (++index < length2) {
          var current = iteratee(array[index]);
          if (current !== undefined2) {
            result = result === undefined2 ? current : result + current;
          }
        }
        return result;
      }
      function baseTimes(n15, iteratee) {
        var index = -1, result = Array(n15);
        while (++index < n15) {
          result[index] = iteratee(index);
        }
        return result;
      }
      function baseToPairs(object, props) {
        return arrayMap(props, function(key) {
          return [key, object[key]];
        });
      }
      function baseTrim(string) {
        return string ? string.slice(0, trimmedEndIndex(string) + 1).replace(reTrimStart, "") : string;
      }
      function baseUnary(func) {
        return function(value) {
          return func(value);
        };
      }
      function baseValues(object, props) {
        return arrayMap(props, function(key) {
          return object[key];
        });
      }
      function cacheHas(cache, key) {
        return cache.has(key);
      }
      function charsStartIndex(strSymbols, chrSymbols) {
        var index = -1, length2 = strSymbols.length;
        while (++index < length2 && baseIndexOf(chrSymbols, strSymbols[index], 0) > -1) {
        }
        return index;
      }
      function charsEndIndex(strSymbols, chrSymbols) {
        var index = strSymbols.length;
        while (index-- && baseIndexOf(chrSymbols, strSymbols[index], 0) > -1) {
        }
        return index;
      }
      function countHolders(array, placeholder) {
        var length2 = array.length, result = 0;
        while (length2--) {
          if (array[length2] === placeholder) {
            ++result;
          }
        }
        return result;
      }
      var deburrLetter = basePropertyOf(deburredLetters);
      var escapeHtmlChar = basePropertyOf(htmlEscapes);
      function escapeStringChar(chr) {
        return "\\" + stringEscapes[chr];
      }
      function getValue(object, key) {
        return object == null ? undefined2 : object[key];
      }
      function hasUnicode(string) {
        return reHasUnicode.test(string);
      }
      function hasUnicodeWord(string) {
        return reHasUnicodeWord.test(string);
      }
      function iteratorToArray(iterator) {
        var data, result = [];
        while (!(data = iterator.next()).done) {
          result.push(data.value);
        }
        return result;
      }
      function mapToArray(map) {
        var index = -1, result = Array(map.size);
        map.forEach(function(value, key) {
          result[++index] = [key, value];
        });
        return result;
      }
      function overArg(func, transform) {
        return function(arg) {
          return func(transform(arg));
        };
      }
      function replaceHolders(array, placeholder) {
        var index = -1, length2 = array.length, resIndex = 0, result = [];
        while (++index < length2) {
          var value = array[index];
          if (value === placeholder || value === PLACEHOLDER) {
            array[index] = PLACEHOLDER;
            result[resIndex++] = index;
          }
        }
        return result;
      }
      function setToArray(set) {
        var index = -1, result = Array(set.size);
        set.forEach(function(value) {
          result[++index] = value;
        });
        return result;
      }
      function setToPairs(set) {
        var index = -1, result = Array(set.size);
        set.forEach(function(value) {
          result[++index] = [value, value];
        });
        return result;
      }
      function strictIndexOf(array, value, fromIndex) {
        var index = fromIndex - 1, length2 = array.length;
        while (++index < length2) {
          if (array[index] === value) {
            return index;
          }
        }
        return -1;
      }
      function strictLastIndexOf(array, value, fromIndex) {
        var index = fromIndex + 1;
        while (index--) {
          if (array[index] === value) {
            return index;
          }
        }
        return index;
      }
      function stringSize(string) {
        return hasUnicode(string) ? unicodeSize(string) : asciiSize(string);
      }
      function stringToArray(string) {
        return hasUnicode(string) ? unicodeToArray(string) : asciiToArray(string);
      }
      function trimmedEndIndex(string) {
        var index = string.length;
        while (index-- && reWhitespace.test(string.charAt(index))) {
        }
        return index;
      }
      var unescapeHtmlChar = basePropertyOf(htmlUnescapes);
      function unicodeSize(string) {
        var result = reUnicode.lastIndex = 0;
        while (reUnicode.test(string)) {
          ++result;
        }
        return result;
      }
      function unicodeToArray(string) {
        return string.match(reUnicode) || [];
      }
      function unicodeWords(string) {
        return string.match(reUnicodeWord) || [];
      }
      var runInContext = (function runInContext2(context) {
        context = context == null ? root : _8.defaults(root.Object(), context, _8.pick(root, contextProps));
        var Array2 = context.Array, Date2 = context.Date, Error2 = context.Error, Function2 = context.Function, Math2 = context.Math, Object2 = context.Object, RegExp2 = context.RegExp, String2 = context.String, TypeError2 = context.TypeError;
        var arrayProto = Array2.prototype, funcProto = Function2.prototype, objectProto = Object2.prototype;
        var coreJsData = context["__core-js_shared__"];
        var funcToString = funcProto.toString;
        var hasOwnProperty = objectProto.hasOwnProperty;
        var idCounter2 = 0;
        var maskSrcKey = (function() {
          var uid = /[^.]+$/.exec(coreJsData && coreJsData.keys && coreJsData.keys.IE_PROTO || "");
          return uid ? "Symbol(src)_1." + uid : "";
        })();
        var nativeObjectToString = objectProto.toString;
        var objectCtorString = funcToString.call(Object2);
        var oldDash = root._;
        var reIsNative = RegExp2(
          "^" + funcToString.call(hasOwnProperty).replace(reRegExpChar, "\\$&").replace(/hasOwnProperty|(function).*?(?=\\\()| for .+?(?=\\\])/g, "$1.*?") + "$"
        );
        var Buffer = moduleExports ? context.Buffer : undefined2, Symbol2 = context.Symbol, Uint8Array = context.Uint8Array, allocUnsafe = Buffer ? Buffer.allocUnsafe : undefined2, getPrototype = overArg(Object2.getPrototypeOf, Object2), objectCreate = Object2.create, propertyIsEnumerable = objectProto.propertyIsEnumerable, splice = arrayProto.splice, spreadableSymbol = Symbol2 ? Symbol2.isConcatSpreadable : undefined2, symIterator = Symbol2 ? Symbol2.iterator : undefined2, symToStringTag = Symbol2 ? Symbol2.toStringTag : undefined2;
        var defineProperty = (function() {
          try {
            var func = getNative(Object2, "defineProperty");
            func({}, "", {});
            return func;
          } catch (e6) {
          }
        })();
        var ctxClearTimeout = context.clearTimeout !== root.clearTimeout && context.clearTimeout, ctxNow = Date2 && Date2.now !== root.Date.now && Date2.now, ctxSetTimeout = context.setTimeout !== root.setTimeout && context.setTimeout;
        var nativeCeil = Math2.ceil, nativeFloor = Math2.floor, nativeGetSymbols = Object2.getOwnPropertySymbols, nativeIsBuffer = Buffer ? Buffer.isBuffer : undefined2, nativeIsFinite = context.isFinite, nativeJoin = arrayProto.join, nativeKeys = overArg(Object2.keys, Object2), nativeMax = Math2.max, nativeMin = Math2.min, nativeNow = Date2.now, nativeParseInt = context.parseInt, nativeRandom = Math2.random, nativeReverse = arrayProto.reverse;
        var DataView = getNative(context, "DataView"), Map2 = getNative(context, "Map"), Promise2 = getNative(context, "Promise"), Set2 = getNative(context, "Set"), WeakMap2 = getNative(context, "WeakMap"), nativeCreate = getNative(Object2, "create");
        var metaMap = WeakMap2 && new WeakMap2();
        var realNames = {};
        var dataViewCtorString = toSource(DataView), mapCtorString = toSource(Map2), promiseCtorString = toSource(Promise2), setCtorString = toSource(Set2), weakMapCtorString = toSource(WeakMap2);
        var symbolProto = Symbol2 ? Symbol2.prototype : undefined2, symbolValueOf = symbolProto ? symbolProto.valueOf : undefined2, symbolToString = symbolProto ? symbolProto.toString : undefined2;
        function lodash(value) {
          if (isObjectLike(value) && !isArray(value) && !(value instanceof LazyWrapper)) {
            if (value instanceof LodashWrapper) {
              return value;
            }
            if (hasOwnProperty.call(value, "__wrapped__")) {
              return wrapperClone(value);
            }
          }
          return new LodashWrapper(value);
        }
        var baseCreate = /* @__PURE__ */ (function() {
          function object() {
          }
          return function(proto) {
            if (!isObject(proto)) {
              return {};
            }
            if (objectCreate) {
              return objectCreate(proto);
            }
            object.prototype = proto;
            var result2 = new object();
            object.prototype = undefined2;
            return result2;
          };
        })();
        function baseLodash() {
        }
        function LodashWrapper(value, chainAll) {
          this.__wrapped__ = value;
          this.__actions__ = [];
          this.__chain__ = !!chainAll;
          this.__index__ = 0;
          this.__values__ = undefined2;
        }
        lodash.templateSettings = {
          /**
           * Used to detect `data` property values to be HTML-escaped.
           *
           * @memberOf _.templateSettings
           * @type {RegExp}
           */
          "escape": reEscape,
          /**
           * Used to detect code to be evaluated.
           *
           * @memberOf _.templateSettings
           * @type {RegExp}
           */
          "evaluate": reEvaluate,
          /**
           * Used to detect `data` property values to inject.
           *
           * @memberOf _.templateSettings
           * @type {RegExp}
           */
          "interpolate": reInterpolate,
          /**
           * Used to reference the data object in the template text.
           *
           * @memberOf _.templateSettings
           * @type {string}
           */
          "variable": "",
          /**
           * Used to import variables into the compiled template.
           *
           * @memberOf _.templateSettings
           * @type {Object}
           */
          "imports": {
            /**
             * A reference to the `lodash` function.
             *
             * @memberOf _.templateSettings.imports
             * @type {Function}
             */
            "_": lodash
          }
        };
        lodash.prototype = baseLodash.prototype;
        lodash.prototype.constructor = lodash;
        LodashWrapper.prototype = baseCreate(baseLodash.prototype);
        LodashWrapper.prototype.constructor = LodashWrapper;
        function LazyWrapper(value) {
          this.__wrapped__ = value;
          this.__actions__ = [];
          this.__dir__ = 1;
          this.__filtered__ = false;
          this.__iteratees__ = [];
          this.__takeCount__ = MAX_ARRAY_LENGTH;
          this.__views__ = [];
        }
        function lazyClone() {
          var result2 = new LazyWrapper(this.__wrapped__);
          result2.__actions__ = copyArray(this.__actions__);
          result2.__dir__ = this.__dir__;
          result2.__filtered__ = this.__filtered__;
          result2.__iteratees__ = copyArray(this.__iteratees__);
          result2.__takeCount__ = this.__takeCount__;
          result2.__views__ = copyArray(this.__views__);
          return result2;
        }
        function lazyReverse() {
          if (this.__filtered__) {
            var result2 = new LazyWrapper(this);
            result2.__dir__ = -1;
            result2.__filtered__ = true;
          } else {
            result2 = this.clone();
            result2.__dir__ *= -1;
          }
          return result2;
        }
        function lazyValue() {
          var array = this.__wrapped__.value(), dir = this.__dir__, isArr = isArray(array), isRight = dir < 0, arrLength = isArr ? array.length : 0, view = getView(0, arrLength, this.__views__), start = view.start, end = view.end, length2 = end - start, index = isRight ? end : start - 1, iteratees = this.__iteratees__, iterLength = iteratees.length, resIndex = 0, takeCount = nativeMin(length2, this.__takeCount__);
          if (!isArr || !isRight && arrLength == length2 && takeCount == length2) {
            return baseWrapperValue(array, this.__actions__);
          }
          var result2 = [];
          outer:
            while (length2-- && resIndex < takeCount) {
              index += dir;
              var iterIndex = -1, value = array[index];
              while (++iterIndex < iterLength) {
                var data = iteratees[iterIndex], iteratee2 = data.iteratee, type = data.type, computed = iteratee2(value);
                if (type == LAZY_MAP_FLAG) {
                  value = computed;
                } else if (!computed) {
                  if (type == LAZY_FILTER_FLAG) {
                    continue outer;
                  } else {
                    break outer;
                  }
                }
              }
              result2[resIndex++] = value;
            }
          return result2;
        }
        LazyWrapper.prototype = baseCreate(baseLodash.prototype);
        LazyWrapper.prototype.constructor = LazyWrapper;
        function Hash(entries) {
          var index = -1, length2 = entries == null ? 0 : entries.length;
          this.clear();
          while (++index < length2) {
            var entry = entries[index];
            this.set(entry[0], entry[1]);
          }
        }
        function hashClear() {
          this.__data__ = nativeCreate ? nativeCreate(null) : {};
          this.size = 0;
        }
        function hashDelete(key) {
          var result2 = this.has(key) && delete this.__data__[key];
          this.size -= result2 ? 1 : 0;
          return result2;
        }
        function hashGet(key) {
          var data = this.__data__;
          if (nativeCreate) {
            var result2 = data[key];
            return result2 === HASH_UNDEFINED ? undefined2 : result2;
          }
          return hasOwnProperty.call(data, key) ? data[key] : undefined2;
        }
        function hashHas(key) {
          var data = this.__data__;
          return nativeCreate ? data[key] !== undefined2 : hasOwnProperty.call(data, key);
        }
        function hashSet(key, value) {
          var data = this.__data__;
          this.size += this.has(key) ? 0 : 1;
          data[key] = nativeCreate && value === undefined2 ? HASH_UNDEFINED : value;
          return this;
        }
        Hash.prototype.clear = hashClear;
        Hash.prototype["delete"] = hashDelete;
        Hash.prototype.get = hashGet;
        Hash.prototype.has = hashHas;
        Hash.prototype.set = hashSet;
        function ListCache(entries) {
          var index = -1, length2 = entries == null ? 0 : entries.length;
          this.clear();
          while (++index < length2) {
            var entry = entries[index];
            this.set(entry[0], entry[1]);
          }
        }
        function listCacheClear() {
          this.__data__ = [];
          this.size = 0;
        }
        function listCacheDelete(key) {
          var data = this.__data__, index = assocIndexOf(data, key);
          if (index < 0) {
            return false;
          }
          var lastIndex = data.length - 1;
          if (index == lastIndex) {
            data.pop();
          } else {
            splice.call(data, index, 1);
          }
          --this.size;
          return true;
        }
        function listCacheGet(key) {
          var data = this.__data__, index = assocIndexOf(data, key);
          return index < 0 ? undefined2 : data[index][1];
        }
        function listCacheHas(key) {
          return assocIndexOf(this.__data__, key) > -1;
        }
        function listCacheSet(key, value) {
          var data = this.__data__, index = assocIndexOf(data, key);
          if (index < 0) {
            ++this.size;
            data.push([key, value]);
          } else {
            data[index][1] = value;
          }
          return this;
        }
        ListCache.prototype.clear = listCacheClear;
        ListCache.prototype["delete"] = listCacheDelete;
        ListCache.prototype.get = listCacheGet;
        ListCache.prototype.has = listCacheHas;
        ListCache.prototype.set = listCacheSet;
        function MapCache(entries) {
          var index = -1, length2 = entries == null ? 0 : entries.length;
          this.clear();
          while (++index < length2) {
            var entry = entries[index];
            this.set(entry[0], entry[1]);
          }
        }
        function mapCacheClear() {
          this.size = 0;
          this.__data__ = {
            "hash": new Hash(),
            "map": new (Map2 || ListCache)(),
            "string": new Hash()
          };
        }
        function mapCacheDelete(key) {
          var result2 = getMapData(this, key)["delete"](key);
          this.size -= result2 ? 1 : 0;
          return result2;
        }
        function mapCacheGet(key) {
          return getMapData(this, key).get(key);
        }
        function mapCacheHas(key) {
          return getMapData(this, key).has(key);
        }
        function mapCacheSet(key, value) {
          var data = getMapData(this, key), size2 = data.size;
          data.set(key, value);
          this.size += data.size == size2 ? 0 : 1;
          return this;
        }
        MapCache.prototype.clear = mapCacheClear;
        MapCache.prototype["delete"] = mapCacheDelete;
        MapCache.prototype.get = mapCacheGet;
        MapCache.prototype.has = mapCacheHas;
        MapCache.prototype.set = mapCacheSet;
        function SetCache(values2) {
          var index = -1, length2 = values2 == null ? 0 : values2.length;
          this.__data__ = new MapCache();
          while (++index < length2) {
            this.add(values2[index]);
          }
        }
        function setCacheAdd(value) {
          this.__data__.set(value, HASH_UNDEFINED);
          return this;
        }
        function setCacheHas(value) {
          return this.__data__.has(value);
        }
        SetCache.prototype.add = SetCache.prototype.push = setCacheAdd;
        SetCache.prototype.has = setCacheHas;
        function Stack(entries) {
          var data = this.__data__ = new ListCache(entries);
          this.size = data.size;
        }
        function stackClear() {
          this.__data__ = new ListCache();
          this.size = 0;
        }
        function stackDelete(key) {
          var data = this.__data__, result2 = data["delete"](key);
          this.size = data.size;
          return result2;
        }
        function stackGet(key) {
          return this.__data__.get(key);
        }
        function stackHas(key) {
          return this.__data__.has(key);
        }
        function stackSet(key, value) {
          var data = this.__data__;
          if (data instanceof ListCache) {
            var pairs = data.__data__;
            if (!Map2 || pairs.length < LARGE_ARRAY_SIZE - 1) {
              pairs.push([key, value]);
              this.size = ++data.size;
              return this;
            }
            data = this.__data__ = new MapCache(pairs);
          }
          data.set(key, value);
          this.size = data.size;
          return this;
        }
        Stack.prototype.clear = stackClear;
        Stack.prototype["delete"] = stackDelete;
        Stack.prototype.get = stackGet;
        Stack.prototype.has = stackHas;
        Stack.prototype.set = stackSet;
        function arrayLikeKeys(value, inherited) {
          var isArr = isArray(value), isArg = !isArr && isArguments(value), isBuff = !isArr && !isArg && isBuffer(value), isType = !isArr && !isArg && !isBuff && isTypedArray(value), skipIndexes = isArr || isArg || isBuff || isType, result2 = skipIndexes ? baseTimes(value.length, String2) : [], length2 = result2.length;
          for (var key in value) {
            if ((inherited || hasOwnProperty.call(value, key)) && !(skipIndexes && // Safari 9 has enumerable `arguments.length` in strict mode.
            (key == "length" || // Node.js 0.10 has enumerable non-index properties on buffers.
            isBuff && (key == "offset" || key == "parent") || // PhantomJS 2 has enumerable non-index properties on typed arrays.
            isType && (key == "buffer" || key == "byteLength" || key == "byteOffset") || // Skip index properties.
            isIndex(key, length2)))) {
              result2.push(key);
            }
          }
          return result2;
        }
        function arraySample(array) {
          var length2 = array.length;
          return length2 ? array[baseRandom(0, length2 - 1)] : undefined2;
        }
        function arraySampleSize(array, n15) {
          return shuffleSelf(copyArray(array), baseClamp(n15, 0, array.length));
        }
        function arrayShuffle(array) {
          return shuffleSelf(copyArray(array));
        }
        function assignMergeValue(object, key, value) {
          if (value !== undefined2 && !eq(object[key], value) || value === undefined2 && !(key in object)) {
            baseAssignValue(object, key, value);
          }
        }
        function assignValue(object, key, value) {
          var objValue = object[key];
          if (!(hasOwnProperty.call(object, key) && eq(objValue, value)) || value === undefined2 && !(key in object)) {
            baseAssignValue(object, key, value);
          }
        }
        function assocIndexOf(array, key) {
          var length2 = array.length;
          while (length2--) {
            if (eq(array[length2][0], key)) {
              return length2;
            }
          }
          return -1;
        }
        function baseAggregator(collection, setter, iteratee2, accumulator) {
          baseEach(collection, function(value, key, collection2) {
            setter(accumulator, value, iteratee2(value), collection2);
          });
          return accumulator;
        }
        function baseAssign(object, source) {
          return object && copyObject(source, keys(source), object);
        }
        function baseAssignIn(object, source) {
          return object && copyObject(source, keysIn(source), object);
        }
        function baseAssignValue(object, key, value) {
          if (key == "__proto__" && defineProperty) {
            defineProperty(object, key, {
              "configurable": true,
              "enumerable": true,
              "value": value,
              "writable": true
            });
          } else {
            object[key] = value;
          }
        }
        function baseAt(object, paths) {
          var index = -1, length2 = paths.length, result2 = Array2(length2), skip = object == null;
          while (++index < length2) {
            result2[index] = skip ? undefined2 : get(object, paths[index]);
          }
          return result2;
        }
        function baseClamp(number, lower, upper) {
          if (number === number) {
            if (upper !== undefined2) {
              number = number <= upper ? number : upper;
            }
            if (lower !== undefined2) {
              number = number >= lower ? number : lower;
            }
          }
          return number;
        }
        function baseClone(value, bitmask, customizer, key, object, stack) {
          var result2, isDeep = bitmask & CLONE_DEEP_FLAG, isFlat = bitmask & CLONE_FLAT_FLAG, isFull = bitmask & CLONE_SYMBOLS_FLAG;
          if (customizer) {
            result2 = object ? customizer(value, key, object, stack) : customizer(value);
          }
          if (result2 !== undefined2) {
            return result2;
          }
          if (!isObject(value)) {
            return value;
          }
          var isArr = isArray(value);
          if (isArr) {
            result2 = initCloneArray(value);
            if (!isDeep) {
              return copyArray(value, result2);
            }
          } else {
            var tag = getTag(value), isFunc = tag == funcTag || tag == genTag;
            if (isBuffer(value)) {
              return cloneBuffer(value, isDeep);
            }
            if (tag == objectTag || tag == argsTag || isFunc && !object) {
              result2 = isFlat || isFunc ? {} : initCloneObject(value);
              if (!isDeep) {
                return isFlat ? copySymbolsIn(value, baseAssignIn(result2, value)) : copySymbols(value, baseAssign(result2, value));
              }
            } else {
              if (!cloneableTags[tag]) {
                return object ? value : {};
              }
              result2 = initCloneByTag(value, tag, isDeep);
            }
          }
          stack || (stack = new Stack());
          var stacked = stack.get(value);
          if (stacked) {
            return stacked;
          }
          stack.set(value, result2);
          if (isSet(value)) {
            value.forEach(function(subValue) {
              result2.add(baseClone(subValue, bitmask, customizer, subValue, value, stack));
            });
          } else if (isMap(value)) {
            value.forEach(function(subValue, key2) {
              result2.set(key2, baseClone(subValue, bitmask, customizer, key2, value, stack));
            });
          }
          var keysFunc = isFull ? isFlat ? getAllKeysIn : getAllKeys : isFlat ? keysIn : keys;
          var props = isArr ? undefined2 : keysFunc(value);
          arrayEach(props || value, function(subValue, key2) {
            if (props) {
              key2 = subValue;
              subValue = value[key2];
            }
            assignValue(result2, key2, baseClone(subValue, bitmask, customizer, key2, value, stack));
          });
          return result2;
        }
        function baseConforms(source) {
          var props = keys(source);
          return function(object) {
            return baseConformsTo(object, source, props);
          };
        }
        function baseConformsTo(object, source, props) {
          var length2 = props.length;
          if (object == null) {
            return !length2;
          }
          object = Object2(object);
          while (length2--) {
            var key = props[length2], predicate = source[key], value = object[key];
            if (value === undefined2 && !(key in object) || !predicate(value)) {
              return false;
            }
          }
          return true;
        }
        function baseDelay(func, wait, args) {
          if (typeof func != "function") {
            throw new TypeError2(FUNC_ERROR_TEXT);
          }
          return setTimeout2(function() {
            func.apply(undefined2, args);
          }, wait);
        }
        function baseDifference(array, values2, iteratee2, comparator) {
          var index = -1, includes2 = arrayIncludes, isCommon = true, length2 = array.length, result2 = [], valuesLength = values2.length;
          if (!length2) {
            return result2;
          }
          if (iteratee2) {
            values2 = arrayMap(values2, baseUnary(iteratee2));
          }
          if (comparator) {
            includes2 = arrayIncludesWith;
            isCommon = false;
          } else if (values2.length >= LARGE_ARRAY_SIZE) {
            includes2 = cacheHas;
            isCommon = false;
            values2 = new SetCache(values2);
          }
          outer:
            while (++index < length2) {
              var value = array[index], computed = iteratee2 == null ? value : iteratee2(value);
              value = comparator || value !== 0 ? value : 0;
              if (isCommon && computed === computed) {
                var valuesIndex = valuesLength;
                while (valuesIndex--) {
                  if (values2[valuesIndex] === computed) {
                    continue outer;
                  }
                }
                result2.push(value);
              } else if (!includes2(values2, computed, comparator)) {
                result2.push(value);
              }
            }
          return result2;
        }
        var baseEach = createBaseEach(baseForOwn);
        var baseEachRight = createBaseEach(baseForOwnRight, true);
        function baseEvery(collection, predicate) {
          var result2 = true;
          baseEach(collection, function(value, index, collection2) {
            result2 = !!predicate(value, index, collection2);
            return result2;
          });
          return result2;
        }
        function baseExtremum(array, iteratee2, comparator) {
          var index = -1, length2 = array.length;
          while (++index < length2) {
            var value = array[index], current = iteratee2(value);
            if (current != null && (computed === undefined2 ? current === current && !isSymbol(current) : comparator(current, computed))) {
              var computed = current, result2 = value;
            }
          }
          return result2;
        }
        function baseFill(array, value, start, end) {
          var length2 = array.length;
          start = toInteger(start);
          if (start < 0) {
            start = -start > length2 ? 0 : length2 + start;
          }
          end = end === undefined2 || end > length2 ? length2 : toInteger(end);
          if (end < 0) {
            end += length2;
          }
          end = start > end ? 0 : toLength(end);
          while (start < end) {
            array[start++] = value;
          }
          return array;
        }
        function baseFilter(collection, predicate) {
          var result2 = [];
          baseEach(collection, function(value, index, collection2) {
            if (predicate(value, index, collection2)) {
              result2.push(value);
            }
          });
          return result2;
        }
        function baseFlatten(array, depth, predicate, isStrict, result2) {
          var index = -1, length2 = array.length;
          predicate || (predicate = isFlattenable);
          result2 || (result2 = []);
          while (++index < length2) {
            var value = array[index];
            if (depth > 0 && predicate(value)) {
              if (depth > 1) {
                baseFlatten(value, depth - 1, predicate, isStrict, result2);
              } else {
                arrayPush(result2, value);
              }
            } else if (!isStrict) {
              result2[result2.length] = value;
            }
          }
          return result2;
        }
        var baseFor = createBaseFor();
        var baseForRight = createBaseFor(true);
        function baseForOwn(object, iteratee2) {
          return object && baseFor(object, iteratee2, keys);
        }
        function baseForOwnRight(object, iteratee2) {
          return object && baseForRight(object, iteratee2, keys);
        }
        function baseFunctions(object, props) {
          return arrayFilter(props, function(key) {
            return isFunction(object[key]);
          });
        }
        function baseGet(object, path) {
          path = castPath(path, object);
          var index = 0, length2 = path.length;
          while (object != null && index < length2) {
            object = object[toKey(path[index++])];
          }
          return index && index == length2 ? object : undefined2;
        }
        function baseGetAllKeys(object, keysFunc, symbolsFunc) {
          var result2 = keysFunc(object);
          return isArray(object) ? result2 : arrayPush(result2, symbolsFunc(object));
        }
        function baseGetTag(value) {
          if (value == null) {
            return value === undefined2 ? undefinedTag : nullTag;
          }
          return symToStringTag && symToStringTag in Object2(value) ? getRawTag(value) : objectToString(value);
        }
        function baseGt(value, other) {
          return value > other;
        }
        function baseHas(object, key) {
          return object != null && hasOwnProperty.call(object, key);
        }
        function baseHasIn(object, key) {
          return object != null && key in Object2(object);
        }
        function baseInRange(number, start, end) {
          return number >= nativeMin(start, end) && number < nativeMax(start, end);
        }
        function baseIntersection(arrays, iteratee2, comparator) {
          var includes2 = comparator ? arrayIncludesWith : arrayIncludes, length2 = arrays[0].length, othLength = arrays.length, othIndex = othLength, caches = Array2(othLength), maxLength = Infinity, result2 = [];
          while (othIndex--) {
            var array = arrays[othIndex];
            if (othIndex && iteratee2) {
              array = arrayMap(array, baseUnary(iteratee2));
            }
            maxLength = nativeMin(array.length, maxLength);
            caches[othIndex] = !comparator && (iteratee2 || length2 >= 120 && array.length >= 120) ? new SetCache(othIndex && array) : undefined2;
          }
          array = arrays[0];
          var index = -1, seen = caches[0];
          outer:
            while (++index < length2 && result2.length < maxLength) {
              var value = array[index], computed = iteratee2 ? iteratee2(value) : value;
              value = comparator || value !== 0 ? value : 0;
              if (!(seen ? cacheHas(seen, computed) : includes2(result2, computed, comparator))) {
                othIndex = othLength;
                while (--othIndex) {
                  var cache = caches[othIndex];
                  if (!(cache ? cacheHas(cache, computed) : includes2(arrays[othIndex], computed, comparator))) {
                    continue outer;
                  }
                }
                if (seen) {
                  seen.push(computed);
                }
                result2.push(value);
              }
            }
          return result2;
        }
        function baseInverter(object, setter, iteratee2, accumulator) {
          baseForOwn(object, function(value, key, object2) {
            setter(accumulator, iteratee2(value), key, object2);
          });
          return accumulator;
        }
        function baseInvoke(object, path, args) {
          path = castPath(path, object);
          object = parent(object, path);
          var func = object == null ? object : object[toKey(last(path))];
          return func == null ? undefined2 : apply(func, object, args);
        }
        function baseIsArguments(value) {
          return isObjectLike(value) && baseGetTag(value) == argsTag;
        }
        function baseIsArrayBuffer(value) {
          return isObjectLike(value) && baseGetTag(value) == arrayBufferTag;
        }
        function baseIsDate(value) {
          return isObjectLike(value) && baseGetTag(value) == dateTag;
        }
        function baseIsEqual(value, other, bitmask, customizer, stack) {
          if (value === other) {
            return true;
          }
          if (value == null || other == null || !isObjectLike(value) && !isObjectLike(other)) {
            return value !== value && other !== other;
          }
          return baseIsEqualDeep(value, other, bitmask, customizer, baseIsEqual, stack);
        }
        function baseIsEqualDeep(object, other, bitmask, customizer, equalFunc, stack) {
          var objIsArr = isArray(object), othIsArr = isArray(other), objTag = objIsArr ? arrayTag : getTag(object), othTag = othIsArr ? arrayTag : getTag(other);
          objTag = objTag == argsTag ? objectTag : objTag;
          othTag = othTag == argsTag ? objectTag : othTag;
          var objIsObj = objTag == objectTag, othIsObj = othTag == objectTag, isSameTag = objTag == othTag;
          if (isSameTag && isBuffer(object)) {
            if (!isBuffer(other)) {
              return false;
            }
            objIsArr = true;
            objIsObj = false;
          }
          if (isSameTag && !objIsObj) {
            stack || (stack = new Stack());
            return objIsArr || isTypedArray(object) ? equalArrays(object, other, bitmask, customizer, equalFunc, stack) : equalByTag(object, other, objTag, bitmask, customizer, equalFunc, stack);
          }
          if (!(bitmask & COMPARE_PARTIAL_FLAG)) {
            var objIsWrapped = objIsObj && hasOwnProperty.call(object, "__wrapped__"), othIsWrapped = othIsObj && hasOwnProperty.call(other, "__wrapped__");
            if (objIsWrapped || othIsWrapped) {
              var objUnwrapped = objIsWrapped ? object.value() : object, othUnwrapped = othIsWrapped ? other.value() : other;
              stack || (stack = new Stack());
              return equalFunc(objUnwrapped, othUnwrapped, bitmask, customizer, stack);
            }
          }
          if (!isSameTag) {
            return false;
          }
          stack || (stack = new Stack());
          return equalObjects(object, other, bitmask, customizer, equalFunc, stack);
        }
        function baseIsMap(value) {
          return isObjectLike(value) && getTag(value) == mapTag;
        }
        function baseIsMatch(object, source, matchData, customizer) {
          var index = matchData.length, length2 = index, noCustomizer = !customizer;
          if (object == null) {
            return !length2;
          }
          object = Object2(object);
          while (index--) {
            var data = matchData[index];
            if (noCustomizer && data[2] ? data[1] !== object[data[0]] : !(data[0] in object)) {
              return false;
            }
          }
          while (++index < length2) {
            data = matchData[index];
            var key = data[0], objValue = object[key], srcValue = data[1];
            if (noCustomizer && data[2]) {
              if (objValue === undefined2 && !(key in object)) {
                return false;
              }
            } else {
              var stack = new Stack();
              if (customizer) {
                var result2 = customizer(objValue, srcValue, key, object, source, stack);
              }
              if (!(result2 === undefined2 ? baseIsEqual(srcValue, objValue, COMPARE_PARTIAL_FLAG | COMPARE_UNORDERED_FLAG, customizer, stack) : result2)) {
                return false;
              }
            }
          }
          return true;
        }
        function baseIsNative(value) {
          if (!isObject(value) || isMasked(value)) {
            return false;
          }
          var pattern = isFunction(value) ? reIsNative : reIsHostCtor;
          return pattern.test(toSource(value));
        }
        function baseIsRegExp(value) {
          return isObjectLike(value) && baseGetTag(value) == regexpTag;
        }
        function baseIsSet(value) {
          return isObjectLike(value) && getTag(value) == setTag;
        }
        function baseIsTypedArray(value) {
          return isObjectLike(value) && isLength(value.length) && !!typedArrayTags[baseGetTag(value)];
        }
        function baseIteratee(value) {
          if (typeof value == "function") {
            return value;
          }
          if (value == null) {
            return identity2;
          }
          if (typeof value == "object") {
            return isArray(value) ? baseMatchesProperty(value[0], value[1]) : baseMatches(value);
          }
          return property(value);
        }
        function baseKeys(object) {
          if (!isPrototype(object)) {
            return nativeKeys(object);
          }
          var result2 = [];
          for (var key in Object2(object)) {
            if (hasOwnProperty.call(object, key) && key != "constructor") {
              result2.push(key);
            }
          }
          return result2;
        }
        function baseKeysIn(object) {
          if (!isObject(object)) {
            return nativeKeysIn(object);
          }
          var isProto = isPrototype(object), result2 = [];
          for (var key in object) {
            if (!(key == "constructor" && (isProto || !hasOwnProperty.call(object, key)))) {
              result2.push(key);
            }
          }
          return result2;
        }
        function baseLt(value, other) {
          return value < other;
        }
        function baseMap(collection, iteratee2) {
          var index = -1, result2 = isArrayLike(collection) ? Array2(collection.length) : [];
          baseEach(collection, function(value, key, collection2) {
            result2[++index] = iteratee2(value, key, collection2);
          });
          return result2;
        }
        function baseMatches(source) {
          var matchData = getMatchData(source);
          if (matchData.length == 1 && matchData[0][2]) {
            return matchesStrictComparable(matchData[0][0], matchData[0][1]);
          }
          return function(object) {
            return object === source || baseIsMatch(object, source, matchData);
          };
        }
        function baseMatchesProperty(path, srcValue) {
          if (isKey(path) && isStrictComparable(srcValue)) {
            return matchesStrictComparable(toKey(path), srcValue);
          }
          return function(object) {
            var objValue = get(object, path);
            return objValue === undefined2 && objValue === srcValue ? hasIn(object, path) : baseIsEqual(srcValue, objValue, COMPARE_PARTIAL_FLAG | COMPARE_UNORDERED_FLAG);
          };
        }
        function baseMerge(object, source, srcIndex, customizer, stack) {
          if (object === source) {
            return;
          }
          baseFor(source, function(srcValue, key) {
            stack || (stack = new Stack());
            if (isObject(srcValue)) {
              baseMergeDeep(object, source, key, srcIndex, baseMerge, customizer, stack);
            } else {
              var newValue = customizer ? customizer(safeGet(object, key), srcValue, key + "", object, source, stack) : undefined2;
              if (newValue === undefined2) {
                newValue = srcValue;
              }
              assignMergeValue(object, key, newValue);
            }
          }, keysIn);
        }
        function baseMergeDeep(object, source, key, srcIndex, mergeFunc, customizer, stack) {
          var objValue = safeGet(object, key), srcValue = safeGet(source, key), stacked = stack.get(srcValue);
          if (stacked) {
            assignMergeValue(object, key, stacked);
            return;
          }
          var newValue = customizer ? customizer(objValue, srcValue, key + "", object, source, stack) : undefined2;
          var isCommon = newValue === undefined2;
          if (isCommon) {
            var isArr = isArray(srcValue), isBuff = !isArr && isBuffer(srcValue), isTyped = !isArr && !isBuff && isTypedArray(srcValue);
            newValue = srcValue;
            if (isArr || isBuff || isTyped) {
              if (isArray(objValue)) {
                newValue = objValue;
              } else if (isArrayLikeObject(objValue)) {
                newValue = copyArray(objValue);
              } else if (isBuff) {
                isCommon = false;
                newValue = cloneBuffer(srcValue, true);
              } else if (isTyped) {
                isCommon = false;
                newValue = cloneTypedArray(srcValue, true);
              } else {
                newValue = [];
              }
            } else if (isPlainObject(srcValue) || isArguments(srcValue)) {
              newValue = objValue;
              if (isArguments(objValue)) {
                newValue = toPlainObject(objValue);
              } else if (!isObject(objValue) || isFunction(objValue)) {
                newValue = initCloneObject(srcValue);
              }
            } else {
              isCommon = false;
            }
          }
          if (isCommon) {
            stack.set(srcValue, newValue);
            mergeFunc(newValue, srcValue, srcIndex, customizer, stack);
            stack["delete"](srcValue);
          }
          assignMergeValue(object, key, newValue);
        }
        function baseNth(array, n15) {
          var length2 = array.length;
          if (!length2) {
            return;
          }
          n15 += n15 < 0 ? length2 : 0;
          return isIndex(n15, length2) ? array[n15] : undefined2;
        }
        function baseOrderBy(collection, iteratees, orders) {
          if (iteratees.length) {
            iteratees = arrayMap(iteratees, function(iteratee2) {
              if (isArray(iteratee2)) {
                return function(value) {
                  return baseGet(value, iteratee2.length === 1 ? iteratee2[0] : iteratee2);
                };
              }
              return iteratee2;
            });
          } else {
            iteratees = [identity2];
          }
          var index = -1;
          iteratees = arrayMap(iteratees, baseUnary(getIteratee()));
          var result2 = baseMap(collection, function(value, key, collection2) {
            var criteria = arrayMap(iteratees, function(iteratee2) {
              return iteratee2(value);
            });
            return { "criteria": criteria, "index": ++index, "value": value };
          });
          return baseSortBy(result2, function(object, other) {
            return compareMultiple(object, other, orders);
          });
        }
        function basePick(object, paths) {
          return basePickBy(object, paths, function(value, path) {
            return hasIn(object, path);
          });
        }
        function basePickBy(object, paths, predicate) {
          var index = -1, length2 = paths.length, result2 = {};
          while (++index < length2) {
            var path = paths[index], value = baseGet(object, path);
            if (predicate(value, path)) {
              baseSet(result2, castPath(path, object), value);
            }
          }
          return result2;
        }
        function basePropertyDeep(path) {
          return function(object) {
            return baseGet(object, path);
          };
        }
        function basePullAll(array, values2, iteratee2, comparator) {
          var indexOf2 = comparator ? baseIndexOfWith : baseIndexOf, index = -1, length2 = values2.length, seen = array;
          if (array === values2) {
            values2 = copyArray(values2);
          }
          if (iteratee2) {
            seen = arrayMap(array, baseUnary(iteratee2));
          }
          while (++index < length2) {
            var fromIndex = 0, value = values2[index], computed = iteratee2 ? iteratee2(value) : value;
            while ((fromIndex = indexOf2(seen, computed, fromIndex, comparator)) > -1) {
              if (seen !== array) {
                splice.call(seen, fromIndex, 1);
              }
              splice.call(array, fromIndex, 1);
            }
          }
          return array;
        }
        function basePullAt(array, indexes) {
          var length2 = array ? indexes.length : 0, lastIndex = length2 - 1;
          while (length2--) {
            var index = indexes[length2];
            if (length2 == lastIndex || index !== previous) {
              var previous = index;
              if (isIndex(index)) {
                splice.call(array, index, 1);
              } else {
                baseUnset(array, index);
              }
            }
          }
          return array;
        }
        function baseRandom(lower, upper) {
          return lower + nativeFloor(nativeRandom() * (upper - lower + 1));
        }
        function baseRange(start, end, step, fromRight) {
          var index = -1, length2 = nativeMax(nativeCeil((end - start) / (step || 1)), 0), result2 = Array2(length2);
          while (length2--) {
            result2[fromRight ? length2 : ++index] = start;
            start += step;
          }
          return result2;
        }
        function baseRepeat(string, n15) {
          var result2 = "";
          if (!string || n15 < 1 || n15 > MAX_SAFE_INTEGER) {
            return result2;
          }
          do {
            if (n15 % 2) {
              result2 += string;
            }
            n15 = nativeFloor(n15 / 2);
            if (n15) {
              string += string;
            }
          } while (n15);
          return result2;
        }
        function baseRest(func, start) {
          return setToString(overRest(func, start, identity2), func + "");
        }
        function baseSample(collection) {
          return arraySample(values(collection));
        }
        function baseSampleSize(collection, n15) {
          var array = values(collection);
          return shuffleSelf(array, baseClamp(n15, 0, array.length));
        }
        function baseSet(object, path, value, customizer) {
          if (!isObject(object)) {
            return object;
          }
          path = castPath(path, object);
          var index = -1, length2 = path.length, lastIndex = length2 - 1, nested = object;
          while (nested != null && ++index < length2) {
            var key = toKey(path[index]), newValue = value;
            if (key === "__proto__" || key === "constructor" || key === "prototype") {
              return object;
            }
            if (index != lastIndex) {
              var objValue = nested[key];
              newValue = customizer ? customizer(objValue, key, nested) : undefined2;
              if (newValue === undefined2) {
                newValue = isObject(objValue) ? objValue : isIndex(path[index + 1]) ? [] : {};
              }
            }
            assignValue(nested, key, newValue);
            nested = nested[key];
          }
          return object;
        }
        var baseSetData = !metaMap ? identity2 : function(func, data) {
          metaMap.set(func, data);
          return func;
        };
        var baseSetToString = !defineProperty ? identity2 : function(func, string) {
          return defineProperty(func, "toString", {
            "configurable": true,
            "enumerable": false,
            "value": constant(string),
            "writable": true
          });
        };
        function baseShuffle(collection) {
          return shuffleSelf(values(collection));
        }
        function baseSlice(array, start, end) {
          var index = -1, length2 = array.length;
          if (start < 0) {
            start = -start > length2 ? 0 : length2 + start;
          }
          end = end > length2 ? length2 : end;
          if (end < 0) {
            end += length2;
          }
          length2 = start > end ? 0 : end - start >>> 0;
          start >>>= 0;
          var result2 = Array2(length2);
          while (++index < length2) {
            result2[index] = array[index + start];
          }
          return result2;
        }
        function baseSome(collection, predicate) {
          var result2;
          baseEach(collection, function(value, index, collection2) {
            result2 = predicate(value, index, collection2);
            return !result2;
          });
          return !!result2;
        }
        function baseSortedIndex(array, value, retHighest) {
          var low = 0, high = array == null ? low : array.length;
          if (typeof value == "number" && value === value && high <= HALF_MAX_ARRAY_LENGTH) {
            while (low < high) {
              var mid = low + high >>> 1, computed = array[mid];
              if (computed !== null && !isSymbol(computed) && (retHighest ? computed <= value : computed < value)) {
                low = mid + 1;
              } else {
                high = mid;
              }
            }
            return high;
          }
          return baseSortedIndexBy(array, value, identity2, retHighest);
        }
        function baseSortedIndexBy(array, value, iteratee2, retHighest) {
          var low = 0, high = array == null ? 0 : array.length;
          if (high === 0) {
            return 0;
          }
          value = iteratee2(value);
          var valIsNaN = value !== value, valIsNull = value === null, valIsSymbol = isSymbol(value), valIsUndefined = value === undefined2;
          while (low < high) {
            var mid = nativeFloor((low + high) / 2), computed = iteratee2(array[mid]), othIsDefined = computed !== undefined2, othIsNull = computed === null, othIsReflexive = computed === computed, othIsSymbol = isSymbol(computed);
            if (valIsNaN) {
              var setLow = retHighest || othIsReflexive;
            } else if (valIsUndefined) {
              setLow = othIsReflexive && (retHighest || othIsDefined);
            } else if (valIsNull) {
              setLow = othIsReflexive && othIsDefined && (retHighest || !othIsNull);
            } else if (valIsSymbol) {
              setLow = othIsReflexive && othIsDefined && !othIsNull && (retHighest || !othIsSymbol);
            } else if (othIsNull || othIsSymbol) {
              setLow = false;
            } else {
              setLow = retHighest ? computed <= value : computed < value;
            }
            if (setLow) {
              low = mid + 1;
            } else {
              high = mid;
            }
          }
          return nativeMin(high, MAX_ARRAY_INDEX);
        }
        function baseSortedUniq(array, iteratee2) {
          var index = -1, length2 = array.length, resIndex = 0, result2 = [];
          while (++index < length2) {
            var value = array[index], computed = iteratee2 ? iteratee2(value) : value;
            if (!index || !eq(computed, seen)) {
              var seen = computed;
              result2[resIndex++] = value === 0 ? 0 : value;
            }
          }
          return result2;
        }
        function baseToNumber(value) {
          if (typeof value == "number") {
            return value;
          }
          if (isSymbol(value)) {
            return NAN;
          }
          return +value;
        }
        function baseToString(value) {
          if (typeof value == "string") {
            return value;
          }
          if (isArray(value)) {
            return arrayMap(value, baseToString) + "";
          }
          if (isSymbol(value)) {
            return symbolToString ? symbolToString.call(value) : "";
          }
          var result2 = value + "";
          return result2 == "0" && 1 / value == -INFINITY ? "-0" : result2;
        }
        function baseUniq(array, iteratee2, comparator) {
          var index = -1, includes2 = arrayIncludes, length2 = array.length, isCommon = true, result2 = [], seen = result2;
          if (comparator) {
            isCommon = false;
            includes2 = arrayIncludesWith;
          } else if (length2 >= LARGE_ARRAY_SIZE) {
            var set2 = iteratee2 ? null : createSet(array);
            if (set2) {
              return setToArray(set2);
            }
            isCommon = false;
            includes2 = cacheHas;
            seen = new SetCache();
          } else {
            seen = iteratee2 ? [] : result2;
          }
          outer:
            while (++index < length2) {
              var value = array[index], computed = iteratee2 ? iteratee2(value) : value;
              value = comparator || value !== 0 ? value : 0;
              if (isCommon && computed === computed) {
                var seenIndex = seen.length;
                while (seenIndex--) {
                  if (seen[seenIndex] === computed) {
                    continue outer;
                  }
                }
                if (iteratee2) {
                  seen.push(computed);
                }
                result2.push(value);
              } else if (!includes2(seen, computed, comparator)) {
                if (seen !== result2) {
                  seen.push(computed);
                }
                result2.push(value);
              }
            }
          return result2;
        }
        function baseUnset(object, path) {
          path = castPath(path, object);
          object = parent(object, path);
          return object == null || delete object[toKey(last(path))];
        }
        function baseUpdate(object, path, updater, customizer) {
          return baseSet(object, path, updater(baseGet(object, path)), customizer);
        }
        function baseWhile(array, predicate, isDrop, fromRight) {
          var length2 = array.length, index = fromRight ? length2 : -1;
          while ((fromRight ? index-- : ++index < length2) && predicate(array[index], index, array)) {
          }
          return isDrop ? baseSlice(array, fromRight ? 0 : index, fromRight ? index + 1 : length2) : baseSlice(array, fromRight ? index + 1 : 0, fromRight ? length2 : index);
        }
        function baseWrapperValue(value, actions) {
          var result2 = value;
          if (result2 instanceof LazyWrapper) {
            result2 = result2.value();
          }
          return arrayReduce(actions, function(result3, action) {
            return action.func.apply(action.thisArg, arrayPush([result3], action.args));
          }, result2);
        }
        function baseXor(arrays, iteratee2, comparator) {
          var length2 = arrays.length;
          if (length2 < 2) {
            return length2 ? baseUniq(arrays[0]) : [];
          }
          var index = -1, result2 = Array2(length2);
          while (++index < length2) {
            var array = arrays[index], othIndex = -1;
            while (++othIndex < length2) {
              if (othIndex != index) {
                result2[index] = baseDifference(result2[index] || array, arrays[othIndex], iteratee2, comparator);
              }
            }
          }
          return baseUniq(baseFlatten(result2, 1), iteratee2, comparator);
        }
        function baseZipObject(props, values2, assignFunc) {
          var index = -1, length2 = props.length, valsLength = values2.length, result2 = {};
          while (++index < length2) {
            var value = index < valsLength ? values2[index] : undefined2;
            assignFunc(result2, props[index], value);
          }
          return result2;
        }
        function castArrayLikeObject(value) {
          return isArrayLikeObject(value) ? value : [];
        }
        function castFunction(value) {
          return typeof value == "function" ? value : identity2;
        }
        function castPath(value, object) {
          if (isArray(value)) {
            return value;
          }
          return isKey(value, object) ? [value] : stringToPath(toString(value));
        }
        var castRest = baseRest;
        function castSlice(array, start, end) {
          var length2 = array.length;
          end = end === undefined2 ? length2 : end;
          return !start && end >= length2 ? array : baseSlice(array, start, end);
        }
        var clearTimeout2 = ctxClearTimeout || function(id) {
          return root.clearTimeout(id);
        };
        function cloneBuffer(buffer, isDeep) {
          if (isDeep) {
            return buffer.slice();
          }
          var length2 = buffer.length, result2 = allocUnsafe ? allocUnsafe(length2) : new buffer.constructor(length2);
          buffer.copy(result2);
          return result2;
        }
        function cloneArrayBuffer(arrayBuffer) {
          var result2 = new arrayBuffer.constructor(arrayBuffer.byteLength);
          new Uint8Array(result2).set(new Uint8Array(arrayBuffer));
          return result2;
        }
        function cloneDataView(dataView, isDeep) {
          var buffer = isDeep ? cloneArrayBuffer(dataView.buffer) : dataView.buffer;
          return new dataView.constructor(buffer, dataView.byteOffset, dataView.byteLength);
        }
        function cloneRegExp(regexp) {
          var result2 = new regexp.constructor(regexp.source, reFlags.exec(regexp));
          result2.lastIndex = regexp.lastIndex;
          return result2;
        }
        function cloneSymbol(symbol) {
          return symbolValueOf ? Object2(symbolValueOf.call(symbol)) : {};
        }
        function cloneTypedArray(typedArray, isDeep) {
          var buffer = isDeep ? cloneArrayBuffer(typedArray.buffer) : typedArray.buffer;
          return new typedArray.constructor(buffer, typedArray.byteOffset, typedArray.length);
        }
        function compareAscending(value, other) {
          if (value !== other) {
            var valIsDefined = value !== undefined2, valIsNull = value === null, valIsReflexive = value === value, valIsSymbol = isSymbol(value);
            var othIsDefined = other !== undefined2, othIsNull = other === null, othIsReflexive = other === other, othIsSymbol = isSymbol(other);
            if (!othIsNull && !othIsSymbol && !valIsSymbol && value > other || valIsSymbol && othIsDefined && othIsReflexive && !othIsNull && !othIsSymbol || valIsNull && othIsDefined && othIsReflexive || !valIsDefined && othIsReflexive || !valIsReflexive) {
              return 1;
            }
            if (!valIsNull && !valIsSymbol && !othIsSymbol && value < other || othIsSymbol && valIsDefined && valIsReflexive && !valIsNull && !valIsSymbol || othIsNull && valIsDefined && valIsReflexive || !othIsDefined && valIsReflexive || !othIsReflexive) {
              return -1;
            }
          }
          return 0;
        }
        function compareMultiple(object, other, orders) {
          var index = -1, objCriteria = object.criteria, othCriteria = other.criteria, length2 = objCriteria.length, ordersLength = orders.length;
          while (++index < length2) {
            var result2 = compareAscending(objCriteria[index], othCriteria[index]);
            if (result2) {
              if (index >= ordersLength) {
                return result2;
              }
              var order = orders[index];
              return result2 * (order == "desc" ? -1 : 1);
            }
          }
          return object.index - other.index;
        }
        function composeArgs(args, partials, holders, isCurried) {
          var argsIndex = -1, argsLength = args.length, holdersLength = holders.length, leftIndex = -1, leftLength = partials.length, rangeLength = nativeMax(argsLength - holdersLength, 0), result2 = Array2(leftLength + rangeLength), isUncurried = !isCurried;
          while (++leftIndex < leftLength) {
            result2[leftIndex] = partials[leftIndex];
          }
          while (++argsIndex < holdersLength) {
            if (isUncurried || argsIndex < argsLength) {
              result2[holders[argsIndex]] = args[argsIndex];
            }
          }
          while (rangeLength--) {
            result2[leftIndex++] = args[argsIndex++];
          }
          return result2;
        }
        function composeArgsRight(args, partials, holders, isCurried) {
          var argsIndex = -1, argsLength = args.length, holdersIndex = -1, holdersLength = holders.length, rightIndex = -1, rightLength = partials.length, rangeLength = nativeMax(argsLength - holdersLength, 0), result2 = Array2(rangeLength + rightLength), isUncurried = !isCurried;
          while (++argsIndex < rangeLength) {
            result2[argsIndex] = args[argsIndex];
          }
          var offset = argsIndex;
          while (++rightIndex < rightLength) {
            result2[offset + rightIndex] = partials[rightIndex];
          }
          while (++holdersIndex < holdersLength) {
            if (isUncurried || argsIndex < argsLength) {
              result2[offset + holders[holdersIndex]] = args[argsIndex++];
            }
          }
          return result2;
        }
        function copyArray(source, array) {
          var index = -1, length2 = source.length;
          array || (array = Array2(length2));
          while (++index < length2) {
            array[index] = source[index];
          }
          return array;
        }
        function copyObject(source, props, object, customizer) {
          var isNew = !object;
          object || (object = {});
          var index = -1, length2 = props.length;
          while (++index < length2) {
            var key = props[index];
            var newValue = customizer ? customizer(object[key], source[key], key, object, source) : undefined2;
            if (newValue === undefined2) {
              newValue = source[key];
            }
            if (isNew) {
              baseAssignValue(object, key, newValue);
            } else {
              assignValue(object, key, newValue);
            }
          }
          return object;
        }
        function copySymbols(source, object) {
          return copyObject(source, getSymbols(source), object);
        }
        function copySymbolsIn(source, object) {
          return copyObject(source, getSymbolsIn(source), object);
        }
        function createAggregator(setter, initializer) {
          return function(collection, iteratee2) {
            var func = isArray(collection) ? arrayAggregator : baseAggregator, accumulator = initializer ? initializer() : {};
            return func(collection, setter, getIteratee(iteratee2, 2), accumulator);
          };
        }
        function createAssigner(assigner) {
          return baseRest(function(object, sources) {
            var index = -1, length2 = sources.length, customizer = length2 > 1 ? sources[length2 - 1] : undefined2, guard = length2 > 2 ? sources[2] : undefined2;
            customizer = assigner.length > 3 && typeof customizer == "function" ? (length2--, customizer) : undefined2;
            if (guard && isIterateeCall(sources[0], sources[1], guard)) {
              customizer = length2 < 3 ? undefined2 : customizer;
              length2 = 1;
            }
            object = Object2(object);
            while (++index < length2) {
              var source = sources[index];
              if (source) {
                assigner(object, source, index, customizer);
              }
            }
            return object;
          });
        }
        function createBaseEach(eachFunc, fromRight) {
          return function(collection, iteratee2) {
            if (collection == null) {
              return collection;
            }
            if (!isArrayLike(collection)) {
              return eachFunc(collection, iteratee2);
            }
            var length2 = collection.length, index = fromRight ? length2 : -1, iterable = Object2(collection);
            while (fromRight ? index-- : ++index < length2) {
              if (iteratee2(iterable[index], index, iterable) === false) {
                break;
              }
            }
            return collection;
          };
        }
        function createBaseFor(fromRight) {
          return function(object, iteratee2, keysFunc) {
            var index = -1, iterable = Object2(object), props = keysFunc(object), length2 = props.length;
            while (length2--) {
              var key = props[fromRight ? length2 : ++index];
              if (iteratee2(iterable[key], key, iterable) === false) {
                break;
              }
            }
            return object;
          };
        }
        function createBind(func, bitmask, thisArg) {
          var isBind = bitmask & WRAP_BIND_FLAG, Ctor = createCtor(func);
          function wrapper() {
            var fn = this && this !== root && this instanceof wrapper ? Ctor : func;
            return fn.apply(isBind ? thisArg : this, arguments);
          }
          return wrapper;
        }
        function createCaseFirst(methodName) {
          return function(string) {
            string = toString(string);
            var strSymbols = hasUnicode(string) ? stringToArray(string) : undefined2;
            var chr = strSymbols ? strSymbols[0] : string.charAt(0);
            var trailing = strSymbols ? castSlice(strSymbols, 1).join("") : string.slice(1);
            return chr[methodName]() + trailing;
          };
        }
        function createCompounder(callback) {
          return function(string) {
            return arrayReduce(words(deburr(string).replace(reApos, "")), callback, "");
          };
        }
        function createCtor(Ctor) {
          return function() {
            var args = arguments;
            switch (args.length) {
              case 0:
                return new Ctor();
              case 1:
                return new Ctor(args[0]);
              case 2:
                return new Ctor(args[0], args[1]);
              case 3:
                return new Ctor(args[0], args[1], args[2]);
              case 4:
                return new Ctor(args[0], args[1], args[2], args[3]);
              case 5:
                return new Ctor(args[0], args[1], args[2], args[3], args[4]);
              case 6:
                return new Ctor(args[0], args[1], args[2], args[3], args[4], args[5]);
              case 7:
                return new Ctor(args[0], args[1], args[2], args[3], args[4], args[5], args[6]);
            }
            var thisBinding = baseCreate(Ctor.prototype), result2 = Ctor.apply(thisBinding, args);
            return isObject(result2) ? result2 : thisBinding;
          };
        }
        function createCurry(func, bitmask, arity) {
          var Ctor = createCtor(func);
          function wrapper() {
            var length2 = arguments.length, args = Array2(length2), index = length2, placeholder = getHolder(wrapper);
            while (index--) {
              args[index] = arguments[index];
            }
            var holders = length2 < 3 && args[0] !== placeholder && args[length2 - 1] !== placeholder ? [] : replaceHolders(args, placeholder);
            length2 -= holders.length;
            if (length2 < arity) {
              return createRecurry(
                func,
                bitmask,
                createHybrid,
                wrapper.placeholder,
                undefined2,
                args,
                holders,
                undefined2,
                undefined2,
                arity - length2
              );
            }
            var fn = this && this !== root && this instanceof wrapper ? Ctor : func;
            return apply(fn, this, args);
          }
          return wrapper;
        }
        function createFind(findIndexFunc) {
          return function(collection, predicate, fromIndex) {
            var iterable = Object2(collection);
            if (!isArrayLike(collection)) {
              var iteratee2 = getIteratee(predicate, 3);
              collection = keys(collection);
              predicate = function(key) {
                return iteratee2(iterable[key], key, iterable);
              };
            }
            var index = findIndexFunc(collection, predicate, fromIndex);
            return index > -1 ? iterable[iteratee2 ? collection[index] : index] : undefined2;
          };
        }
        function createFlow(fromRight) {
          return flatRest(function(funcs) {
            var length2 = funcs.length, index = length2, prereq = LodashWrapper.prototype.thru;
            if (fromRight) {
              funcs.reverse();
            }
            while (index--) {
              var func = funcs[index];
              if (typeof func != "function") {
                throw new TypeError2(FUNC_ERROR_TEXT);
              }
              if (prereq && !wrapper && getFuncName(func) == "wrapper") {
                var wrapper = new LodashWrapper([], true);
              }
            }
            index = wrapper ? index : length2;
            while (++index < length2) {
              func = funcs[index];
              var funcName = getFuncName(func), data = funcName == "wrapper" ? getData(func) : undefined2;
              if (data && isLaziable(data[0]) && data[1] == (WRAP_ARY_FLAG | WRAP_CURRY_FLAG | WRAP_PARTIAL_FLAG | WRAP_REARG_FLAG) && !data[4].length && data[9] == 1) {
                wrapper = wrapper[getFuncName(data[0])].apply(wrapper, data[3]);
              } else {
                wrapper = func.length == 1 && isLaziable(func) ? wrapper[funcName]() : wrapper.thru(func);
              }
            }
            return function() {
              var args = arguments, value = args[0];
              if (wrapper && args.length == 1 && isArray(value)) {
                return wrapper.plant(value).value();
              }
              var index2 = 0, result2 = length2 ? funcs[index2].apply(this, args) : value;
              while (++index2 < length2) {
                result2 = funcs[index2].call(this, result2);
              }
              return result2;
            };
          });
        }
        function createHybrid(func, bitmask, thisArg, partials, holders, partialsRight, holdersRight, argPos, ary2, arity) {
          var isAry = bitmask & WRAP_ARY_FLAG, isBind = bitmask & WRAP_BIND_FLAG, isBindKey = bitmask & WRAP_BIND_KEY_FLAG, isCurried = bitmask & (WRAP_CURRY_FLAG | WRAP_CURRY_RIGHT_FLAG), isFlip = bitmask & WRAP_FLIP_FLAG, Ctor = isBindKey ? undefined2 : createCtor(func);
          function wrapper() {
            var length2 = arguments.length, args = Array2(length2), index = length2;
            while (index--) {
              args[index] = arguments[index];
            }
            if (isCurried) {
              var placeholder = getHolder(wrapper), holdersCount = countHolders(args, placeholder);
            }
            if (partials) {
              args = composeArgs(args, partials, holders, isCurried);
            }
            if (partialsRight) {
              args = composeArgsRight(args, partialsRight, holdersRight, isCurried);
            }
            length2 -= holdersCount;
            if (isCurried && length2 < arity) {
              var newHolders = replaceHolders(args, placeholder);
              return createRecurry(
                func,
                bitmask,
                createHybrid,
                wrapper.placeholder,
                thisArg,
                args,
                newHolders,
                argPos,
                ary2,
                arity - length2
              );
            }
            var thisBinding = isBind ? thisArg : this, fn = isBindKey ? thisBinding[func] : func;
            length2 = args.length;
            if (argPos) {
              args = reorder(args, argPos);
            } else if (isFlip && length2 > 1) {
              args.reverse();
            }
            if (isAry && ary2 < length2) {
              args.length = ary2;
            }
            if (this && this !== root && this instanceof wrapper) {
              fn = Ctor || createCtor(fn);
            }
            return fn.apply(thisBinding, args);
          }
          return wrapper;
        }
        function createInverter(setter, toIteratee) {
          return function(object, iteratee2) {
            return baseInverter(object, setter, toIteratee(iteratee2), {});
          };
        }
        function createMathOperation(operator, defaultValue) {
          return function(value, other) {
            var result2;
            if (value === undefined2 && other === undefined2) {
              return defaultValue;
            }
            if (value !== undefined2) {
              result2 = value;
            }
            if (other !== undefined2) {
              if (result2 === undefined2) {
                return other;
              }
              if (typeof value == "string" || typeof other == "string") {
                value = baseToString(value);
                other = baseToString(other);
              } else {
                value = baseToNumber(value);
                other = baseToNumber(other);
              }
              result2 = operator(value, other);
            }
            return result2;
          };
        }
        function createOver(arrayFunc) {
          return flatRest(function(iteratees) {
            iteratees = arrayMap(iteratees, baseUnary(getIteratee()));
            return baseRest(function(args) {
              var thisArg = this;
              return arrayFunc(iteratees, function(iteratee2) {
                return apply(iteratee2, thisArg, args);
              });
            });
          });
        }
        function createPadding(length2, chars) {
          chars = chars === undefined2 ? " " : baseToString(chars);
          var charsLength = chars.length;
          if (charsLength < 2) {
            return charsLength ? baseRepeat(chars, length2) : chars;
          }
          var result2 = baseRepeat(chars, nativeCeil(length2 / stringSize(chars)));
          return hasUnicode(chars) ? castSlice(stringToArray(result2), 0, length2).join("") : result2.slice(0, length2);
        }
        function createPartial(func, bitmask, thisArg, partials) {
          var isBind = bitmask & WRAP_BIND_FLAG, Ctor = createCtor(func);
          function wrapper() {
            var argsIndex = -1, argsLength = arguments.length, leftIndex = -1, leftLength = partials.length, args = Array2(leftLength + argsLength), fn = this && this !== root && this instanceof wrapper ? Ctor : func;
            while (++leftIndex < leftLength) {
              args[leftIndex] = partials[leftIndex];
            }
            while (argsLength--) {
              args[leftIndex++] = arguments[++argsIndex];
            }
            return apply(fn, isBind ? thisArg : this, args);
          }
          return wrapper;
        }
        function createRange(fromRight) {
          return function(start, end, step) {
            if (step && typeof step != "number" && isIterateeCall(start, end, step)) {
              end = step = undefined2;
            }
            start = toFinite(start);
            if (end === undefined2) {
              end = start;
              start = 0;
            } else {
              end = toFinite(end);
            }
            step = step === undefined2 ? start < end ? 1 : -1 : toFinite(step);
            return baseRange(start, end, step, fromRight);
          };
        }
        function createRelationalOperation(operator) {
          return function(value, other) {
            if (!(typeof value == "string" && typeof other == "string")) {
              value = toNumber(value);
              other = toNumber(other);
            }
            return operator(value, other);
          };
        }
        function createRecurry(func, bitmask, wrapFunc, placeholder, thisArg, partials, holders, argPos, ary2, arity) {
          var isCurry = bitmask & WRAP_CURRY_FLAG, newHolders = isCurry ? holders : undefined2, newHoldersRight = isCurry ? undefined2 : holders, newPartials = isCurry ? partials : undefined2, newPartialsRight = isCurry ? undefined2 : partials;
          bitmask |= isCurry ? WRAP_PARTIAL_FLAG : WRAP_PARTIAL_RIGHT_FLAG;
          bitmask &= ~(isCurry ? WRAP_PARTIAL_RIGHT_FLAG : WRAP_PARTIAL_FLAG);
          if (!(bitmask & WRAP_CURRY_BOUND_FLAG)) {
            bitmask &= ~(WRAP_BIND_FLAG | WRAP_BIND_KEY_FLAG);
          }
          var newData = [
            func,
            bitmask,
            thisArg,
            newPartials,
            newHolders,
            newPartialsRight,
            newHoldersRight,
            argPos,
            ary2,
            arity
          ];
          var result2 = wrapFunc.apply(undefined2, newData);
          if (isLaziable(func)) {
            setData(result2, newData);
          }
          result2.placeholder = placeholder;
          return setWrapToString(result2, func, bitmask);
        }
        function createRound(methodName) {
          var func = Math2[methodName];
          return function(number, precision) {
            number = toNumber(number);
            precision = precision == null ? 0 : nativeMin(toInteger(precision), 292);
            if (precision && nativeIsFinite(number)) {
              var pair = (toString(number) + "e").split("e"), value = func(pair[0] + "e" + (+pair[1] + precision));
              pair = (toString(value) + "e").split("e");
              return +(pair[0] + "e" + (+pair[1] - precision));
            }
            return func(number);
          };
        }
        var createSet = !(Set2 && 1 / setToArray(new Set2([, -0]))[1] == INFINITY) ? noop : function(values2) {
          return new Set2(values2);
        };
        function createToPairs(keysFunc) {
          return function(object) {
            var tag = getTag(object);
            if (tag == mapTag) {
              return mapToArray(object);
            }
            if (tag == setTag) {
              return setToPairs(object);
            }
            return baseToPairs(object, keysFunc(object));
          };
        }
        function createWrap(func, bitmask, thisArg, partials, holders, argPos, ary2, arity) {
          var isBindKey = bitmask & WRAP_BIND_KEY_FLAG;
          if (!isBindKey && typeof func != "function") {
            throw new TypeError2(FUNC_ERROR_TEXT);
          }
          var length2 = partials ? partials.length : 0;
          if (!length2) {
            bitmask &= ~(WRAP_PARTIAL_FLAG | WRAP_PARTIAL_RIGHT_FLAG);
            partials = holders = undefined2;
          }
          ary2 = ary2 === undefined2 ? ary2 : nativeMax(toInteger(ary2), 0);
          arity = arity === undefined2 ? arity : toInteger(arity);
          length2 -= holders ? holders.length : 0;
          if (bitmask & WRAP_PARTIAL_RIGHT_FLAG) {
            var partialsRight = partials, holdersRight = holders;
            partials = holders = undefined2;
          }
          var data = isBindKey ? undefined2 : getData(func);
          var newData = [
            func,
            bitmask,
            thisArg,
            partials,
            holders,
            partialsRight,
            holdersRight,
            argPos,
            ary2,
            arity
          ];
          if (data) {
            mergeData(newData, data);
          }
          func = newData[0];
          bitmask = newData[1];
          thisArg = newData[2];
          partials = newData[3];
          holders = newData[4];
          arity = newData[9] = newData[9] === undefined2 ? isBindKey ? 0 : func.length : nativeMax(newData[9] - length2, 0);
          if (!arity && bitmask & (WRAP_CURRY_FLAG | WRAP_CURRY_RIGHT_FLAG)) {
            bitmask &= ~(WRAP_CURRY_FLAG | WRAP_CURRY_RIGHT_FLAG);
          }
          if (!bitmask || bitmask == WRAP_BIND_FLAG) {
            var result2 = createBind(func, bitmask, thisArg);
          } else if (bitmask == WRAP_CURRY_FLAG || bitmask == WRAP_CURRY_RIGHT_FLAG) {
            result2 = createCurry(func, bitmask, arity);
          } else if ((bitmask == WRAP_PARTIAL_FLAG || bitmask == (WRAP_BIND_FLAG | WRAP_PARTIAL_FLAG)) && !holders.length) {
            result2 = createPartial(func, bitmask, thisArg, partials);
          } else {
            result2 = createHybrid.apply(undefined2, newData);
          }
          var setter = data ? baseSetData : setData;
          return setWrapToString(setter(result2, newData), func, bitmask);
        }
        function customDefaultsAssignIn(objValue, srcValue, key, object) {
          if (objValue === undefined2 || eq(objValue, objectProto[key]) && !hasOwnProperty.call(object, key)) {
            return srcValue;
          }
          return objValue;
        }
        function customDefaultsMerge(objValue, srcValue, key, object, source, stack) {
          if (isObject(objValue) && isObject(srcValue)) {
            stack.set(srcValue, objValue);
            baseMerge(objValue, srcValue, undefined2, customDefaultsMerge, stack);
            stack["delete"](srcValue);
          }
          return objValue;
        }
        function customOmitClone(value) {
          return isPlainObject(value) ? undefined2 : value;
        }
        function equalArrays(array, other, bitmask, customizer, equalFunc, stack) {
          var isPartial = bitmask & COMPARE_PARTIAL_FLAG, arrLength = array.length, othLength = other.length;
          if (arrLength != othLength && !(isPartial && othLength > arrLength)) {
            return false;
          }
          var arrStacked = stack.get(array);
          var othStacked = stack.get(other);
          if (arrStacked && othStacked) {
            return arrStacked == other && othStacked == array;
          }
          var index = -1, result2 = true, seen = bitmask & COMPARE_UNORDERED_FLAG ? new SetCache() : undefined2;
          stack.set(array, other);
          stack.set(other, array);
          while (++index < arrLength) {
            var arrValue = array[index], othValue = other[index];
            if (customizer) {
              var compared = isPartial ? customizer(othValue, arrValue, index, other, array, stack) : customizer(arrValue, othValue, index, array, other, stack);
            }
            if (compared !== undefined2) {
              if (compared) {
                continue;
              }
              result2 = false;
              break;
            }
            if (seen) {
              if (!arraySome(other, function(othValue2, othIndex) {
                if (!cacheHas(seen, othIndex) && (arrValue === othValue2 || equalFunc(arrValue, othValue2, bitmask, customizer, stack))) {
                  return seen.push(othIndex);
                }
              })) {
                result2 = false;
                break;
              }
            } else if (!(arrValue === othValue || equalFunc(arrValue, othValue, bitmask, customizer, stack))) {
              result2 = false;
              break;
            }
          }
          stack["delete"](array);
          stack["delete"](other);
          return result2;
        }
        function equalByTag(object, other, tag, bitmask, customizer, equalFunc, stack) {
          switch (tag) {
            case dataViewTag:
              if (object.byteLength != other.byteLength || object.byteOffset != other.byteOffset) {
                return false;
              }
              object = object.buffer;
              other = other.buffer;
            case arrayBufferTag:
              if (object.byteLength != other.byteLength || !equalFunc(new Uint8Array(object), new Uint8Array(other))) {
                return false;
              }
              return true;
            case boolTag:
            case dateTag:
            case numberTag:
              return eq(+object, +other);
            case errorTag:
              return object.name == other.name && object.message == other.message;
            case regexpTag:
            case stringTag:
              return object == other + "";
            case mapTag:
              var convert = mapToArray;
            case setTag:
              var isPartial = bitmask & COMPARE_PARTIAL_FLAG;
              convert || (convert = setToArray);
              if (object.size != other.size && !isPartial) {
                return false;
              }
              var stacked = stack.get(object);
              if (stacked) {
                return stacked == other;
              }
              bitmask |= COMPARE_UNORDERED_FLAG;
              stack.set(object, other);
              var result2 = equalArrays(convert(object), convert(other), bitmask, customizer, equalFunc, stack);
              stack["delete"](object);
              return result2;
            case symbolTag:
              if (symbolValueOf) {
                return symbolValueOf.call(object) == symbolValueOf.call(other);
              }
          }
          return false;
        }
        function equalObjects(object, other, bitmask, customizer, equalFunc, stack) {
          var isPartial = bitmask & COMPARE_PARTIAL_FLAG, objProps = getAllKeys(object), objLength = objProps.length, othProps = getAllKeys(other), othLength = othProps.length;
          if (objLength != othLength && !isPartial) {
            return false;
          }
          var index = objLength;
          while (index--) {
            var key = objProps[index];
            if (!(isPartial ? key in other : hasOwnProperty.call(other, key))) {
              return false;
            }
          }
          var objStacked = stack.get(object);
          var othStacked = stack.get(other);
          if (objStacked && othStacked) {
            return objStacked == other && othStacked == object;
          }
          var result2 = true;
          stack.set(object, other);
          stack.set(other, object);
          var skipCtor = isPartial;
          while (++index < objLength) {
            key = objProps[index];
            var objValue = object[key], othValue = other[key];
            if (customizer) {
              var compared = isPartial ? customizer(othValue, objValue, key, other, object, stack) : customizer(objValue, othValue, key, object, other, stack);
            }
            if (!(compared === undefined2 ? objValue === othValue || equalFunc(objValue, othValue, bitmask, customizer, stack) : compared)) {
              result2 = false;
              break;
            }
            skipCtor || (skipCtor = key == "constructor");
          }
          if (result2 && !skipCtor) {
            var objCtor = object.constructor, othCtor = other.constructor;
            if (objCtor != othCtor && ("constructor" in object && "constructor" in other) && !(typeof objCtor == "function" && objCtor instanceof objCtor && typeof othCtor == "function" && othCtor instanceof othCtor)) {
              result2 = false;
            }
          }
          stack["delete"](object);
          stack["delete"](other);
          return result2;
        }
        function flatRest(func) {
          return setToString(overRest(func, undefined2, flatten), func + "");
        }
        function getAllKeys(object) {
          return baseGetAllKeys(object, keys, getSymbols);
        }
        function getAllKeysIn(object) {
          return baseGetAllKeys(object, keysIn, getSymbolsIn);
        }
        var getData = !metaMap ? noop : function(func) {
          return metaMap.get(func);
        };
        function getFuncName(func) {
          var result2 = func.name + "", array = realNames[result2], length2 = hasOwnProperty.call(realNames, result2) ? array.length : 0;
          while (length2--) {
            var data = array[length2], otherFunc = data.func;
            if (otherFunc == null || otherFunc == func) {
              return data.name;
            }
          }
          return result2;
        }
        function getHolder(func) {
          var object = hasOwnProperty.call(lodash, "placeholder") ? lodash : func;
          return object.placeholder;
        }
        function getIteratee() {
          var result2 = lodash.iteratee || iteratee;
          result2 = result2 === iteratee ? baseIteratee : result2;
          return arguments.length ? result2(arguments[0], arguments[1]) : result2;
        }
        function getMapData(map2, key) {
          var data = map2.__data__;
          return isKeyable(key) ? data[typeof key == "string" ? "string" : "hash"] : data.map;
        }
        function getMatchData(object) {
          var result2 = keys(object), length2 = result2.length;
          while (length2--) {
            var key = result2[length2], value = object[key];
            result2[length2] = [key, value, isStrictComparable(value)];
          }
          return result2;
        }
        function getNative(object, key) {
          var value = getValue(object, key);
          return baseIsNative(value) ? value : undefined2;
        }
        function getRawTag(value) {
          var isOwn = hasOwnProperty.call(value, symToStringTag), tag = value[symToStringTag];
          try {
            value[symToStringTag] = undefined2;
            var unmasked = true;
          } catch (e6) {
          }
          var result2 = nativeObjectToString.call(value);
          if (unmasked) {
            if (isOwn) {
              value[symToStringTag] = tag;
            } else {
              delete value[symToStringTag];
            }
          }
          return result2;
        }
        var getSymbols = !nativeGetSymbols ? stubArray : function(object) {
          if (object == null) {
            return [];
          }
          object = Object2(object);
          return arrayFilter(nativeGetSymbols(object), function(symbol) {
            return propertyIsEnumerable.call(object, symbol);
          });
        };
        var getSymbolsIn = !nativeGetSymbols ? stubArray : function(object) {
          var result2 = [];
          while (object) {
            arrayPush(result2, getSymbols(object));
            object = getPrototype(object);
          }
          return result2;
        };
        var getTag = baseGetTag;
        if (DataView && getTag(new DataView(new ArrayBuffer(1))) != dataViewTag || Map2 && getTag(new Map2()) != mapTag || Promise2 && getTag(Promise2.resolve()) != promiseTag || Set2 && getTag(new Set2()) != setTag || WeakMap2 && getTag(new WeakMap2()) != weakMapTag) {
          getTag = function(value) {
            var result2 = baseGetTag(value), Ctor = result2 == objectTag ? value.constructor : undefined2, ctorString = Ctor ? toSource(Ctor) : "";
            if (ctorString) {
              switch (ctorString) {
                case dataViewCtorString:
                  return dataViewTag;
                case mapCtorString:
                  return mapTag;
                case promiseCtorString:
                  return promiseTag;
                case setCtorString:
                  return setTag;
                case weakMapCtorString:
                  return weakMapTag;
              }
            }
            return result2;
          };
        }
        function getView(start, end, transforms) {
          var index = -1, length2 = transforms.length;
          while (++index < length2) {
            var data = transforms[index], size2 = data.size;
            switch (data.type) {
              case "drop":
                start += size2;
                break;
              case "dropRight":
                end -= size2;
                break;
              case "take":
                end = nativeMin(end, start + size2);
                break;
              case "takeRight":
                start = nativeMax(start, end - size2);
                break;
            }
          }
          return { "start": start, "end": end };
        }
        function getWrapDetails(source) {
          var match2 = source.match(reWrapDetails);
          return match2 ? match2[1].split(reSplitDetails) : [];
        }
        function hasPath(object, path, hasFunc) {
          path = castPath(path, object);
          var index = -1, length2 = path.length, result2 = false;
          while (++index < length2) {
            var key = toKey(path[index]);
            if (!(result2 = object != null && hasFunc(object, key))) {
              break;
            }
            object = object[key];
          }
          if (result2 || ++index != length2) {
            return result2;
          }
          length2 = object == null ? 0 : object.length;
          return !!length2 && isLength(length2) && isIndex(key, length2) && (isArray(object) || isArguments(object));
        }
        function initCloneArray(array) {
          var length2 = array.length, result2 = new array.constructor(length2);
          if (length2 && typeof array[0] == "string" && hasOwnProperty.call(array, "index")) {
            result2.index = array.index;
            result2.input = array.input;
          }
          return result2;
        }
        function initCloneObject(object) {
          return typeof object.constructor == "function" && !isPrototype(object) ? baseCreate(getPrototype(object)) : {};
        }
        function initCloneByTag(object, tag, isDeep) {
          var Ctor = object.constructor;
          switch (tag) {
            case arrayBufferTag:
              return cloneArrayBuffer(object);
            case boolTag:
            case dateTag:
              return new Ctor(+object);
            case dataViewTag:
              return cloneDataView(object, isDeep);
            case float32Tag:
            case float64Tag:
            case int8Tag:
            case int16Tag:
            case int32Tag:
            case uint8Tag:
            case uint8ClampedTag:
            case uint16Tag:
            case uint32Tag:
              return cloneTypedArray(object, isDeep);
            case mapTag:
              return new Ctor();
            case numberTag:
            case stringTag:
              return new Ctor(object);
            case regexpTag:
              return cloneRegExp(object);
            case setTag:
              return new Ctor();
            case symbolTag:
              return cloneSymbol(object);
          }
        }
        function insertWrapDetails(source, details) {
          var length2 = details.length;
          if (!length2) {
            return source;
          }
          var lastIndex = length2 - 1;
          details[lastIndex] = (length2 > 1 ? "& " : "") + details[lastIndex];
          details = details.join(length2 > 2 ? ", " : " ");
          return source.replace(reWrapComment, "{\n/* [wrapped with " + details + "] */\n");
        }
        function isFlattenable(value) {
          return isArray(value) || isArguments(value) || !!(spreadableSymbol && value && value[spreadableSymbol]);
        }
        function isIndex(value, length2) {
          var type = typeof value;
          length2 = length2 == null ? MAX_SAFE_INTEGER : length2;
          return !!length2 && (type == "number" || type != "symbol" && reIsUint.test(value)) && (value > -1 && value % 1 == 0 && value < length2);
        }
        function isIterateeCall(value, index, object) {
          if (!isObject(object)) {
            return false;
          }
          var type = typeof index;
          if (type == "number" ? isArrayLike(object) && isIndex(index, object.length) : type == "string" && index in object) {
            return eq(object[index], value);
          }
          return false;
        }
        function isKey(value, object) {
          if (isArray(value)) {
            return false;
          }
          var type = typeof value;
          if (type == "number" || type == "symbol" || type == "boolean" || value == null || isSymbol(value)) {
            return true;
          }
          return reIsPlainProp.test(value) || !reIsDeepProp.test(value) || object != null && value in Object2(object);
        }
        function isKeyable(value) {
          var type = typeof value;
          return type == "string" || type == "number" || type == "symbol" || type == "boolean" ? value !== "__proto__" : value === null;
        }
        function isLaziable(func) {
          var funcName = getFuncName(func), other = lodash[funcName];
          if (typeof other != "function" || !(funcName in LazyWrapper.prototype)) {
            return false;
          }
          if (func === other) {
            return true;
          }
          var data = getData(other);
          return !!data && func === data[0];
        }
        function isMasked(func) {
          return !!maskSrcKey && maskSrcKey in func;
        }
        var isMaskable = coreJsData ? isFunction : stubFalse;
        function isPrototype(value) {
          var Ctor = value && value.constructor, proto = typeof Ctor == "function" && Ctor.prototype || objectProto;
          return value === proto;
        }
        function isStrictComparable(value) {
          return value === value && !isObject(value);
        }
        function matchesStrictComparable(key, srcValue) {
          return function(object) {
            if (object == null) {
              return false;
            }
            return object[key] === srcValue && (srcValue !== undefined2 || key in Object2(object));
          };
        }
        function memoizeCapped(func) {
          var result2 = memoize2(func, function(key) {
            if (cache.size === MAX_MEMOIZE_SIZE) {
              cache.clear();
            }
            return key;
          });
          var cache = result2.cache;
          return result2;
        }
        function mergeData(data, source) {
          var bitmask = data[1], srcBitmask = source[1], newBitmask = bitmask | srcBitmask, isCommon = newBitmask < (WRAP_BIND_FLAG | WRAP_BIND_KEY_FLAG | WRAP_ARY_FLAG);
          var isCombo = srcBitmask == WRAP_ARY_FLAG && bitmask == WRAP_CURRY_FLAG || srcBitmask == WRAP_ARY_FLAG && bitmask == WRAP_REARG_FLAG && data[7].length <= source[8] || srcBitmask == (WRAP_ARY_FLAG | WRAP_REARG_FLAG) && source[7].length <= source[8] && bitmask == WRAP_CURRY_FLAG;
          if (!(isCommon || isCombo)) {
            return data;
          }
          if (srcBitmask & WRAP_BIND_FLAG) {
            data[2] = source[2];
            newBitmask |= bitmask & WRAP_BIND_FLAG ? 0 : WRAP_CURRY_BOUND_FLAG;
          }
          var value = source[3];
          if (value) {
            var partials = data[3];
            data[3] = partials ? composeArgs(partials, value, source[4]) : value;
            data[4] = partials ? replaceHolders(data[3], PLACEHOLDER) : source[4];
          }
          value = source[5];
          if (value) {
            partials = data[5];
            data[5] = partials ? composeArgsRight(partials, value, source[6]) : value;
            data[6] = partials ? replaceHolders(data[5], PLACEHOLDER) : source[6];
          }
          value = source[7];
          if (value) {
            data[7] = value;
          }
          if (srcBitmask & WRAP_ARY_FLAG) {
            data[8] = data[8] == null ? source[8] : nativeMin(data[8], source[8]);
          }
          if (data[9] == null) {
            data[9] = source[9];
          }
          data[0] = source[0];
          data[1] = newBitmask;
          return data;
        }
        function nativeKeysIn(object) {
          var result2 = [];
          if (object != null) {
            for (var key in Object2(object)) {
              result2.push(key);
            }
          }
          return result2;
        }
        function objectToString(value) {
          return nativeObjectToString.call(value);
        }
        function overRest(func, start, transform2) {
          start = nativeMax(start === undefined2 ? func.length - 1 : start, 0);
          return function() {
            var args = arguments, index = -1, length2 = nativeMax(args.length - start, 0), array = Array2(length2);
            while (++index < length2) {
              array[index] = args[start + index];
            }
            index = -1;
            var otherArgs = Array2(start + 1);
            while (++index < start) {
              otherArgs[index] = args[index];
            }
            otherArgs[start] = transform2(array);
            return apply(func, this, otherArgs);
          };
        }
        function parent(object, path) {
          return path.length < 2 ? object : baseGet(object, baseSlice(path, 0, -1));
        }
        function reorder(array, indexes) {
          var arrLength = array.length, length2 = nativeMin(indexes.length, arrLength), oldArray = copyArray(array);
          while (length2--) {
            var index = indexes[length2];
            array[length2] = isIndex(index, arrLength) ? oldArray[index] : undefined2;
          }
          return array;
        }
        function safeGet(object, key) {
          if (key === "constructor" && typeof object[key] === "function") {
            return;
          }
          if (key == "__proto__") {
            return;
          }
          return object[key];
        }
        var setData = shortOut(baseSetData);
        var setTimeout2 = ctxSetTimeout || function(func, wait) {
          return root.setTimeout(func, wait);
        };
        var setToString = shortOut(baseSetToString);
        function setWrapToString(wrapper, reference, bitmask) {
          var source = reference + "";
          return setToString(wrapper, insertWrapDetails(source, updateWrapDetails(getWrapDetails(source), bitmask)));
        }
        function shortOut(func) {
          var count = 0, lastCalled = 0;
          return function() {
            var stamp = nativeNow(), remaining = HOT_SPAN - (stamp - lastCalled);
            lastCalled = stamp;
            if (remaining > 0) {
              if (++count >= HOT_COUNT) {
                return arguments[0];
              }
            } else {
              count = 0;
            }
            return func.apply(undefined2, arguments);
          };
        }
        function shuffleSelf(array, size2) {
          var index = -1, length2 = array.length, lastIndex = length2 - 1;
          size2 = size2 === undefined2 ? length2 : size2;
          while (++index < size2) {
            var rand = baseRandom(index, lastIndex), value = array[rand];
            array[rand] = array[index];
            array[index] = value;
          }
          array.length = size2;
          return array;
        }
        var stringToPath = memoizeCapped(function(string) {
          var result2 = [];
          if (string.charCodeAt(0) === 46) {
            result2.push("");
          }
          string.replace(rePropName, function(match2, number, quote, subString) {
            result2.push(quote ? subString.replace(reEscapeChar, "$1") : number || match2);
          });
          return result2;
        });
        function toKey(value) {
          if (typeof value == "string" || isSymbol(value)) {
            return value;
          }
          var result2 = value + "";
          return result2 == "0" && 1 / value == -INFINITY ? "-0" : result2;
        }
        function toSource(func) {
          if (func != null) {
            try {
              return funcToString.call(func);
            } catch (e6) {
            }
            try {
              return func + "";
            } catch (e6) {
            }
          }
          return "";
        }
        function updateWrapDetails(details, bitmask) {
          arrayEach(wrapFlags, function(pair) {
            var value = "_." + pair[0];
            if (bitmask & pair[1] && !arrayIncludes(details, value)) {
              details.push(value);
            }
          });
          return details.sort();
        }
        function wrapperClone(wrapper) {
          if (wrapper instanceof LazyWrapper) {
            return wrapper.clone();
          }
          var result2 = new LodashWrapper(wrapper.__wrapped__, wrapper.__chain__);
          result2.__actions__ = copyArray(wrapper.__actions__);
          result2.__index__ = wrapper.__index__;
          result2.__values__ = wrapper.__values__;
          return result2;
        }
        function chunk(array, size2, guard) {
          if (guard ? isIterateeCall(array, size2, guard) : size2 === undefined2) {
            size2 = 1;
          } else {
            size2 = nativeMax(toInteger(size2), 0);
          }
          var length2 = array == null ? 0 : array.length;
          if (!length2 || size2 < 1) {
            return [];
          }
          var index = 0, resIndex = 0, result2 = Array2(nativeCeil(length2 / size2));
          while (index < length2) {
            result2[resIndex++] = baseSlice(array, index, index += size2);
          }
          return result2;
        }
        function compact(array) {
          var index = -1, length2 = array == null ? 0 : array.length, resIndex = 0, result2 = [];
          while (++index < length2) {
            var value = array[index];
            if (value) {
              result2[resIndex++] = value;
            }
          }
          return result2;
        }
        function concat() {
          var length2 = arguments.length;
          if (!length2) {
            return [];
          }
          var args = Array2(length2 - 1), array = arguments[0], index = length2;
          while (index--) {
            args[index - 1] = arguments[index];
          }
          return arrayPush(isArray(array) ? copyArray(array) : [array], baseFlatten(args, 1));
        }
        var difference = baseRest(function(array, values2) {
          return isArrayLikeObject(array) ? baseDifference(array, baseFlatten(values2, 1, isArrayLikeObject, true)) : [];
        });
        var differenceBy = baseRest(function(array, values2) {
          var iteratee2 = last(values2);
          if (isArrayLikeObject(iteratee2)) {
            iteratee2 = undefined2;
          }
          return isArrayLikeObject(array) ? baseDifference(array, baseFlatten(values2, 1, isArrayLikeObject, true), getIteratee(iteratee2, 2)) : [];
        });
        var differenceWith = baseRest(function(array, values2) {
          var comparator = last(values2);
          if (isArrayLikeObject(comparator)) {
            comparator = undefined2;
          }
          return isArrayLikeObject(array) ? baseDifference(array, baseFlatten(values2, 1, isArrayLikeObject, true), undefined2, comparator) : [];
        });
        function drop(array, n15, guard) {
          var length2 = array == null ? 0 : array.length;
          if (!length2) {
            return [];
          }
          n15 = guard || n15 === undefined2 ? 1 : toInteger(n15);
          return baseSlice(array, n15 < 0 ? 0 : n15, length2);
        }
        function dropRight(array, n15, guard) {
          var length2 = array == null ? 0 : array.length;
          if (!length2) {
            return [];
          }
          n15 = guard || n15 === undefined2 ? 1 : toInteger(n15);
          n15 = length2 - n15;
          return baseSlice(array, 0, n15 < 0 ? 0 : n15);
        }
        function dropRightWhile(array, predicate) {
          return array && array.length ? baseWhile(array, getIteratee(predicate, 3), true, true) : [];
        }
        function dropWhile(array, predicate) {
          return array && array.length ? baseWhile(array, getIteratee(predicate, 3), true) : [];
        }
        function fill(array, value, start, end) {
          var length2 = array == null ? 0 : array.length;
          if (!length2) {
            return [];
          }
          if (start && typeof start != "number" && isIterateeCall(array, value, start)) {
            start = 0;
            end = length2;
          }
          return baseFill(array, value, start, end);
        }
        function findIndex(array, predicate, fromIndex) {
          var length2 = array == null ? 0 : array.length;
          if (!length2) {
            return -1;
          }
          var index = fromIndex == null ? 0 : toInteger(fromIndex);
          if (index < 0) {
            index = nativeMax(length2 + index, 0);
          }
          return baseFindIndex(array, getIteratee(predicate, 3), index);
        }
        function findLastIndex(array, predicate, fromIndex) {
          var length2 = array == null ? 0 : array.length;
          if (!length2) {
            return -1;
          }
          var index = length2 - 1;
          if (fromIndex !== undefined2) {
            index = toInteger(fromIndex);
            index = fromIndex < 0 ? nativeMax(length2 + index, 0) : nativeMin(index, length2 - 1);
          }
          return baseFindIndex(array, getIteratee(predicate, 3), index, true);
        }
        function flatten(array) {
          var length2 = array == null ? 0 : array.length;
          return length2 ? baseFlatten(array, 1) : [];
        }
        function flattenDeep(array) {
          var length2 = array == null ? 0 : array.length;
          return length2 ? baseFlatten(array, INFINITY) : [];
        }
        function flattenDepth(array, depth) {
          var length2 = array == null ? 0 : array.length;
          if (!length2) {
            return [];
          }
          depth = depth === undefined2 ? 1 : toInteger(depth);
          return baseFlatten(array, depth);
        }
        function fromPairs(pairs) {
          var index = -1, length2 = pairs == null ? 0 : pairs.length, result2 = {};
          while (++index < length2) {
            var pair = pairs[index];
            result2[pair[0]] = pair[1];
          }
          return result2;
        }
        function head(array) {
          return array && array.length ? array[0] : undefined2;
        }
        function indexOf(array, value, fromIndex) {
          var length2 = array == null ? 0 : array.length;
          if (!length2) {
            return -1;
          }
          var index = fromIndex == null ? 0 : toInteger(fromIndex);
          if (index < 0) {
            index = nativeMax(length2 + index, 0);
          }
          return baseIndexOf(array, value, index);
        }
        function initial(array) {
          var length2 = array == null ? 0 : array.length;
          return length2 ? baseSlice(array, 0, -1) : [];
        }
        var intersection = baseRest(function(arrays) {
          var mapped = arrayMap(arrays, castArrayLikeObject);
          return mapped.length && mapped[0] === arrays[0] ? baseIntersection(mapped) : [];
        });
        var intersectionBy = baseRest(function(arrays) {
          var iteratee2 = last(arrays), mapped = arrayMap(arrays, castArrayLikeObject);
          if (iteratee2 === last(mapped)) {
            iteratee2 = undefined2;
          } else {
            mapped.pop();
          }
          return mapped.length && mapped[0] === arrays[0] ? baseIntersection(mapped, getIteratee(iteratee2, 2)) : [];
        });
        var intersectionWith = baseRest(function(arrays) {
          var comparator = last(arrays), mapped = arrayMap(arrays, castArrayLikeObject);
          comparator = typeof comparator == "function" ? comparator : undefined2;
          if (comparator) {
            mapped.pop();
          }
          return mapped.length && mapped[0] === arrays[0] ? baseIntersection(mapped, undefined2, comparator) : [];
        });
        function join(array, separator2) {
          return array == null ? "" : nativeJoin.call(array, separator2);
        }
        function last(array) {
          var length2 = array == null ? 0 : array.length;
          return length2 ? array[length2 - 1] : undefined2;
        }
        function lastIndexOf(array, value, fromIndex) {
          var length2 = array == null ? 0 : array.length;
          if (!length2) {
            return -1;
          }
          var index = length2;
          if (fromIndex !== undefined2) {
            index = toInteger(fromIndex);
            index = index < 0 ? nativeMax(length2 + index, 0) : nativeMin(index, length2 - 1);
          }
          return value === value ? strictLastIndexOf(array, value, index) : baseFindIndex(array, baseIsNaN, index, true);
        }
        function nth(array, n15) {
          return array && array.length ? baseNth(array, toInteger(n15)) : undefined2;
        }
        var pull = baseRest(pullAll);
        function pullAll(array, values2) {
          return array && array.length && values2 && values2.length ? basePullAll(array, values2) : array;
        }
        function pullAllBy(array, values2, iteratee2) {
          return array && array.length && values2 && values2.length ? basePullAll(array, values2, getIteratee(iteratee2, 2)) : array;
        }
        function pullAllWith(array, values2, comparator) {
          return array && array.length && values2 && values2.length ? basePullAll(array, values2, undefined2, comparator) : array;
        }
        var pullAt = flatRest(function(array, indexes) {
          var length2 = array == null ? 0 : array.length, result2 = baseAt(array, indexes);
          basePullAt(array, arrayMap(indexes, function(index) {
            return isIndex(index, length2) ? +index : index;
          }).sort(compareAscending));
          return result2;
        });
        function remove(array, predicate) {
          var result2 = [];
          if (!(array && array.length)) {
            return result2;
          }
          var index = -1, indexes = [], length2 = array.length;
          predicate = getIteratee(predicate, 3);
          while (++index < length2) {
            var value = array[index];
            if (predicate(value, index, array)) {
              result2.push(value);
              indexes.push(index);
            }
          }
          basePullAt(array, indexes);
          return result2;
        }
        function reverse(array) {
          return array == null ? array : nativeReverse.call(array);
        }
        function slice2(array, start, end) {
          var length2 = array == null ? 0 : array.length;
          if (!length2) {
            return [];
          }
          if (end && typeof end != "number" && isIterateeCall(array, start, end)) {
            start = 0;
            end = length2;
          } else {
            start = start == null ? 0 : toInteger(start);
            end = end === undefined2 ? length2 : toInteger(end);
          }
          return baseSlice(array, start, end);
        }
        function sortedIndex(array, value) {
          return baseSortedIndex(array, value);
        }
        function sortedIndexBy(array, value, iteratee2) {
          return baseSortedIndexBy(array, value, getIteratee(iteratee2, 2));
        }
        function sortedIndexOf(array, value) {
          var length2 = array == null ? 0 : array.length;
          if (length2) {
            var index = baseSortedIndex(array, value);
            if (index < length2 && eq(array[index], value)) {
              return index;
            }
          }
          return -1;
        }
        function sortedLastIndex(array, value) {
          return baseSortedIndex(array, value, true);
        }
        function sortedLastIndexBy(array, value, iteratee2) {
          return baseSortedIndexBy(array, value, getIteratee(iteratee2, 2), true);
        }
        function sortedLastIndexOf(array, value) {
          var length2 = array == null ? 0 : array.length;
          if (length2) {
            var index = baseSortedIndex(array, value, true) - 1;
            if (eq(array[index], value)) {
              return index;
            }
          }
          return -1;
        }
        function sortedUniq(array) {
          return array && array.length ? baseSortedUniq(array) : [];
        }
        function sortedUniqBy(array, iteratee2) {
          return array && array.length ? baseSortedUniq(array, getIteratee(iteratee2, 2)) : [];
        }
        function tail(array) {
          var length2 = array == null ? 0 : array.length;
          return length2 ? baseSlice(array, 1, length2) : [];
        }
        function take(array, n15, guard) {
          if (!(array && array.length)) {
            return [];
          }
          n15 = guard || n15 === undefined2 ? 1 : toInteger(n15);
          return baseSlice(array, 0, n15 < 0 ? 0 : n15);
        }
        function takeRight(array, n15, guard) {
          var length2 = array == null ? 0 : array.length;
          if (!length2) {
            return [];
          }
          n15 = guard || n15 === undefined2 ? 1 : toInteger(n15);
          n15 = length2 - n15;
          return baseSlice(array, n15 < 0 ? 0 : n15, length2);
        }
        function takeRightWhile(array, predicate) {
          return array && array.length ? baseWhile(array, getIteratee(predicate, 3), false, true) : [];
        }
        function takeWhile(array, predicate) {
          return array && array.length ? baseWhile(array, getIteratee(predicate, 3)) : [];
        }
        var union = baseRest(function(arrays) {
          return baseUniq(baseFlatten(arrays, 1, isArrayLikeObject, true));
        });
        var unionBy = baseRest(function(arrays) {
          var iteratee2 = last(arrays);
          if (isArrayLikeObject(iteratee2)) {
            iteratee2 = undefined2;
          }
          return baseUniq(baseFlatten(arrays, 1, isArrayLikeObject, true), getIteratee(iteratee2, 2));
        });
        var unionWith = baseRest(function(arrays) {
          var comparator = last(arrays);
          comparator = typeof comparator == "function" ? comparator : undefined2;
          return baseUniq(baseFlatten(arrays, 1, isArrayLikeObject, true), undefined2, comparator);
        });
        function uniq(array) {
          return array && array.length ? baseUniq(array) : [];
        }
        function uniqBy(array, iteratee2) {
          return array && array.length ? baseUniq(array, getIteratee(iteratee2, 2)) : [];
        }
        function uniqWith(array, comparator) {
          comparator = typeof comparator == "function" ? comparator : undefined2;
          return array && array.length ? baseUniq(array, undefined2, comparator) : [];
        }
        function unzip(array) {
          if (!(array && array.length)) {
            return [];
          }
          var length2 = 0;
          array = arrayFilter(array, function(group) {
            if (isArrayLikeObject(group)) {
              length2 = nativeMax(group.length, length2);
              return true;
            }
          });
          return baseTimes(length2, function(index) {
            return arrayMap(array, baseProperty(index));
          });
        }
        function unzipWith(array, iteratee2) {
          if (!(array && array.length)) {
            return [];
          }
          var result2 = unzip(array);
          if (iteratee2 == null) {
            return result2;
          }
          return arrayMap(result2, function(group) {
            return apply(iteratee2, undefined2, group);
          });
        }
        var without = baseRest(function(array, values2) {
          return isArrayLikeObject(array) ? baseDifference(array, values2) : [];
        });
        var xor = baseRest(function(arrays) {
          return baseXor(arrayFilter(arrays, isArrayLikeObject));
        });
        var xorBy = baseRest(function(arrays) {
          var iteratee2 = last(arrays);
          if (isArrayLikeObject(iteratee2)) {
            iteratee2 = undefined2;
          }
          return baseXor(arrayFilter(arrays, isArrayLikeObject), getIteratee(iteratee2, 2));
        });
        var xorWith = baseRest(function(arrays) {
          var comparator = last(arrays);
          comparator = typeof comparator == "function" ? comparator : undefined2;
          return baseXor(arrayFilter(arrays, isArrayLikeObject), undefined2, comparator);
        });
        var zip = baseRest(unzip);
        function zipObject(props, values2) {
          return baseZipObject(props || [], values2 || [], assignValue);
        }
        function zipObjectDeep(props, values2) {
          return baseZipObject(props || [], values2 || [], baseSet);
        }
        var zipWith = baseRest(function(arrays) {
          var length2 = arrays.length, iteratee2 = length2 > 1 ? arrays[length2 - 1] : undefined2;
          iteratee2 = typeof iteratee2 == "function" ? (arrays.pop(), iteratee2) : undefined2;
          return unzipWith(arrays, iteratee2);
        });
        function chain(value) {
          var result2 = lodash(value);
          result2.__chain__ = true;
          return result2;
        }
        function tap(value, interceptor) {
          interceptor(value);
          return value;
        }
        function thru(value, interceptor) {
          return interceptor(value);
        }
        var wrapperAt = flatRest(function(paths) {
          var length2 = paths.length, start = length2 ? paths[0] : 0, value = this.__wrapped__, interceptor = function(object) {
            return baseAt(object, paths);
          };
          if (length2 > 1 || this.__actions__.length || !(value instanceof LazyWrapper) || !isIndex(start)) {
            return this.thru(interceptor);
          }
          value = value.slice(start, +start + (length2 ? 1 : 0));
          value.__actions__.push({
            "func": thru,
            "args": [interceptor],
            "thisArg": undefined2
          });
          return new LodashWrapper(value, this.__chain__).thru(function(array) {
            if (length2 && !array.length) {
              array.push(undefined2);
            }
            return array;
          });
        });
        function wrapperChain() {
          return chain(this);
        }
        function wrapperCommit() {
          return new LodashWrapper(this.value(), this.__chain__);
        }
        function wrapperNext() {
          if (this.__values__ === undefined2) {
            this.__values__ = toArray(this.value());
          }
          var done = this.__index__ >= this.__values__.length, value = done ? undefined2 : this.__values__[this.__index__++];
          return { "done": done, "value": value };
        }
        function wrapperToIterator() {
          return this;
        }
        function wrapperPlant(value) {
          var result2, parent2 = this;
          while (parent2 instanceof baseLodash) {
            var clone2 = wrapperClone(parent2);
            clone2.__index__ = 0;
            clone2.__values__ = undefined2;
            if (result2) {
              previous.__wrapped__ = clone2;
            } else {
              result2 = clone2;
            }
            var previous = clone2;
            parent2 = parent2.__wrapped__;
          }
          previous.__wrapped__ = value;
          return result2;
        }
        function wrapperReverse() {
          var value = this.__wrapped__;
          if (value instanceof LazyWrapper) {
            var wrapped = value;
            if (this.__actions__.length) {
              wrapped = new LazyWrapper(this);
            }
            wrapped = wrapped.reverse();
            wrapped.__actions__.push({
              "func": thru,
              "args": [reverse],
              "thisArg": undefined2
            });
            return new LodashWrapper(wrapped, this.__chain__);
          }
          return this.thru(reverse);
        }
        function wrapperValue() {
          return baseWrapperValue(this.__wrapped__, this.__actions__);
        }
        var countBy = createAggregator(function(result2, value, key) {
          if (hasOwnProperty.call(result2, key)) {
            ++result2[key];
          } else {
            baseAssignValue(result2, key, 1);
          }
        });
        function every(collection, predicate, guard) {
          var func = isArray(collection) ? arrayEvery : baseEvery;
          if (guard && isIterateeCall(collection, predicate, guard)) {
            predicate = undefined2;
          }
          return func(collection, getIteratee(predicate, 3));
        }
        function filter(collection, predicate) {
          var func = isArray(collection) ? arrayFilter : baseFilter;
          return func(collection, getIteratee(predicate, 3));
        }
        var find = createFind(findIndex);
        var findLast = createFind(findLastIndex);
        function flatMap(collection, iteratee2) {
          return baseFlatten(map(collection, iteratee2), 1);
        }
        function flatMapDeep(collection, iteratee2) {
          return baseFlatten(map(collection, iteratee2), INFINITY);
        }
        function flatMapDepth(collection, iteratee2, depth) {
          depth = depth === undefined2 ? 1 : toInteger(depth);
          return baseFlatten(map(collection, iteratee2), depth);
        }
        function forEach(collection, iteratee2) {
          var func = isArray(collection) ? arrayEach : baseEach;
          return func(collection, getIteratee(iteratee2, 3));
        }
        function forEachRight(collection, iteratee2) {
          var func = isArray(collection) ? arrayEachRight : baseEachRight;
          return func(collection, getIteratee(iteratee2, 3));
        }
        var groupBy = createAggregator(function(result2, value, key) {
          if (hasOwnProperty.call(result2, key)) {
            result2[key].push(value);
          } else {
            baseAssignValue(result2, key, [value]);
          }
        });
        function includes(collection, value, fromIndex, guard) {
          collection = isArrayLike(collection) ? collection : values(collection);
          fromIndex = fromIndex && !guard ? toInteger(fromIndex) : 0;
          var length2 = collection.length;
          if (fromIndex < 0) {
            fromIndex = nativeMax(length2 + fromIndex, 0);
          }
          return isString(collection) ? fromIndex <= length2 && collection.indexOf(value, fromIndex) > -1 : !!length2 && baseIndexOf(collection, value, fromIndex) > -1;
        }
        var invokeMap = baseRest(function(collection, path, args) {
          var index = -1, isFunc = typeof path == "function", result2 = isArrayLike(collection) ? Array2(collection.length) : [];
          baseEach(collection, function(value) {
            result2[++index] = isFunc ? apply(path, value, args) : baseInvoke(value, path, args);
          });
          return result2;
        });
        var keyBy = createAggregator(function(result2, value, key) {
          baseAssignValue(result2, key, value);
        });
        function map(collection, iteratee2) {
          var func = isArray(collection) ? arrayMap : baseMap;
          return func(collection, getIteratee(iteratee2, 3));
        }
        function orderBy(collection, iteratees, orders, guard) {
          if (collection == null) {
            return [];
          }
          if (!isArray(iteratees)) {
            iteratees = iteratees == null ? [] : [iteratees];
          }
          orders = guard ? undefined2 : orders;
          if (!isArray(orders)) {
            orders = orders == null ? [] : [orders];
          }
          return baseOrderBy(collection, iteratees, orders);
        }
        var partition = createAggregator(function(result2, value, key) {
          result2[key ? 0 : 1].push(value);
        }, function() {
          return [[], []];
        });
        function reduce(collection, iteratee2, accumulator) {
          var func = isArray(collection) ? arrayReduce : baseReduce, initAccum = arguments.length < 3;
          return func(collection, getIteratee(iteratee2, 4), accumulator, initAccum, baseEach);
        }
        function reduceRight(collection, iteratee2, accumulator) {
          var func = isArray(collection) ? arrayReduceRight : baseReduce, initAccum = arguments.length < 3;
          return func(collection, getIteratee(iteratee2, 4), accumulator, initAccum, baseEachRight);
        }
        function reject(collection, predicate) {
          var func = isArray(collection) ? arrayFilter : baseFilter;
          return func(collection, negate(getIteratee(predicate, 3)));
        }
        function sample(collection) {
          var func = isArray(collection) ? arraySample : baseSample;
          return func(collection);
        }
        function sampleSize(collection, n15, guard) {
          if (guard ? isIterateeCall(collection, n15, guard) : n15 === undefined2) {
            n15 = 1;
          } else {
            n15 = toInteger(n15);
          }
          var func = isArray(collection) ? arraySampleSize : baseSampleSize;
          return func(collection, n15);
        }
        function shuffle(collection) {
          var func = isArray(collection) ? arrayShuffle : baseShuffle;
          return func(collection);
        }
        function size(collection) {
          if (collection == null) {
            return 0;
          }
          if (isArrayLike(collection)) {
            return isString(collection) ? stringSize(collection) : collection.length;
          }
          var tag = getTag(collection);
          if (tag == mapTag || tag == setTag) {
            return collection.size;
          }
          return baseKeys(collection).length;
        }
        function some(collection, predicate, guard) {
          var func = isArray(collection) ? arraySome : baseSome;
          if (guard && isIterateeCall(collection, predicate, guard)) {
            predicate = undefined2;
          }
          return func(collection, getIteratee(predicate, 3));
        }
        var sortBy = baseRest(function(collection, iteratees) {
          if (collection == null) {
            return [];
          }
          var length2 = iteratees.length;
          if (length2 > 1 && isIterateeCall(collection, iteratees[0], iteratees[1])) {
            iteratees = [];
          } else if (length2 > 2 && isIterateeCall(iteratees[0], iteratees[1], iteratees[2])) {
            iteratees = [iteratees[0]];
          }
          return baseOrderBy(collection, baseFlatten(iteratees, 1), []);
        });
        var now = ctxNow || function() {
          return root.Date.now();
        };
        function after(n15, func) {
          if (typeof func != "function") {
            throw new TypeError2(FUNC_ERROR_TEXT);
          }
          n15 = toInteger(n15);
          return function() {
            if (--n15 < 1) {
              return func.apply(this, arguments);
            }
          };
        }
        function ary(func, n15, guard) {
          n15 = guard ? undefined2 : n15;
          n15 = func && n15 == null ? func.length : n15;
          return createWrap(func, WRAP_ARY_FLAG, undefined2, undefined2, undefined2, undefined2, n15);
        }
        function before(n15, func) {
          var result2;
          if (typeof func != "function") {
            throw new TypeError2(FUNC_ERROR_TEXT);
          }
          n15 = toInteger(n15);
          return function() {
            if (--n15 > 0) {
              result2 = func.apply(this, arguments);
            }
            if (n15 <= 1) {
              func = undefined2;
            }
            return result2;
          };
        }
        var bind = baseRest(function(func, thisArg, partials) {
          var bitmask = WRAP_BIND_FLAG;
          if (partials.length) {
            var holders = replaceHolders(partials, getHolder(bind));
            bitmask |= WRAP_PARTIAL_FLAG;
          }
          return createWrap(func, bitmask, thisArg, partials, holders);
        });
        var bindKey = baseRest(function(object, key, partials) {
          var bitmask = WRAP_BIND_FLAG | WRAP_BIND_KEY_FLAG;
          if (partials.length) {
            var holders = replaceHolders(partials, getHolder(bindKey));
            bitmask |= WRAP_PARTIAL_FLAG;
          }
          return createWrap(key, bitmask, object, partials, holders);
        });
        function curry(func, arity, guard) {
          arity = guard ? undefined2 : arity;
          var result2 = createWrap(func, WRAP_CURRY_FLAG, undefined2, undefined2, undefined2, undefined2, undefined2, arity);
          result2.placeholder = curry.placeholder;
          return result2;
        }
        function curryRight(func, arity, guard) {
          arity = guard ? undefined2 : arity;
          var result2 = createWrap(func, WRAP_CURRY_RIGHT_FLAG, undefined2, undefined2, undefined2, undefined2, undefined2, arity);
          result2.placeholder = curryRight.placeholder;
          return result2;
        }
        function debounce2(func, wait, options) {
          var lastArgs, lastThis, maxWait, result2, timerId, lastCallTime, lastInvokeTime = 0, leading = false, maxing = false, trailing = true;
          if (typeof func != "function") {
            throw new TypeError2(FUNC_ERROR_TEXT);
          }
          wait = toNumber(wait) || 0;
          if (isObject(options)) {
            leading = !!options.leading;
            maxing = "maxWait" in options;
            maxWait = maxing ? nativeMax(toNumber(options.maxWait) || 0, wait) : maxWait;
            trailing = "trailing" in options ? !!options.trailing : trailing;
          }
          function invokeFunc(time) {
            var args = lastArgs, thisArg = lastThis;
            lastArgs = lastThis = undefined2;
            lastInvokeTime = time;
            result2 = func.apply(thisArg, args);
            return result2;
          }
          function leadingEdge(time) {
            lastInvokeTime = time;
            timerId = setTimeout2(timerExpired, wait);
            return leading ? invokeFunc(time) : result2;
          }
          function remainingWait(time) {
            var timeSinceLastCall = time - lastCallTime, timeSinceLastInvoke = time - lastInvokeTime, timeWaiting = wait - timeSinceLastCall;
            return maxing ? nativeMin(timeWaiting, maxWait - timeSinceLastInvoke) : timeWaiting;
          }
          function shouldInvoke(time) {
            var timeSinceLastCall = time - lastCallTime, timeSinceLastInvoke = time - lastInvokeTime;
            return lastCallTime === undefined2 || timeSinceLastCall >= wait || timeSinceLastCall < 0 || maxing && timeSinceLastInvoke >= maxWait;
          }
          function timerExpired() {
            var time = now();
            if (shouldInvoke(time)) {
              return trailingEdge(time);
            }
            timerId = setTimeout2(timerExpired, remainingWait(time));
          }
          function trailingEdge(time) {
            timerId = undefined2;
            if (trailing && lastArgs) {
              return invokeFunc(time);
            }
            lastArgs = lastThis = undefined2;
            return result2;
          }
          function cancel() {
            if (timerId !== undefined2) {
              clearTimeout2(timerId);
            }
            lastInvokeTime = 0;
            lastArgs = lastCallTime = lastThis = timerId = undefined2;
          }
          function flush() {
            return timerId === undefined2 ? result2 : trailingEdge(now());
          }
          function debounced() {
            var time = now(), isInvoking = shouldInvoke(time);
            lastArgs = arguments;
            lastThis = this;
            lastCallTime = time;
            if (isInvoking) {
              if (timerId === undefined2) {
                return leadingEdge(lastCallTime);
              }
              if (maxing) {
                clearTimeout2(timerId);
                timerId = setTimeout2(timerExpired, wait);
                return invokeFunc(lastCallTime);
              }
            }
            if (timerId === undefined2) {
              timerId = setTimeout2(timerExpired, wait);
            }
            return result2;
          }
          debounced.cancel = cancel;
          debounced.flush = flush;
          return debounced;
        }
        var defer = baseRest(function(func, args) {
          return baseDelay(func, 1, args);
        });
        var delay = baseRest(function(func, wait, args) {
          return baseDelay(func, toNumber(wait) || 0, args);
        });
        function flip(func) {
          return createWrap(func, WRAP_FLIP_FLAG);
        }
        function memoize2(func, resolver) {
          if (typeof func != "function" || resolver != null && typeof resolver != "function") {
            throw new TypeError2(FUNC_ERROR_TEXT);
          }
          var memoized = function() {
            var args = arguments, key = resolver ? resolver.apply(this, args) : args[0], cache = memoized.cache;
            if (cache.has(key)) {
              return cache.get(key);
            }
            var result2 = func.apply(this, args);
            memoized.cache = cache.set(key, result2) || cache;
            return result2;
          };
          memoized.cache = new (memoize2.Cache || MapCache)();
          return memoized;
        }
        memoize2.Cache = MapCache;
        function negate(predicate) {
          if (typeof predicate != "function") {
            throw new TypeError2(FUNC_ERROR_TEXT);
          }
          return function() {
            var args = arguments;
            switch (args.length) {
              case 0:
                return !predicate.call(this);
              case 1:
                return !predicate.call(this, args[0]);
              case 2:
                return !predicate.call(this, args[0], args[1]);
              case 3:
                return !predicate.call(this, args[0], args[1], args[2]);
            }
            return !predicate.apply(this, args);
          };
        }
        function once(func) {
          return before(2, func);
        }
        var overArgs = castRest(function(func, transforms) {
          transforms = transforms.length == 1 && isArray(transforms[0]) ? arrayMap(transforms[0], baseUnary(getIteratee())) : arrayMap(baseFlatten(transforms, 1), baseUnary(getIteratee()));
          var funcsLength = transforms.length;
          return baseRest(function(args) {
            var index = -1, length2 = nativeMin(args.length, funcsLength);
            while (++index < length2) {
              args[index] = transforms[index].call(this, args[index]);
            }
            return apply(func, this, args);
          });
        });
        var partial = baseRest(function(func, partials) {
          var holders = replaceHolders(partials, getHolder(partial));
          return createWrap(func, WRAP_PARTIAL_FLAG, undefined2, partials, holders);
        });
        var partialRight = baseRest(function(func, partials) {
          var holders = replaceHolders(partials, getHolder(partialRight));
          return createWrap(func, WRAP_PARTIAL_RIGHT_FLAG, undefined2, partials, holders);
        });
        var rearg = flatRest(function(func, indexes) {
          return createWrap(func, WRAP_REARG_FLAG, undefined2, undefined2, undefined2, indexes);
        });
        function rest(func, start) {
          if (typeof func != "function") {
            throw new TypeError2(FUNC_ERROR_TEXT);
          }
          start = start === undefined2 ? start : toInteger(start);
          return baseRest(func, start);
        }
        function spread(func, start) {
          if (typeof func != "function") {
            throw new TypeError2(FUNC_ERROR_TEXT);
          }
          start = start == null ? 0 : nativeMax(toInteger(start), 0);
          return baseRest(function(args) {
            var array = args[start], otherArgs = castSlice(args, 0, start);
            if (array) {
              arrayPush(otherArgs, array);
            }
            return apply(func, this, otherArgs);
          });
        }
        function throttle(func, wait, options) {
          var leading = true, trailing = true;
          if (typeof func != "function") {
            throw new TypeError2(FUNC_ERROR_TEXT);
          }
          if (isObject(options)) {
            leading = "leading" in options ? !!options.leading : leading;
            trailing = "trailing" in options ? !!options.trailing : trailing;
          }
          return debounce2(func, wait, {
            "leading": leading,
            "maxWait": wait,
            "trailing": trailing
          });
        }
        function unary(func) {
          return ary(func, 1);
        }
        function wrap(value, wrapper) {
          return partial(castFunction(wrapper), value);
        }
        function castArray() {
          if (!arguments.length) {
            return [];
          }
          var value = arguments[0];
          return isArray(value) ? value : [value];
        }
        function clone(value) {
          return baseClone(value, CLONE_SYMBOLS_FLAG);
        }
        function cloneWith(value, customizer) {
          customizer = typeof customizer == "function" ? customizer : undefined2;
          return baseClone(value, CLONE_SYMBOLS_FLAG, customizer);
        }
        function cloneDeep(value) {
          return baseClone(value, CLONE_DEEP_FLAG | CLONE_SYMBOLS_FLAG);
        }
        function cloneDeepWith(value, customizer) {
          customizer = typeof customizer == "function" ? customizer : undefined2;
          return baseClone(value, CLONE_DEEP_FLAG | CLONE_SYMBOLS_FLAG, customizer);
        }
        function conformsTo(object, source) {
          return source == null || baseConformsTo(object, source, keys(source));
        }
        function eq(value, other) {
          return value === other || value !== value && other !== other;
        }
        var gt2 = createRelationalOperation(baseGt);
        var gte = createRelationalOperation(function(value, other) {
          return value >= other;
        });
        var isArguments = baseIsArguments(/* @__PURE__ */ (function() {
          return arguments;
        })()) ? baseIsArguments : function(value) {
          return isObjectLike(value) && hasOwnProperty.call(value, "callee") && !propertyIsEnumerable.call(value, "callee");
        };
        var isArray = Array2.isArray;
        var isArrayBuffer = nodeIsArrayBuffer ? baseUnary(nodeIsArrayBuffer) : baseIsArrayBuffer;
        function isArrayLike(value) {
          return value != null && isLength(value.length) && !isFunction(value);
        }
        function isArrayLikeObject(value) {
          return isObjectLike(value) && isArrayLike(value);
        }
        function isBoolean(value) {
          return value === true || value === false || isObjectLike(value) && baseGetTag(value) == boolTag;
        }
        var isBuffer = nativeIsBuffer || stubFalse;
        var isDate = nodeIsDate ? baseUnary(nodeIsDate) : baseIsDate;
        function isElement(value) {
          return isObjectLike(value) && value.nodeType === 1 && !isPlainObject(value);
        }
        function isEmpty(value) {
          if (value == null) {
            return true;
          }
          if (isArrayLike(value) && (isArray(value) || typeof value == "string" || typeof value.splice == "function" || isBuffer(value) || isTypedArray(value) || isArguments(value))) {
            return !value.length;
          }
          var tag = getTag(value);
          if (tag == mapTag || tag == setTag) {
            return !value.size;
          }
          if (isPrototype(value)) {
            return !baseKeys(value).length;
          }
          for (var key in value) {
            if (hasOwnProperty.call(value, key)) {
              return false;
            }
          }
          return true;
        }
        function isEqual(value, other) {
          return baseIsEqual(value, other);
        }
        function isEqualWith(value, other, customizer) {
          customizer = typeof customizer == "function" ? customizer : undefined2;
          var result2 = customizer ? customizer(value, other) : undefined2;
          return result2 === undefined2 ? baseIsEqual(value, other, undefined2, customizer) : !!result2;
        }
        function isError(value) {
          if (!isObjectLike(value)) {
            return false;
          }
          var tag = baseGetTag(value);
          return tag == errorTag || tag == domExcTag || typeof value.message == "string" && typeof value.name == "string" && !isPlainObject(value);
        }
        function isFinite2(value) {
          return typeof value == "number" && nativeIsFinite(value);
        }
        function isFunction(value) {
          if (!isObject(value)) {
            return false;
          }
          var tag = baseGetTag(value);
          return tag == funcTag || tag == genTag || tag == asyncTag || tag == proxyTag;
        }
        function isInteger(value) {
          return typeof value == "number" && value == toInteger(value);
        }
        function isLength(value) {
          return typeof value == "number" && value > -1 && value % 1 == 0 && value <= MAX_SAFE_INTEGER;
        }
        function isObject(value) {
          var type = typeof value;
          return value != null && (type == "object" || type == "function");
        }
        function isObjectLike(value) {
          return value != null && typeof value == "object";
        }
        var isMap = nodeIsMap ? baseUnary(nodeIsMap) : baseIsMap;
        function isMatch(object, source) {
          return object === source || baseIsMatch(object, source, getMatchData(source));
        }
        function isMatchWith(object, source, customizer) {
          customizer = typeof customizer == "function" ? customizer : undefined2;
          return baseIsMatch(object, source, getMatchData(source), customizer);
        }
        function isNaN2(value) {
          return isNumber(value) && value != +value;
        }
        function isNative(value) {
          if (isMaskable(value)) {
            throw new Error2(CORE_ERROR_TEXT);
          }
          return baseIsNative(value);
        }
        function isNull(value) {
          return value === null;
        }
        function isNil(value) {
          return value == null;
        }
        function isNumber(value) {
          return typeof value == "number" || isObjectLike(value) && baseGetTag(value) == numberTag;
        }
        function isPlainObject(value) {
          if (!isObjectLike(value) || baseGetTag(value) != objectTag) {
            return false;
          }
          var proto = getPrototype(value);
          if (proto === null) {
            return true;
          }
          var Ctor = hasOwnProperty.call(proto, "constructor") && proto.constructor;
          return typeof Ctor == "function" && Ctor instanceof Ctor && funcToString.call(Ctor) == objectCtorString;
        }
        var isRegExp = nodeIsRegExp ? baseUnary(nodeIsRegExp) : baseIsRegExp;
        function isSafeInteger(value) {
          return isInteger(value) && value >= -MAX_SAFE_INTEGER && value <= MAX_SAFE_INTEGER;
        }
        var isSet = nodeIsSet ? baseUnary(nodeIsSet) : baseIsSet;
        function isString(value) {
          return typeof value == "string" || !isArray(value) && isObjectLike(value) && baseGetTag(value) == stringTag;
        }
        function isSymbol(value) {
          return typeof value == "symbol" || isObjectLike(value) && baseGetTag(value) == symbolTag;
        }
        var isTypedArray = nodeIsTypedArray ? baseUnary(nodeIsTypedArray) : baseIsTypedArray;
        function isUndefined(value) {
          return value === undefined2;
        }
        function isWeakMap(value) {
          return isObjectLike(value) && getTag(value) == weakMapTag;
        }
        function isWeakSet(value) {
          return isObjectLike(value) && baseGetTag(value) == weakSetTag;
        }
        var lt2 = createRelationalOperation(baseLt);
        var lte = createRelationalOperation(function(value, other) {
          return value <= other;
        });
        function toArray(value) {
          if (!value) {
            return [];
          }
          if (isArrayLike(value)) {
            return isString(value) ? stringToArray(value) : copyArray(value);
          }
          if (symIterator && value[symIterator]) {
            return iteratorToArray(value[symIterator]());
          }
          var tag = getTag(value), func = tag == mapTag ? mapToArray : tag == setTag ? setToArray : values;
          return func(value);
        }
        function toFinite(value) {
          if (!value) {
            return value === 0 ? value : 0;
          }
          value = toNumber(value);
          if (value === INFINITY || value === -INFINITY) {
            var sign = value < 0 ? -1 : 1;
            return sign * MAX_INTEGER;
          }
          return value === value ? value : 0;
        }
        function toInteger(value) {
          var result2 = toFinite(value), remainder = result2 % 1;
          return result2 === result2 ? remainder ? result2 - remainder : result2 : 0;
        }
        function toLength(value) {
          return value ? baseClamp(toInteger(value), 0, MAX_ARRAY_LENGTH) : 0;
        }
        function toNumber(value) {
          if (typeof value == "number") {
            return value;
          }
          if (isSymbol(value)) {
            return NAN;
          }
          if (isObject(value)) {
            var other = typeof value.valueOf == "function" ? value.valueOf() : value;
            value = isObject(other) ? other + "" : other;
          }
          if (typeof value != "string") {
            return value === 0 ? value : +value;
          }
          value = baseTrim(value);
          var isBinary = reIsBinary.test(value);
          return isBinary || reIsOctal.test(value) ? freeParseInt(value.slice(2), isBinary ? 2 : 8) : reIsBadHex.test(value) ? NAN : +value;
        }
        function toPlainObject(value) {
          return copyObject(value, keysIn(value));
        }
        function toSafeInteger(value) {
          return value ? baseClamp(toInteger(value), -MAX_SAFE_INTEGER, MAX_SAFE_INTEGER) : value === 0 ? value : 0;
        }
        function toString(value) {
          return value == null ? "" : baseToString(value);
        }
        var assign2 = createAssigner(function(object, source) {
          if (isPrototype(source) || isArrayLike(source)) {
            copyObject(source, keys(source), object);
            return;
          }
          for (var key in source) {
            if (hasOwnProperty.call(source, key)) {
              assignValue(object, key, source[key]);
            }
          }
        });
        var assignIn = createAssigner(function(object, source) {
          copyObject(source, keysIn(source), object);
        });
        var assignInWith = createAssigner(function(object, source, srcIndex, customizer) {
          copyObject(source, keysIn(source), object, customizer);
        });
        var assignWith = createAssigner(function(object, source, srcIndex, customizer) {
          copyObject(source, keys(source), object, customizer);
        });
        var at2 = flatRest(baseAt);
        function create2(prototype, properties) {
          var result2 = baseCreate(prototype);
          return properties == null ? result2 : baseAssign(result2, properties);
        }
        var defaults = baseRest(function(object, sources) {
          object = Object2(object);
          var index = -1;
          var length2 = sources.length;
          var guard = length2 > 2 ? sources[2] : undefined2;
          if (guard && isIterateeCall(sources[0], sources[1], guard)) {
            length2 = 1;
          }
          while (++index < length2) {
            var source = sources[index];
            var props = keysIn(source);
            var propsIndex = -1;
            var propsLength = props.length;
            while (++propsIndex < propsLength) {
              var key = props[propsIndex];
              var value = object[key];
              if (value === undefined2 || eq(value, objectProto[key]) && !hasOwnProperty.call(object, key)) {
                object[key] = source[key];
              }
            }
          }
          return object;
        });
        var defaultsDeep = baseRest(function(args) {
          args.push(undefined2, customDefaultsMerge);
          return apply(mergeWith, undefined2, args);
        });
        function findKey(object, predicate) {
          return baseFindKey(object, getIteratee(predicate, 3), baseForOwn);
        }
        function findLastKey(object, predicate) {
          return baseFindKey(object, getIteratee(predicate, 3), baseForOwnRight);
        }
        function forIn(object, iteratee2) {
          return object == null ? object : baseFor(object, getIteratee(iteratee2, 3), keysIn);
        }
        function forInRight(object, iteratee2) {
          return object == null ? object : baseForRight(object, getIteratee(iteratee2, 3), keysIn);
        }
        function forOwn(object, iteratee2) {
          return object && baseForOwn(object, getIteratee(iteratee2, 3));
        }
        function forOwnRight(object, iteratee2) {
          return object && baseForOwnRight(object, getIteratee(iteratee2, 3));
        }
        function functions(object) {
          return object == null ? [] : baseFunctions(object, keys(object));
        }
        function functionsIn(object) {
          return object == null ? [] : baseFunctions(object, keysIn(object));
        }
        function get(object, path, defaultValue) {
          var result2 = object == null ? undefined2 : baseGet(object, path);
          return result2 === undefined2 ? defaultValue : result2;
        }
        function has(object, path) {
          return object != null && hasPath(object, path, baseHas);
        }
        function hasIn(object, path) {
          return object != null && hasPath(object, path, baseHasIn);
        }
        var invert = createInverter(function(result2, value, key) {
          if (value != null && typeof value.toString != "function") {
            value = nativeObjectToString.call(value);
          }
          result2[value] = key;
        }, constant(identity2));
        var invertBy = createInverter(function(result2, value, key) {
          if (value != null && typeof value.toString != "function") {
            value = nativeObjectToString.call(value);
          }
          if (hasOwnProperty.call(result2, value)) {
            result2[value].push(key);
          } else {
            result2[value] = [key];
          }
        }, getIteratee);
        var invoke = baseRest(baseInvoke);
        function keys(object) {
          return isArrayLike(object) ? arrayLikeKeys(object) : baseKeys(object);
        }
        function keysIn(object) {
          return isArrayLike(object) ? arrayLikeKeys(object, true) : baseKeysIn(object);
        }
        function mapKeys(object, iteratee2) {
          var result2 = {};
          iteratee2 = getIteratee(iteratee2, 3);
          baseForOwn(object, function(value, key, object2) {
            baseAssignValue(result2, iteratee2(value, key, object2), value);
          });
          return result2;
        }
        function mapValues(object, iteratee2) {
          var result2 = {};
          iteratee2 = getIteratee(iteratee2, 3);
          baseForOwn(object, function(value, key, object2) {
            baseAssignValue(result2, key, iteratee2(value, key, object2));
          });
          return result2;
        }
        var merge2 = createAssigner(function(object, source, srcIndex) {
          baseMerge(object, source, srcIndex);
        });
        var mergeWith = createAssigner(function(object, source, srcIndex, customizer) {
          baseMerge(object, source, srcIndex, customizer);
        });
        var omit = flatRest(function(object, paths) {
          var result2 = {};
          if (object == null) {
            return result2;
          }
          var isDeep = false;
          paths = arrayMap(paths, function(path) {
            path = castPath(path, object);
            isDeep || (isDeep = path.length > 1);
            return path;
          });
          copyObject(object, getAllKeysIn(object), result2);
          if (isDeep) {
            result2 = baseClone(result2, CLONE_DEEP_FLAG | CLONE_FLAT_FLAG | CLONE_SYMBOLS_FLAG, customOmitClone);
          }
          var length2 = paths.length;
          while (length2--) {
            baseUnset(result2, paths[length2]);
          }
          return result2;
        });
        function omitBy(object, predicate) {
          return pickBy(object, negate(getIteratee(predicate)));
        }
        var pick = flatRest(function(object, paths) {
          return object == null ? {} : basePick(object, paths);
        });
        function pickBy(object, predicate) {
          if (object == null) {
            return {};
          }
          var props = arrayMap(getAllKeysIn(object), function(prop) {
            return [prop];
          });
          predicate = getIteratee(predicate);
          return basePickBy(object, props, function(value, path) {
            return predicate(value, path[0]);
          });
        }
        function result(object, path, defaultValue) {
          path = castPath(path, object);
          var index = -1, length2 = path.length;
          if (!length2) {
            length2 = 1;
            object = undefined2;
          }
          while (++index < length2) {
            var value = object == null ? undefined2 : object[toKey(path[index])];
            if (value === undefined2) {
              index = length2;
              value = defaultValue;
            }
            object = isFunction(value) ? value.call(object) : value;
          }
          return object;
        }
        function set(object, path, value) {
          return object == null ? object : baseSet(object, path, value);
        }
        function setWith(object, path, value, customizer) {
          customizer = typeof customizer == "function" ? customizer : undefined2;
          return object == null ? object : baseSet(object, path, value, customizer);
        }
        var toPairs = createToPairs(keys);
        var toPairsIn = createToPairs(keysIn);
        function transform(object, iteratee2, accumulator) {
          var isArr = isArray(object), isArrLike = isArr || isBuffer(object) || isTypedArray(object);
          iteratee2 = getIteratee(iteratee2, 4);
          if (accumulator == null) {
            var Ctor = object && object.constructor;
            if (isArrLike) {
              accumulator = isArr ? new Ctor() : [];
            } else if (isObject(object)) {
              accumulator = isFunction(Ctor) ? baseCreate(getPrototype(object)) : {};
            } else {
              accumulator = {};
            }
          }
          (isArrLike ? arrayEach : baseForOwn)(object, function(value, index, object2) {
            return iteratee2(accumulator, value, index, object2);
          });
          return accumulator;
        }
        function unset(object, path) {
          return object == null ? true : baseUnset(object, path);
        }
        function update(object, path, updater) {
          return object == null ? object : baseUpdate(object, path, castFunction(updater));
        }
        function updateWith(object, path, updater, customizer) {
          customizer = typeof customizer == "function" ? customizer : undefined2;
          return object == null ? object : baseUpdate(object, path, castFunction(updater), customizer);
        }
        function values(object) {
          return object == null ? [] : baseValues(object, keys(object));
        }
        function valuesIn(object) {
          return object == null ? [] : baseValues(object, keysIn(object));
        }
        function clamp(number, lower, upper) {
          if (upper === undefined2) {
            upper = lower;
            lower = undefined2;
          }
          if (upper !== undefined2) {
            upper = toNumber(upper);
            upper = upper === upper ? upper : 0;
          }
          if (lower !== undefined2) {
            lower = toNumber(lower);
            lower = lower === lower ? lower : 0;
          }
          return baseClamp(toNumber(number), lower, upper);
        }
        function inRange(number, start, end) {
          start = toFinite(start);
          if (end === undefined2) {
            end = start;
            start = 0;
          } else {
            end = toFinite(end);
          }
          number = toNumber(number);
          return baseInRange(number, start, end);
        }
        function random(lower, upper, floating) {
          if (floating && typeof floating != "boolean" && isIterateeCall(lower, upper, floating)) {
            upper = floating = undefined2;
          }
          if (floating === undefined2) {
            if (typeof upper == "boolean") {
              floating = upper;
              upper = undefined2;
            } else if (typeof lower == "boolean") {
              floating = lower;
              lower = undefined2;
            }
          }
          if (lower === undefined2 && upper === undefined2) {
            lower = 0;
            upper = 1;
          } else {
            lower = toFinite(lower);
            if (upper === undefined2) {
              upper = lower;
              lower = 0;
            } else {
              upper = toFinite(upper);
            }
          }
          if (lower > upper) {
            var temp = lower;
            lower = upper;
            upper = temp;
          }
          if (floating || lower % 1 || upper % 1) {
            var rand = nativeRandom();
            return nativeMin(lower + rand * (upper - lower + freeParseFloat("1e-" + ((rand + "").length - 1))), upper);
          }
          return baseRandom(lower, upper);
        }
        var camelCase = createCompounder(function(result2, word, index) {
          word = word.toLowerCase();
          return result2 + (index ? capitalize(word) : word);
        });
        function capitalize(string) {
          return upperFirst(toString(string).toLowerCase());
        }
        function deburr(string) {
          string = toString(string);
          return string && string.replace(reLatin, deburrLetter).replace(reComboMark, "");
        }
        function endsWith(string, target, position2) {
          string = toString(string);
          target = baseToString(target);
          var length2 = string.length;
          position2 = position2 === undefined2 ? length2 : baseClamp(toInteger(position2), 0, length2);
          var end = position2;
          position2 -= target.length;
          return position2 >= 0 && string.slice(position2, end) == target;
        }
        function escape(string) {
          string = toString(string);
          return string && reHasUnescapedHtml.test(string) ? string.replace(reUnescapedHtml, escapeHtmlChar) : string;
        }
        function escapeRegExp(string) {
          string = toString(string);
          return string && reHasRegExpChar.test(string) ? string.replace(reRegExpChar, "\\$&") : string;
        }
        var kebabCase = createCompounder(function(result2, word, index) {
          return result2 + (index ? "-" : "") + word.toLowerCase();
        });
        var lowerCase = createCompounder(function(result2, word, index) {
          return result2 + (index ? " " : "") + word.toLowerCase();
        });
        var lowerFirst = createCaseFirst("toLowerCase");
        function pad(string, length2, chars) {
          string = toString(string);
          length2 = toInteger(length2);
          var strLength = length2 ? stringSize(string) : 0;
          if (!length2 || strLength >= length2) {
            return string;
          }
          var mid = (length2 - strLength) / 2;
          return createPadding(nativeFloor(mid), chars) + string + createPadding(nativeCeil(mid), chars);
        }
        function padEnd(string, length2, chars) {
          string = toString(string);
          length2 = toInteger(length2);
          var strLength = length2 ? stringSize(string) : 0;
          return length2 && strLength < length2 ? string + createPadding(length2 - strLength, chars) : string;
        }
        function padStart(string, length2, chars) {
          string = toString(string);
          length2 = toInteger(length2);
          var strLength = length2 ? stringSize(string) : 0;
          return length2 && strLength < length2 ? createPadding(length2 - strLength, chars) + string : string;
        }
        function parseInt2(string, radix, guard) {
          if (guard || radix == null) {
            radix = 0;
          } else if (radix) {
            radix = +radix;
          }
          return nativeParseInt(toString(string).replace(reTrimStart, ""), radix || 0);
        }
        function repeat(string, n15, guard) {
          if (guard ? isIterateeCall(string, n15, guard) : n15 === undefined2) {
            n15 = 1;
          } else {
            n15 = toInteger(n15);
          }
          return baseRepeat(toString(string), n15);
        }
        function replace2() {
          var args = arguments, string = toString(args[0]);
          return args.length < 3 ? string : string.replace(args[1], args[2]);
        }
        var snakeCase = createCompounder(function(result2, word, index) {
          return result2 + (index ? "_" : "") + word.toLowerCase();
        });
        function split(string, separator2, limit) {
          if (limit && typeof limit != "number" && isIterateeCall(string, separator2, limit)) {
            separator2 = limit = undefined2;
          }
          limit = limit === undefined2 ? MAX_ARRAY_LENGTH : limit >>> 0;
          if (!limit) {
            return [];
          }
          string = toString(string);
          if (string && (typeof separator2 == "string" || separator2 != null && !isRegExp(separator2))) {
            separator2 = baseToString(separator2);
            if (!separator2 && hasUnicode(string)) {
              return castSlice(stringToArray(string), 0, limit);
            }
          }
          return string.split(separator2, limit);
        }
        var startCase = createCompounder(function(result2, word, index) {
          return result2 + (index ? " " : "") + upperFirst(word);
        });
        function startsWith(string, target, position2) {
          string = toString(string);
          position2 = position2 == null ? 0 : baseClamp(toInteger(position2), 0, string.length);
          target = baseToString(target);
          return string.slice(position2, position2 + target.length) == target;
        }
        function template(string, options, guard) {
          var settings = lodash.templateSettings;
          if (guard && isIterateeCall(string, options, guard)) {
            options = undefined2;
          }
          string = toString(string);
          options = assignInWith({}, options, settings, customDefaultsAssignIn);
          var imports = assignInWith({}, options.imports, settings.imports, customDefaultsAssignIn), importsKeys = keys(imports), importsValues = baseValues(imports, importsKeys);
          var isEscaping, isEvaluating, index = 0, interpolate = options.interpolate || reNoMatch, source = "__p += '";
          var reDelimiters = RegExp2(
            (options.escape || reNoMatch).source + "|" + interpolate.source + "|" + (interpolate === reInterpolate ? reEsTemplate : reNoMatch).source + "|" + (options.evaluate || reNoMatch).source + "|$",
            "g"
          );
          var sourceURL = "//# sourceURL=" + (hasOwnProperty.call(options, "sourceURL") ? (options.sourceURL + "").replace(/\s/g, " ") : "lodash.templateSources[" + ++templateCounter + "]") + "\n";
          string.replace(reDelimiters, function(match2, escapeValue, interpolateValue, esTemplateValue, evaluateValue, offset) {
            interpolateValue || (interpolateValue = esTemplateValue);
            source += string.slice(index, offset).replace(reUnescapedString, escapeStringChar);
            if (escapeValue) {
              isEscaping = true;
              source += "' +\n__e(" + escapeValue + ") +\n'";
            }
            if (evaluateValue) {
              isEvaluating = true;
              source += "';\n" + evaluateValue + ";\n__p += '";
            }
            if (interpolateValue) {
              source += "' +\n((__t = (" + interpolateValue + ")) == null ? '' : __t) +\n'";
            }
            index = offset + match2.length;
            return match2;
          });
          source += "';\n";
          var variable = hasOwnProperty.call(options, "variable") && options.variable;
          if (!variable) {
            source = "with (obj) {\n" + source + "\n}\n";
          } else if (reForbiddenIdentifierChars.test(variable)) {
            throw new Error2(INVALID_TEMPL_VAR_ERROR_TEXT);
          }
          source = (isEvaluating ? source.replace(reEmptyStringLeading, "") : source).replace(reEmptyStringMiddle, "$1").replace(reEmptyStringTrailing, "$1;");
          source = "function(" + (variable || "obj") + ") {\n" + (variable ? "" : "obj || (obj = {});\n") + "var __t, __p = ''" + (isEscaping ? ", __e = _.escape" : "") + (isEvaluating ? ", __j = Array.prototype.join;\nfunction print() { __p += __j.call(arguments, '') }\n" : ";\n") + source + "return __p\n}";
          var result2 = attempt(function() {
            return Function2(importsKeys, sourceURL + "return " + source).apply(undefined2, importsValues);
          });
          result2.source = source;
          if (isError(result2)) {
            throw result2;
          }
          return result2;
        }
        function toLower(value) {
          return toString(value).toLowerCase();
        }
        function toUpper(value) {
          return toString(value).toUpperCase();
        }
        function trim2(string, chars, guard) {
          string = toString(string);
          if (string && (guard || chars === undefined2)) {
            return baseTrim(string);
          }
          if (!string || !(chars = baseToString(chars))) {
            return string;
          }
          var strSymbols = stringToArray(string), chrSymbols = stringToArray(chars), start = charsStartIndex(strSymbols, chrSymbols), end = charsEndIndex(strSymbols, chrSymbols) + 1;
          return castSlice(strSymbols, start, end).join("");
        }
        function trimEnd(string, chars, guard) {
          string = toString(string);
          if (string && (guard || chars === undefined2)) {
            return string.slice(0, trimmedEndIndex(string) + 1);
          }
          if (!string || !(chars = baseToString(chars))) {
            return string;
          }
          var strSymbols = stringToArray(string), end = charsEndIndex(strSymbols, stringToArray(chars)) + 1;
          return castSlice(strSymbols, 0, end).join("");
        }
        function trimStart(string, chars, guard) {
          string = toString(string);
          if (string && (guard || chars === undefined2)) {
            return string.replace(reTrimStart, "");
          }
          if (!string || !(chars = baseToString(chars))) {
            return string;
          }
          var strSymbols = stringToArray(string), start = charsStartIndex(strSymbols, stringToArray(chars));
          return castSlice(strSymbols, start).join("");
        }
        function truncate(string, options) {
          var length2 = DEFAULT_TRUNC_LENGTH, omission = DEFAULT_TRUNC_OMISSION;
          if (isObject(options)) {
            var separator2 = "separator" in options ? options.separator : separator2;
            length2 = "length" in options ? toInteger(options.length) : length2;
            omission = "omission" in options ? baseToString(options.omission) : omission;
          }
          string = toString(string);
          var strLength = string.length;
          if (hasUnicode(string)) {
            var strSymbols = stringToArray(string);
            strLength = strSymbols.length;
          }
          if (length2 >= strLength) {
            return string;
          }
          var end = length2 - stringSize(omission);
          if (end < 1) {
            return omission;
          }
          var result2 = strSymbols ? castSlice(strSymbols, 0, end).join("") : string.slice(0, end);
          if (separator2 === undefined2) {
            return result2 + omission;
          }
          if (strSymbols) {
            end += result2.length - end;
          }
          if (isRegExp(separator2)) {
            if (string.slice(end).search(separator2)) {
              var match2, substring = result2;
              if (!separator2.global) {
                separator2 = RegExp2(separator2.source, toString(reFlags.exec(separator2)) + "g");
              }
              separator2.lastIndex = 0;
              while (match2 = separator2.exec(substring)) {
                var newEnd = match2.index;
              }
              result2 = result2.slice(0, newEnd === undefined2 ? end : newEnd);
            }
          } else if (string.indexOf(baseToString(separator2), end) != end) {
            var index = result2.lastIndexOf(separator2);
            if (index > -1) {
              result2 = result2.slice(0, index);
            }
          }
          return result2 + omission;
        }
        function unescape(string) {
          string = toString(string);
          return string && reHasEscapedHtml.test(string) ? string.replace(reEscapedHtml, unescapeHtmlChar) : string;
        }
        var upperCase = createCompounder(function(result2, word, index) {
          return result2 + (index ? " " : "") + word.toUpperCase();
        });
        var upperFirst = createCaseFirst("toUpperCase");
        function words(string, pattern, guard) {
          string = toString(string);
          pattern = guard ? undefined2 : pattern;
          if (pattern === undefined2) {
            return hasUnicodeWord(string) ? unicodeWords(string) : asciiWords(string);
          }
          return string.match(pattern) || [];
        }
        var attempt = baseRest(function(func, args) {
          try {
            return apply(func, undefined2, args);
          } catch (e6) {
            return isError(e6) ? e6 : new Error2(e6);
          }
        });
        var bindAll = flatRest(function(object, methodNames) {
          arrayEach(methodNames, function(key) {
            key = toKey(key);
            baseAssignValue(object, key, bind(object[key], object));
          });
          return object;
        });
        function cond(pairs) {
          var length2 = pairs == null ? 0 : pairs.length, toIteratee = getIteratee();
          pairs = !length2 ? [] : arrayMap(pairs, function(pair) {
            if (typeof pair[1] != "function") {
              throw new TypeError2(FUNC_ERROR_TEXT);
            }
            return [toIteratee(pair[0]), pair[1]];
          });
          return baseRest(function(args) {
            var index = -1;
            while (++index < length2) {
              var pair = pairs[index];
              if (apply(pair[0], this, args)) {
                return apply(pair[1], this, args);
              }
            }
          });
        }
        function conforms(source) {
          return baseConforms(baseClone(source, CLONE_DEEP_FLAG));
        }
        function constant(value) {
          return function() {
            return value;
          };
        }
        function defaultTo(value, defaultValue) {
          return value == null || value !== value ? defaultValue : value;
        }
        var flow = createFlow();
        var flowRight = createFlow(true);
        function identity2(value) {
          return value;
        }
        function iteratee(func) {
          return baseIteratee(typeof func == "function" ? func : baseClone(func, CLONE_DEEP_FLAG));
        }
        function matches(source) {
          return baseMatches(baseClone(source, CLONE_DEEP_FLAG));
        }
        function matchesProperty(path, srcValue) {
          return baseMatchesProperty(path, baseClone(srcValue, CLONE_DEEP_FLAG));
        }
        var method = baseRest(function(path, args) {
          return function(object) {
            return baseInvoke(object, path, args);
          };
        });
        var methodOf = baseRest(function(object, args) {
          return function(path) {
            return baseInvoke(object, path, args);
          };
        });
        function mixin(object, source, options) {
          var props = keys(source), methodNames = baseFunctions(source, props);
          if (options == null && !(isObject(source) && (methodNames.length || !props.length))) {
            options = source;
            source = object;
            object = this;
            methodNames = baseFunctions(source, keys(source));
          }
          var chain2 = !(isObject(options) && "chain" in options) || !!options.chain, isFunc = isFunction(object);
          arrayEach(methodNames, function(methodName) {
            var func = source[methodName];
            object[methodName] = func;
            if (isFunc) {
              object.prototype[methodName] = function() {
                var chainAll = this.__chain__;
                if (chain2 || chainAll) {
                  var result2 = object(this.__wrapped__), actions = result2.__actions__ = copyArray(this.__actions__);
                  actions.push({ "func": func, "args": arguments, "thisArg": object });
                  result2.__chain__ = chainAll;
                  return result2;
                }
                return func.apply(object, arrayPush([this.value()], arguments));
              };
            }
          });
          return object;
        }
        function noConflict() {
          if (root._ === this) {
            root._ = oldDash;
          }
          return this;
        }
        function noop() {
        }
        function nthArg(n15) {
          n15 = toInteger(n15);
          return baseRest(function(args) {
            return baseNth(args, n15);
          });
        }
        var over = createOver(arrayMap);
        var overEvery = createOver(arrayEvery);
        var overSome = createOver(arraySome);
        function property(path) {
          return isKey(path) ? baseProperty(toKey(path)) : basePropertyDeep(path);
        }
        function propertyOf(object) {
          return function(path) {
            return object == null ? undefined2 : baseGet(object, path);
          };
        }
        var range = createRange();
        var rangeRight = createRange(true);
        function stubArray() {
          return [];
        }
        function stubFalse() {
          return false;
        }
        function stubObject() {
          return {};
        }
        function stubString() {
          return "";
        }
        function stubTrue() {
          return true;
        }
        function times(n15, iteratee2) {
          n15 = toInteger(n15);
          if (n15 < 1 || n15 > MAX_SAFE_INTEGER) {
            return [];
          }
          var index = MAX_ARRAY_LENGTH, length2 = nativeMin(n15, MAX_ARRAY_LENGTH);
          iteratee2 = getIteratee(iteratee2);
          n15 -= MAX_ARRAY_LENGTH;
          var result2 = baseTimes(length2, iteratee2);
          while (++index < n15) {
            iteratee2(index);
          }
          return result2;
        }
        function toPath(value) {
          if (isArray(value)) {
            return arrayMap(value, toKey);
          }
          return isSymbol(value) ? [value] : copyArray(stringToPath(toString(value)));
        }
        function uniqueId(prefix3) {
          var id = ++idCounter2;
          return toString(prefix3) + id;
        }
        var add = createMathOperation(function(augend, addend) {
          return augend + addend;
        }, 0);
        var ceil = createRound("ceil");
        var divide = createMathOperation(function(dividend, divisor) {
          return dividend / divisor;
        }, 1);
        var floor = createRound("floor");
        function max(array) {
          return array && array.length ? baseExtremum(array, identity2, baseGt) : undefined2;
        }
        function maxBy(array, iteratee2) {
          return array && array.length ? baseExtremum(array, getIteratee(iteratee2, 2), baseGt) : undefined2;
        }
        function mean(array) {
          return baseMean(array, identity2);
        }
        function meanBy(array, iteratee2) {
          return baseMean(array, getIteratee(iteratee2, 2));
        }
        function min(array) {
          return array && array.length ? baseExtremum(array, identity2, baseLt) : undefined2;
        }
        function minBy(array, iteratee2) {
          return array && array.length ? baseExtremum(array, getIteratee(iteratee2, 2), baseLt) : undefined2;
        }
        var multiply = createMathOperation(function(multiplier, multiplicand) {
          return multiplier * multiplicand;
        }, 1);
        var round = createRound("round");
        var subtract = createMathOperation(function(minuend, subtrahend) {
          return minuend - subtrahend;
        }, 0);
        function sum(array) {
          return array && array.length ? baseSum(array, identity2) : 0;
        }
        function sumBy(array, iteratee2) {
          return array && array.length ? baseSum(array, getIteratee(iteratee2, 2)) : 0;
        }
        lodash.after = after;
        lodash.ary = ary;
        lodash.assign = assign2;
        lodash.assignIn = assignIn;
        lodash.assignInWith = assignInWith;
        lodash.assignWith = assignWith;
        lodash.at = at2;
        lodash.before = before;
        lodash.bind = bind;
        lodash.bindAll = bindAll;
        lodash.bindKey = bindKey;
        lodash.castArray = castArray;
        lodash.chain = chain;
        lodash.chunk = chunk;
        lodash.compact = compact;
        lodash.concat = concat;
        lodash.cond = cond;
        lodash.conforms = conforms;
        lodash.constant = constant;
        lodash.countBy = countBy;
        lodash.create = create2;
        lodash.curry = curry;
        lodash.curryRight = curryRight;
        lodash.debounce = debounce2;
        lodash.defaults = defaults;
        lodash.defaultsDeep = defaultsDeep;
        lodash.defer = defer;
        lodash.delay = delay;
        lodash.difference = difference;
        lodash.differenceBy = differenceBy;
        lodash.differenceWith = differenceWith;
        lodash.drop = drop;
        lodash.dropRight = dropRight;
        lodash.dropRightWhile = dropRightWhile;
        lodash.dropWhile = dropWhile;
        lodash.fill = fill;
        lodash.filter = filter;
        lodash.flatMap = flatMap;
        lodash.flatMapDeep = flatMapDeep;
        lodash.flatMapDepth = flatMapDepth;
        lodash.flatten = flatten;
        lodash.flattenDeep = flattenDeep;
        lodash.flattenDepth = flattenDepth;
        lodash.flip = flip;
        lodash.flow = flow;
        lodash.flowRight = flowRight;
        lodash.fromPairs = fromPairs;
        lodash.functions = functions;
        lodash.functionsIn = functionsIn;
        lodash.groupBy = groupBy;
        lodash.initial = initial;
        lodash.intersection = intersection;
        lodash.intersectionBy = intersectionBy;
        lodash.intersectionWith = intersectionWith;
        lodash.invert = invert;
        lodash.invertBy = invertBy;
        lodash.invokeMap = invokeMap;
        lodash.iteratee = iteratee;
        lodash.keyBy = keyBy;
        lodash.keys = keys;
        lodash.keysIn = keysIn;
        lodash.map = map;
        lodash.mapKeys = mapKeys;
        lodash.mapValues = mapValues;
        lodash.matches = matches;
        lodash.matchesProperty = matchesProperty;
        lodash.memoize = memoize2;
        lodash.merge = merge2;
        lodash.mergeWith = mergeWith;
        lodash.method = method;
        lodash.methodOf = methodOf;
        lodash.mixin = mixin;
        lodash.negate = negate;
        lodash.nthArg = nthArg;
        lodash.omit = omit;
        lodash.omitBy = omitBy;
        lodash.once = once;
        lodash.orderBy = orderBy;
        lodash.over = over;
        lodash.overArgs = overArgs;
        lodash.overEvery = overEvery;
        lodash.overSome = overSome;
        lodash.partial = partial;
        lodash.partialRight = partialRight;
        lodash.partition = partition;
        lodash.pick = pick;
        lodash.pickBy = pickBy;
        lodash.property = property;
        lodash.propertyOf = propertyOf;
        lodash.pull = pull;
        lodash.pullAll = pullAll;
        lodash.pullAllBy = pullAllBy;
        lodash.pullAllWith = pullAllWith;
        lodash.pullAt = pullAt;
        lodash.range = range;
        lodash.rangeRight = rangeRight;
        lodash.rearg = rearg;
        lodash.reject = reject;
        lodash.remove = remove;
        lodash.rest = rest;
        lodash.reverse = reverse;
        lodash.sampleSize = sampleSize;
        lodash.set = set;
        lodash.setWith = setWith;
        lodash.shuffle = shuffle;
        lodash.slice = slice2;
        lodash.sortBy = sortBy;
        lodash.sortedUniq = sortedUniq;
        lodash.sortedUniqBy = sortedUniqBy;
        lodash.split = split;
        lodash.spread = spread;
        lodash.tail = tail;
        lodash.take = take;
        lodash.takeRight = takeRight;
        lodash.takeRightWhile = takeRightWhile;
        lodash.takeWhile = takeWhile;
        lodash.tap = tap;
        lodash.throttle = throttle;
        lodash.thru = thru;
        lodash.toArray = toArray;
        lodash.toPairs = toPairs;
        lodash.toPairsIn = toPairsIn;
        lodash.toPath = toPath;
        lodash.toPlainObject = toPlainObject;
        lodash.transform = transform;
        lodash.unary = unary;
        lodash.union = union;
        lodash.unionBy = unionBy;
        lodash.unionWith = unionWith;
        lodash.uniq = uniq;
        lodash.uniqBy = uniqBy;
        lodash.uniqWith = uniqWith;
        lodash.unset = unset;
        lodash.unzip = unzip;
        lodash.unzipWith = unzipWith;
        lodash.update = update;
        lodash.updateWith = updateWith;
        lodash.values = values;
        lodash.valuesIn = valuesIn;
        lodash.without = without;
        lodash.words = words;
        lodash.wrap = wrap;
        lodash.xor = xor;
        lodash.xorBy = xorBy;
        lodash.xorWith = xorWith;
        lodash.zip = zip;
        lodash.zipObject = zipObject;
        lodash.zipObjectDeep = zipObjectDeep;
        lodash.zipWith = zipWith;
        lodash.entries = toPairs;
        lodash.entriesIn = toPairsIn;
        lodash.extend = assignIn;
        lodash.extendWith = assignInWith;
        mixin(lodash, lodash);
        lodash.add = add;
        lodash.attempt = attempt;
        lodash.camelCase = camelCase;
        lodash.capitalize = capitalize;
        lodash.ceil = ceil;
        lodash.clamp = clamp;
        lodash.clone = clone;
        lodash.cloneDeep = cloneDeep;
        lodash.cloneDeepWith = cloneDeepWith;
        lodash.cloneWith = cloneWith;
        lodash.conformsTo = conformsTo;
        lodash.deburr = deburr;
        lodash.defaultTo = defaultTo;
        lodash.divide = divide;
        lodash.endsWith = endsWith;
        lodash.eq = eq;
        lodash.escape = escape;
        lodash.escapeRegExp = escapeRegExp;
        lodash.every = every;
        lodash.find = find;
        lodash.findIndex = findIndex;
        lodash.findKey = findKey;
        lodash.findLast = findLast;
        lodash.findLastIndex = findLastIndex;
        lodash.findLastKey = findLastKey;
        lodash.floor = floor;
        lodash.forEach = forEach;
        lodash.forEachRight = forEachRight;
        lodash.forIn = forIn;
        lodash.forInRight = forInRight;
        lodash.forOwn = forOwn;
        lodash.forOwnRight = forOwnRight;
        lodash.get = get;
        lodash.gt = gt2;
        lodash.gte = gte;
        lodash.has = has;
        lodash.hasIn = hasIn;
        lodash.head = head;
        lodash.identity = identity2;
        lodash.includes = includes;
        lodash.indexOf = indexOf;
        lodash.inRange = inRange;
        lodash.invoke = invoke;
        lodash.isArguments = isArguments;
        lodash.isArray = isArray;
        lodash.isArrayBuffer = isArrayBuffer;
        lodash.isArrayLike = isArrayLike;
        lodash.isArrayLikeObject = isArrayLikeObject;
        lodash.isBoolean = isBoolean;
        lodash.isBuffer = isBuffer;
        lodash.isDate = isDate;
        lodash.isElement = isElement;
        lodash.isEmpty = isEmpty;
        lodash.isEqual = isEqual;
        lodash.isEqualWith = isEqualWith;
        lodash.isError = isError;
        lodash.isFinite = isFinite2;
        lodash.isFunction = isFunction;
        lodash.isInteger = isInteger;
        lodash.isLength = isLength;
        lodash.isMap = isMap;
        lodash.isMatch = isMatch;
        lodash.isMatchWith = isMatchWith;
        lodash.isNaN = isNaN2;
        lodash.isNative = isNative;
        lodash.isNil = isNil;
        lodash.isNull = isNull;
        lodash.isNumber = isNumber;
        lodash.isObject = isObject;
        lodash.isObjectLike = isObjectLike;
        lodash.isPlainObject = isPlainObject;
        lodash.isRegExp = isRegExp;
        lodash.isSafeInteger = isSafeInteger;
        lodash.isSet = isSet;
        lodash.isString = isString;
        lodash.isSymbol = isSymbol;
        lodash.isTypedArray = isTypedArray;
        lodash.isUndefined = isUndefined;
        lodash.isWeakMap = isWeakMap;
        lodash.isWeakSet = isWeakSet;
        lodash.join = join;
        lodash.kebabCase = kebabCase;
        lodash.last = last;
        lodash.lastIndexOf = lastIndexOf;
        lodash.lowerCase = lowerCase;
        lodash.lowerFirst = lowerFirst;
        lodash.lt = lt2;
        lodash.lte = lte;
        lodash.max = max;
        lodash.maxBy = maxBy;
        lodash.mean = mean;
        lodash.meanBy = meanBy;
        lodash.min = min;
        lodash.minBy = minBy;
        lodash.stubArray = stubArray;
        lodash.stubFalse = stubFalse;
        lodash.stubObject = stubObject;
        lodash.stubString = stubString;
        lodash.stubTrue = stubTrue;
        lodash.multiply = multiply;
        lodash.nth = nth;
        lodash.noConflict = noConflict;
        lodash.noop = noop;
        lodash.now = now;
        lodash.pad = pad;
        lodash.padEnd = padEnd;
        lodash.padStart = padStart;
        lodash.parseInt = parseInt2;
        lodash.random = random;
        lodash.reduce = reduce;
        lodash.reduceRight = reduceRight;
        lodash.repeat = repeat;
        lodash.replace = replace2;
        lodash.result = result;
        lodash.round = round;
        lodash.runInContext = runInContext2;
        lodash.sample = sample;
        lodash.size = size;
        lodash.snakeCase = snakeCase;
        lodash.some = some;
        lodash.sortedIndex = sortedIndex;
        lodash.sortedIndexBy = sortedIndexBy;
        lodash.sortedIndexOf = sortedIndexOf;
        lodash.sortedLastIndex = sortedLastIndex;
        lodash.sortedLastIndexBy = sortedLastIndexBy;
        lodash.sortedLastIndexOf = sortedLastIndexOf;
        lodash.startCase = startCase;
        lodash.startsWith = startsWith;
        lodash.subtract = subtract;
        lodash.sum = sum;
        lodash.sumBy = sumBy;
        lodash.template = template;
        lodash.times = times;
        lodash.toFinite = toFinite;
        lodash.toInteger = toInteger;
        lodash.toLength = toLength;
        lodash.toLower = toLower;
        lodash.toNumber = toNumber;
        lodash.toSafeInteger = toSafeInteger;
        lodash.toString = toString;
        lodash.toUpper = toUpper;
        lodash.trim = trim2;
        lodash.trimEnd = trimEnd;
        lodash.trimStart = trimStart;
        lodash.truncate = truncate;
        lodash.unescape = unescape;
        lodash.uniqueId = uniqueId;
        lodash.upperCase = upperCase;
        lodash.upperFirst = upperFirst;
        lodash.each = forEach;
        lodash.eachRight = forEachRight;
        lodash.first = head;
        mixin(lodash, (function() {
          var source = {};
          baseForOwn(lodash, function(func, methodName) {
            if (!hasOwnProperty.call(lodash.prototype, methodName)) {
              source[methodName] = func;
            }
          });
          return source;
        })(), { "chain": false });
        lodash.VERSION = VERSION;
        arrayEach(["bind", "bindKey", "curry", "curryRight", "partial", "partialRight"], function(methodName) {
          lodash[methodName].placeholder = lodash;
        });
        arrayEach(["drop", "take"], function(methodName, index) {
          LazyWrapper.prototype[methodName] = function(n15) {
            n15 = n15 === undefined2 ? 1 : nativeMax(toInteger(n15), 0);
            var result2 = this.__filtered__ && !index ? new LazyWrapper(this) : this.clone();
            if (result2.__filtered__) {
              result2.__takeCount__ = nativeMin(n15, result2.__takeCount__);
            } else {
              result2.__views__.push({
                "size": nativeMin(n15, MAX_ARRAY_LENGTH),
                "type": methodName + (result2.__dir__ < 0 ? "Right" : "")
              });
            }
            return result2;
          };
          LazyWrapper.prototype[methodName + "Right"] = function(n15) {
            return this.reverse()[methodName](n15).reverse();
          };
        });
        arrayEach(["filter", "map", "takeWhile"], function(methodName, index) {
          var type = index + 1, isFilter = type == LAZY_FILTER_FLAG || type == LAZY_WHILE_FLAG;
          LazyWrapper.prototype[methodName] = function(iteratee2) {
            var result2 = this.clone();
            result2.__iteratees__.push({
              "iteratee": getIteratee(iteratee2, 3),
              "type": type
            });
            result2.__filtered__ = result2.__filtered__ || isFilter;
            return result2;
          };
        });
        arrayEach(["head", "last"], function(methodName, index) {
          var takeName = "take" + (index ? "Right" : "");
          LazyWrapper.prototype[methodName] = function() {
            return this[takeName](1).value()[0];
          };
        });
        arrayEach(["initial", "tail"], function(methodName, index) {
          var dropName = "drop" + (index ? "" : "Right");
          LazyWrapper.prototype[methodName] = function() {
            return this.__filtered__ ? new LazyWrapper(this) : this[dropName](1);
          };
        });
        LazyWrapper.prototype.compact = function() {
          return this.filter(identity2);
        };
        LazyWrapper.prototype.find = function(predicate) {
          return this.filter(predicate).head();
        };
        LazyWrapper.prototype.findLast = function(predicate) {
          return this.reverse().find(predicate);
        };
        LazyWrapper.prototype.invokeMap = baseRest(function(path, args) {
          if (typeof path == "function") {
            return new LazyWrapper(this);
          }
          return this.map(function(value) {
            return baseInvoke(value, path, args);
          });
        });
        LazyWrapper.prototype.reject = function(predicate) {
          return this.filter(negate(getIteratee(predicate)));
        };
        LazyWrapper.prototype.slice = function(start, end) {
          start = toInteger(start);
          var result2 = this;
          if (result2.__filtered__ && (start > 0 || end < 0)) {
            return new LazyWrapper(result2);
          }
          if (start < 0) {
            result2 = result2.takeRight(-start);
          } else if (start) {
            result2 = result2.drop(start);
          }
          if (end !== undefined2) {
            end = toInteger(end);
            result2 = end < 0 ? result2.dropRight(-end) : result2.take(end - start);
          }
          return result2;
        };
        LazyWrapper.prototype.takeRightWhile = function(predicate) {
          return this.reverse().takeWhile(predicate).reverse();
        };
        LazyWrapper.prototype.toArray = function() {
          return this.take(MAX_ARRAY_LENGTH);
        };
        baseForOwn(LazyWrapper.prototype, function(func, methodName) {
          var checkIteratee = /^(?:filter|find|map|reject)|While$/.test(methodName), isTaker = /^(?:head|last)$/.test(methodName), lodashFunc = lodash[isTaker ? "take" + (methodName == "last" ? "Right" : "") : methodName], retUnwrapped = isTaker || /^find/.test(methodName);
          if (!lodashFunc) {
            return;
          }
          lodash.prototype[methodName] = function() {
            var value = this.__wrapped__, args = isTaker ? [1] : arguments, isLazy = value instanceof LazyWrapper, iteratee2 = args[0], useLazy = isLazy || isArray(value);
            var interceptor = function(value2) {
              var result3 = lodashFunc.apply(lodash, arrayPush([value2], args));
              return isTaker && chainAll ? result3[0] : result3;
            };
            if (useLazy && checkIteratee && typeof iteratee2 == "function" && iteratee2.length != 1) {
              isLazy = useLazy = false;
            }
            var chainAll = this.__chain__, isHybrid = !!this.__actions__.length, isUnwrapped = retUnwrapped && !chainAll, onlyLazy = isLazy && !isHybrid;
            if (!retUnwrapped && useLazy) {
              value = onlyLazy ? value : new LazyWrapper(this);
              var result2 = func.apply(value, args);
              result2.__actions__.push({ "func": thru, "args": [interceptor], "thisArg": undefined2 });
              return new LodashWrapper(result2, chainAll);
            }
            if (isUnwrapped && onlyLazy) {
              return func.apply(this, args);
            }
            result2 = this.thru(interceptor);
            return isUnwrapped ? isTaker ? result2.value()[0] : result2.value() : result2;
          };
        });
        arrayEach(["pop", "push", "shift", "sort", "splice", "unshift"], function(methodName) {
          var func = arrayProto[methodName], chainName = /^(?:push|sort|unshift)$/.test(methodName) ? "tap" : "thru", retUnwrapped = /^(?:pop|shift)$/.test(methodName);
          lodash.prototype[methodName] = function() {
            var args = arguments;
            if (retUnwrapped && !this.__chain__) {
              var value = this.value();
              return func.apply(isArray(value) ? value : [], args);
            }
            return this[chainName](function(value2) {
              return func.apply(isArray(value2) ? value2 : [], args);
            });
          };
        });
        baseForOwn(LazyWrapper.prototype, function(func, methodName) {
          var lodashFunc = lodash[methodName];
          if (lodashFunc) {
            var key = lodashFunc.name + "";
            if (!hasOwnProperty.call(realNames, key)) {
              realNames[key] = [];
            }
            realNames[key].push({ "name": methodName, "func": lodashFunc });
          }
        });
        realNames[createHybrid(undefined2, WRAP_BIND_KEY_FLAG).name] = [{
          "name": "wrapper",
          "func": undefined2
        }];
        LazyWrapper.prototype.clone = lazyClone;
        LazyWrapper.prototype.reverse = lazyReverse;
        LazyWrapper.prototype.value = lazyValue;
        lodash.prototype.at = wrapperAt;
        lodash.prototype.chain = wrapperChain;
        lodash.prototype.commit = wrapperCommit;
        lodash.prototype.next = wrapperNext;
        lodash.prototype.plant = wrapperPlant;
        lodash.prototype.reverse = wrapperReverse;
        lodash.prototype.toJSON = lodash.prototype.valueOf = lodash.prototype.value = wrapperValue;
        lodash.prototype.first = lodash.prototype.head;
        if (symIterator) {
          lodash.prototype[symIterator] = wrapperToIterator;
        }
        return lodash;
      });
      var _8 = runInContext();
      if (typeof define == "function" && typeof define.amd == "object" && define.amd) {
        root._ = _8;
        define(function() {
          return _8;
        });
      } else if (freeModule) {
        (freeModule.exports = _8)._ = _8;
        freeExports._ = _8;
      } else {
        root._ = _8;
      }
    }).call(exports);
  }
});

// node_modules/react-is/cjs/react-is.development.js
var require_react_is_development = __commonJS({
  "node_modules/react-is/cjs/react-is.development.js"(exports) {
    "use strict";
    if (true) {
      (function() {
        "use strict";
        var hasSymbol = typeof Symbol === "function" && Symbol.for;
        var REACT_ELEMENT_TYPE = hasSymbol ? Symbol.for("react.element") : 60103;
        var REACT_PORTAL_TYPE = hasSymbol ? Symbol.for("react.portal") : 60106;
        var REACT_FRAGMENT_TYPE = hasSymbol ? Symbol.for("react.fragment") : 60107;
        var REACT_STRICT_MODE_TYPE = hasSymbol ? Symbol.for("react.strict_mode") : 60108;
        var REACT_PROFILER_TYPE = hasSymbol ? Symbol.for("react.profiler") : 60114;
        var REACT_PROVIDER_TYPE = hasSymbol ? Symbol.for("react.provider") : 60109;
        var REACT_CONTEXT_TYPE = hasSymbol ? Symbol.for("react.context") : 60110;
        var REACT_ASYNC_MODE_TYPE = hasSymbol ? Symbol.for("react.async_mode") : 60111;
        var REACT_CONCURRENT_MODE_TYPE = hasSymbol ? Symbol.for("react.concurrent_mode") : 60111;
        var REACT_FORWARD_REF_TYPE = hasSymbol ? Symbol.for("react.forward_ref") : 60112;
        var REACT_SUSPENSE_TYPE = hasSymbol ? Symbol.for("react.suspense") : 60113;
        var REACT_SUSPENSE_LIST_TYPE = hasSymbol ? Symbol.for("react.suspense_list") : 60120;
        var REACT_MEMO_TYPE = hasSymbol ? Symbol.for("react.memo") : 60115;
        var REACT_LAZY_TYPE = hasSymbol ? Symbol.for("react.lazy") : 60116;
        var REACT_BLOCK_TYPE = hasSymbol ? Symbol.for("react.block") : 60121;
        var REACT_FUNDAMENTAL_TYPE = hasSymbol ? Symbol.for("react.fundamental") : 60117;
        var REACT_RESPONDER_TYPE = hasSymbol ? Symbol.for("react.responder") : 60118;
        var REACT_SCOPE_TYPE = hasSymbol ? Symbol.for("react.scope") : 60119;
        function isValidElementType(type) {
          return typeof type === "string" || typeof type === "function" || // Note: its typeof might be other than 'symbol' or 'number' if it's a polyfill.
          type === REACT_FRAGMENT_TYPE || type === REACT_CONCURRENT_MODE_TYPE || type === REACT_PROFILER_TYPE || type === REACT_STRICT_MODE_TYPE || type === REACT_SUSPENSE_TYPE || type === REACT_SUSPENSE_LIST_TYPE || typeof type === "object" && type !== null && (type.$$typeof === REACT_LAZY_TYPE || type.$$typeof === REACT_MEMO_TYPE || type.$$typeof === REACT_PROVIDER_TYPE || type.$$typeof === REACT_CONTEXT_TYPE || type.$$typeof === REACT_FORWARD_REF_TYPE || type.$$typeof === REACT_FUNDAMENTAL_TYPE || type.$$typeof === REACT_RESPONDER_TYPE || type.$$typeof === REACT_SCOPE_TYPE || type.$$typeof === REACT_BLOCK_TYPE);
        }
        function typeOf(object) {
          if (typeof object === "object" && object !== null) {
            var $$typeof = object.$$typeof;
            switch ($$typeof) {
              case REACT_ELEMENT_TYPE:
                var type = object.type;
                switch (type) {
                  case REACT_ASYNC_MODE_TYPE:
                  case REACT_CONCURRENT_MODE_TYPE:
                  case REACT_FRAGMENT_TYPE:
                  case REACT_PROFILER_TYPE:
                  case REACT_STRICT_MODE_TYPE:
                  case REACT_SUSPENSE_TYPE:
                    return type;
                  default:
                    var $$typeofType = type && type.$$typeof;
                    switch ($$typeofType) {
                      case REACT_CONTEXT_TYPE:
                      case REACT_FORWARD_REF_TYPE:
                      case REACT_LAZY_TYPE:
                      case REACT_MEMO_TYPE:
                      case REACT_PROVIDER_TYPE:
                        return $$typeofType;
                      default:
                        return $$typeof;
                    }
                }
              case REACT_PORTAL_TYPE:
                return $$typeof;
            }
          }
          return void 0;
        }
        var AsyncMode = REACT_ASYNC_MODE_TYPE;
        var ConcurrentMode = REACT_CONCURRENT_MODE_TYPE;
        var ContextConsumer = REACT_CONTEXT_TYPE;
        var ContextProvider = REACT_PROVIDER_TYPE;
        var Element = REACT_ELEMENT_TYPE;
        var ForwardRef = REACT_FORWARD_REF_TYPE;
        var Fragment6 = REACT_FRAGMENT_TYPE;
        var Lazy = REACT_LAZY_TYPE;
        var Memo = REACT_MEMO_TYPE;
        var Portal = REACT_PORTAL_TYPE;
        var Profiler = REACT_PROFILER_TYPE;
        var StrictMode = REACT_STRICT_MODE_TYPE;
        var Suspense = REACT_SUSPENSE_TYPE;
        var hasWarnedAboutDeprecatedIsAsyncMode = false;
        function isAsyncMode(object) {
          {
            if (!hasWarnedAboutDeprecatedIsAsyncMode) {
              hasWarnedAboutDeprecatedIsAsyncMode = true;
              console["warn"]("The ReactIs.isAsyncMode() alias has been deprecated, and will be removed in React 17+. Update your code to use ReactIs.isConcurrentMode() instead. It has the exact same API.");
            }
          }
          return isConcurrentMode(object) || typeOf(object) === REACT_ASYNC_MODE_TYPE;
        }
        function isConcurrentMode(object) {
          return typeOf(object) === REACT_CONCURRENT_MODE_TYPE;
        }
        function isContextConsumer(object) {
          return typeOf(object) === REACT_CONTEXT_TYPE;
        }
        function isContextProvider(object) {
          return typeOf(object) === REACT_PROVIDER_TYPE;
        }
        function isElement(object) {
          return typeof object === "object" && object !== null && object.$$typeof === REACT_ELEMENT_TYPE;
        }
        function isForwardRef(object) {
          return typeOf(object) === REACT_FORWARD_REF_TYPE;
        }
        function isFragment(object) {
          return typeOf(object) === REACT_FRAGMENT_TYPE;
        }
        function isLazy(object) {
          return typeOf(object) === REACT_LAZY_TYPE;
        }
        function isMemo(object) {
          return typeOf(object) === REACT_MEMO_TYPE;
        }
        function isPortal(object) {
          return typeOf(object) === REACT_PORTAL_TYPE;
        }
        function isProfiler(object) {
          return typeOf(object) === REACT_PROFILER_TYPE;
        }
        function isStrictMode(object) {
          return typeOf(object) === REACT_STRICT_MODE_TYPE;
        }
        function isSuspense(object) {
          return typeOf(object) === REACT_SUSPENSE_TYPE;
        }
        exports.AsyncMode = AsyncMode;
        exports.ConcurrentMode = ConcurrentMode;
        exports.ContextConsumer = ContextConsumer;
        exports.ContextProvider = ContextProvider;
        exports.Element = Element;
        exports.ForwardRef = ForwardRef;
        exports.Fragment = Fragment6;
        exports.Lazy = Lazy;
        exports.Memo = Memo;
        exports.Portal = Portal;
        exports.Profiler = Profiler;
        exports.StrictMode = StrictMode;
        exports.Suspense = Suspense;
        exports.isAsyncMode = isAsyncMode;
        exports.isConcurrentMode = isConcurrentMode;
        exports.isContextConsumer = isContextConsumer;
        exports.isContextProvider = isContextProvider;
        exports.isElement = isElement;
        exports.isForwardRef = isForwardRef;
        exports.isFragment = isFragment;
        exports.isLazy = isLazy;
        exports.isMemo = isMemo;
        exports.isPortal = isPortal;
        exports.isProfiler = isProfiler;
        exports.isStrictMode = isStrictMode;
        exports.isSuspense = isSuspense;
        exports.isValidElementType = isValidElementType;
        exports.typeOf = typeOf;
      })();
    }
  }
});

// node_modules/react-is/index.js
var require_react_is = __commonJS({
  "node_modules/react-is/index.js"(exports, module) {
    "use strict";
    if (false) {
      module.exports = null;
    } else {
      module.exports = require_react_is_development();
    }
  }
});

// node_modules/hoist-non-react-statics/dist/hoist-non-react-statics.cjs.js
var require_hoist_non_react_statics_cjs = __commonJS({
  "node_modules/hoist-non-react-statics/dist/hoist-non-react-statics.cjs.js"(exports, module) {
    "use strict";
    var reactIs = require_react_is();
    var REACT_STATICS = {
      childContextTypes: true,
      contextType: true,
      contextTypes: true,
      defaultProps: true,
      displayName: true,
      getDefaultProps: true,
      getDerivedStateFromError: true,
      getDerivedStateFromProps: true,
      mixins: true,
      propTypes: true,
      type: true
    };
    var KNOWN_STATICS = {
      name: true,
      length: true,
      prototype: true,
      caller: true,
      callee: true,
      arguments: true,
      arity: true
    };
    var FORWARD_REF_STATICS = {
      "$$typeof": true,
      render: true,
      defaultProps: true,
      displayName: true,
      propTypes: true
    };
    var MEMO_STATICS = {
      "$$typeof": true,
      compare: true,
      defaultProps: true,
      displayName: true,
      propTypes: true,
      type: true
    };
    var TYPE_STATICS = {};
    TYPE_STATICS[reactIs.ForwardRef] = FORWARD_REF_STATICS;
    TYPE_STATICS[reactIs.Memo] = MEMO_STATICS;
    function getStatics(component) {
      if (reactIs.isMemo(component)) {
        return MEMO_STATICS;
      }
      return TYPE_STATICS[component["$$typeof"]] || REACT_STATICS;
    }
    var defineProperty = Object.defineProperty;
    var getOwnPropertyNames = Object.getOwnPropertyNames;
    var getOwnPropertySymbols = Object.getOwnPropertySymbols;
    var getOwnPropertyDescriptor = Object.getOwnPropertyDescriptor;
    var getPrototypeOf = Object.getPrototypeOf;
    var objectPrototype = Object.prototype;
    function hoistNonReactStatics2(targetComponent, sourceComponent, blacklist) {
      if (typeof sourceComponent !== "string") {
        if (objectPrototype) {
          var inheritedComponent = getPrototypeOf(sourceComponent);
          if (inheritedComponent && inheritedComponent !== objectPrototype) {
            hoistNonReactStatics2(targetComponent, inheritedComponent, blacklist);
          }
        }
        var keys = getOwnPropertyNames(sourceComponent);
        if (getOwnPropertySymbols) {
          keys = keys.concat(getOwnPropertySymbols(sourceComponent));
        }
        var targetStatics = getStatics(targetComponent);
        var sourceStatics = getStatics(sourceComponent);
        for (var i11 = 0; i11 < keys.length; ++i11) {
          var key = keys[i11];
          if (!KNOWN_STATICS[key] && !(blacklist && blacklist[key]) && !(sourceStatics && sourceStatics[key]) && !(targetStatics && targetStatics[key])) {
            var descriptor = getOwnPropertyDescriptor(sourceComponent, key);
            try {
              defineProperty(targetComponent, key, descriptor);
            } catch (e6) {
            }
          }
        }
      }
      return targetComponent;
    }
    module.exports = hoistNonReactStatics2;
  }
});

// node_modules/@hakit/core/dist/es/utils/colors/convert-light-color.js
var import_lodash = __toESM(require_lodash(), 1);
var _ = 2700;
var p = 6500;
var I = (t9) => {
  const r13 = t9 / 100;
  return [R(r13), T(r13), A(r13)];
};
var R = (t9) => {
  if (t9 <= 66)
    return 255;
  const r13 = 329.698727446 * (t9 - 60) ** -0.1332047592;
  return (0, import_lodash.clamp)(r13, 0, 255);
};
var T = (t9) => {
  let r13;
  return t9 <= 66 ? r13 = 99.4708025861 * Math.log(t9) - 161.1195681661 : r13 = 288.1221695283 * (t9 - 60) ** -0.0755148492, (0, import_lodash.clamp)(r13, 0, 255);
};
var A = (t9) => {
  if (t9 >= 66)
    return 255;
  if (t9 <= 19)
    return 0;
  const r13 = 138.5177312231 * Math.log(t9 - 10) - 305.0447927307;
  return (0, import_lodash.clamp)(r13, 0, 255);
};
var h = (t9, r13) => {
  const e6 = Math.max(...t9), o13 = Math.max(...r13);
  let n15;
  return o13 === 0 ? n15 = 0 : n15 = e6 / o13, r13.map((c15) => Math.round(c15 * n15));
};
var K = (t9) => Math.floor(1e6 / t9);
var u = (t9) => Math.floor(1e6 / t9);
var k = (t9, r13, e6) => {
  const [o13, n15, c15, a13, s9] = t9, w3 = u(r13 ?? _), g10 = u(e6 ?? p), b11 = w3 - g10;
  let l10;
  try {
    l10 = s9 / (a13 + s9);
  } catch (L3) {
    console.log("Error:", L3), l10 = 0.5;
  }
  const M4 = g10 + l10 * b11, d12 = M4 ? K(M4) : 0, [f13, x6, v10] = I(d12), i11 = Math.max(a13, s9) / 255, E7 = [o13 + f13 * i11, n15 + x6 * i11, c15 + v10 * i11];
  return h([o13, n15, c15, a13, s9], E7);
};
var B = (t9) => {
  const [r13, e6, o13, n15] = t9, c15 = [r13 + n15, e6 + n15, o13 + n15];
  return h([r13, e6, o13, n15], c15);
};

// node_modules/@hakit/core/dist/es/utils/colors/hex.js
var a = (t9) => {
  if (t9 = t9.replace("#", ""), t9.length === 6) return t9;
  let r13 = "";
  for (const e6 of t9)
    r13 += e6 + e6;
  return r13;
};
var f = (t9, r13, e6 = 50) => {
  let l10 = "";
  t9 = a(t9), r13 = a(r13);
  for (let n15 = 0; n15 <= 5; n15 += 2) {
    const u15 = parseInt(t9.substr(n15, 2), 16), s9 = parseInt(r13.substr(n15, 2), 16);
    let o13 = Math.floor(s9 + (u15 - s9) * (e6 / 100)).toString(16);
    for (; o13.length < 2; ) o13 = "0" + o13;
    l10 += o13;
  }
  return `#${l10}`;
};

// node_modules/@hakit/core/dist/es/utils/colors/convert-color.js
var b = (t9) => {
  const s9 = Math.round(Math.min(Math.max(t9, 0), 255)).toString(16);
  return s9.length === 1 ? `0${s9}` : s9;
};
var S = (t9) => (t9 = a(t9), [parseInt(t9.substring(0, 2), 16), parseInt(t9.substring(2, 4), 16), parseInt(t9.substring(4, 6), 16)]);
var f2 = (t9) => `#${b(t9[0])}${b(t9[1])}${b(t9[2])}`;
var h2 = 0.95047;
var i = 1;
var l = 1.08883;
var m2 = 0.137931034;
var v = 0.206896552;
var y = 0.12841855;
var N = 8856452e-9;
var a2 = (t9) => (t9 /= 255, t9 <= 0.04045 ? t9 / 12.92 : ((t9 + 0.055) / 1.055) ** 2.4);
var g = (t9) => t9 > N ? t9 ** (1 / 3) : t9 / y + m2;
var u2 = (t9) => 255 * (t9 <= 304e-5 ? 12.92 * t9 : 1.055 * t9 ** (1 / 2.4) - 0.055);
var x = (t9) => t9 > v ? t9 * t9 * t9 : y * (t9 - m2);
var $ = (t9) => {
  let [s9, r13, o13] = t9;
  s9 = a2(s9), r13 = a2(r13), o13 = a2(o13);
  const n15 = g((0.4124564 * s9 + 0.3575761 * r13 + 0.1804375 * o13) / h2), c15 = g((0.2126729 * s9 + 0.7151522 * r13 + 0.072175 * o13) / i), e6 = g((0.0193339 * s9 + 0.119192 * r13 + 0.9503041 * o13) / l);
  return [n15, c15, e6];
};
var X = (t9) => {
  const [s9, r13, o13] = $(t9), n15 = 116 * r13 - 16;
  return [n15 < 0 ? 0 : n15, 500 * (s9 - r13), 200 * (r13 - o13)];
};
var I2 = (t9) => {
  const [s9, r13, o13] = t9;
  let n15 = (s9 + 16) / 116, c15 = isNaN(r13) ? n15 : n15 + r13 / 500, e6 = isNaN(o13) ? n15 : n15 - o13 / 200;
  n15 = i * x(n15), c15 = h2 * x(c15), e6 = l * x(e6);
  const z5 = u2(3.2404542 * c15 - 1.5371385 * n15 - 0.4985314 * e6), M4 = u2(-0.969266 * c15 + 1.8760108 * n15 + 0.041556 * e6), p8 = u2(0.0556434 * c15 - 0.2040259 * n15 + 1.0572252 * e6);
  return [z5, M4, p8];
};
var Y = (t9) => {
  const s9 = I2(t9);
  return f2(s9);
};
var d = (t9) => {
  const [s9, r13, o13] = t9, n15 = Math.max(s9, r13, o13), c15 = n15 - Math.min(s9, r13, o13), e6 = c15 && (n15 === s9 ? (r13 - o13) / c15 : n15 === r13 ? 2 + (o13 - s9) / c15 : 4 + (s9 - r13) / c15);
  return [60 * (e6 < 0 ? e6 + 6 : e6), n15 && c15 / n15, n15];
};
var k2 = (t9) => {
  const [s9, r13, o13] = t9, n15 = (c15) => {
    const e6 = (c15 + s9 / 60) % 6;
    return o13 - o13 * r13 * Math.max(Math.min(e6, 4 - e6, 1), 0);
  };
  return [n15(5), n15(3), n15(1)];
};
var Z = (t9) => d(t9).slice(0, 2);
var j = (t9) => k2([t9[0], t9[1], 255]);

// node_modules/@hakit/core/dist/es/utils/colors/colors.js
var f3 = [
  "#44739e",
  "#984ea3",
  "#00d2d5",
  "#ff7f00",
  "#af8d00",
  "#7f80cd",
  "#b3e900",
  "#c42e60",
  "#a65628",
  "#f781bf",
  "#8dd3c7",
  "#bebada",
  "#fb8072",
  "#80b1d3",
  "#fdb462",
  "#fccde5",
  "#bc80bd",
  "#ffed6f",
  "#c4eaff",
  "#cf8c00",
  "#1b9e77",
  "#d95f02",
  "#e7298a",
  "#e6ab02",
  "#a6761d",
  "#0097ff",
  "#00d067",
  "#f43600",
  "#4ba93b",
  "#5779bb",
  "#927acc",
  "#97ee3f",
  "#bf3947",
  "#9f5b00",
  "#f48758",
  "#8caed6",
  "#f2b94f",
  "#eff26e",
  "#e43872",
  "#d9b100",
  "#9d7a00",
  "#698cff",
  "#d9d9d9",
  "#00d27e",
  "#d06800",
  "#009f82",
  "#c49200",
  "#cbe8ff",
  "#fecddf",
  "#c27eb6",
  "#8cd2ce",
  "#c4b8d9",
  "#f883b0",
  "#a49100",
  "#f48800",
  "#27d0df",
  "#a04a9b"
];
function e(d12) {
  return f3[d12 % f3.length];
}

// node_modules/@hakit/core/dist/es/utils/colors/compute-color.js
var r = /* @__PURE__ */ new Set([
  "primary",
  "accent",
  "disabled",
  "red",
  "pink",
  "purple",
  "deep-purple",
  "indigo",
  "blue",
  "light-blue",
  "cyan",
  "teal",
  "green",
  "light-green",
  "lime",
  "yellow",
  "amber",
  "orange",
  "deep-orange",
  "brown",
  "light-grey",
  "grey",
  "dark-grey",
  "blue-grey",
  "black",
  "white"
]);
function n(e6) {
  return r.has(e6) ? `var(--${e6}-color)` : e6;
}

// node_modules/@hakit/core/dist/es/utils/colors/rgb.js
var s = (r13) => {
  const t9 = [0, 0, 0];
  for (let n15 = 0; n15 < r13.length; n15++) {
    const o13 = r13[n15] / 255;
    t9[n15] = o13 <= 0.03928 ? o13 / 12.92 : ((o13 + 0.055) / 1.055) ** 2.4;
  }
  return 0.2126 * t9[0] + 0.7152 * t9[1] + 0.0722 * t9[2];
};
var u3 = (r13, t9) => {
  const n15 = s(r13), o13 = s(t9);
  return n15 > o13 ? (n15 + 0.05) / (o13 + 0.05) : (o13 + 0.05) / (n15 + 0.05);
};
var c = (r13, t9) => Math.round((u3(r13, t9) + Number.EPSILON) * 100) / 100;

// node_modules/@hakit/core/dist/es/utils/colors/index.js
var import_lodash7 = __toESM(require_lodash(), 1);

// node_modules/@hakit/core/dist/index-1tDtVGr2.js
var q2 = __toESM(require_jsx_runtime(), 1);
var H2 = __toESM(require_react(), 1);
var import_react8 = __toESM(require_react(), 1);

// node_modules/@emotion/sheet/dist/emotion-sheet.development.esm.js
var isDevelopment = true;
function sheetForTag(tag) {
  if (tag.sheet) {
    return tag.sheet;
  }
  for (var i11 = 0; i11 < document.styleSheets.length; i11++) {
    if (document.styleSheets[i11].ownerNode === tag) {
      return document.styleSheets[i11];
    }
  }
  return void 0;
}
function createStyleElement(options) {
  var tag = document.createElement("style");
  tag.setAttribute("data-emotion", options.key);
  if (options.nonce !== void 0) {
    tag.setAttribute("nonce", options.nonce);
  }
  tag.appendChild(document.createTextNode(""));
  tag.setAttribute("data-s", "");
  return tag;
}
var StyleSheet = (function() {
  function StyleSheet2(options) {
    var _this = this;
    this._insertTag = function(tag) {
      var before;
      if (_this.tags.length === 0) {
        if (_this.insertionPoint) {
          before = _this.insertionPoint.nextSibling;
        } else if (_this.prepend) {
          before = _this.container.firstChild;
        } else {
          before = _this.before;
        }
      } else {
        before = _this.tags[_this.tags.length - 1].nextSibling;
      }
      _this.container.insertBefore(tag, before);
      _this.tags.push(tag);
    };
    this.isSpeedy = options.speedy === void 0 ? !isDevelopment : options.speedy;
    this.tags = [];
    this.ctr = 0;
    this.nonce = options.nonce;
    this.key = options.key;
    this.container = options.container;
    this.prepend = options.prepend;
    this.insertionPoint = options.insertionPoint;
    this.before = null;
  }
  var _proto = StyleSheet2.prototype;
  _proto.hydrate = function hydrate(nodes) {
    nodes.forEach(this._insertTag);
  };
  _proto.insert = function insert(rule) {
    if (this.ctr % (this.isSpeedy ? 65e3 : 1) === 0) {
      this._insertTag(createStyleElement(this));
    }
    var tag = this.tags[this.tags.length - 1];
    {
      var isImportRule3 = rule.charCodeAt(0) === 64 && rule.charCodeAt(1) === 105;
      if (isImportRule3 && this._alreadyInsertedOrderInsensitiveRule) {
        console.error("You're attempting to insert the following rule:\n" + rule + "\n\n`@import` rules must be before all other types of rules in a stylesheet but other rules have already been inserted. Please ensure that `@import` rules are before all other rules.");
      }
      this._alreadyInsertedOrderInsensitiveRule = this._alreadyInsertedOrderInsensitiveRule || !isImportRule3;
    }
    if (this.isSpeedy) {
      var sheet = sheetForTag(tag);
      try {
        sheet.insertRule(rule, sheet.cssRules.length);
      } catch (e6) {
        if (!/:(-moz-placeholder|-moz-focus-inner|-moz-focusring|-ms-input-placeholder|-moz-read-write|-moz-read-only|-ms-clear|-ms-expand|-ms-reveal){/.test(rule)) {
          console.error('There was a problem inserting the following rule: "' + rule + '"', e6);
        }
      }
    } else {
      tag.appendChild(document.createTextNode(rule));
    }
    this.ctr++;
  };
  _proto.flush = function flush() {
    this.tags.forEach(function(tag) {
      var _tag$parentNode;
      return (_tag$parentNode = tag.parentNode) == null ? void 0 : _tag$parentNode.removeChild(tag);
    });
    this.tags = [];
    this.ctr = 0;
    {
      this._alreadyInsertedOrderInsensitiveRule = false;
    }
  };
  return StyleSheet2;
})();

// node_modules/stylis/src/Enum.js
var MS = "-ms-";
var MOZ = "-moz-";
var WEBKIT = "-webkit-";
var COMMENT = "comm";
var RULESET = "rule";
var DECLARATION = "decl";
var IMPORT = "@import";
var KEYFRAMES = "@keyframes";
var LAYER = "@layer";

// node_modules/stylis/src/Utility.js
var abs = Math.abs;
var from = String.fromCharCode;
var assign = Object.assign;
function hash(value, length2) {
  return charat(value, 0) ^ 45 ? (((length2 << 2 ^ charat(value, 0)) << 2 ^ charat(value, 1)) << 2 ^ charat(value, 2)) << 2 ^ charat(value, 3) : 0;
}
function trim(value) {
  return value.trim();
}
function match(value, pattern) {
  return (value = pattern.exec(value)) ? value[0] : value;
}
function replace(value, pattern, replacement) {
  return value.replace(pattern, replacement);
}
function indexof(value, search) {
  return value.indexOf(search);
}
function charat(value, index) {
  return value.charCodeAt(index) | 0;
}
function substr(value, begin, end) {
  return value.slice(begin, end);
}
function strlen(value) {
  return value.length;
}
function sizeof(value) {
  return value.length;
}
function append(value, array) {
  return array.push(value), value;
}
function combine(array, callback) {
  return array.map(callback).join("");
}

// node_modules/stylis/src/Tokenizer.js
var line = 1;
var column = 1;
var length = 0;
var position = 0;
var character = 0;
var characters = "";
function node(value, root, parent, type, props, children, length2) {
  return { value, root, parent, type, props, children, line, column, length: length2, return: "" };
}
function copy(root, props) {
  return assign(node("", null, null, "", null, null, 0), root, { length: -root.length }, props);
}
function char() {
  return character;
}
function prev() {
  character = position > 0 ? charat(characters, --position) : 0;
  if (column--, character === 10)
    column = 1, line--;
  return character;
}
function next() {
  character = position < length ? charat(characters, position++) : 0;
  if (column++, character === 10)
    column = 1, line++;
  return character;
}
function peek() {
  return charat(characters, position);
}
function caret() {
  return position;
}
function slice(begin, end) {
  return substr(characters, begin, end);
}
function token(type) {
  switch (type) {
    // \0 \t \n \r \s whitespace token
    case 0:
    case 9:
    case 10:
    case 13:
    case 32:
      return 5;
    // ! + , / > @ ~ isolate token
    case 33:
    case 43:
    case 44:
    case 47:
    case 62:
    case 64:
    case 126:
    // ; { } breakpoint token
    case 59:
    case 123:
    case 125:
      return 4;
    // : accompanied token
    case 58:
      return 3;
    // " ' ( [ opening delimit token
    case 34:
    case 39:
    case 40:
    case 91:
      return 2;
    // ) ] closing delimit token
    case 41:
    case 93:
      return 1;
  }
  return 0;
}
function alloc(value) {
  return line = column = 1, length = strlen(characters = value), position = 0, [];
}
function dealloc(value) {
  return characters = "", value;
}
function delimit(type) {
  return trim(slice(position - 1, delimiter(type === 91 ? type + 2 : type === 40 ? type + 1 : type)));
}
function whitespace(type) {
  while (character = peek())
    if (character < 33)
      next();
    else
      break;
  return token(type) > 2 || token(character) > 3 ? "" : " ";
}
function escaping(index, count) {
  while (--count && next())
    if (character < 48 || character > 102 || character > 57 && character < 65 || character > 70 && character < 97)
      break;
  return slice(index, caret() + (count < 6 && peek() == 32 && next() == 32));
}
function delimiter(type) {
  while (next())
    switch (character) {
      // ] ) " '
      case type:
        return position;
      // " '
      case 34:
      case 39:
        if (type !== 34 && type !== 39)
          delimiter(character);
        break;
      // (
      case 40:
        if (type === 41)
          delimiter(type);
        break;
      // \
      case 92:
        next();
        break;
    }
  return position;
}
function commenter(type, index) {
  while (next())
    if (type + character === 47 + 10)
      break;
    else if (type + character === 42 + 42 && peek() === 47)
      break;
  return "/*" + slice(index, position - 1) + "*" + from(type === 47 ? type : next());
}
function identifier(index) {
  while (!token(peek()))
    next();
  return slice(index, position);
}

// node_modules/stylis/src/Parser.js
function compile(value) {
  return dealloc(parse("", null, null, null, [""], value = alloc(value), 0, [0], value));
}
function parse(value, root, parent, rule, rules, rulesets, pseudo, points, declarations) {
  var index = 0;
  var offset = 0;
  var length2 = pseudo;
  var atrule = 0;
  var property = 0;
  var previous = 0;
  var variable = 1;
  var scanning = 1;
  var ampersand = 1;
  var character2 = 0;
  var type = "";
  var props = rules;
  var children = rulesets;
  var reference = rule;
  var characters2 = type;
  while (scanning)
    switch (previous = character2, character2 = next()) {
      // (
      case 40:
        if (previous != 108 && charat(characters2, length2 - 1) == 58) {
          if (indexof(characters2 += replace(delimit(character2), "&", "&\f"), "&\f") != -1)
            ampersand = -1;
          break;
        }
      // " ' [
      case 34:
      case 39:
      case 91:
        characters2 += delimit(character2);
        break;
      // \t \n \r \s
      case 9:
      case 10:
      case 13:
      case 32:
        characters2 += whitespace(previous);
        break;
      // \
      case 92:
        characters2 += escaping(caret() - 1, 7);
        continue;
      // /
      case 47:
        switch (peek()) {
          case 42:
          case 47:
            append(comment(commenter(next(), caret()), root, parent), declarations);
            break;
          default:
            characters2 += "/";
        }
        break;
      // {
      case 123 * variable:
        points[index++] = strlen(characters2) * ampersand;
      // } ; \0
      case 125 * variable:
      case 59:
      case 0:
        switch (character2) {
          // \0 }
          case 0:
          case 125:
            scanning = 0;
          // ;
          case 59 + offset:
            if (ampersand == -1) characters2 = replace(characters2, /\f/g, "");
            if (property > 0 && strlen(characters2) - length2)
              append(property > 32 ? declaration(characters2 + ";", rule, parent, length2 - 1) : declaration(replace(characters2, " ", "") + ";", rule, parent, length2 - 2), declarations);
            break;
          // @ ;
          case 59:
            characters2 += ";";
          // { rule/at-rule
          default:
            append(reference = ruleset(characters2, root, parent, index, offset, rules, points, type, props = [], children = [], length2), rulesets);
            if (character2 === 123)
              if (offset === 0)
                parse(characters2, root, reference, reference, props, rulesets, length2, points, children);
              else
                switch (atrule === 99 && charat(characters2, 3) === 110 ? 100 : atrule) {
                  // d l m s
                  case 100:
                  case 108:
                  case 109:
                  case 115:
                    parse(value, reference, reference, rule && append(ruleset(value, reference, reference, 0, 0, rules, points, type, rules, props = [], length2), children), rules, children, length2, points, rule ? props : children);
                    break;
                  default:
                    parse(characters2, reference, reference, reference, [""], children, 0, points, children);
                }
        }
        index = offset = property = 0, variable = ampersand = 1, type = characters2 = "", length2 = pseudo;
        break;
      // :
      case 58:
        length2 = 1 + strlen(characters2), property = previous;
      default:
        if (variable < 1) {
          if (character2 == 123)
            --variable;
          else if (character2 == 125 && variable++ == 0 && prev() == 125)
            continue;
        }
        switch (characters2 += from(character2), character2 * variable) {
          // &
          case 38:
            ampersand = offset > 0 ? 1 : (characters2 += "\f", -1);
            break;
          // ,
          case 44:
            points[index++] = (strlen(characters2) - 1) * ampersand, ampersand = 1;
            break;
          // @
          case 64:
            if (peek() === 45)
              characters2 += delimit(next());
            atrule = peek(), offset = length2 = strlen(type = characters2 += identifier(caret())), character2++;
            break;
          // -
          case 45:
            if (previous === 45 && strlen(characters2) == 2)
              variable = 0;
        }
    }
  return rulesets;
}
function ruleset(value, root, parent, index, offset, rules, points, type, props, children, length2) {
  var post = offset - 1;
  var rule = offset === 0 ? rules : [""];
  var size = sizeof(rule);
  for (var i11 = 0, j6 = 0, k8 = 0; i11 < index; ++i11)
    for (var x6 = 0, y6 = substr(value, post + 1, post = abs(j6 = points[i11])), z5 = value; x6 < size; ++x6)
      if (z5 = trim(j6 > 0 ? rule[x6] + " " + y6 : replace(y6, /&\f/g, rule[x6])))
        props[k8++] = z5;
  return node(value, root, parent, offset === 0 ? RULESET : type, props, children, length2);
}
function comment(value, root, parent) {
  return node(value, root, parent, COMMENT, from(char()), substr(value, 2, -2), 0);
}
function declaration(value, root, parent, length2) {
  return node(value, root, parent, DECLARATION, substr(value, 0, length2), substr(value, length2 + 1, -1), length2);
}

// node_modules/stylis/src/Serializer.js
function serialize(children, callback) {
  var output = "";
  var length2 = sizeof(children);
  for (var i11 = 0; i11 < length2; i11++)
    output += callback(children[i11], i11, children, callback) || "";
  return output;
}
function stringify(element, index, children, callback) {
  switch (element.type) {
    case LAYER:
      if (element.children.length) break;
    case IMPORT:
    case DECLARATION:
      return element.return = element.return || element.value;
    case COMMENT:
      return "";
    case KEYFRAMES:
      return element.return = element.value + "{" + serialize(element.children, callback) + "}";
    case RULESET:
      element.value = element.props.join(",");
  }
  return strlen(children = serialize(element.children, callback)) ? element.return = element.value + "{" + children + "}" : "";
}

// node_modules/stylis/src/Middleware.js
function middleware(collection) {
  var length2 = sizeof(collection);
  return function(element, index, children, callback) {
    var output = "";
    for (var i11 = 0; i11 < length2; i11++)
      output += collection[i11](element, index, children, callback) || "";
    return output;
  };
}

// node_modules/@emotion/weak-memoize/dist/emotion-weak-memoize.esm.js
var weakMemoize = function weakMemoize2(func) {
  var cache = /* @__PURE__ */ new WeakMap();
  return function(arg) {
    if (cache.has(arg)) {
      return cache.get(arg);
    }
    var ret = func(arg);
    cache.set(arg, ret);
    return ret;
  };
};

// node_modules/@emotion/memoize/dist/emotion-memoize.esm.js
function memoize(fn) {
  var cache = /* @__PURE__ */ Object.create(null);
  return function(arg) {
    if (cache[arg] === void 0) cache[arg] = fn(arg);
    return cache[arg];
  };
}

// node_modules/@emotion/cache/dist/emotion-cache.browser.development.esm.js
var identifierWithPointTracking = function identifierWithPointTracking2(begin, points, index) {
  var previous = 0;
  var character2 = 0;
  while (true) {
    previous = character2;
    character2 = peek();
    if (previous === 38 && character2 === 12) {
      points[index] = 1;
    }
    if (token(character2)) {
      break;
    }
    next();
  }
  return slice(begin, position);
};
var toRules = function toRules2(parsed, points) {
  var index = -1;
  var character2 = 44;
  do {
    switch (token(character2)) {
      case 0:
        if (character2 === 38 && peek() === 12) {
          points[index] = 1;
        }
        parsed[index] += identifierWithPointTracking(position - 1, points, index);
        break;
      case 2:
        parsed[index] += delimit(character2);
        break;
      case 4:
        if (character2 === 44) {
          parsed[++index] = peek() === 58 ? "&\f" : "";
          points[index] = parsed[index].length;
          break;
        }
      // fallthrough
      default:
        parsed[index] += from(character2);
    }
  } while (character2 = next());
  return parsed;
};
var getRules = function getRules2(value, points) {
  return dealloc(toRules(alloc(value), points));
};
var fixedElements = /* @__PURE__ */ new WeakMap();
var compat = function compat2(element) {
  if (element.type !== "rule" || !element.parent || // positive .length indicates that this rule contains pseudo
  // negative .length indicates that this rule has been already prefixed
  element.length < 1) {
    return;
  }
  var value = element.value;
  var parent = element.parent;
  var isImplicitRule = element.column === parent.column && element.line === parent.line;
  while (parent.type !== "rule") {
    parent = parent.parent;
    if (!parent) return;
  }
  if (element.props.length === 1 && value.charCodeAt(0) !== 58 && !fixedElements.get(parent)) {
    return;
  }
  if (isImplicitRule) {
    return;
  }
  fixedElements.set(element, true);
  var points = [];
  var rules = getRules(value, points);
  var parentRules = parent.props;
  for (var i11 = 0, k8 = 0; i11 < rules.length; i11++) {
    for (var j6 = 0; j6 < parentRules.length; j6++, k8++) {
      element.props[k8] = points[i11] ? rules[i11].replace(/&\f/g, parentRules[j6]) : parentRules[j6] + " " + rules[i11];
    }
  }
};
var removeLabel = function removeLabel2(element) {
  if (element.type === "decl") {
    var value = element.value;
    if (
      // charcode for l
      value.charCodeAt(0) === 108 && // charcode for b
      value.charCodeAt(2) === 98
    ) {
      element["return"] = "";
      element.value = "";
    }
  }
};
var ignoreFlag = "emotion-disable-server-rendering-unsafe-selector-warning-please-do-not-use-this-the-warning-exists-for-a-reason";
var isIgnoringComment = function isIgnoringComment2(element) {
  return element.type === "comm" && element.children.indexOf(ignoreFlag) > -1;
};
var createUnsafeSelectorsAlarm = function createUnsafeSelectorsAlarm2(cache) {
  return function(element, index, children) {
    if (element.type !== "rule" || cache.compat) return;
    var unsafePseudoClasses = element.value.match(/(:first|:nth|:nth-last)-child/g);
    if (unsafePseudoClasses) {
      var isNested = !!element.parent;
      var commentContainer = isNested ? element.parent.children : (
        // global rule at the root level
        children
      );
      for (var i11 = commentContainer.length - 1; i11 >= 0; i11--) {
        var node2 = commentContainer[i11];
        if (node2.line < element.line) {
          break;
        }
        if (node2.column < element.column) {
          if (isIgnoringComment(node2)) {
            return;
          }
          break;
        }
      }
      unsafePseudoClasses.forEach(function(unsafePseudoClass) {
        console.error('The pseudo class "' + unsafePseudoClass + '" is potentially unsafe when doing server-side rendering. Try changing it to "' + unsafePseudoClass.split("-child")[0] + '-of-type".');
      });
    }
  };
};
var isImportRule = function isImportRule2(element) {
  return element.type.charCodeAt(1) === 105 && element.type.charCodeAt(0) === 64;
};
var isPrependedWithRegularRules = function isPrependedWithRegularRules2(index, children) {
  for (var i11 = index - 1; i11 >= 0; i11--) {
    if (!isImportRule(children[i11])) {
      return true;
    }
  }
  return false;
};
var nullifyElement = function nullifyElement2(element) {
  element.type = "";
  element.value = "";
  element["return"] = "";
  element.children = "";
  element.props = "";
};
var incorrectImportAlarm = function incorrectImportAlarm2(element, index, children) {
  if (!isImportRule(element)) {
    return;
  }
  if (element.parent) {
    console.error("`@import` rules can't be nested inside other rules. Please move it to the top level and put it before regular rules. Keep in mind that they can only be used within global styles.");
    nullifyElement(element);
  } else if (isPrependedWithRegularRules(index, children)) {
    console.error("`@import` rules can't be after other rules. Please put your `@import` rules before your other rules.");
    nullifyElement(element);
  }
};
function prefix2(value, length2) {
  switch (hash(value, length2)) {
    // color-adjust
    case 5103:
      return WEBKIT + "print-" + value + value;
    // animation, animation-(delay|direction|duration|fill-mode|iteration-count|name|play-state|timing-function)
    case 5737:
    case 4201:
    case 3177:
    case 3433:
    case 1641:
    case 4457:
    case 2921:
    // text-decoration, filter, clip-path, backface-visibility, column, box-decoration-break
    case 5572:
    case 6356:
    case 5844:
    case 3191:
    case 6645:
    case 3005:
    // mask, mask-image, mask-(mode|clip|size), mask-(repeat|origin), mask-position, mask-composite,
    case 6391:
    case 5879:
    case 5623:
    case 6135:
    case 4599:
    case 4855:
    // background-clip, columns, column-(count|fill|gap|rule|rule-color|rule-style|rule-width|span|width)
    case 4215:
    case 6389:
    case 5109:
    case 5365:
    case 5621:
    case 3829:
      return WEBKIT + value + value;
    // appearance, user-select, transform, hyphens, text-size-adjust
    case 5349:
    case 4246:
    case 4810:
    case 6968:
    case 2756:
      return WEBKIT + value + MOZ + value + MS + value + value;
    // flex, flex-direction
    case 6828:
    case 4268:
      return WEBKIT + value + MS + value + value;
    // order
    case 6165:
      return WEBKIT + value + MS + "flex-" + value + value;
    // align-items
    case 5187:
      return WEBKIT + value + replace(value, /(\w+).+(:[^]+)/, WEBKIT + "box-$1$2" + MS + "flex-$1$2") + value;
    // align-self
    case 5443:
      return WEBKIT + value + MS + "flex-item-" + replace(value, /flex-|-self/, "") + value;
    // align-content
    case 4675:
      return WEBKIT + value + MS + "flex-line-pack" + replace(value, /align-content|flex-|-self/, "") + value;
    // flex-shrink
    case 5548:
      return WEBKIT + value + MS + replace(value, "shrink", "negative") + value;
    // flex-basis
    case 5292:
      return WEBKIT + value + MS + replace(value, "basis", "preferred-size") + value;
    // flex-grow
    case 6060:
      return WEBKIT + "box-" + replace(value, "-grow", "") + WEBKIT + value + MS + replace(value, "grow", "positive") + value;
    // transition
    case 4554:
      return WEBKIT + replace(value, /([^-])(transform)/g, "$1" + WEBKIT + "$2") + value;
    // cursor
    case 6187:
      return replace(replace(replace(value, /(zoom-|grab)/, WEBKIT + "$1"), /(image-set)/, WEBKIT + "$1"), value, "") + value;
    // background, background-image
    case 5495:
    case 3959:
      return replace(value, /(image-set\([^]*)/, WEBKIT + "$1$`$1");
    // justify-content
    case 4968:
      return replace(replace(value, /(.+:)(flex-)?(.*)/, WEBKIT + "box-pack:$3" + MS + "flex-pack:$3"), /s.+-b[^;]+/, "justify") + WEBKIT + value + value;
    // (margin|padding)-inline-(start|end)
    case 4095:
    case 3583:
    case 4068:
    case 2532:
      return replace(value, /(.+)-inline(.+)/, WEBKIT + "$1$2") + value;
    // (min|max)?(width|height|inline-size|block-size)
    case 8116:
    case 7059:
    case 5753:
    case 5535:
    case 5445:
    case 5701:
    case 4933:
    case 4677:
    case 5533:
    case 5789:
    case 5021:
    case 4765:
      if (strlen(value) - 1 - length2 > 6) switch (charat(value, length2 + 1)) {
        // (m)ax-content, (m)in-content
        case 109:
          if (charat(value, length2 + 4) !== 45) break;
        // (f)ill-available, (f)it-content
        case 102:
          return replace(value, /(.+:)(.+)-([^]+)/, "$1" + WEBKIT + "$2-$3$1" + MOZ + (charat(value, length2 + 3) == 108 ? "$3" : "$2-$3")) + value;
        // (s)tretch
        case 115:
          return ~indexof(value, "stretch") ? prefix2(replace(value, "stretch", "fill-available"), length2) + value : value;
      }
      break;
    // position: sticky
    case 4949:
      if (charat(value, length2 + 1) !== 115) break;
    // display: (flex|inline-flex)
    case 6444:
      switch (charat(value, strlen(value) - 3 - (~indexof(value, "!important") && 10))) {
        // stic(k)y
        case 107:
          return replace(value, ":", ":" + WEBKIT) + value;
        // (inline-)?fl(e)x
        case 101:
          return replace(value, /(.+:)([^;!]+)(;|!.+)?/, "$1" + WEBKIT + (charat(value, 14) === 45 ? "inline-" : "") + "box$3$1" + WEBKIT + "$2$3$1" + MS + "$2box$3") + value;
      }
      break;
    // writing-mode
    case 5936:
      switch (charat(value, length2 + 11)) {
        // vertical-l(r)
        case 114:
          return WEBKIT + value + MS + replace(value, /[svh]\w+-[tblr]{2}/, "tb") + value;
        // vertical-r(l)
        case 108:
          return WEBKIT + value + MS + replace(value, /[svh]\w+-[tblr]{2}/, "tb-rl") + value;
        // horizontal(-)tb
        case 45:
          return WEBKIT + value + MS + replace(value, /[svh]\w+-[tblr]{2}/, "lr") + value;
      }
      return WEBKIT + value + MS + value + value;
  }
  return value;
}
var prefixer = function prefixer2(element, index, children, callback) {
  if (element.length > -1) {
    if (!element["return"]) switch (element.type) {
      case DECLARATION:
        element["return"] = prefix2(element.value, element.length);
        break;
      case KEYFRAMES:
        return serialize([copy(element, {
          value: replace(element.value, "@", "@" + WEBKIT)
        })], callback);
      case RULESET:
        if (element.length) return combine(element.props, function(value) {
          switch (match(value, /(::plac\w+|:read-\w+)/)) {
            // :read-(only|write)
            case ":read-only":
            case ":read-write":
              return serialize([copy(element, {
                props: [replace(value, /:(read-\w+)/, ":" + MOZ + "$1")]
              })], callback);
            // :placeholder
            case "::placeholder":
              return serialize([copy(element, {
                props: [replace(value, /:(plac\w+)/, ":" + WEBKIT + "input-$1")]
              }), copy(element, {
                props: [replace(value, /:(plac\w+)/, ":" + MOZ + "$1")]
              }), copy(element, {
                props: [replace(value, /:(plac\w+)/, MS + "input-$1")]
              })], callback);
          }
          return "";
        });
    }
  }
};
var defaultStylisPlugins = [prefixer];
var getSourceMap;
{
  sourceMapPattern = /\/\*#\ssourceMappingURL=data:application\/json;\S+\s+\*\//g;
  getSourceMap = function getSourceMap2(styles) {
    var matches = styles.match(sourceMapPattern);
    if (!matches) return;
    return matches[matches.length - 1];
  };
}
var sourceMapPattern;
var createCache = function createCache2(options) {
  var key = options.key;
  if (!key) {
    throw new Error("You have to configure `key` for your cache. Please make sure it's unique (and not equal to 'css') as it's used for linking styles to your cache.\nIf multiple caches share the same key they might \"fight\" for each other's style elements.");
  }
  if (key === "css") {
    var ssrStyles = document.querySelectorAll("style[data-emotion]:not([data-s])");
    Array.prototype.forEach.call(ssrStyles, function(node2) {
      var dataEmotionAttribute = node2.getAttribute("data-emotion");
      if (dataEmotionAttribute.indexOf(" ") === -1) {
        return;
      }
      document.head.appendChild(node2);
      node2.setAttribute("data-s", "");
    });
  }
  var stylisPlugins = options.stylisPlugins || defaultStylisPlugins;
  {
    if (/[^a-z-]/.test(key)) {
      throw new Error('Emotion key must only contain lower case alphabetical characters and - but "' + key + '" was passed');
    }
  }
  var inserted = {};
  var container;
  var nodesToHydrate = [];
  {
    container = options.container || document.head;
    Array.prototype.forEach.call(
      // this means we will ignore elements which don't have a space in them which
      // means that the style elements we're looking at are only Emotion 11 server-rendered style elements
      document.querySelectorAll('style[data-emotion^="' + key + ' "]'),
      function(node2) {
        var attrib = node2.getAttribute("data-emotion").split(" ");
        for (var i11 = 1; i11 < attrib.length; i11++) {
          inserted[attrib[i11]] = true;
        }
        nodesToHydrate.push(node2);
      }
    );
  }
  var _insert;
  var omnipresentPlugins = [compat, removeLabel];
  {
    omnipresentPlugins.push(createUnsafeSelectorsAlarm({
      get compat() {
        return cache.compat;
      }
    }), incorrectImportAlarm);
  }
  {
    var currentSheet;
    var finalizingPlugins = [stringify, function(element) {
      if (!element.root) {
        if (element["return"]) {
          currentSheet.insert(element["return"]);
        } else if (element.value && element.type !== COMMENT) {
          currentSheet.insert(element.value + "{}");
        }
      }
    }];
    var serializer = middleware(omnipresentPlugins.concat(stylisPlugins, finalizingPlugins));
    var stylis = function stylis2(styles) {
      return serialize(compile(styles), serializer);
    };
    _insert = function insert(selector, serialized, sheet, shouldCache) {
      currentSheet = sheet;
      if (getSourceMap) {
        var sourceMap = getSourceMap(serialized.styles);
        if (sourceMap) {
          currentSheet = {
            insert: function insert2(rule) {
              sheet.insert(rule + sourceMap);
            }
          };
        }
      }
      stylis(selector ? selector + "{" + serialized.styles + "}" : serialized.styles);
      if (shouldCache) {
        cache.inserted[serialized.name] = true;
      }
    };
  }
  var cache = {
    key,
    sheet: new StyleSheet({
      key,
      container,
      nonce: options.nonce,
      speedy: options.speedy,
      prepend: options.prepend,
      insertionPoint: options.insertionPoint
    }),
    nonce: options.nonce,
    inserted,
    registered: {},
    insert: _insert
  };
  cache.sheet.hydrate(nodesToHydrate);
  return cache;
};

// node_modules/@emotion/utils/dist/emotion-utils.browser.esm.js
var isBrowser = true;
function getRegisteredStyles(registered, registeredStyles, classNames) {
  var rawClassName = "";
  classNames.split(" ").forEach(function(className) {
    if (registered[className] !== void 0) {
      registeredStyles.push(registered[className] + ";");
    } else if (className) {
      rawClassName += className + " ";
    }
  });
  return rawClassName;
}
var registerStyles = function registerStyles2(cache, serialized, isStringTag) {
  var className = cache.key + "-" + serialized.name;
  if (
    // we only need to add the styles to the registered cache if the
    // class name could be used further down
    // the tree but if it's a string tag, we know it won't
    // so we don't have to add it to registered cache.
    // this improves memory usage since we can avoid storing the whole style string
    (isStringTag === false || // we need to always store it if we're in compat mode and
    // in node since emotion-server relies on whether a style is in
    // the registered cache to know whether a style is global or not
    // also, note that this check will be dead code eliminated in the browser
    isBrowser === false) && cache.registered[className] === void 0
  ) {
    cache.registered[className] = serialized.styles;
  }
};
var insertStyles = function insertStyles2(cache, serialized, isStringTag) {
  registerStyles(cache, serialized, isStringTag);
  var className = cache.key + "-" + serialized.name;
  if (cache.inserted[serialized.name] === void 0) {
    var current = serialized;
    do {
      cache.insert(serialized === current ? "." + className : "", current, cache.sheet, true);
      current = current.next;
    } while (current !== void 0);
  }
};

// node_modules/@emotion/hash/dist/emotion-hash.esm.js
function murmur2(str) {
  var h5 = 0;
  var k8, i11 = 0, len = str.length;
  for (; len >= 4; ++i11, len -= 4) {
    k8 = str.charCodeAt(i11) & 255 | (str.charCodeAt(++i11) & 255) << 8 | (str.charCodeAt(++i11) & 255) << 16 | (str.charCodeAt(++i11) & 255) << 24;
    k8 = /* Math.imul(k, m): */
    (k8 & 65535) * 1540483477 + ((k8 >>> 16) * 59797 << 16);
    k8 ^= /* k >>> r: */
    k8 >>> 24;
    h5 = /* Math.imul(k, m): */
    (k8 & 65535) * 1540483477 + ((k8 >>> 16) * 59797 << 16) ^ /* Math.imul(h, m): */
    (h5 & 65535) * 1540483477 + ((h5 >>> 16) * 59797 << 16);
  }
  switch (len) {
    case 3:
      h5 ^= (str.charCodeAt(i11 + 2) & 255) << 16;
    case 2:
      h5 ^= (str.charCodeAt(i11 + 1) & 255) << 8;
    case 1:
      h5 ^= str.charCodeAt(i11) & 255;
      h5 = /* Math.imul(h, m): */
      (h5 & 65535) * 1540483477 + ((h5 >>> 16) * 59797 << 16);
  }
  h5 ^= h5 >>> 13;
  h5 = /* Math.imul(h, m): */
  (h5 & 65535) * 1540483477 + ((h5 >>> 16) * 59797 << 16);
  return ((h5 ^ h5 >>> 15) >>> 0).toString(36);
}

// node_modules/@emotion/unitless/dist/emotion-unitless.esm.js
var unitlessKeys = {
  animationIterationCount: 1,
  aspectRatio: 1,
  borderImageOutset: 1,
  borderImageSlice: 1,
  borderImageWidth: 1,
  boxFlex: 1,
  boxFlexGroup: 1,
  boxOrdinalGroup: 1,
  columnCount: 1,
  columns: 1,
  flex: 1,
  flexGrow: 1,
  flexPositive: 1,
  flexShrink: 1,
  flexNegative: 1,
  flexOrder: 1,
  gridRow: 1,
  gridRowEnd: 1,
  gridRowSpan: 1,
  gridRowStart: 1,
  gridColumn: 1,
  gridColumnEnd: 1,
  gridColumnSpan: 1,
  gridColumnStart: 1,
  msGridRow: 1,
  msGridRowSpan: 1,
  msGridColumn: 1,
  msGridColumnSpan: 1,
  fontWeight: 1,
  lineHeight: 1,
  opacity: 1,
  order: 1,
  orphans: 1,
  scale: 1,
  tabSize: 1,
  widows: 1,
  zIndex: 1,
  zoom: 1,
  WebkitLineClamp: 1,
  // SVG-related properties
  fillOpacity: 1,
  floodOpacity: 1,
  stopOpacity: 1,
  strokeDasharray: 1,
  strokeDashoffset: 1,
  strokeMiterlimit: 1,
  strokeOpacity: 1,
  strokeWidth: 1
};

// node_modules/@emotion/serialize/dist/emotion-serialize.development.esm.js
var isDevelopment2 = true;
var ILLEGAL_ESCAPE_SEQUENCE_ERROR = `You have illegal escape sequence in your template literal, most likely inside content's property value.
Because you write your CSS inside a JavaScript string you actually have to do double escaping, so for example "content: '\\00d7';" should become "content: '\\\\00d7';".
You can read more about this here:
https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Template_literals#ES2018_revision_of_illegal_escape_sequences`;
var UNDEFINED_AS_OBJECT_KEY_ERROR = "You have passed in falsy value as style object's key (can happen when in example you pass unexported component as computed key).";
var hyphenateRegex = /[A-Z]|^ms/g;
var animationRegex = /_EMO_([^_]+?)_([^]*?)_EMO_/g;
var isCustomProperty = function isCustomProperty2(property) {
  return property.charCodeAt(1) === 45;
};
var isProcessableValue = function isProcessableValue2(value) {
  return value != null && typeof value !== "boolean";
};
var processStyleName = memoize(function(styleName) {
  return isCustomProperty(styleName) ? styleName : styleName.replace(hyphenateRegex, "-$&").toLowerCase();
});
var processStyleValue = function processStyleValue2(key, value) {
  switch (key) {
    case "animation":
    case "animationName": {
      if (typeof value === "string") {
        return value.replace(animationRegex, function(match2, p1, p22) {
          cursor = {
            name: p1,
            styles: p22,
            next: cursor
          };
          return p1;
        });
      }
    }
  }
  if (unitlessKeys[key] !== 1 && !isCustomProperty(key) && typeof value === "number" && value !== 0) {
    return value + "px";
  }
  return value;
};
{
  contentValuePattern = /(var|attr|counters?|url|element|(((repeating-)?(linear|radial))|conic)-gradient)\(|(no-)?(open|close)-quote/;
  contentValues = ["normal", "none", "initial", "inherit", "unset"];
  oldProcessStyleValue = processStyleValue;
  msPattern = /^-ms-/;
  hyphenPattern = /-(.)/g;
  hyphenatedCache = {};
  processStyleValue = function processStyleValue3(key, value) {
    if (key === "content") {
      if (typeof value !== "string" || contentValues.indexOf(value) === -1 && !contentValuePattern.test(value) && (value.charAt(0) !== value.charAt(value.length - 1) || value.charAt(0) !== '"' && value.charAt(0) !== "'")) {
        throw new Error("You seem to be using a value for 'content' without quotes, try replacing it with `content: '\"" + value + "\"'`");
      }
    }
    var processed = oldProcessStyleValue(key, value);
    if (processed !== "" && !isCustomProperty(key) && key.indexOf("-") !== -1 && hyphenatedCache[key] === void 0) {
      hyphenatedCache[key] = true;
      console.error("Using kebab-case for css properties in objects is not supported. Did you mean " + key.replace(msPattern, "ms-").replace(hyphenPattern, function(str, _char) {
        return _char.toUpperCase();
      }) + "?");
    }
    return processed;
  };
}
var contentValuePattern;
var contentValues;
var oldProcessStyleValue;
var msPattern;
var hyphenPattern;
var hyphenatedCache;
var noComponentSelectorMessage = "Component selectors can only be used in conjunction with @emotion/babel-plugin, the swc Emotion plugin, or another Emotion-aware compiler transform.";
function handleInterpolation(mergedProps, registered, interpolation) {
  if (interpolation == null) {
    return "";
  }
  var componentSelector = interpolation;
  if (componentSelector.__emotion_styles !== void 0) {
    if (String(componentSelector) === "NO_COMPONENT_SELECTOR") {
      throw new Error(noComponentSelectorMessage);
    }
    return componentSelector;
  }
  switch (typeof interpolation) {
    case "boolean": {
      return "";
    }
    case "object": {
      var keyframes2 = interpolation;
      if (keyframes2.anim === 1) {
        cursor = {
          name: keyframes2.name,
          styles: keyframes2.styles,
          next: cursor
        };
        return keyframes2.name;
      }
      var serializedStyles = interpolation;
      if (serializedStyles.styles !== void 0) {
        var next2 = serializedStyles.next;
        if (next2 !== void 0) {
          while (next2 !== void 0) {
            cursor = {
              name: next2.name,
              styles: next2.styles,
              next: cursor
            };
            next2 = next2.next;
          }
        }
        var styles = serializedStyles.styles + ";";
        return styles;
      }
      return createStringFromObject(mergedProps, registered, interpolation);
    }
    case "function": {
      if (mergedProps !== void 0) {
        var previousCursor = cursor;
        var result = interpolation(mergedProps);
        cursor = previousCursor;
        return handleInterpolation(mergedProps, registered, result);
      } else {
        console.error("Functions that are interpolated in css calls will be stringified.\nIf you want to have a css call based on props, create a function that returns a css call like this\nlet dynamicStyle = (props) => css`color: ${props.color}`\nIt can be called directly with props or interpolated in a styled call like this\nlet SomeComponent = styled('div')`${dynamicStyle}`");
      }
      break;
    }
    case "string":
      {
        var matched = [];
        var replaced = interpolation.replace(animationRegex, function(_match, _p1, p22) {
          var fakeVarName = "animation" + matched.length;
          matched.push("const " + fakeVarName + " = keyframes`" + p22.replace(/^@keyframes animation-\w+/, "") + "`");
          return "${" + fakeVarName + "}";
        });
        if (matched.length) {
          console.error("`keyframes` output got interpolated into plain string, please wrap it with `css`.\n\nInstead of doing this:\n\n" + [].concat(matched, ["`" + replaced + "`"]).join("\n") + "\n\nYou should wrap it with `css` like this:\n\ncss`" + replaced + "`");
        }
      }
      break;
  }
  var asString = interpolation;
  if (registered == null) {
    return asString;
  }
  var cached = registered[asString];
  return cached !== void 0 ? cached : asString;
}
function createStringFromObject(mergedProps, registered, obj) {
  var string = "";
  if (Array.isArray(obj)) {
    for (var i11 = 0; i11 < obj.length; i11++) {
      string += handleInterpolation(mergedProps, registered, obj[i11]) + ";";
    }
  } else {
    for (var key in obj) {
      var value = obj[key];
      if (typeof value !== "object") {
        var asString = value;
        if (registered != null && registered[asString] !== void 0) {
          string += key + "{" + registered[asString] + "}";
        } else if (isProcessableValue(asString)) {
          string += processStyleName(key) + ":" + processStyleValue(key, asString) + ";";
        }
      } else {
        if (key === "NO_COMPONENT_SELECTOR" && isDevelopment2) {
          throw new Error(noComponentSelectorMessage);
        }
        if (Array.isArray(value) && typeof value[0] === "string" && (registered == null || registered[value[0]] === void 0)) {
          for (var _i = 0; _i < value.length; _i++) {
            if (isProcessableValue(value[_i])) {
              string += processStyleName(key) + ":" + processStyleValue(key, value[_i]) + ";";
            }
          }
        } else {
          var interpolated = handleInterpolation(mergedProps, registered, value);
          switch (key) {
            case "animation":
            case "animationName": {
              string += processStyleName(key) + ":" + interpolated + ";";
              break;
            }
            default: {
              if (key === "undefined") {
                console.error(UNDEFINED_AS_OBJECT_KEY_ERROR);
              }
              string += key + "{" + interpolated + "}";
            }
          }
        }
      }
    }
  }
  return string;
}
var labelPattern = /label:\s*([^\s;{]+)\s*(;|$)/g;
var cursor;
function serializeStyles(args, registered, mergedProps) {
  if (args.length === 1 && typeof args[0] === "object" && args[0] !== null && args[0].styles !== void 0) {
    return args[0];
  }
  var stringMode = true;
  var styles = "";
  cursor = void 0;
  var strings = args[0];
  if (strings == null || strings.raw === void 0) {
    stringMode = false;
    styles += handleInterpolation(mergedProps, registered, strings);
  } else {
    var asTemplateStringsArr = strings;
    if (asTemplateStringsArr[0] === void 0) {
      console.error(ILLEGAL_ESCAPE_SEQUENCE_ERROR);
    }
    styles += asTemplateStringsArr[0];
  }
  for (var i11 = 1; i11 < args.length; i11++) {
    styles += handleInterpolation(mergedProps, registered, args[i11]);
    if (stringMode) {
      var templateStringsArr = strings;
      if (templateStringsArr[i11] === void 0) {
        console.error(ILLEGAL_ESCAPE_SEQUENCE_ERROR);
      }
      styles += templateStringsArr[i11];
    }
  }
  labelPattern.lastIndex = 0;
  var identifierName = "";
  var match2;
  while ((match2 = labelPattern.exec(styles)) !== null) {
    identifierName += "-" + match2[1];
  }
  var name = murmur2(styles) + identifierName;
  {
    var devStyles = {
      name,
      styles,
      next: cursor,
      toString: function toString() {
        return "You have tried to stringify object returned from `css` function. It isn't supposed to be used directly (e.g. as value of the `className` prop), but rather handed to emotion so it can handle it (e.g. as value of `css` prop).";
      }
    };
    return devStyles;
  }
}

// node_modules/home-assistant-js-websocket/dist/errors.js
var ERR_CANNOT_CONNECT = 1;
var ERR_INVALID_AUTH = 2;
var ERR_CONNECTION_LOST = 3;
var ERR_HASS_HOST_REQUIRED = 4;
var ERR_INVALID_HTTPS_TO_HTTP = 5;
var ERR_INVALID_AUTH_CALLBACK = 6;

// node_modules/home-assistant-js-websocket/dist/messages.js
function auth(accessToken) {
  return {
    type: "auth",
    access_token: accessToken
  };
}
function supportedFeatures() {
  return {
    type: "supported_features",
    id: 1,
    // Always the first message after auth
    features: { coalesce_messages: 1 }
  };
}
function states() {
  return {
    type: "get_states"
  };
}
function config() {
  return {
    type: "get_config"
  };
}
function services() {
  return {
    type: "get_services"
  };
}
function user() {
  return {
    type: "auth/current_user"
  };
}
function callService(domain, service, serviceData, target, returnResponse) {
  const message = {
    type: "call_service",
    domain,
    service,
    target,
    return_response: returnResponse
  };
  if (serviceData) {
    message.service_data = serviceData;
  }
  return message;
}
function subscribeEvents(eventType) {
  const message = {
    type: "subscribe_events"
  };
  if (eventType) {
    message.event_type = eventType;
  }
  return message;
}
function unsubscribeEvents(subscription) {
  return {
    type: "unsubscribe_events",
    subscription
  };
}
function ping() {
  return {
    type: "ping"
  };
}
function error(code, message) {
  return {
    type: "result",
    success: false,
    error: {
      code,
      message
    }
  };
}

// node_modules/home-assistant-js-websocket/dist/util.js
function parseQuery(queryString) {
  const query = {};
  const items = queryString.split("&");
  for (let i11 = 0; i11 < items.length; i11++) {
    const item = items[i11].split("=");
    const key = decodeURIComponent(item[0]);
    const value = item.length > 1 ? decodeURIComponent(item[1]) : void 0;
    query[key] = value;
  }
  return query;
}
var debounce = (func, wait, immediate = false) => {
  let timeout;
  return function(...args) {
    const context = this;
    const later = () => {
      timeout = void 0;
      if (!immediate) {
        func.apply(context, args);
      }
    };
    const callNow = immediate && !timeout;
    clearTimeout(timeout);
    timeout = setTimeout(later, wait);
    if (callNow) {
      func.apply(context, args);
    }
  };
};
var atLeastHaVersion = (version, major, minor, patch) => {
  const [haMajor, haMinor, haPatch] = version.split(".", 3);
  return Number(haMajor) > major || Number(haMajor) === major && (patch === void 0 ? Number(haMinor) >= minor : Number(haMinor) > minor) || patch !== void 0 && Number(haMajor) === major && Number(haMinor) === minor && Number(haPatch) >= patch;
};

// node_modules/home-assistant-js-websocket/dist/socket.js
var DEBUG = false;
var MSG_TYPE_AUTH_REQUIRED = "auth_required";
var MSG_TYPE_AUTH_INVALID = "auth_invalid";
var MSG_TYPE_AUTH_OK = "auth_ok";
function createSocket(options) {
  if (!options.auth) {
    throw ERR_HASS_HOST_REQUIRED;
  }
  const auth2 = options.auth;
  let authRefreshTask = auth2.expired ? auth2.refreshAccessToken().then(() => {
    authRefreshTask = void 0;
  }, () => {
    authRefreshTask = void 0;
  }) : void 0;
  const url = auth2.wsUrl;
  if (DEBUG) {
    console.log("[Auth phase] Initializing", url);
  }
  function connect(triesLeft, promResolve, promReject) {
    if (DEBUG) {
      console.log("[Auth Phase] New connection", url);
    }
    const socket = new WebSocket(url);
    let invalidAuth = false;
    const closeMessage = () => {
      socket.removeEventListener("close", closeMessage);
      if (invalidAuth) {
        promReject(ERR_INVALID_AUTH);
        return;
      }
      if (triesLeft === 0) {
        promReject(ERR_CANNOT_CONNECT);
        return;
      }
      const newTries = triesLeft === -1 ? -1 : triesLeft - 1;
      setTimeout(() => connect(newTries, promResolve, promReject), 1e3);
    };
    const handleOpen = async (event) => {
      try {
        if (auth2.expired) {
          await (authRefreshTask ? authRefreshTask : auth2.refreshAccessToken());
        }
        socket.send(JSON.stringify(auth(auth2.accessToken)));
      } catch (err) {
        invalidAuth = err === ERR_INVALID_AUTH;
        socket.close();
      }
    };
    const handleMessage = async (event) => {
      const message = JSON.parse(event.data);
      if (DEBUG) {
        console.log("[Auth phase] Received", message);
      }
      switch (message.type) {
        case MSG_TYPE_AUTH_INVALID:
          invalidAuth = true;
          socket.close();
          break;
        case MSG_TYPE_AUTH_OK:
          socket.removeEventListener("open", handleOpen);
          socket.removeEventListener("message", handleMessage);
          socket.removeEventListener("close", closeMessage);
          socket.removeEventListener("error", closeMessage);
          socket.haVersion = message.ha_version;
          if (atLeastHaVersion(socket.haVersion, 2022, 9)) {
            socket.send(JSON.stringify(supportedFeatures()));
          }
          promResolve(socket);
          break;
        default:
          if (DEBUG) {
            if (message.type !== MSG_TYPE_AUTH_REQUIRED) {
              console.warn("[Auth phase] Unhandled message", message);
            }
          }
      }
    };
    socket.addEventListener("open", handleOpen);
    socket.addEventListener("message", handleMessage);
    socket.addEventListener("close", closeMessage);
    socket.addEventListener("error", closeMessage);
  }
  return new Promise((resolve, reject) => connect(options.setupRetry, resolve, reject));
}

// node_modules/home-assistant-js-websocket/dist/connection.js
var DEBUG2 = false;
var Connection = class {
  constructor(socket, options) {
    this._handleMessage = (event) => {
      let messageGroup = JSON.parse(event.data);
      if (!Array.isArray(messageGroup)) {
        messageGroup = [messageGroup];
      }
      messageGroup.forEach((message) => {
        if (DEBUG2) {
          console.log("Received", message);
        }
        const info = this.commands.get(message.id);
        switch (message.type) {
          case "event":
            if (info) {
              info.callback(message.event);
            } else {
              console.warn(`Received event for unknown subscription ${message.id}. Unsubscribing.`);
              this.sendMessagePromise(unsubscribeEvents(message.id)).catch((err) => {
                if (DEBUG2) {
                  console.warn(` Error unsubsribing from unknown subscription ${message.id}`, err);
                }
              });
            }
            break;
          case "result":
            if (info) {
              if (message.success) {
                info.resolve(message.result);
                if (!("subscribe" in info)) {
                  this.commands.delete(message.id);
                }
              } else {
                info.reject(message.error);
                this.commands.delete(message.id);
              }
            }
            break;
          case "pong":
            if (info) {
              info.resolve();
              this.commands.delete(message.id);
            } else {
              console.warn(`Received unknown pong response ${message.id}`);
            }
            break;
          default:
            if (DEBUG2) {
              console.warn("Unhandled message", message);
            }
        }
      });
    };
    this._handleClose = async () => {
      const oldCommands = this.commands;
      this.commandId = 1;
      this.oldSubscriptions = this.commands;
      this.commands = /* @__PURE__ */ new Map();
      this.socket = void 0;
      oldCommands.forEach((info) => {
        if (!("subscribe" in info)) {
          info.reject(error(ERR_CONNECTION_LOST, "Connection lost"));
        }
      });
      if (this.closeRequested) {
        return;
      }
      this.fireEvent("disconnected");
      const options2 = Object.assign(Object.assign({}, this.options), { setupRetry: 0 });
      const reconnect = (tries) => {
        setTimeout(async () => {
          if (this.closeRequested) {
            return;
          }
          if (DEBUG2) {
            console.log("Trying to reconnect");
          }
          try {
            const socket2 = await options2.createSocket(options2);
            this._setSocket(socket2);
          } catch (err) {
            if (this._queuedMessages) {
              const queuedMessages = this._queuedMessages;
              this._queuedMessages = void 0;
              for (const msg of queuedMessages) {
                if (msg.reject) {
                  msg.reject(ERR_CONNECTION_LOST);
                }
              }
            }
            if (err === ERR_INVALID_AUTH) {
              this.fireEvent("reconnect-error", err);
            } else {
              reconnect(tries + 1);
            }
          }
        }, Math.min(tries, 5) * 1e3);
      };
      if (this.suspendReconnectPromise) {
        await this.suspendReconnectPromise;
        this.suspendReconnectPromise = void 0;
        this._queuedMessages = [];
      }
      reconnect(0);
    };
    this.options = options;
    this.commandId = 2;
    this.commands = /* @__PURE__ */ new Map();
    this.eventListeners = /* @__PURE__ */ new Map();
    this.closeRequested = false;
    this._setSocket(socket);
  }
  get connected() {
    return this.socket !== void 0 && this.socket.readyState == this.socket.OPEN;
  }
  _setSocket(socket) {
    this.socket = socket;
    this.haVersion = socket.haVersion;
    socket.addEventListener("message", this._handleMessage);
    socket.addEventListener("close", this._handleClose);
    const oldSubscriptions = this.oldSubscriptions;
    if (oldSubscriptions) {
      this.oldSubscriptions = void 0;
      oldSubscriptions.forEach((info) => {
        if ("subscribe" in info && info.subscribe) {
          info.subscribe().then((unsub) => {
            info.unsubscribe = unsub;
            info.resolve();
          });
        }
      });
    }
    const queuedMessages = this._queuedMessages;
    if (queuedMessages) {
      this._queuedMessages = void 0;
      for (const queuedMsg of queuedMessages) {
        queuedMsg.resolve();
      }
    }
    this.fireEvent("ready");
  }
  addEventListener(eventType, callback) {
    let listeners = this.eventListeners.get(eventType);
    if (!listeners) {
      listeners = [];
      this.eventListeners.set(eventType, listeners);
    }
    listeners.push(callback);
  }
  removeEventListener(eventType, callback) {
    const listeners = this.eventListeners.get(eventType);
    if (!listeners) {
      return;
    }
    const index = listeners.indexOf(callback);
    if (index !== -1) {
      listeners.splice(index, 1);
    }
  }
  fireEvent(eventType, eventData) {
    (this.eventListeners.get(eventType) || []).forEach((callback) => callback(this, eventData));
  }
  suspendReconnectUntil(suspendPromise) {
    this.suspendReconnectPromise = suspendPromise;
  }
  suspend() {
    if (!this.suspendReconnectPromise) {
      throw new Error("Suspend promise not set");
    }
    if (this.socket) {
      this.socket.close();
    }
  }
  /**
   * Reconnect the websocket connection.
   * @param force discard old socket instead of gracefully closing it.
   */
  reconnect(force = false) {
    if (!this.socket) {
      return;
    }
    if (!force) {
      this.socket.close();
      return;
    }
    this.socket.removeEventListener("message", this._handleMessage);
    this.socket.removeEventListener("close", this._handleClose);
    this.socket.close();
    this._handleClose();
  }
  close() {
    this.closeRequested = true;
    if (this.socket) {
      this.socket.close();
    }
  }
  /**
   * Subscribe to a specific or all events.
   *
   * @param callback Callback  to be called when a new event fires
   * @param eventType
   * @returns promise that resolves to an unsubscribe function
   */
  async subscribeEvents(callback, eventType) {
    return this.subscribeMessage(callback, subscribeEvents(eventType));
  }
  ping() {
    return this.sendMessagePromise(ping());
  }
  sendMessage(message, commandId) {
    if (!this.connected) {
      throw ERR_CONNECTION_LOST;
    }
    if (DEBUG2) {
      console.log("Sending", message);
    }
    if (this._queuedMessages) {
      if (commandId) {
        throw new Error("Cannot queue with commandId");
      }
      this._queuedMessages.push({ resolve: () => this.sendMessage(message) });
      return;
    }
    if (!commandId) {
      commandId = this._genCmdId();
    }
    message.id = commandId;
    this.socket.send(JSON.stringify(message));
  }
  sendMessagePromise(message) {
    return new Promise((resolve, reject) => {
      if (this._queuedMessages) {
        this._queuedMessages.push({
          reject,
          resolve: async () => {
            try {
              resolve(await this.sendMessagePromise(message));
            } catch (err) {
              reject(err);
            }
          }
        });
        return;
      }
      const commandId = this._genCmdId();
      this.commands.set(commandId, { resolve, reject });
      this.sendMessage(message, commandId);
    });
  }
  /**
   * Call a websocket command that starts a subscription on the backend.
   *
   * @param message the message to start the subscription
   * @param callback the callback to be called when a new item arrives
   * @param [options.resubscribe] re-established a subscription after a reconnect. Defaults to true.
   * @returns promise that resolves to an unsubscribe function
   */
  async subscribeMessage(callback, subscribeMessage, options) {
    if (this._queuedMessages) {
      await new Promise((resolve, reject) => {
        this._queuedMessages.push({ resolve, reject });
      });
    }
    if (options === null || options === void 0 ? void 0 : options.preCheck) {
      const precheck = await options.preCheck();
      if (!precheck) {
        throw new Error("Pre-check failed");
      }
    }
    let info;
    await new Promise((resolve, reject) => {
      const commandId = this._genCmdId();
      info = {
        resolve,
        reject,
        callback,
        subscribe: (options === null || options === void 0 ? void 0 : options.resubscribe) !== false ? () => this.subscribeMessage(callback, subscribeMessage, options) : void 0,
        unsubscribe: async () => {
          if (this.connected) {
            await this.sendMessagePromise(unsubscribeEvents(commandId));
          }
          this.commands.delete(commandId);
        }
      };
      this.commands.set(commandId, info);
      try {
        this.sendMessage(subscribeMessage, commandId);
      } catch (err) {
      }
    });
    return () => info.unsubscribe();
  }
  _genCmdId() {
    return ++this.commandId;
  }
};

// node_modules/home-assistant-js-websocket/dist/auth.js
var genClientId = () => `${location.protocol}//${location.host}/`;
var genExpires = (expires_in) => {
  return expires_in * 1e3 + Date.now();
};
function genRedirectUrl() {
  const { protocol, host, pathname, search } = location;
  return `${protocol}//${host}${pathname}${search}`;
}
function genAuthorizeUrl(hassUrl, clientId, redirectUrl, state) {
  let authorizeUrl = `${hassUrl}/auth/authorize?response_type=code&redirect_uri=${encodeURIComponent(redirectUrl)}`;
  if (clientId !== null) {
    authorizeUrl += `&client_id=${encodeURIComponent(clientId)}`;
  }
  if (state) {
    authorizeUrl += `&state=${encodeURIComponent(state)}`;
  }
  return authorizeUrl;
}
function redirectAuthorize(hassUrl, clientId, redirectUrl, state) {
  redirectUrl += (redirectUrl.includes("?") ? "&" : "?") + "auth_callback=1";
  document.location.href = genAuthorizeUrl(hassUrl, clientId, redirectUrl, state);
}
async function tokenRequest(hassUrl, clientId, data) {
  const l10 = typeof location !== "undefined" && location;
  if (l10 && l10.protocol === "https:") {
    const a13 = document.createElement("a");
    a13.href = hassUrl;
    if (a13.protocol === "http:" && a13.hostname !== "localhost") {
      throw ERR_INVALID_HTTPS_TO_HTTP;
    }
  }
  const formData = new FormData();
  if (clientId !== null) {
    formData.append("client_id", clientId);
  }
  Object.keys(data).forEach((key) => {
    formData.append(key, data[key]);
  });
  const resp = await fetch(`${hassUrl}/auth/token`, {
    method: "POST",
    credentials: "same-origin",
    body: formData
  });
  if (!resp.ok) {
    throw resp.status === 400 || resp.status === 403 ? ERR_INVALID_AUTH : new Error("Unable to fetch tokens");
  }
  const tokens = await resp.json();
  tokens.hassUrl = hassUrl;
  tokens.clientId = clientId;
  tokens.expires = genExpires(tokens.expires_in);
  return tokens;
}
function fetchToken(hassUrl, clientId, code) {
  return tokenRequest(hassUrl, clientId, {
    code,
    grant_type: "authorization_code"
  });
}
function encodeOAuthState(state) {
  return btoa(JSON.stringify(state));
}
function decodeOAuthState(encoded) {
  return JSON.parse(atob(encoded));
}
var Auth = class {
  constructor(data, saveTokens) {
    this.data = data;
    this._saveTokens = saveTokens;
  }
  get wsUrl() {
    return `ws${this.data.hassUrl.substr(4)}/api/websocket`;
  }
  get accessToken() {
    return this.data.access_token;
  }
  get expired() {
    return Date.now() > this.data.expires;
  }
  /**
   * Refresh the access token.
   */
  async refreshAccessToken() {
    if (!this.data.refresh_token)
      throw new Error("No refresh_token");
    const data = await tokenRequest(this.data.hassUrl, this.data.clientId, {
      grant_type: "refresh_token",
      refresh_token: this.data.refresh_token
    });
    data.refresh_token = this.data.refresh_token;
    this.data = data;
    if (this._saveTokens)
      this._saveTokens(data);
  }
  /**
   * Revoke the refresh & access tokens.
   */
  async revoke() {
    if (!this.data.refresh_token)
      throw new Error("No refresh_token to revoke");
    const formData = new FormData();
    formData.append("token", this.data.refresh_token);
    await fetch(`${this.data.hassUrl}/auth/revoke`, {
      method: "POST",
      credentials: "same-origin",
      body: formData
    });
    if (this._saveTokens) {
      this._saveTokens(null);
    }
  }
};
function createLongLivedTokenAuth(hassUrl, access_token) {
  return new Auth({
    hassUrl,
    clientId: null,
    expires: Date.now() + 1e11,
    refresh_token: "",
    access_token,
    expires_in: 1e11
  });
}
async function getAuth(options = {}) {
  let data;
  let hassUrl = options.hassUrl;
  if (hassUrl && hassUrl[hassUrl.length - 1] === "/") {
    hassUrl = hassUrl.substr(0, hassUrl.length - 1);
  }
  const clientId = options.clientId !== void 0 ? options.clientId : genClientId();
  const limitHassInstance = options.limitHassInstance === true;
  if (options.authCode && hassUrl) {
    data = await fetchToken(hassUrl, clientId, options.authCode);
    if (options.saveTokens) {
      options.saveTokens(data);
    }
  }
  if (!data) {
    const query = parseQuery(location.search.substr(1));
    if ("auth_callback" in query) {
      const state = decodeOAuthState(query.state);
      if (limitHassInstance && (state.hassUrl !== hassUrl || state.clientId !== clientId)) {
        throw ERR_INVALID_AUTH_CALLBACK;
      }
      data = await fetchToken(state.hassUrl, state.clientId, query.code);
      if (options.saveTokens) {
        options.saveTokens(data);
      }
    }
  }
  if (!data && options.loadTokens) {
    data = await options.loadTokens();
  }
  if (data && (hassUrl === void 0 || data.hassUrl === hassUrl)) {
    return new Auth(data, options.saveTokens);
  }
  if (hassUrl === void 0) {
    throw ERR_HASS_HOST_REQUIRED;
  }
  redirectAuthorize(hassUrl, clientId, options.redirectUrl || genRedirectUrl(), encodeOAuthState({
    hassUrl,
    clientId
  }));
  return new Promise(() => {
  });
}

// node_modules/home-assistant-js-websocket/dist/store.js
var createStore = (state) => {
  let listeners = [];
  function unsubscribe(listener) {
    let out = [];
    for (let i11 = 0; i11 < listeners.length; i11++) {
      if (listeners[i11] === listener) {
        listener = null;
      } else {
        out.push(listeners[i11]);
      }
    }
    listeners = out;
  }
  function setState(update, overwrite) {
    state = overwrite ? update : Object.assign(Object.assign({}, state), update);
    let currentListeners = listeners;
    for (let i11 = 0; i11 < currentListeners.length; i11++) {
      currentListeners[i11](state);
    }
  }
  return {
    get state() {
      return state;
    },
    /**
     * Create a bound copy of the given action function.
     * The bound returned function invokes action() and persists the result back to the store.
     * If the return value of `action` is a Promise, the resolved value will be used as state.
     * @param {Function} action	An action of the form `action(state, ...args) -> stateUpdate`
     * @returns {Function} boundAction()
     */
    action(action) {
      function apply(result) {
        setState(result, false);
      }
      return function() {
        let args = [state];
        for (let i11 = 0; i11 < arguments.length; i11++)
          args.push(arguments[i11]);
        let ret = action.apply(this, args);
        if (ret != null) {
          return ret instanceof Promise ? ret.then(apply) : apply(ret);
        }
      };
    },
    /**
     * Apply a partial state object to the current state, invoking registered listeners.
     * @param {Object} update				An object with properties to be merged into state
     * @param {Boolean} [overwrite=false]	If `true`, update will replace state instead of being merged into it
     */
    setState,
    clearState() {
      state = void 0;
    },
    /**
     * Register a listener function to be called whenever state is changed. Returns an `unsubscribe()` function.
     * @param {Function} listener	A function to call when state changes. Gets passed the new state.
     * @returns {Function} unsubscribe()
     */
    subscribe(listener) {
      listeners.push(listener);
      return () => {
        unsubscribe(listener);
      };
    }
    // /**
    //  * Remove a previously-registered listener function.
    //  * @param {Function} listener	The callback previously passed to `subscribe()` that should be removed.
    //  * @function
    //  */
    // unsubscribe,
  };
};

// node_modules/home-assistant-js-websocket/dist/collection.js
var UNSUB_GRACE_PERIOD = 5e3;
var DEBUG3 = false;
var getCollection = (conn, key, fetchCollection, subscribeUpdates4, options = { unsubGrace: true }) => {
  if (conn[key]) {
    return conn[key];
  }
  let active = 0;
  let unsubProm;
  let unsubTimer;
  let store = createStore();
  const refresh = () => {
    if (!fetchCollection) {
      throw new Error("Collection does not support refresh");
    }
    return fetchCollection(conn).then((state) => store.setState(state, true));
  };
  const refreshSwallow = () => refresh().catch((err) => {
    if (conn.connected) {
      throw err;
    }
  });
  const setupUpdateSubscription = () => {
    if (unsubTimer !== void 0) {
      if (DEBUG3) {
        console.log(`Prevented unsubscribe for ${key}`);
      }
      clearTimeout(unsubTimer);
      unsubTimer = void 0;
      return;
    }
    if (DEBUG3) {
      console.log(`Subscribing to ${key}`);
    }
    if (subscribeUpdates4) {
      unsubProm = subscribeUpdates4(conn, store);
    }
    if (fetchCollection) {
      conn.addEventListener("ready", refreshSwallow);
      refreshSwallow();
    }
    conn.addEventListener("disconnected", handleDisconnect);
  };
  const teardownUpdateSubscription = () => {
    if (DEBUG3) {
      console.log(`Unsubscribing from ${key}`);
    }
    unsubTimer = void 0;
    if (unsubProm)
      unsubProm.then((unsub) => {
        unsub();
      });
    store.clearState();
    conn.removeEventListener("ready", refresh);
    conn.removeEventListener("disconnected", handleDisconnect);
  };
  const scheduleTeardownUpdateSubscription = () => {
    if (DEBUG3) {
      console.log(`Scheduling unsubscribing from ${key}`);
    }
    unsubTimer = setTimeout(teardownUpdateSubscription, UNSUB_GRACE_PERIOD);
  };
  const handleDisconnect = () => {
    if (unsubTimer) {
      clearTimeout(unsubTimer);
      teardownUpdateSubscription();
    }
  };
  conn[key] = {
    get state() {
      return store.state;
    },
    refresh,
    subscribe(subscriber) {
      active++;
      if (DEBUG3) {
        console.log(`New subscriber for ${key}. Active subscribers: ${active}`);
      }
      if (active === 1) {
        setupUpdateSubscription();
      }
      const unsub = store.subscribe(subscriber);
      if (store.state !== void 0) {
        setTimeout(() => subscriber(store.state), 0);
      }
      return () => {
        unsub();
        active--;
        if (DEBUG3) {
          console.log(`Unsubscribe for ${key}. Active subscribers: ${active}`);
        }
        if (!active) {
          options.unsubGrace ? scheduleTeardownUpdateSubscription() : teardownUpdateSubscription();
        }
      };
    }
  };
  return conn[key];
};
var createCollection = (key, fetchCollection, subscribeUpdates4, conn, onChange) => getCollection(conn, key, fetchCollection, subscribeUpdates4).subscribe(onChange);

// node_modules/home-assistant-js-websocket/dist/commands.js
var getStates = (connection) => connection.sendMessagePromise(states());
var getServices = (connection) => connection.sendMessagePromise(services());
var getConfig = (connection) => connection.sendMessagePromise(config());
var getUser = (connection) => connection.sendMessagePromise(user());
var callService2 = (connection, domain, service, serviceData, target, returnResponse) => connection.sendMessagePromise(callService(domain, service, serviceData, target, returnResponse));

// node_modules/home-assistant-js-websocket/dist/config.js
function processComponentLoaded(state, event) {
  if (state === void 0)
    return null;
  return {
    components: state.components.concat(event.data.component)
  };
}
var fetchConfig = (conn) => getConfig(conn);
var subscribeUpdates = (conn, store) => Promise.all([
  conn.subscribeEvents(store.action(processComponentLoaded), "component_loaded"),
  conn.subscribeEvents(() => fetchConfig(conn).then((config2) => store.setState(config2, true)), "core_config_updated")
]).then((unsubs) => () => unsubs.forEach((unsub) => unsub()));
var configColl = (conn) => getCollection(conn, "_cnf", fetchConfig, subscribeUpdates);
var subscribeConfig = (conn, onChange) => configColl(conn).subscribe(onChange);

// node_modules/home-assistant-js-websocket/dist/services.js
function processServiceRegistered(conn, store, event) {
  var _a;
  const state = store.state;
  if (state === void 0)
    return;
  const { domain, service } = event.data;
  if (!((_a = state.domain) === null || _a === void 0 ? void 0 : _a.service)) {
    const domainInfo = Object.assign(Object.assign({}, state[domain]), { [service]: { description: "", fields: {} } });
    store.setState({ [domain]: domainInfo });
  }
  debouncedFetchServices(conn, store);
}
function processServiceRemoved(state, event) {
  if (state === void 0)
    return null;
  const { domain, service } = event.data;
  const curDomainInfo = state[domain];
  if (!curDomainInfo || !(service in curDomainInfo))
    return null;
  const domainInfo = {};
  Object.keys(curDomainInfo).forEach((sKey) => {
    if (sKey !== service)
      domainInfo[sKey] = curDomainInfo[sKey];
  });
  return { [domain]: domainInfo };
}
var debouncedFetchServices = debounce((conn, store) => fetchServices(conn).then((services2) => store.setState(services2, true)), 5e3);
var fetchServices = (conn) => getServices(conn);
var subscribeUpdates2 = (conn, store) => Promise.all([
  conn.subscribeEvents((ev) => processServiceRegistered(conn, store, ev), "service_registered"),
  conn.subscribeEvents(store.action(processServiceRemoved), "service_removed")
]).then((unsubs) => () => unsubs.forEach((fn) => fn()));
var servicesColl = (conn) => getCollection(conn, "_srv", fetchServices, subscribeUpdates2);
var subscribeServices = (conn, onChange) => servicesColl(conn).subscribe(onChange);

// node_modules/home-assistant-js-websocket/dist/entities.js
function processEvent(store, updates) {
  const state = Object.assign({}, store.state);
  if (updates.a) {
    for (const entityId in updates.a) {
      const newState = updates.a[entityId];
      let last_changed = new Date(newState.lc * 1e3).toISOString();
      state[entityId] = {
        entity_id: entityId,
        state: newState.s,
        attributes: newState.a,
        context: typeof newState.c === "string" ? { id: newState.c, parent_id: null, user_id: null } : newState.c,
        last_changed,
        last_updated: newState.lu ? new Date(newState.lu * 1e3).toISOString() : last_changed
      };
    }
  }
  if (updates.r) {
    for (const entityId of updates.r) {
      delete state[entityId];
    }
  }
  if (updates.c) {
    for (const entityId in updates.c) {
      let entityState = state[entityId];
      if (!entityState) {
        console.warn("Received state update for unknown entity", entityId);
        continue;
      }
      entityState = Object.assign({}, entityState);
      const { "+": toAdd, "-": toRemove } = updates.c[entityId];
      const attributesChanged = (toAdd === null || toAdd === void 0 ? void 0 : toAdd.a) || (toRemove === null || toRemove === void 0 ? void 0 : toRemove.a);
      const attributes = attributesChanged ? Object.assign({}, entityState.attributes) : entityState.attributes;
      if (toAdd) {
        if (toAdd.s !== void 0) {
          entityState.state = toAdd.s;
        }
        if (toAdd.c) {
          if (typeof toAdd.c === "string") {
            entityState.context = Object.assign(Object.assign({}, entityState.context), { id: toAdd.c });
          } else {
            entityState.context = Object.assign(Object.assign({}, entityState.context), toAdd.c);
          }
        }
        if (toAdd.lc) {
          entityState.last_updated = entityState.last_changed = new Date(toAdd.lc * 1e3).toISOString();
        } else if (toAdd.lu) {
          entityState.last_updated = new Date(toAdd.lu * 1e3).toISOString();
        }
        if (toAdd.a) {
          Object.assign(attributes, toAdd.a);
        }
      }
      if (toRemove === null || toRemove === void 0 ? void 0 : toRemove.a) {
        for (const key of toRemove.a) {
          delete attributes[key];
        }
      }
      if (attributesChanged) {
        entityState.attributes = attributes;
      }
      state[entityId] = entityState;
    }
  }
  store.setState(state, true);
}
var subscribeUpdates3 = (conn, store) => conn.subscribeMessage((ev) => processEvent(store, ev), {
  type: "subscribe_entities"
});
function legacyProcessEvent(store, event) {
  const state = store.state;
  if (state === void 0)
    return;
  const { entity_id, new_state } = event.data;
  if (new_state) {
    store.setState({ [new_state.entity_id]: new_state });
  } else {
    const newEntities = Object.assign({}, state);
    delete newEntities[entity_id];
    store.setState(newEntities, true);
  }
}
async function legacyFetchEntities(conn) {
  const states2 = await getStates(conn);
  const entities = {};
  for (let i11 = 0; i11 < states2.length; i11++) {
    const state = states2[i11];
    entities[state.entity_id] = state;
  }
  return entities;
}
var legacySubscribeUpdates = (conn, store) => conn.subscribeEvents((ev) => legacyProcessEvent(store, ev), "state_changed");
var entitiesColl = (conn) => atLeastHaVersion(conn.haVersion, 2022, 4, 0) ? getCollection(conn, "_ent", void 0, subscribeUpdates3) : getCollection(conn, "_ent", legacyFetchEntities, legacySubscribeUpdates);
var subscribeEntities = (conn, onChange) => entitiesColl(conn).subscribe(onChange);

// node_modules/home-assistant-js-websocket/dist/index.js
async function createConnection(options) {
  const connOptions = Object.assign({ setupRetry: 0, createSocket }, options);
  const socket = await connOptions.createSocket(connOptions);
  const conn = new Connection(socket, connOptions);
  return conn;
}

// node_modules/@hakit/core/dist/es/HassConnect/token-storage.js
var o = typeof window < "u" ? window.localStorage : null;
var t = o !== null;
function l2() {
  t ? o.removeItem("hassTokens") : console.error("Local storage not supported on this device.");
}
function a3(r13) {
  if (t)
    try {
      o.setItem("hassTokens", JSON.stringify(r13));
    } catch (s9) {
      console.error("Failed to save tokens, probably due to private mode or storage full", s9);
    }
  else
    console.error("Local storage not supported on this device.");
}
function c2(r13, s9 = true) {
  if (!t)
    return console.error("Local storage not supported on this device."), null;
  const n15 = o.getItem("hassTokens");
  if (n15)
    try {
      const e6 = JSON.parse(n15);
      if (e6.hassUrl === r13)
        return e6;
      if (s9)
        return l2(), null;
    } catch (e6) {
      return console.error("Error parsing stored tokens.", e6?.message || ""), l2(), null;
    }
  return null;
}

// node_modules/zustand/esm/vanilla.mjs
var createStoreImpl = (createState) => {
  let state;
  const listeners = /* @__PURE__ */ new Set();
  const setState = (partial, replace2) => {
    const nextState = typeof partial === "function" ? partial(state) : partial;
    if (!Object.is(nextState, state)) {
      const previousState = state;
      state = (replace2 != null ? replace2 : typeof nextState !== "object" || nextState === null) ? nextState : Object.assign({}, state, nextState);
      listeners.forEach((listener) => listener(state, previousState));
    }
  };
  const getState = () => state;
  const getInitialState = () => initialState;
  const subscribe = (listener) => {
    listeners.add(listener);
    return () => listeners.delete(listener);
  };
  const api = { setState, getState, getInitialState, subscribe };
  const initialState = state = createState(setState, getState, api);
  return api;
};
var createStore2 = ((createState) => createState ? createStoreImpl(createState) : createStoreImpl);

// node_modules/zustand/esm/react.mjs
var import_react = __toESM(require_react(), 1);
var identity = (arg) => arg;
function useStore(api, selector = identity) {
  const slice2 = import_react.default.useSyncExternalStore(
    api.subscribe,
    import_react.default.useCallback(() => selector(api.getState()), [api, selector]),
    import_react.default.useCallback(() => selector(api.getInitialState()), [api, selector])
  );
  import_react.default.useDebugValue(slice2);
  return slice2;
}
var createImpl = (createState) => {
  const api = createStore2(createState);
  const useBoundStore = (selector) => useStore(api, selector);
  Object.assign(useBoundStore, api);
  return useBoundStore;
};
var create = ((createState) => createState ? createImpl(createState) : createImpl);

// node_modules/@hakit/core/dist/index-1tDtVGr2.js
var import_lodash6 = __toESM(require_lodash(), 1);

// node_modules/@hakit/core/dist/es/hooks/useLocale/locales/index.js
var f4 = (e6, a13, o13) => {
  const c15 = e6[a13];
  return c15 ? typeof c15 == "function" ? c15() : Promise.resolve(c15) : new Promise((h5, n15) => {
    (typeof queueMicrotask == "function" ? queueMicrotask : setTimeout)(n15.bind(null, new Error("Unknown variable dynamic import: " + a13 + (a13.split("/").length !== o13 ? ". Note that variables only represent file names one level deep." : ""))));
  });
};
var d2 = {};
var s2 = [
  {
    code: "af",
    hash: "3b8bee04ffbe094d52342826e8002dd2",
    name: "Afrikaans"
  },
  {
    code: "ar",
    hash: "9580553d8e52d154b16cd0c79814183f",
    name: "العربية"
  },
  {
    code: "bg",
    hash: "0c6ed9238246818ea7283b8d74eeaa6a",
    name: "Български"
  },
  {
    code: "bn",
    hash: "d9c44418ff5c761608dd4afcd2041f8e",
    name: "বাংলা"
  },
  {
    code: "bs",
    hash: "889a68a8041733c08f461b7f97079a9c",
    name: "Bosanski"
  },
  {
    code: "ca",
    hash: "ffda1382845c0cd5c0882447f9b2b26e",
    name: "Català"
  },
  {
    code: "cs",
    hash: "68ac6a53d52eb2b18a4eae27547b41b7",
    name: "Čeština"
  },
  {
    code: "cy",
    hash: "fd490087c157d89cfbcb6fde07523e0d",
    name: "Cymraeg"
  },
  {
    code: "da",
    hash: "6d674fbd6ff9afc8ba86356b877816c4",
    name: "Dansk"
  },
  {
    code: "de",
    hash: "6519d59921f5c2f16436b6a507dfc94c",
    name: "Deutsch"
  },
  {
    code: "el",
    hash: "e7ddd164153cd5eb4247b749553777d1",
    name: "Ελληνικά"
  },
  {
    code: "en",
    hash: "46bbeca39326be543b2e8ff82ab9c8bb",
    name: "English"
  },
  {
    code: "en-GB",
    hash: "1183e7edf08e79832ba93aa5bd2c2efd",
    name: "English (GB)"
  },
  {
    code: "eo",
    hash: "a46ae9c4574799b8a03f655c20f49480",
    name: "Esperanto"
  },
  {
    code: "es",
    hash: "4d14a7a7ec610a3d231d08ab2eaccc6b",
    name: "Español"
  },
  {
    code: "es-419",
    hash: "68927188ac4333cfeca3c403c09eb992",
    name: "Español (Latin America)"
  },
  {
    code: "et",
    hash: "367411bdf5bb054111378eefb47a54b5",
    name: "Eesti"
  },
  {
    code: "eu",
    hash: "ba5e20c6fab76312d840c10c76adf06d",
    name: "Euskara"
  },
  {
    code: "fa",
    hash: "984078a035352e5355e13fd48f1ebcff",
    name: "فارسی"
  },
  {
    code: "fi",
    hash: "e597b3cea58b5dbf3660fd676fbcab02",
    name: "Suomi"
  },
  {
    code: "fy",
    hash: "4c1cce086131c832dbff101573d5ab55",
    name: "Frysk"
  },
  {
    code: "fr",
    hash: "aef7712e69eeacb271a823d5a7e1f89a",
    name: "Français"
  },
  {
    code: "ga",
    hash: "154d6323ea9578712fee2adb6efdefcb",
    name: "Gaeilge"
  },
  {
    code: "gl",
    hash: "0093c7660a8e6ae25a4683c82053337f",
    name: "Galego"
  },
  {
    code: "gsw",
    hash: "12aa5c7c28bf0c652fde85cdf7ff51dc",
    name: "Schwiizerdütsch"
  },
  {
    code: "he",
    hash: "0a8059f93c82a4bac9bd475a65f1997c",
    name: "עברית"
  },
  {
    code: "hi",
    hash: "11f06d563a288d5252fd9f1102d0bb8f",
    name: "हिन्दी"
  },
  {
    code: "hr",
    hash: "ac4c34fc5f25b0b1ffd715a2f61e528f",
    name: "Hrvatski"
  },
  {
    code: "hu",
    hash: "ed17a1ed1a6f8f660b97f1800f77fa39",
    name: "Magyar"
  },
  {
    code: "hy",
    hash: "98bc88d8075d5a9e7d38494df9e6a838",
    name: "Հայերեն"
  },
  {
    code: "id",
    hash: "b65040647dff218ebca67626ffe2dcfb",
    name: "Indonesia"
  },
  {
    code: "it",
    hash: "c6fbfdc9b531262a140bb190fca4b4dd",
    name: "Italiano"
  },
  {
    code: "is",
    hash: "aa81ea1b5c8fcf1c8591890348bf31e1",
    name: "Íslenska"
  },
  {
    code: "ja",
    hash: "d30bd3af96cc23cfc9364809c6726cf9",
    name: "日本語"
  },
  {
    code: "ka",
    hash: "f06d2024a9da51e2d0bca13dbc7bb316",
    name: "Kartuli"
  },
  {
    code: "ko",
    hash: "374ec1e5aef24cf9d3023ec6914d3e8a",
    name: "한국어"
  },
  {
    code: "lb",
    hash: "2a633f35fe9d2a4de03b1ff04d5c4489",
    name: "Lëtzebuergesch"
  },
  {
    code: "lt",
    hash: "e22e6c196289542ebf4389fede81728a",
    name: "Lietuvių"
  },
  {
    code: "lv",
    hash: "6d2e3f2420c0be730e12540e968b7898",
    name: "Latviešu"
  },
  {
    code: "mk",
    hash: "3a91543c44f3f302a297d03bb4bbc31f",
    name: "Македонски"
  },
  {
    code: "ml",
    hash: "189aab204ab9edd4094eff7eeb9f7599",
    name: "മലയാളം"
  },
  {
    code: "nl",
    hash: "e6c4bc8356870608bf99a12a7da8bdd1",
    name: "Nederlands"
  },
  {
    code: "nb",
    hash: "56e7744726e7131a74670168ae3564c2",
    name: "Norsk Bokmål"
  },
  {
    code: "nn",
    hash: "21427806c183f5940d1ed9e952ed9a37",
    name: "Norsk Nynorsk"
  },
  {
    code: "pl",
    hash: "7de9b2341e37bd0b2ac86f2a5d15b2f8",
    name: "Polski"
  },
  {
    code: "pt",
    hash: "1693b4e31f305dce36a0833d0345a5d0",
    name: "Português"
  },
  {
    code: "pt-BR",
    hash: "4b05dec52f46cfc21dcc6291e6de4fd1",
    name: "Português (BR)"
  },
  {
    code: "ro",
    hash: "757ea03e103edf13e5eb017c88b5df97",
    name: "Română"
  },
  {
    code: "ru",
    hash: "828115fcea627bca0bbf3b3d8b9d8360",
    name: "Русский"
  },
  {
    code: "sk",
    hash: "8a2cf3951092f8daaeb4d80441269fd4",
    name: "Slovenčina"
  },
  {
    code: "sl",
    hash: "2396269cbea97b22d36cd1e05b379876",
    name: "Slovenščina"
  },
  {
    code: "sr",
    hash: "f1081fe07d2187dadc66d4777e6c3238",
    name: "Српски"
  },
  {
    code: "sr-Latn",
    hash: "3077a26444671122f5c164ccce07c072",
    name: "Srpski"
  },
  {
    code: "sv",
    hash: "c38d0220fd879e1c5fc10b4fc98d6a54",
    name: "Svenska"
  },
  {
    code: "sq",
    hash: "4eae31ca1adb60529f9c287e3320de48",
    name: "Shqip"
  },
  {
    code: "ta",
    hash: "2ec2c14e4a88e8288a6eeb1b4cb34294",
    name: "தமிழ்"
  },
  {
    code: "te",
    hash: "242f76c18e06c95ba3adb450315ca8ec",
    name: "తెలుగు"
  },
  {
    code: "th",
    hash: "fcc7c147c4c806e7e7b823b7517d6001",
    name: "ภาษาไทย"
  },
  {
    code: "tr",
    hash: "addff6a2585d02d3a8432d45b62d462f",
    name: "Türkçe"
  },
  {
    code: "uk",
    hash: "c024a48cbc6f280e1c183e3cb6da062e",
    name: "Українська"
  },
  {
    code: "ur",
    hash: "8f0ca71dace68315b35f9e03d57c0505",
    name: "اُردُو"
  },
  {
    code: "vi",
    hash: "e07cb992da4147ff4469dc2108ed1bd3",
    name: "Tiếng Việt"
  },
  {
    code: "zh-Hans",
    hash: "d7fa61371a2d82b2047c85ec80028cd8",
    name: "简体中文"
  },
  {
    code: "zh-Hant",
    hash: "35312260d0320a52e87a637c00aca4fb",
    name: "繁體中文"
  }
];
var t2 = s2;
var m3 = s2.map((e6) => ({
  ...e6,
  async fetch() {
    const a13 = d2[e6.code];
    if (typeof a13 < "u") return a13;
    const o13 = await f4(Object.assign({ "./af/af.json": () => import("./af-M4S6JUKO.js"), "./ar/ar.json": () => import("./ar-MX3IFJO3.js"), "./bg/bg.json": () => import("./bg-2OQJC4XU.js"), "./bn/bn.json": () => import("./bn-HFDPFWQD.js"), "./bs/bs.json": () => import("./bs-3FOOSB2M.js"), "./ca/ca.json": () => import("./ca-ZRO65RCH.js"), "./cs/cs.json": () => import("./cs-V7PG3ADN.js"), "./cy/cy.json": () => import("./cy-BOYG5OLH.js"), "./da/da.json": () => import("./da-6PU7TMI3.js"), "./de/de.json": () => import("./de-Q3CRGAGI.js"), "./el/el.json": () => import("./el-UEBKFESF.js"), "./en-GB/en-GB.json": () => import("./en-GB-IKJC3462.js"), "./en/en.json": () => import("./en-IWF4CYIM.js"), "./eo/eo.json": () => import("./eo-6NRTLCXZ.js"), "./es-419/es-419.json": () => import("./es-419-GNWCESR5.js"), "./es/es.json": () => import("./es-L6TZCEYU.js"), "./et/et.json": () => import("./et-EBD6MXJQ.js"), "./eu/eu.json": () => import("./eu-DJ7PR6HA.js"), "./fa/fa.json": () => import("./fa-LIGBSNJU.js"), "./fi/fi.json": () => import("./fi-VJRZNGTQ.js"), "./fr/fr.json": () => import("./fr-FPTISMID.js"), "./fy/fy.json": () => import("./fy-JPVZLCPR.js"), "./ga/ga.json": () => import("./ga-ZMFC5DF7.js"), "./gl/gl.json": () => import("./gl-H6PDILHI.js"), "./gsw/gsw.json": () => import("./gsw-YF3XMXKT.js"), "./he/he.json": () => import("./he-GH2G747K.js"), "./hi/hi.json": () => import("./hi-AVP3FPDC.js"), "./hr/hr.json": () => import("./hr-R6UZHEXE.js"), "./hu/hu.json": () => import("./hu-4DMVHCHS.js"), "./hy/hy.json": () => import("./hy-VWSGPZHX.js"), "./id/id.json": () => import("./id-JYIVMRLF.js"), "./is/is.json": () => import("./is-5PKXPHX7.js"), "./it/it.json": () => import("./it-RVVQYIKH.js"), "./ja/ja.json": () => import("./ja-SJDTE7UD.js"), "./ka/ka.json": () => import("./ka-PJH6EL7B.js"), "./ko/ko.json": () => import("./ko-2557P5IP.js"), "./lb/lb.json": () => import("./lb-TF2V45HY.js"), "./lt/lt.json": () => import("./lt-GJB3ESIN.js"), "./lv/lv.json": () => import("./lv-GA274TEB.js"), "./mk/mk.json": () => import("./mk-P7VEL2VW.js"), "./ml/ml.json": () => import("./ml-YETH6XK4.js"), "./nb/nb.json": () => import("./nb-C4I6D35P.js"), "./nl/nl.json": () => import("./nl-W2IDIR5X.js"), "./nn/nn.json": () => import("./nn-5ZHBLA3R.js"), "./pl/pl.json": () => import("./pl-RWWCOHBD.js"), "./pt-BR/pt-BR.json": () => import("./pt-BR-SHWHKXLC.js"), "./pt/pt.json": () => import("./pt-OTRCUCWI.js"), "./ro/ro.json": () => import("./ro-2G5SVTDX.js"), "./ru/ru.json": () => import("./ru-TMYKCTLR.js"), "./sk/sk.json": () => import("./sk-4WMJMPIK.js"), "./sl/sl.json": () => import("./sl-SPKTYTNX.js"), "./sq/sq.json": () => import("./sq-3SYWV6W6.js"), "./sr-Latn/sr-Latn.json": () => import("./sr-Latn-P7P6HBNL.js"), "./sr/sr.json": () => import("./sr-EM6T6QK2.js"), "./sv/sv.json": () => import("./sv-VBLOH73V.js"), "./ta/ta.json": () => import("./ta-FN6BGWBB.js"), "./te/te.json": () => import("./te-3NBVY3YR.js"), "./th/th.json": () => import("./th-KHZY3S72.js"), "./tr/tr.json": () => import("./tr-3NT6LQOU.js"), "./uk/uk.json": () => import("./uk-3HYIBYOA.js"), "./ur/ur.json": () => import("./ur-OIQAAT5G.js"), "./vi/vi.json": () => import("./vi-HE5E4IYK.js"), "./zh-Hans/zh-Hans.json": () => import("./zh-Hans-56DN7AX3.js"), "./zh-Hant/zh-Hant.json": () => import("./zh-Hant-VLYQBHFU.js") }), `./${e6.code}/${e6.code}.json`, 3);
    return d2[e6.code] = o13.default, o13.default;
  }
}));

// node_modules/@iconify/react/dist/iconify.js
var import_react2 = __toESM(require_react());
function getIconsTree(data, names) {
  const icons = data.icons;
  const aliases = data.aliases || /* @__PURE__ */ Object.create(null);
  const resolved = /* @__PURE__ */ Object.create(null);
  function resolve(name) {
    if (icons[name]) return resolved[name] = [];
    if (!(name in resolved)) {
      resolved[name] = null;
      const parent = aliases[name] && aliases[name].parent;
      const value = parent && resolve(parent);
      if (value) resolved[name] = [parent].concat(value);
    }
    return resolved[name];
  }
  Object.keys(icons).concat(Object.keys(aliases)).forEach(resolve);
  return resolved;
}
var defaultIconDimensions = Object.freeze({
  left: 0,
  top: 0,
  width: 16,
  height: 16
});
var defaultIconTransformations = Object.freeze({
  rotate: 0,
  vFlip: false,
  hFlip: false
});
var defaultIconProps = Object.freeze({
  ...defaultIconDimensions,
  ...defaultIconTransformations
});
var defaultExtendedIconProps = Object.freeze({
  ...defaultIconProps,
  body: "",
  hidden: false
});
function mergeIconTransformations(obj1, obj2) {
  const result = {};
  if (!obj1.hFlip !== !obj2.hFlip) result.hFlip = true;
  if (!obj1.vFlip !== !obj2.vFlip) result.vFlip = true;
  const rotate = ((obj1.rotate || 0) + (obj2.rotate || 0)) % 4;
  if (rotate) result.rotate = rotate;
  return result;
}
function mergeIconData(parent, child) {
  const result = mergeIconTransformations(parent, child);
  for (const key in defaultExtendedIconProps) if (key in defaultIconTransformations) {
    if (key in parent && !(key in result)) result[key] = defaultIconTransformations[key];
  } else if (key in child) result[key] = child[key];
  else if (key in parent) result[key] = parent[key];
  return result;
}
function internalGetIconData(data, name, tree) {
  const icons = data.icons;
  const aliases = data.aliases || /* @__PURE__ */ Object.create(null);
  let currentProps = {};
  function parse2(name$1) {
    currentProps = mergeIconData(icons[name$1] || aliases[name$1], currentProps);
  }
  parse2(name);
  tree.forEach(parse2);
  return mergeIconData(data, currentProps);
}
function parseIconSet(data, callback) {
  const names = [];
  if (typeof data !== "object" || typeof data.icons !== "object") return names;
  if (data.not_found instanceof Array) data.not_found.forEach((name) => {
    callback(name, null);
    names.push(name);
  });
  const tree = getIconsTree(data);
  for (const name in tree) {
    const item = tree[name];
    if (item) {
      callback(name, internalGetIconData(data, name, item));
      names.push(name);
    }
  }
  return names;
}
var optionalPropertyDefaults = {
  provider: "",
  aliases: {},
  not_found: {},
  ...defaultIconDimensions
};
function checkOptionalProps(item, defaults) {
  for (const prop in defaults) if (prop in item && typeof item[prop] !== typeof defaults[prop]) return false;
  return true;
}
function quicklyValidateIconSet(obj) {
  if (typeof obj !== "object" || obj === null) return null;
  const data = obj;
  if (typeof data.prefix !== "string" || !obj.icons || typeof obj.icons !== "object") return null;
  if (!checkOptionalProps(obj, optionalPropertyDefaults)) return null;
  const icons = data.icons;
  for (const name in icons) {
    const icon = icons[name];
    if (!name || typeof icon.body !== "string" || !checkOptionalProps(icon, defaultExtendedIconProps)) return null;
  }
  const aliases = data.aliases || /* @__PURE__ */ Object.create(null);
  for (const name in aliases) {
    const icon = aliases[name];
    const parent = icon.parent;
    if (!name || typeof parent !== "string" || !icons[parent] && !aliases[parent] || !checkOptionalProps(icon, defaultExtendedIconProps)) return null;
  }
  return data;
}
var dataStorage = /* @__PURE__ */ Object.create(null);
function newStorage(provider, prefix3) {
  return {
    provider,
    prefix: prefix3,
    icons: /* @__PURE__ */ Object.create(null),
    missing: /* @__PURE__ */ new Set()
  };
}
function getStorage(provider, prefix3) {
  const providerStorage = dataStorage[provider] || (dataStorage[provider] = /* @__PURE__ */ Object.create(null));
  return providerStorage[prefix3] || (providerStorage[prefix3] = newStorage(provider, prefix3));
}
function addIconSet(storage2, data) {
  if (!quicklyValidateIconSet(data)) return [];
  return parseIconSet(data, (name, icon) => {
    if (icon) storage2.icons[name] = icon;
    else storage2.missing.add(name);
  });
}
function addIconToStorage(storage2, name, icon) {
  try {
    if (typeof icon.body === "string") {
      storage2.icons[name] = { ...icon };
      return true;
    }
  } catch (err) {
  }
  return false;
}
var matchIconName = /^[a-z0-9]+(-[a-z0-9]+)*$/;
var stringToIcon = (value, validate, allowSimpleName, provider = "") => {
  const colonSeparated = value.split(":");
  if (value.slice(0, 1) === "@") {
    if (colonSeparated.length < 2 || colonSeparated.length > 3) return null;
    provider = colonSeparated.shift().slice(1);
  }
  if (colonSeparated.length > 3 || !colonSeparated.length) return null;
  if (colonSeparated.length > 1) {
    const name$1 = colonSeparated.pop();
    const prefix3 = colonSeparated.pop();
    const result = {
      provider: colonSeparated.length > 0 ? colonSeparated[0] : provider,
      prefix: prefix3,
      name: name$1
    };
    return validate && !validateIconName(result) ? null : result;
  }
  const name = colonSeparated[0];
  const dashSeparated = name.split("-");
  if (dashSeparated.length > 1) {
    const result = {
      provider,
      prefix: dashSeparated.shift(),
      name: dashSeparated.join("-")
    };
    return validate && !validateIconName(result) ? null : result;
  }
  if (allowSimpleName && provider === "") {
    const result = {
      provider,
      prefix: "",
      name
    };
    return validate && !validateIconName(result, allowSimpleName) ? null : result;
  }
  return null;
};
var validateIconName = (icon, allowSimpleName) => {
  if (!icon) return false;
  return !!((allowSimpleName && icon.prefix === "" || !!icon.prefix) && !!icon.name);
};
var simpleNames = false;
function allowSimpleNames(allow) {
  if (typeof allow === "boolean") simpleNames = allow;
  return simpleNames;
}
function getIconData(name) {
  const icon = typeof name === "string" ? stringToIcon(name, true, simpleNames) : name;
  if (icon) {
    const storage2 = getStorage(icon.provider, icon.prefix);
    const iconName = icon.name;
    return storage2.icons[iconName] || (storage2.missing.has(iconName) ? null : void 0);
  }
}
function addIcon(name, data) {
  const icon = stringToIcon(name, true, simpleNames);
  if (!icon) return false;
  const storage2 = getStorage(icon.provider, icon.prefix);
  if (data) return addIconToStorage(storage2, icon.name, data);
  else {
    storage2.missing.add(icon.name);
    return true;
  }
}
function addCollection(data, provider) {
  if (typeof data !== "object") return false;
  if (typeof provider !== "string") provider = data.provider || "";
  if (simpleNames && !provider && !data.prefix) {
    let added = false;
    if (quicklyValidateIconSet(data)) {
      data.prefix = "";
      parseIconSet(data, (name, icon) => {
        if (addIcon(name, icon)) added = true;
      });
    }
    return added;
  }
  const prefix3 = data.prefix;
  if (!validateIconName({
    prefix: prefix3,
    name: "a"
  })) return false;
  const storage2 = getStorage(provider, prefix3);
  return !!addIconSet(storage2, data);
}
var defaultIconSizeCustomisations = Object.freeze({
  width: null,
  height: null
});
var defaultIconCustomisations = Object.freeze({
  ...defaultIconSizeCustomisations,
  ...defaultIconTransformations
});
var unitsSplit = /(-?[0-9.]*[0-9]+[0-9.]*)/g;
var unitsTest = /^-?[0-9.]*[0-9]+[0-9.]*$/g;
function calculateSize(size, ratio, precision) {
  if (ratio === 1) return size;
  precision = precision || 100;
  if (typeof size === "number") return Math.ceil(size * ratio * precision) / precision;
  if (typeof size !== "string") return size;
  const oldParts = size.split(unitsSplit);
  if (oldParts === null || !oldParts.length) return size;
  const newParts = [];
  let code = oldParts.shift();
  let isNumber = unitsTest.test(code);
  while (true) {
    if (isNumber) {
      const num = parseFloat(code);
      if (isNaN(num)) newParts.push(code);
      else newParts.push(Math.ceil(num * ratio * precision) / precision);
    } else newParts.push(code);
    code = oldParts.shift();
    if (code === void 0) return newParts.join("");
    isNumber = !isNumber;
  }
}
function splitSVGDefs(content, tag = "defs") {
  let defs = "";
  const index = content.indexOf("<" + tag);
  while (index >= 0) {
    const start = content.indexOf(">", index);
    const end = content.indexOf("</" + tag);
    if (start === -1 || end === -1) break;
    const endEnd = content.indexOf(">", end);
    if (endEnd === -1) break;
    defs += content.slice(start + 1, end).trim();
    content = content.slice(0, index).trim() + content.slice(endEnd + 1);
  }
  return {
    defs,
    content
  };
}
function mergeDefsAndContent(defs, content) {
  return defs ? "<defs>" + defs + "</defs>" + content : content;
}
function wrapSVGContent(body, start, end) {
  const split = splitSVGDefs(body);
  return mergeDefsAndContent(split.defs, start + split.content + end);
}
var isUnsetKeyword = (value) => value === "unset" || value === "undefined" || value === "none";
function iconToSVG(icon, customisations) {
  const fullIcon = {
    ...defaultIconProps,
    ...icon
  };
  const fullCustomisations = {
    ...defaultIconCustomisations,
    ...customisations
  };
  const box = {
    left: fullIcon.left,
    top: fullIcon.top,
    width: fullIcon.width,
    height: fullIcon.height
  };
  let body = fullIcon.body;
  [fullIcon, fullCustomisations].forEach((props) => {
    const transformations = [];
    const hFlip = props.hFlip;
    const vFlip = props.vFlip;
    let rotation = props.rotate;
    if (hFlip) if (vFlip) rotation += 2;
    else {
      transformations.push("translate(" + (box.width + box.left).toString() + " " + (0 - box.top).toString() + ")");
      transformations.push("scale(-1 1)");
      box.top = box.left = 0;
    }
    else if (vFlip) {
      transformations.push("translate(" + (0 - box.left).toString() + " " + (box.height + box.top).toString() + ")");
      transformations.push("scale(1 -1)");
      box.top = box.left = 0;
    }
    let tempValue;
    if (rotation < 0) rotation -= Math.floor(rotation / 4) * 4;
    rotation = rotation % 4;
    switch (rotation) {
      case 1:
        tempValue = box.height / 2 + box.top;
        transformations.unshift("rotate(90 " + tempValue.toString() + " " + tempValue.toString() + ")");
        break;
      case 2:
        transformations.unshift("rotate(180 " + (box.width / 2 + box.left).toString() + " " + (box.height / 2 + box.top).toString() + ")");
        break;
      case 3:
        tempValue = box.width / 2 + box.left;
        transformations.unshift("rotate(-90 " + tempValue.toString() + " " + tempValue.toString() + ")");
        break;
    }
    if (rotation % 2 === 1) {
      if (box.left !== box.top) {
        tempValue = box.left;
        box.left = box.top;
        box.top = tempValue;
      }
      if (box.width !== box.height) {
        tempValue = box.width;
        box.width = box.height;
        box.height = tempValue;
      }
    }
    if (transformations.length) body = wrapSVGContent(body, '<g transform="' + transformations.join(" ") + '">', "</g>");
  });
  const customisationsWidth = fullCustomisations.width;
  const customisationsHeight = fullCustomisations.height;
  const boxWidth = box.width;
  const boxHeight = box.height;
  let width;
  let height;
  if (customisationsWidth === null) {
    height = customisationsHeight === null ? "1em" : customisationsHeight === "auto" ? boxHeight : customisationsHeight;
    width = calculateSize(height, boxWidth / boxHeight);
  } else {
    width = customisationsWidth === "auto" ? boxWidth : customisationsWidth;
    height = customisationsHeight === null ? calculateSize(width, boxHeight / boxWidth) : customisationsHeight === "auto" ? boxHeight : customisationsHeight;
  }
  const attributes = {};
  const setAttr = (prop, value) => {
    if (!isUnsetKeyword(value)) attributes[prop] = value.toString();
  };
  setAttr("width", width);
  setAttr("height", height);
  const viewBox = [
    box.left,
    box.top,
    boxWidth,
    boxHeight
  ];
  attributes.viewBox = viewBox.join(" ");
  return {
    attributes,
    viewBox,
    body
  };
}
var regex = /\sid="(\S+)"/g;
var randomPrefix = "IconifyId" + Date.now().toString(16) + (Math.random() * 16777216 | 0).toString(16);
var counter = 0;
function replaceIDs(body, prefix3 = randomPrefix) {
  const ids = [];
  let match2;
  while (match2 = regex.exec(body)) ids.push(match2[1]);
  if (!ids.length) return body;
  const suffix = "suffix" + (Math.random() * 16777216 | Date.now()).toString(16);
  ids.forEach((id) => {
    const newID = typeof prefix3 === "function" ? prefix3(id) : prefix3 + (counter++).toString();
    const escapedID = id.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
    body = body.replace(new RegExp('([#;"])(' + escapedID + ')([")]|\\.[a-z])', "g"), "$1" + newID + suffix + "$3");
  });
  body = body.replace(new RegExp(suffix, "g"), "");
  return body;
}
var storage = /* @__PURE__ */ Object.create(null);
function setAPIModule(provider, item) {
  storage[provider] = item;
}
function getAPIModule(provider) {
  return storage[provider] || storage[""];
}
function createAPIConfig(source) {
  let resources;
  if (typeof source.resources === "string") resources = [source.resources];
  else {
    resources = source.resources;
    if (!(resources instanceof Array) || !resources.length) return null;
  }
  const result = {
    resources,
    path: source.path || "/",
    maxURL: source.maxURL || 500,
    rotate: source.rotate || 750,
    timeout: source.timeout || 5e3,
    random: source.random === true,
    index: source.index || 0,
    dataAfterTimeout: source.dataAfterTimeout !== false
  };
  return result;
}
var configStorage = /* @__PURE__ */ Object.create(null);
var fallBackAPISources = ["https://api.simplesvg.com", "https://api.unisvg.com"];
var fallBackAPI = [];
while (fallBackAPISources.length > 0) if (fallBackAPISources.length === 1) fallBackAPI.push(fallBackAPISources.shift());
else if (Math.random() > 0.5) fallBackAPI.push(fallBackAPISources.shift());
else fallBackAPI.push(fallBackAPISources.pop());
configStorage[""] = createAPIConfig({ resources: ["https://api.iconify.design"].concat(fallBackAPI) });
function addAPIProvider(provider, customConfig) {
  const config2 = createAPIConfig(customConfig);
  if (config2 === null) return false;
  configStorage[provider] = config2;
  return true;
}
function getAPIConfig(provider) {
  return configStorage[provider];
}
var detectFetch = () => {
  let callback;
  try {
    callback = fetch;
    if (typeof callback === "function") return callback;
  } catch (err) {
  }
};
var fetchModule = detectFetch();
function calculateMaxLength(provider, prefix3) {
  const config2 = getAPIConfig(provider);
  if (!config2) return 0;
  let result;
  if (!config2.maxURL) result = 0;
  else {
    let maxHostLength = 0;
    config2.resources.forEach((item) => {
      const host = item;
      maxHostLength = Math.max(maxHostLength, host.length);
    });
    const url = prefix3 + ".json?icons=";
    result = config2.maxURL - maxHostLength - config2.path.length - url.length;
  }
  return result;
}
function shouldAbort(status) {
  return status === 404;
}
var prepare = (provider, prefix3, icons) => {
  const results = [];
  const maxLength = calculateMaxLength(provider, prefix3);
  const type = "icons";
  let item = {
    type,
    provider,
    prefix: prefix3,
    icons: []
  };
  let length2 = 0;
  icons.forEach((name, index) => {
    length2 += name.length + 1;
    if (length2 >= maxLength && index > 0) {
      results.push(item);
      item = {
        type,
        provider,
        prefix: prefix3,
        icons: []
      };
      length2 = name.length;
    }
    item.icons.push(name);
  });
  results.push(item);
  return results;
};
function getPath(provider) {
  if (typeof provider === "string") {
    const config2 = getAPIConfig(provider);
    if (config2) return config2.path;
  }
  return "/";
}
var send = (host, params, callback) => {
  if (!fetchModule) {
    callback("abort", 424);
    return;
  }
  let path = getPath(params.provider);
  switch (params.type) {
    case "icons": {
      const prefix3 = params.prefix;
      const icons = params.icons;
      const iconsList = icons.join(",");
      const urlParams = new URLSearchParams({ icons: iconsList });
      path += prefix3 + ".json?" + urlParams.toString();
      break;
    }
    case "custom": {
      const uri = params.uri;
      path += uri.slice(0, 1) === "/" ? uri.slice(1) : uri;
      break;
    }
    default:
      callback("abort", 400);
      return;
  }
  let defaultError = 503;
  fetchModule(host + path).then((response) => {
    const status = response.status;
    if (status !== 200) {
      setTimeout(() => {
        callback(shouldAbort(status) ? "abort" : "next", status);
      });
      return;
    }
    defaultError = 501;
    return response.json();
  }).then((data) => {
    if (typeof data !== "object" || data === null) {
      setTimeout(() => {
        if (data === 404) callback("abort", data);
        else callback("next", defaultError);
      });
      return;
    }
    setTimeout(() => {
      callback("success", data);
    });
  }).catch(() => {
    callback("next", defaultError);
  });
};
var fetchAPIModule = {
  prepare,
  send
};
function removeCallback(storages, id) {
  storages.forEach((storage2) => {
    const items = storage2.loaderCallbacks;
    if (items) storage2.loaderCallbacks = items.filter((row) => row.id !== id);
  });
}
function updateCallbacks(storage2) {
  if (!storage2.pendingCallbacksFlag) {
    storage2.pendingCallbacksFlag = true;
    setTimeout(() => {
      storage2.pendingCallbacksFlag = false;
      const items = storage2.loaderCallbacks ? storage2.loaderCallbacks.slice(0) : [];
      if (!items.length) return;
      let hasPending = false;
      const provider = storage2.provider;
      const prefix3 = storage2.prefix;
      items.forEach((item) => {
        const icons = item.icons;
        const oldLength = icons.pending.length;
        icons.pending = icons.pending.filter((icon) => {
          if (icon.prefix !== prefix3) return true;
          const name = icon.name;
          if (storage2.icons[name]) icons.loaded.push({
            provider,
            prefix: prefix3,
            name
          });
          else if (storage2.missing.has(name)) icons.missing.push({
            provider,
            prefix: prefix3,
            name
          });
          else {
            hasPending = true;
            return true;
          }
          return false;
        });
        if (icons.pending.length !== oldLength) {
          if (!hasPending) removeCallback([storage2], item.id);
          item.callback(icons.loaded.slice(0), icons.missing.slice(0), icons.pending.slice(0), item.abort);
        }
      });
    });
  }
}
var idCounter = 0;
function storeCallback(callback, icons, pendingSources) {
  const id = idCounter++;
  const abort = removeCallback.bind(null, pendingSources, id);
  if (!icons.pending.length) return abort;
  const item = {
    id,
    icons,
    callback,
    abort
  };
  pendingSources.forEach((storage2) => {
    (storage2.loaderCallbacks || (storage2.loaderCallbacks = [])).push(item);
  });
  return abort;
}
function sortIcons(icons) {
  const result = {
    loaded: [],
    missing: [],
    pending: []
  };
  const storage2 = /* @__PURE__ */ Object.create(null);
  icons.sort((a13, b11) => {
    if (a13.provider !== b11.provider) return a13.provider.localeCompare(b11.provider);
    if (a13.prefix !== b11.prefix) return a13.prefix.localeCompare(b11.prefix);
    return a13.name.localeCompare(b11.name);
  });
  let lastIcon = {
    provider: "",
    prefix: "",
    name: ""
  };
  icons.forEach((icon) => {
    if (lastIcon.name === icon.name && lastIcon.prefix === icon.prefix && lastIcon.provider === icon.provider) return;
    lastIcon = icon;
    const provider = icon.provider;
    const prefix3 = icon.prefix;
    const name = icon.name;
    const providerStorage = storage2[provider] || (storage2[provider] = /* @__PURE__ */ Object.create(null));
    const localStorage = providerStorage[prefix3] || (providerStorage[prefix3] = getStorage(provider, prefix3));
    let list;
    if (name in localStorage.icons) list = result.loaded;
    else if (prefix3 === "" || localStorage.missing.has(name)) list = result.missing;
    else list = result.pending;
    const item = {
      provider,
      prefix: prefix3,
      name
    };
    list.push(item);
  });
  return result;
}
function listToIcons(list, validate = true, simpleNames2 = false) {
  const result = [];
  list.forEach((item) => {
    const icon = typeof item === "string" ? stringToIcon(item, validate, simpleNames2) : item;
    if (icon) result.push(icon);
  });
  return result;
}
var defaultConfig = {
  resources: [],
  index: 0,
  timeout: 2e3,
  rotate: 750,
  random: false,
  dataAfterTimeout: false
};
function sendQuery(config2, payload, query, done) {
  const resourcesCount = config2.resources.length;
  const startIndex = config2.random ? Math.floor(Math.random() * resourcesCount) : config2.index;
  let resources;
  if (config2.random) {
    let list = config2.resources.slice(0);
    resources = [];
    while (list.length > 1) {
      const nextIndex = Math.floor(Math.random() * list.length);
      resources.push(list[nextIndex]);
      list = list.slice(0, nextIndex).concat(list.slice(nextIndex + 1));
    }
    resources = resources.concat(list);
  } else resources = config2.resources.slice(startIndex).concat(config2.resources.slice(0, startIndex));
  const startTime = Date.now();
  let status = "pending";
  let queriesSent = 0;
  let lastError;
  let timer = null;
  let queue = [];
  let doneCallbacks = [];
  if (typeof done === "function") doneCallbacks.push(done);
  function resetTimer() {
    if (timer) {
      clearTimeout(timer);
      timer = null;
    }
  }
  function abort() {
    if (status === "pending") status = "aborted";
    resetTimer();
    queue.forEach((item) => {
      if (item.status === "pending") item.status = "aborted";
    });
    queue = [];
  }
  function subscribe(callback, overwrite) {
    if (overwrite) doneCallbacks = [];
    if (typeof callback === "function") doneCallbacks.push(callback);
  }
  function getQueryStatus() {
    return {
      startTime,
      payload,
      status,
      queriesSent,
      queriesPending: queue.length,
      subscribe,
      abort
    };
  }
  function failQuery() {
    status = "failed";
    doneCallbacks.forEach((callback) => {
      callback(void 0, lastError);
    });
  }
  function clearQueue() {
    queue.forEach((item) => {
      if (item.status === "pending") item.status = "aborted";
    });
    queue = [];
  }
  function moduleResponse(item, response, data) {
    const isError = response !== "success";
    queue = queue.filter((queued) => queued !== item);
    switch (status) {
      case "pending":
        break;
      case "failed":
        if (isError || !config2.dataAfterTimeout) return;
        break;
      default:
        return;
    }
    if (response === "abort") {
      lastError = data;
      failQuery();
      return;
    }
    if (isError) {
      lastError = data;
      if (!queue.length) if (!resources.length) failQuery();
      else execNext();
      return;
    }
    resetTimer();
    clearQueue();
    if (!config2.random) {
      const index = config2.resources.indexOf(item.resource);
      if (index !== -1 && index !== config2.index) config2.index = index;
    }
    status = "completed";
    doneCallbacks.forEach((callback) => {
      callback(data);
    });
  }
  function execNext() {
    if (status !== "pending") return;
    resetTimer();
    const resource = resources.shift();
    if (resource === void 0) {
      if (queue.length) {
        timer = setTimeout(() => {
          resetTimer();
          if (status === "pending") {
            clearQueue();
            failQuery();
          }
        }, config2.timeout);
        return;
      }
      failQuery();
      return;
    }
    const item = {
      status: "pending",
      resource,
      callback: (status$1, data) => {
        moduleResponse(item, status$1, data);
      }
    };
    queue.push(item);
    queriesSent++;
    timer = setTimeout(execNext, config2.rotate);
    query(resource, payload, item.callback);
  }
  setTimeout(execNext);
  return getQueryStatus;
}
function initRedundancy(cfg) {
  const config2 = {
    ...defaultConfig,
    ...cfg
  };
  let queries = [];
  function cleanup() {
    queries = queries.filter((item) => item().status === "pending");
  }
  function query(payload, queryCallback, doneCallback) {
    const query$1 = sendQuery(config2, payload, queryCallback, (data, error2) => {
      cleanup();
      if (doneCallback) doneCallback(data, error2);
    });
    queries.push(query$1);
    return query$1;
  }
  function find(callback) {
    return queries.find((value) => {
      return callback(value);
    }) || null;
  }
  const instance = {
    query,
    find,
    setIndex: (index) => {
      config2.index = index;
    },
    getIndex: () => config2.index,
    cleanup
  };
  return instance;
}
function emptyCallback$1() {
}
var redundancyCache = /* @__PURE__ */ Object.create(null);
function getRedundancyCache(provider) {
  if (!redundancyCache[provider]) {
    const config2 = getAPIConfig(provider);
    if (!config2) return;
    const redundancy = initRedundancy(config2);
    const cachedReundancy = {
      config: config2,
      redundancy
    };
    redundancyCache[provider] = cachedReundancy;
  }
  return redundancyCache[provider];
}
function sendAPIQuery(target, query, callback) {
  let redundancy;
  let send2;
  if (typeof target === "string") {
    const api = getAPIModule(target);
    if (!api) {
      callback(void 0, 424);
      return emptyCallback$1;
    }
    send2 = api.send;
    const cached = getRedundancyCache(target);
    if (cached) redundancy = cached.redundancy;
  } else {
    const config2 = createAPIConfig(target);
    if (config2) {
      redundancy = initRedundancy(config2);
      const moduleKey = target.resources ? target.resources[0] : "";
      const api = getAPIModule(moduleKey);
      if (api) send2 = api.send;
    }
  }
  if (!redundancy || !send2) {
    callback(void 0, 424);
    return emptyCallback$1;
  }
  return redundancy.query(query, send2, callback)().abort;
}
function emptyCallback() {
}
function loadedNewIcons(storage2) {
  if (!storage2.iconsLoaderFlag) {
    storage2.iconsLoaderFlag = true;
    setTimeout(() => {
      storage2.iconsLoaderFlag = false;
      updateCallbacks(storage2);
    });
  }
}
function checkIconNamesForAPI(icons) {
  const valid = [];
  const invalid = [];
  icons.forEach((name) => {
    (name.match(matchIconName) ? valid : invalid).push(name);
  });
  return {
    valid,
    invalid
  };
}
function parseLoaderResponse(storage2, icons, data) {
  function checkMissing() {
    const pending = storage2.pendingIcons;
    icons.forEach((name) => {
      if (pending) pending.delete(name);
      if (!storage2.icons[name]) storage2.missing.add(name);
    });
  }
  if (data && typeof data === "object") try {
    const parsed = addIconSet(storage2, data);
    if (!parsed.length) {
      checkMissing();
      return;
    }
  } catch (err) {
    console.error(err);
  }
  checkMissing();
  loadedNewIcons(storage2);
}
function parsePossiblyAsyncResponse(response, callback) {
  if (response instanceof Promise) response.then((data) => {
    callback(data);
  }).catch(() => {
    callback(null);
  });
  else callback(response);
}
function loadNewIcons(storage2, icons) {
  if (!storage2.iconsToLoad) storage2.iconsToLoad = icons;
  else storage2.iconsToLoad = storage2.iconsToLoad.concat(icons).sort();
  if (!storage2.iconsQueueFlag) {
    storage2.iconsQueueFlag = true;
    setTimeout(() => {
      storage2.iconsQueueFlag = false;
      const { provider, prefix: prefix3 } = storage2;
      const icons$1 = storage2.iconsToLoad;
      delete storage2.iconsToLoad;
      if (!icons$1 || !icons$1.length) return;
      const customIconLoader = storage2.loadIcon;
      if (storage2.loadIcons && (icons$1.length > 1 || !customIconLoader)) {
        parsePossiblyAsyncResponse(storage2.loadIcons(icons$1, prefix3, provider), (data) => {
          parseLoaderResponse(storage2, icons$1, data);
        });
        return;
      }
      if (customIconLoader) {
        icons$1.forEach((name) => {
          const response = customIconLoader(name, prefix3, provider);
          parsePossiblyAsyncResponse(response, (data) => {
            const iconSet = data ? {
              prefix: prefix3,
              icons: { [name]: data }
            } : null;
            parseLoaderResponse(storage2, [name], iconSet);
          });
        });
        return;
      }
      const { valid, invalid } = checkIconNamesForAPI(icons$1);
      if (invalid.length) parseLoaderResponse(storage2, invalid, null);
      if (!valid.length) return;
      const api = prefix3.match(matchIconName) ? getAPIModule(provider) : null;
      if (!api) {
        parseLoaderResponse(storage2, valid, null);
        return;
      }
      const params = api.prepare(provider, prefix3, valid);
      params.forEach((item) => {
        sendAPIQuery(provider, item, (data) => {
          parseLoaderResponse(storage2, item.icons, data);
        });
      });
    });
  }
}
var loadIcons = (icons, callback) => {
  const cleanedIcons = listToIcons(icons, true, allowSimpleNames());
  const sortedIcons = sortIcons(cleanedIcons);
  if (!sortedIcons.pending.length) {
    let callCallback = true;
    if (callback) setTimeout(() => {
      if (callCallback) callback(sortedIcons.loaded, sortedIcons.missing, sortedIcons.pending, emptyCallback);
    });
    return () => {
      callCallback = false;
    };
  }
  const newIcons = /* @__PURE__ */ Object.create(null);
  const sources = [];
  let lastProvider, lastPrefix;
  sortedIcons.pending.forEach((icon) => {
    const { provider, prefix: prefix3 } = icon;
    if (prefix3 === lastPrefix && provider === lastProvider) return;
    lastProvider = provider;
    lastPrefix = prefix3;
    sources.push(getStorage(provider, prefix3));
    const providerNewIcons = newIcons[provider] || (newIcons[provider] = /* @__PURE__ */ Object.create(null));
    if (!providerNewIcons[prefix3]) providerNewIcons[prefix3] = [];
  });
  sortedIcons.pending.forEach((icon) => {
    const { provider, prefix: prefix3, name } = icon;
    const storage2 = getStorage(provider, prefix3);
    const pendingQueue = storage2.pendingIcons || (storage2.pendingIcons = /* @__PURE__ */ new Set());
    if (!pendingQueue.has(name)) {
      pendingQueue.add(name);
      newIcons[provider][prefix3].push(name);
    }
  });
  sources.forEach((storage2) => {
    const list = newIcons[storage2.provider][storage2.prefix];
    if (list.length) loadNewIcons(storage2, list);
  });
  return callback ? storeCallback(callback, sortedIcons, sources) : emptyCallback;
};
function mergeCustomisations(defaults, item) {
  const result = { ...defaults };
  for (const key in item) {
    const value = item[key];
    const valueType = typeof value;
    if (key in defaultIconSizeCustomisations) {
      if (value === null || value && (valueType === "string" || valueType === "number")) result[key] = value;
    } else if (valueType === typeof result[key]) result[key] = key === "rotate" ? value % 4 : value;
  }
  return result;
}
var separator = /[\s,]+/;
function flipFromString(custom, flip) {
  flip.split(separator).forEach((str) => {
    const value = str.trim();
    switch (value) {
      case "horizontal":
        custom.hFlip = true;
        break;
      case "vertical":
        custom.vFlip = true;
        break;
    }
  });
}
function rotateFromString(value, defaultValue = 0) {
  const units = value.replace(/^-?[0-9.]*/, "");
  function cleanup(value$1) {
    while (value$1 < 0) value$1 += 4;
    return value$1 % 4;
  }
  if (units === "") {
    const num = parseInt(value);
    return isNaN(num) ? 0 : cleanup(num);
  } else if (units !== value) {
    let split = 0;
    switch (units) {
      case "%":
        split = 25;
        break;
      case "deg":
        split = 90;
    }
    if (split) {
      let num = parseFloat(value.slice(0, value.length - units.length));
      if (isNaN(num)) return 0;
      num = num / split;
      return num % 1 === 0 ? cleanup(num) : 0;
    }
  }
  return defaultValue;
}
function iconToHTML(body, attributes) {
  let renderAttribsHTML = body.indexOf("xlink:") === -1 ? "" : ' xmlns:xlink="http://www.w3.org/1999/xlink"';
  for (const attr in attributes) renderAttribsHTML += " " + attr + '="' + attributes[attr] + '"';
  return '<svg xmlns="http://www.w3.org/2000/svg"' + renderAttribsHTML + ">" + body + "</svg>";
}
function encodeSVGforURL(svg) {
  return svg.replace(/"/g, "'").replace(/%/g, "%25").replace(/#/g, "%23").replace(/</g, "%3C").replace(/>/g, "%3E").replace(/\s+/g, " ");
}
function svgToData(svg) {
  return "data:image/svg+xml," + encodeSVGforURL(svg);
}
function svgToURL(svg) {
  return 'url("' + svgToData(svg) + '")';
}
var policy;
function createPolicy() {
  try {
    policy = window.trustedTypes.createPolicy("iconify", { createHTML: (s9) => s9 });
  } catch (err) {
    policy = null;
  }
}
function cleanUpInnerHTML(html) {
  if (policy === void 0) createPolicy();
  return policy ? policy.createHTML(html) : html;
}
var defaultExtendedIconCustomisations = {
  ...defaultIconCustomisations,
  inline: false
};
var svgDefaults = {
  "xmlns": "http://www.w3.org/2000/svg",
  "xmlnsXlink": "http://www.w3.org/1999/xlink",
  "aria-hidden": true,
  "role": "img"
};
var commonProps = {
  display: "inline-block"
};
var monotoneProps = {
  backgroundColor: "currentColor"
};
var coloredProps = {
  backgroundColor: "transparent"
};
var propsToAdd = {
  Image: "var(--svg)",
  Repeat: "no-repeat",
  Size: "100% 100%"
};
var propsToAddTo = {
  WebkitMask: monotoneProps,
  mask: monotoneProps,
  background: coloredProps
};
for (const prefix3 in propsToAddTo) {
  const list = propsToAddTo[prefix3];
  for (const prop in propsToAdd) {
    list[prefix3 + prop] = propsToAdd[prop];
  }
}
var inlineDefaults = {
  ...defaultExtendedIconCustomisations,
  inline: true
};
function fixSize(value) {
  return value + (value.match(/^[-0-9.]+$/) ? "px" : "");
}
var render = (icon, props, name) => {
  const defaultProps = props.inline ? inlineDefaults : defaultExtendedIconCustomisations;
  const customisations = mergeCustomisations(defaultProps, props);
  const mode = props.mode || "svg";
  const style = {};
  const customStyle = props.style || {};
  const componentProps = {
    ...mode === "svg" ? svgDefaults : {}
  };
  if (name) {
    const iconName = stringToIcon(name, false, true);
    if (iconName) {
      const classNames = ["iconify"];
      const props2 = [
        "provider",
        "prefix"
      ];
      for (const prop of props2) {
        if (iconName[prop]) {
          classNames.push("iconify--" + iconName[prop]);
        }
      }
      componentProps.className = classNames.join(" ");
    }
  }
  for (let key in props) {
    const value = props[key];
    if (value === void 0) {
      continue;
    }
    switch (key) {
      // Properties to ignore
      case "icon":
      case "style":
      case "children":
      case "onLoad":
      case "mode":
      case "ssr":
      case "fallback":
        break;
      // Forward ref
      case "_ref":
        componentProps.ref = value;
        break;
      // Merge class names
      case "className":
        componentProps[key] = (componentProps[key] ? componentProps[key] + " " : "") + value;
        break;
      // Boolean attributes
      case "inline":
      case "hFlip":
      case "vFlip":
        customisations[key] = value === true || value === "true" || value === 1;
        break;
      // Flip as string: 'horizontal,vertical'
      case "flip":
        if (typeof value === "string") {
          flipFromString(customisations, value);
        }
        break;
      // Color: copy to style
      case "color":
        style.color = value;
        break;
      // Rotation as string
      case "rotate":
        if (typeof value === "string") {
          customisations[key] = rotateFromString(value);
        } else if (typeof value === "number") {
          customisations[key] = value;
        }
        break;
      // Remove aria-hidden
      case "ariaHidden":
      case "aria-hidden":
        if (value !== true && value !== "true") {
          delete componentProps["aria-hidden"];
        }
        break;
      // Copy missing property if it does not exist in customisations
      default:
        if (defaultProps[key] === void 0) {
          componentProps[key] = value;
        }
    }
  }
  const item = iconToSVG(icon, customisations);
  const renderAttribs = item.attributes;
  if (customisations.inline) {
    style.verticalAlign = "-0.125em";
  }
  if (mode === "svg") {
    componentProps.style = {
      ...style,
      ...customStyle
    };
    Object.assign(componentProps, renderAttribs);
    let localCounter = 0;
    let id = props.id;
    if (typeof id === "string") {
      id = id.replace(/-/g, "_");
    }
    componentProps.dangerouslySetInnerHTML = {
      __html: cleanUpInnerHTML(replaceIDs(item.body, id ? () => id + "ID" + localCounter++ : "iconifyReact"))
    };
    return (0, import_react2.createElement)("svg", componentProps);
  }
  const { body, width, height } = icon;
  const useMask = mode === "mask" || (mode === "bg" ? false : body.indexOf("currentColor") !== -1);
  const html = iconToHTML(body, {
    ...renderAttribs,
    width: width + "",
    height: height + ""
  });
  componentProps.style = {
    ...style,
    "--svg": svgToURL(html),
    "width": fixSize(renderAttribs.width),
    "height": fixSize(renderAttribs.height),
    ...commonProps,
    ...useMask ? monotoneProps : coloredProps,
    ...customStyle
  };
  return (0, import_react2.createElement)("span", componentProps);
};
allowSimpleNames(true);
setAPIModule("", fetchAPIModule);
if (typeof document !== "undefined" && typeof window !== "undefined") {
  const _window = window;
  if (_window.IconifyPreload !== void 0) {
    const preload = _window.IconifyPreload;
    const err = "Invalid IconifyPreload syntax.";
    if (typeof preload === "object" && preload !== null) {
      (preload instanceof Array ? preload : [preload]).forEach((item) => {
        try {
          if (
            // Check if item is an object and not null/array
            typeof item !== "object" || item === null || item instanceof Array || // Check for 'icons' and 'prefix'
            typeof item.icons !== "object" || typeof item.prefix !== "string" || // Add icon set
            !addCollection(item)
          ) {
            console.error(err);
          }
        } catch (e6) {
          console.error(err);
        }
      });
    }
  }
  if (_window.IconifyProviders !== void 0) {
    const providers = _window.IconifyProviders;
    if (typeof providers === "object" && providers !== null) {
      for (let key in providers) {
        const err = "IconifyProviders[" + key + "] is invalid.";
        try {
          const value = providers[key];
          if (typeof value !== "object" || !value || value.resources === void 0) {
            continue;
          }
          if (!addAPIProvider(key, value)) {
            console.error(err);
          }
        } catch (e6) {
          console.error(err);
        }
      }
    }
  }
}
function IconComponent(props) {
  const [mounted, setMounted] = (0, import_react2.useState)(!!props.ssr);
  const [abort, setAbort] = (0, import_react2.useState)({});
  function getInitialState(mounted2) {
    if (mounted2) {
      const name2 = props.icon;
      if (typeof name2 === "object") {
        return {
          name: "",
          data: name2
        };
      }
      const data2 = getIconData(name2);
      if (data2) {
        return {
          name: name2,
          data: data2
        };
      }
    }
    return {
      name: ""
    };
  }
  const [state, setState] = (0, import_react2.useState)(getInitialState(!!props.ssr));
  function cleanup() {
    const callback = abort.callback;
    if (callback) {
      callback();
      setAbort({});
    }
  }
  function changeState(newState) {
    if (JSON.stringify(state) !== JSON.stringify(newState)) {
      cleanup();
      setState(newState);
      return true;
    }
  }
  function updateState() {
    var _a;
    const name2 = props.icon;
    if (typeof name2 === "object") {
      changeState({
        name: "",
        data: name2
      });
      return;
    }
    const data2 = getIconData(name2);
    if (changeState({
      name: name2,
      data: data2
    })) {
      if (data2 === void 0) {
        const callback = loadIcons([name2], updateState);
        setAbort({
          callback
        });
      } else if (data2) {
        (_a = props.onLoad) === null || _a === void 0 ? void 0 : _a.call(props, name2);
      }
    }
  }
  (0, import_react2.useEffect)(() => {
    setMounted(true);
    return cleanup;
  }, []);
  (0, import_react2.useEffect)(() => {
    if (mounted) {
      updateState();
    }
  }, [props.icon, mounted]);
  const { name, data } = state;
  if (!data) {
    return props.children ? props.children : props.fallback ? props.fallback : (0, import_react2.createElement)("span", {});
  }
  return render({
    ...defaultIconProps,
    ...data
  }, props, name);
}
var Icon = (0, import_react2.forwardRef)((props, ref) => IconComponent({
  ...props,
  _ref: ref
}));
var InlineIcon = (0, import_react2.forwardRef)((props, ref) => IconComponent({
  inline: true,
  ...props,
  _ref: ref
}));

// node_modules/use-debounce/dist/index.module.js
var import_react3 = __toESM(require_react());
function c3(e6, u15, c15, i11) {
  var a13 = this, o13 = (0, import_react3.useRef)(null), f13 = (0, import_react3.useRef)(0), l10 = (0, import_react3.useRef)(0), v10 = (0, import_react3.useRef)(null), m15 = (0, import_react3.useRef)([]), d12 = (0, import_react3.useRef)(), g10 = (0, import_react3.useRef)(), p8 = (0, import_react3.useRef)(e6), w3 = (0, import_react3.useRef)(true);
  p8.current = e6;
  var s9 = "undefined" != typeof window, x6 = !u15 && 0 !== u15 && s9;
  if ("function" != typeof e6) throw new TypeError("Expected a function");
  u15 = +u15 || 0;
  var h5 = !!(c15 = c15 || {}).leading, y6 = !("trailing" in c15) || !!c15.trailing, F5 = "maxWait" in c15, A7 = "debounceOnServer" in c15 && !!c15.debounceOnServer, D6 = F5 ? Math.max(+c15.maxWait || 0, u15) : null;
  (0, import_react3.useEffect)(function() {
    return w3.current = true, function() {
      w3.current = false;
    };
  }, []);
  var T7 = (0, import_react3.useMemo)(function() {
    var r13 = function(r14) {
      var n16 = m15.current, t10 = d12.current;
      return m15.current = d12.current = null, f13.current = r14, l10.current = l10.current || r14, g10.current = p8.current.apply(t10, n16);
    }, n15 = function(r14, n16) {
      x6 && cancelAnimationFrame(v10.current), v10.current = x6 ? requestAnimationFrame(r14) : setTimeout(r14, n16);
    }, t9 = function(r14) {
      if (!w3.current) return false;
      var n16 = r14 - o13.current;
      return !o13.current || n16 >= u15 || n16 < 0 || F5 && r14 - f13.current >= D6;
    }, e7 = function(n16) {
      return v10.current = null, y6 && m15.current ? r13(n16) : (m15.current = d12.current = null, g10.current);
    }, c16 = function r14() {
      var c17 = Date.now();
      if (h5 && l10.current === f13.current && T8(), t9(c17)) return e7(c17);
      if (w3.current) {
        var i12 = u15 - (c17 - o13.current), a14 = F5 ? Math.min(i12, D6 - (c17 - f13.current)) : i12;
        n15(r14, a14);
      }
    }, T8 = function() {
      i11 && i11({});
    }, W3 = function() {
      if (s9 || A7) {
        var e8 = Date.now(), i12 = t9(e8);
        if (m15.current = [].slice.call(arguments), d12.current = a13, o13.current = e8, i12) {
          if (!v10.current && w3.current) return f13.current = o13.current, n15(c16, u15), h5 ? r13(o13.current) : g10.current;
          if (F5) return n15(c16, u15), r13(o13.current);
        }
        return v10.current || n15(c16, u15), g10.current;
      }
    };
    return W3.cancel = function() {
      var r14 = v10.current;
      r14 && (x6 ? cancelAnimationFrame(v10.current) : clearTimeout(v10.current)), f13.current = 0, m15.current = o13.current = d12.current = v10.current = null, r14 && i11 && i11({});
    }, W3.isPending = function() {
      return !!v10.current;
    }, W3.flush = function() {
      return v10.current ? e7(Date.now()) : g10.current;
    }, W3;
  }, [h5, F5, u15, D6, y6, x6, s9, A7, i11]);
  return T7;
}

// node_modules/@hakit/core/dist/es/utils/subscribe/frontend_data.js
var u5 = ((n15) => (n15.language = "language", n15.system = "system", n15.comma_decimal = "comma_decimal", n15.decimal_comma = "decimal_comma", n15.quote_decimal = "quote_decimal", n15.space_comma = "space_comma", n15.none = "none", n15))(u5 || {});
var d3 = ((n15) => (n15.language = "language", n15.system = "system", n15.DMY = "DMY", n15.MDY = "MDY", n15.YMD = "YMD", n15))(d3 || {});
var i2 = ((n15) => (n15.language = "language", n15.monday = "monday", n15.tuesday = "tuesday", n15.wednesday = "wednesday", n15.thursday = "thursday", n15.friday = "friday", n15.saturday = "saturday", n15.sunday = "sunday", n15))(i2 || {});
var r3 = (n15, a13, e6) => n15.subscribeMessage(e6, {
  type: "frontend/subscribe_user_data",
  key: a13
});
var o2 = {
  "zh-cn": "zh-Hans",
  "zh-sg": "zh-Hans",
  "zh-my": "zh-Hans",
  "zh-tw": "zh-Hant",
  "zh-hk": "zh-Hant",
  "zh-mo": "zh-Hant",
  zh: "zh-Hant"
  // all other Chinese locales map to Traditional Chinese
};
function t4(n15) {
  if (t2.find((s9) => s9.code === n15))
    return n15;
  const a13 = n15.toLowerCase();
  if (a13 in o2)
    return o2[a13];
  const e6 = t2.find((s9) => s9.code.toLowerCase() === a13);
  if (e6)
    return e6.code;
  if (n15.includes("-"))
    return t4(n15.split("-")[0]);
}
function g2(n15) {
  const a13 = n15.language;
  if (a13) {
    const e6 = t4(a13);
    if (e6)
      return e6;
  }
  return a13;
}

// node_modules/@hakit/core/dist/es/utils/date.js
var D = Intl.DateTimeFormat?.().resolvedOptions?.().timeZone;
var T2 = D ?? "UTC";
var r4 = (t9, e6) => t9 === "local" && D ? T2 : e6;
var M = ((t9) => (t9.language = "language", t9.system = "system", t9.am_pm = "12", t9.twenty_four = "24", t9))(M || {});
var z = ((t9) => (t9.local = "local", t9.server = "server", t9))(z || {});
var i3 = (t9) => {
  if (t9.time_format === "language" || t9.time_format === "system") {
    const e6 = t9.time_format === "language" ? t9.language : void 0;
    return (/* @__PURE__ */ new Date("January 1, 2023 22:00:00")).toLocaleString(e6).includes("10");
  }
  return t9.time_format === "12";
};
var I3 = 864e5;
var F = 36e5;
var w = 6e4;
var p2 = 1e3;
var $2 = {
  ms: 1,
  s: p2,
  min: w,
  h: F,
  d: I3
};
var nt = (t9, e6) => S2(parseFloat(t9) * $2[e6]) || "0";
var d4 = (t9, e6 = 2) => {
  let n15 = "" + t9;
  for (let o13 = 1; o13 < e6; o13++)
    n15 = parseInt(n15) < 10 ** o13 ? `0${n15}` : n15;
  return n15;
};
function S2(t9) {
  const e6 = Math.floor(t9 / 1e3 / 3600), n15 = Math.floor(t9 / 1e3 % 3600 / 60), o13 = Math.floor(t9 / 1e3 % 3600 % 60), a13 = Math.floor(t9 % 1e3);
  return e6 > 0 ? `${e6}:${d4(n15)}:${d4(o13)}` : n15 > 0 ? `${n15}:${d4(o13)}` : o13 > 0 || a13 > 0 ? `${o13}${a13 > 0 ? `.${d4(a13, 3)}` : ""}` : null;
}
var Z2 = /^\d{4}-(0[1-9]|1[0-2])-([12]\d|0[1-9]|3[01])[T| ](((([01]\d|2[0-3])((:?)[0-5]\d)?|24:?00)([.,]\d+(?!:))?)(\8[0-5]\d([.,]\d+)?)?([zZ]|([+-])([01]\d|2[0-3]):?([0-5]\d)?)?)$/;
var ot = (t9) => Z2.test(t9);
var l3 = "^\\d{4}-(0[1-9]|1[0-2])-([12]\\d|0[1-9]|3[01])";
var N2 = new RegExp(l3 + "$");
var E = new RegExp(l3);
var rt = (t9, e6 = false) => e6 ? E.test(t9) : N2.test(t9);
function mt(t9) {
  return t9 ? t9 instanceof Date && !isNaN(t9.valueOf()) : false;
}
function m4(t9) {
  let e6 = null, n15 = null;
  return (...o13) => (e6 && e6.length === o13.length && e6.every((a13, s9) => a13 === o13[s9]) || (e6 = o13, n15 = t9(...o13)), n15);
}
var v2 = m4(
  (t9, e6) => new Intl.DateTimeFormat(t9.language, {
    year: "numeric",
    month: "long",
    day: "numeric",
    timeZone: r4(t9.time_zone, e6)
  })
);
var C = m4(
  (t9, e6) => new Intl.DateTimeFormat(t9.language, {
    hour: i3(t9) ? "numeric" : "2-digit",
    minute: "2-digit",
    hourCycle: i3(t9) ? "h12" : "h23",
    timeZone: r4(t9.time_zone, e6)
  })
);
var L = m4(
  (t9, e6) => new Intl.DateTimeFormat(t9.language, {
    hour: "2-digit",
    minute: "2-digit",
    hourCycle: "h23",
    timeZone: r4(t9.time_zone, e6)
  })
);
var P = m4(
  (t9, e6) => new Intl.DateTimeFormat(t9.language, {
    hour: i3(t9) ? "numeric" : "2-digit",
    hourCycle: i3(t9) ? "h12" : "h23",
    timeZone: r4(t9.time_zone, e6)
  })
);
var W = m4(
  (t9, e6) => new Intl.DateTimeFormat(t9.language, {
    minute: "2-digit",
    timeZone: r4(t9.time_zone, e6)
  })
);
var Y2 = m4(
  (t9, e6) => new Intl.DateTimeFormat(t9.language, {
    second: "2-digit",
    timeZone: r4(t9.time_zone, e6)
  })
);
var k3 = m4(
  (t9, e6) => new Intl.DateTimeFormat(t9.language, {
    year: "numeric",
    month: "long",
    day: "numeric",
    hour: i3(t9) ? "numeric" : "2-digit",
    minute: "2-digit",
    hourCycle: i3(t9) ? "h12" : "h23",
    timeZone: r4(t9.time_zone, e6)
  })
);
var O = m4(
  (t9, e6) => new Intl.DateTimeFormat(t9.language, {
    year: "numeric",
    month: "long",
    day: "numeric",
    hour: i3(t9) ? "numeric" : "2-digit",
    minute: "2-digit",
    second: "2-digit",
    hourCycle: i3(t9) ? "h12" : "h23",
    timeZone: r4(t9.time_zone, e6)
  })
);
var A2 = m4(
  (t9, e6) => new Intl.DateTimeFormat(t9.language, {
    year: "numeric",
    month: "short",
    day: "numeric",
    hour: i3(t9) ? "numeric" : "2-digit",
    minute: "2-digit",
    hourCycle: i3(t9) ? "h12" : "h23",
    timeZone: r4(t9.time_zone, e6)
  })
);
var x2 = m4(
  (t9, e6) => new Intl.DateTimeFormat(t9.language, {
    month: "short",
    day: "numeric",
    hour: i3(t9) ? "numeric" : "2-digit",
    minute: "2-digit",
    hourCycle: i3(t9) ? "h12" : "h23",
    timeZone: r4(t9.time_zone, e6)
  })
);
var R2 = m4(
  () => new Intl.DateTimeFormat(void 0, {
    year: "numeric",
    month: "long",
    day: "numeric",
    hour: "2-digit",
    minute: "2-digit"
  })
);
var U = m4(
  (t9, e6) => new Intl.DateTimeFormat(t9.language, {
    weekday: "long",
    month: "long",
    day: "numeric",
    timeZone: r4(t9.time_zone, e6)
  })
);
var V = m4(
  (t9, e6) => new Intl.DateTimeFormat(t9.language, {
    year: "numeric",
    month: "short",
    day: "numeric",
    timeZone: r4(t9.time_zone, e6)
  })
);
var H = m4(
  (t9, e6) => new Intl.DateTimeFormat(t9.language, {
    day: "numeric",
    month: "short",
    timeZone: r4(t9.time_zone, e6)
  })
);
var B2 = m4(
  (t9, e6) => new Intl.DateTimeFormat(t9.language, {
    month: "long",
    year: "numeric",
    timeZone: r4(t9.time_zone, e6)
  })
);
var J = m4(
  (t9, e6) => new Intl.DateTimeFormat(t9.language, {
    month: "long",
    timeZone: r4(t9.time_zone, e6)
  })
);
var q = m4(
  (t9, e6) => new Intl.DateTimeFormat(t9.language, {
    year: "numeric",
    timeZone: r4(t9.time_zone, e6)
  })
);
var G = m4(
  (t9, e6) => new Intl.DateTimeFormat(t9.language, {
    weekday: "long",
    timeZone: r4(t9.time_zone, e6)
  })
);
var K2 = m4(
  (t9, e6) => new Intl.DateTimeFormat(t9.language, {
    weekday: "short",
    timeZone: r4(t9.time_zone, e6)
  })
);
var Q = m4((t9, e6) => {
  const n15 = t9.date_format === d3.system ? void 0 : t9.language;
  return new Intl.DateTimeFormat(n15, {
    year: "numeric",
    month: "numeric",
    day: "numeric",
    timeZone: r4(t9.time_zone, e6)
  });
});
var at = (t9, e6, n15) => v2(n15, e6.time_zone).format(t9);
var X2 = (t9, e6, n15) => C(n15, e6.time_zone).format(t9);
var it = (t9, e6, n15) => L(n15, e6.time_zone).format(t9);
var st = (t9, e6, n15) => P(n15, e6.time_zone).formatToParts(t9).find((a13) => a13.type === "hour")?.value || "";
var ut = (t9, e6, n15) => W(n15, e6.time_zone).formatToParts(t9).find((a13) => a13.type === "minute")?.value || "";
var gt = (t9, e6, n15) => Y2(n15, e6.time_zone).formatToParts(t9).find((a13) => a13.type === "second")?.value || "";
var ct = (t9, e6, n15) => k3(n15, e6.time_zone).format(t9);
var ft = (t9, e6, n15) => O(e6, n15.time_zone).format(t9);
var b2 = (t9, e6, n15) => x2(e6, n15.time_zone).format(t9);
var j2 = (t9, e6, n15) => A2(e6, n15.time_zone).format(t9);
var dt = (t9, e6, n15) => (/* @__PURE__ */ new Date()).getFullYear() === t9.getFullYear() ? b2(t9, e6, n15) : j2(t9, e6, n15);
var ht = (t9) => R2().format(t9);
var yt = (t9, e6, n15) => `${new Intl.DateTimeFormat(e6.language, {
  year: "numeric",
  month: "numeric",
  day: "numeric",
  timeZone: r4(e6.time_zone, n15.time_zone)
}).format(t9)}, ${X2(t9, n15, e6)}`;
var _t = (t9, e6, n15) => U(e6, n15.time_zone).format(t9);
var Dt = (t9, e6, n15) => V(e6, n15.time_zone).format(t9);
var lt = (t9, e6, n15) => H(e6, n15.time_zone).format(t9);
var Tt = (t9, e6, n15) => B2(e6, n15.time_zone).format(t9);
var Mt = (t9, e6, n15) => J(e6, n15.time_zone).format(t9);
var zt = (t9, e6, n15) => q(e6, n15.time_zone).format(t9);
var It = (t9, e6, n15) => G(e6, n15.time_zone).format(t9);
var Ft = (t9, e6, n15) => K2(e6, n15.time_zone).format(t9);
var wt = (t9, e6, n15) => {
  const o13 = Q(e6, n15.time_zone);
  if (e6.date_format === d3.language || e6.date_format === d3.system)
    return o13.format(t9);
  const a13 = o13.formatToParts(t9), s9 = a13.find((g10) => g10.type === "literal")?.value || "/", c15 = a13.find((g10) => g10.type === "day")?.value || "", h5 = a13.find((g10) => g10.type === "month")?.value || "", y6 = a13.find((g10) => g10.type === "year")?.value || "", _8 = a13[a13.length - 1];
  let f13 = _8?.type === "literal" ? _8.value : "";
  return e6.language === "bg" && e6.date_format === d3.YMD && (f13 = ""), {
    [d3.DMY]: `${c15}${s9}${h5}${s9}${y6}${f13}`,
    [d3.MDY]: `${h5}${s9}${c15}${s9}${y6}${f13}`,
    [d3.YMD]: `${y6}${s9}${h5}${s9}${c15}${f13}`,
    [d3.language]: o13.format(t9),
    [d3.system]: o13.format(t9)
  }[e6.date_format];
};
var pt = (t9, e6, n15) => {
  try {
    return new Intl.DateTimeFormat(e6.language, {
      hour: "numeric",
      hour12: true,
      timeZone: r4(e6.time_zone, n15.time_zone)
    }).formatToParts(t9).find((c15) => c15.type === "dayPeriod")?.value || (t9.getHours() >= 12 ? "PM" : "AM");
  } catch {
    return t9.getHours() >= 12 ? "PM" : "AM";
  }
};

// node_modules/@babel/runtime/helpers/esm/extends.js
function _extends() {
  return _extends = Object.assign ? Object.assign.bind() : function(n15) {
    for (var e6 = 1; e6 < arguments.length; e6++) {
      var t9 = arguments[e6];
      for (var r13 in t9) ({}).hasOwnProperty.call(t9, r13) && (n15[r13] = t9[r13]);
    }
    return n15;
  }, _extends.apply(null, arguments);
}

// node_modules/@emotion/react/dist/emotion-element-489459f2.browser.development.esm.js
var React3 = __toESM(require_react());
var import_react4 = __toESM(require_react());

// node_modules/@emotion/react/_isolated-hnrs/dist/emotion-react-_isolated-hnrs.browser.development.esm.js
var import_hoist_non_react_statics = __toESM(require_hoist_non_react_statics_cjs());

// node_modules/@emotion/use-insertion-effect-with-fallbacks/dist/emotion-use-insertion-effect-with-fallbacks.browser.esm.js
var React2 = __toESM(require_react());
var syncFallback = function syncFallback2(create2) {
  return create2();
};
var useInsertionEffect2 = React2["useInsertionEffect"] ? React2["useInsertionEffect"] : false;
var useInsertionEffectAlwaysWithSyncFallback = useInsertionEffect2 || syncFallback;
var useInsertionEffectWithLayoutFallback = useInsertionEffect2 || React2.useLayoutEffect;

// node_modules/@emotion/react/dist/emotion-element-489459f2.browser.development.esm.js
var EmotionCacheContext = React3.createContext(
  // we're doing this to avoid preconstruct's dead code elimination in this one case
  // because this module is primarily intended for the browser and node
  // but it's also required in react native and similar environments sometimes
  // and we could have a special build just for that
  // but this is much easier and the native packages
  // might use a different theme context in the future anyway
  typeof HTMLElement !== "undefined" ? createCache({
    key: "css"
  }) : null
);
{
  EmotionCacheContext.displayName = "EmotionCacheContext";
}
var CacheProvider = EmotionCacheContext.Provider;
var withEmotionCache = function withEmotionCache2(func) {
  return (0, import_react4.forwardRef)(function(props, ref) {
    var cache = (0, import_react4.useContext)(EmotionCacheContext);
    return func(props, cache, ref);
  });
};
var ThemeContext = React3.createContext({});
{
  ThemeContext.displayName = "EmotionThemeContext";
}
var getTheme = function getTheme2(outerTheme, theme) {
  if (typeof theme === "function") {
    var mergedTheme = theme(outerTheme);
    if (mergedTheme == null || typeof mergedTheme !== "object" || Array.isArray(mergedTheme)) {
      throw new Error("[ThemeProvider] Please return an object from your theme function, i.e. theme={() => ({})}!");
    }
    return mergedTheme;
  }
  if (theme == null || typeof theme !== "object" || Array.isArray(theme)) {
    throw new Error("[ThemeProvider] Please make your theme prop a plain object");
  }
  return _extends({}, outerTheme, theme);
};
var createCacheWithTheme = weakMemoize(function(outerTheme) {
  return weakMemoize(function(theme) {
    return getTheme(outerTheme, theme);
  });
});
var hasOwn = {}.hasOwnProperty;
var getLastPart = function getLastPart2(functionName) {
  var parts = functionName.split(".");
  return parts[parts.length - 1];
};
var getFunctionNameFromStackTraceLine = function getFunctionNameFromStackTraceLine2(line2) {
  var match2 = /^\s+at\s+([A-Za-z0-9$.]+)\s/.exec(line2);
  if (match2) return getLastPart(match2[1]);
  match2 = /^([A-Za-z0-9$.]+)@/.exec(line2);
  if (match2) return getLastPart(match2[1]);
  return void 0;
};
var internalReactFunctionNames = /* @__PURE__ */ new Set(["renderWithHooks", "processChild", "finishClassComponent", "renderToString"]);
var sanitizeIdentifier = function sanitizeIdentifier2(identifier2) {
  return identifier2.replace(/\$/g, "-");
};
var getLabelFromStackTrace = function getLabelFromStackTrace2(stackTrace) {
  if (!stackTrace) return void 0;
  var lines = stackTrace.split("\n");
  for (var i11 = 0; i11 < lines.length; i11++) {
    var functionName = getFunctionNameFromStackTraceLine(lines[i11]);
    if (!functionName) continue;
    if (internalReactFunctionNames.has(functionName)) break;
    if (/^[A-Z]/.test(functionName)) return sanitizeIdentifier(functionName);
  }
  return void 0;
};
var typePropName = "__EMOTION_TYPE_PLEASE_DO_NOT_USE__";
var labelPropName = "__EMOTION_LABEL_PLEASE_DO_NOT_USE__";
var createEmotionProps = function createEmotionProps2(type, props) {
  if (typeof props.css === "string" && // check if there is a css declaration
  props.css.indexOf(":") !== -1) {
    throw new Error("Strings are not allowed as css prop values, please wrap it in a css template literal from '@emotion/react' like this: css`" + props.css + "`");
  }
  var newProps = {};
  for (var _key in props) {
    if (hasOwn.call(props, _key)) {
      newProps[_key] = props[_key];
    }
  }
  newProps[typePropName] = type;
  if (typeof globalThis !== "undefined" && !!globalThis.EMOTION_RUNTIME_AUTO_LABEL && !!props.css && (typeof props.css !== "object" || !("name" in props.css) || typeof props.css.name !== "string" || props.css.name.indexOf("-") === -1)) {
    var label = getLabelFromStackTrace(new Error().stack);
    if (label) newProps[labelPropName] = label;
  }
  return newProps;
};
var Insertion = function Insertion2(_ref) {
  var cache = _ref.cache, serialized = _ref.serialized, isStringTag = _ref.isStringTag;
  registerStyles(cache, serialized, isStringTag);
  useInsertionEffectAlwaysWithSyncFallback(function() {
    return insertStyles(cache, serialized, isStringTag);
  });
  return null;
};
var Emotion = withEmotionCache(function(props, cache, ref) {
  var cssProp = props.css;
  if (typeof cssProp === "string" && cache.registered[cssProp] !== void 0) {
    cssProp = cache.registered[cssProp];
  }
  var WrappedComponent = props[typePropName];
  var registeredStyles = [cssProp];
  var className = "";
  if (typeof props.className === "string") {
    className = getRegisteredStyles(cache.registered, registeredStyles, props.className);
  } else if (props.className != null) {
    className = props.className + " ";
  }
  var serialized = serializeStyles(registeredStyles, void 0, React3.useContext(ThemeContext));
  if (serialized.name.indexOf("-") === -1) {
    var labelFromStack = props[labelPropName];
    if (labelFromStack) {
      serialized = serializeStyles([serialized, "label:" + labelFromStack + ";"]);
    }
  }
  className += cache.key + "-" + serialized.name;
  var newProps = {};
  for (var _key2 in props) {
    if (hasOwn.call(props, _key2) && _key2 !== "css" && _key2 !== typePropName && _key2 !== labelPropName) {
      newProps[_key2] = props[_key2];
    }
  }
  newProps.className = className;
  if (ref) {
    newProps.ref = ref;
  }
  return React3.createElement(React3.Fragment, null, React3.createElement(Insertion, {
    cache,
    serialized,
    isStringTag: typeof WrappedComponent === "string"
  }), React3.createElement(WrappedComponent, newProps));
});
{
  Emotion.displayName = "EmotionCssPropInternal";
}
var Emotion$1 = Emotion;

// node_modules/@emotion/react/dist/emotion-react.browser.development.esm.js
var React4 = __toESM(require_react());
var import_hoist_non_react_statics2 = __toESM(require_hoist_non_react_statics_cjs());
var isDevelopment3 = true;
var pkg = {
  name: "@emotion/react",
  version: "11.14.0",
  main: "dist/emotion-react.cjs.js",
  module: "dist/emotion-react.esm.js",
  types: "dist/emotion-react.cjs.d.ts",
  exports: {
    ".": {
      types: {
        "import": "./dist/emotion-react.cjs.mjs",
        "default": "./dist/emotion-react.cjs.js"
      },
      development: {
        "edge-light": {
          module: "./dist/emotion-react.development.edge-light.esm.js",
          "import": "./dist/emotion-react.development.edge-light.cjs.mjs",
          "default": "./dist/emotion-react.development.edge-light.cjs.js"
        },
        worker: {
          module: "./dist/emotion-react.development.edge-light.esm.js",
          "import": "./dist/emotion-react.development.edge-light.cjs.mjs",
          "default": "./dist/emotion-react.development.edge-light.cjs.js"
        },
        workerd: {
          module: "./dist/emotion-react.development.edge-light.esm.js",
          "import": "./dist/emotion-react.development.edge-light.cjs.mjs",
          "default": "./dist/emotion-react.development.edge-light.cjs.js"
        },
        browser: {
          module: "./dist/emotion-react.browser.development.esm.js",
          "import": "./dist/emotion-react.browser.development.cjs.mjs",
          "default": "./dist/emotion-react.browser.development.cjs.js"
        },
        module: "./dist/emotion-react.development.esm.js",
        "import": "./dist/emotion-react.development.cjs.mjs",
        "default": "./dist/emotion-react.development.cjs.js"
      },
      "edge-light": {
        module: "./dist/emotion-react.edge-light.esm.js",
        "import": "./dist/emotion-react.edge-light.cjs.mjs",
        "default": "./dist/emotion-react.edge-light.cjs.js"
      },
      worker: {
        module: "./dist/emotion-react.edge-light.esm.js",
        "import": "./dist/emotion-react.edge-light.cjs.mjs",
        "default": "./dist/emotion-react.edge-light.cjs.js"
      },
      workerd: {
        module: "./dist/emotion-react.edge-light.esm.js",
        "import": "./dist/emotion-react.edge-light.cjs.mjs",
        "default": "./dist/emotion-react.edge-light.cjs.js"
      },
      browser: {
        module: "./dist/emotion-react.browser.esm.js",
        "import": "./dist/emotion-react.browser.cjs.mjs",
        "default": "./dist/emotion-react.browser.cjs.js"
      },
      module: "./dist/emotion-react.esm.js",
      "import": "./dist/emotion-react.cjs.mjs",
      "default": "./dist/emotion-react.cjs.js"
    },
    "./jsx-runtime": {
      types: {
        "import": "./jsx-runtime/dist/emotion-react-jsx-runtime.cjs.mjs",
        "default": "./jsx-runtime/dist/emotion-react-jsx-runtime.cjs.js"
      },
      development: {
        "edge-light": {
          module: "./jsx-runtime/dist/emotion-react-jsx-runtime.development.edge-light.esm.js",
          "import": "./jsx-runtime/dist/emotion-react-jsx-runtime.development.edge-light.cjs.mjs",
          "default": "./jsx-runtime/dist/emotion-react-jsx-runtime.development.edge-light.cjs.js"
        },
        worker: {
          module: "./jsx-runtime/dist/emotion-react-jsx-runtime.development.edge-light.esm.js",
          "import": "./jsx-runtime/dist/emotion-react-jsx-runtime.development.edge-light.cjs.mjs",
          "default": "./jsx-runtime/dist/emotion-react-jsx-runtime.development.edge-light.cjs.js"
        },
        workerd: {
          module: "./jsx-runtime/dist/emotion-react-jsx-runtime.development.edge-light.esm.js",
          "import": "./jsx-runtime/dist/emotion-react-jsx-runtime.development.edge-light.cjs.mjs",
          "default": "./jsx-runtime/dist/emotion-react-jsx-runtime.development.edge-light.cjs.js"
        },
        browser: {
          module: "./jsx-runtime/dist/emotion-react-jsx-runtime.browser.development.esm.js",
          "import": "./jsx-runtime/dist/emotion-react-jsx-runtime.browser.development.cjs.mjs",
          "default": "./jsx-runtime/dist/emotion-react-jsx-runtime.browser.development.cjs.js"
        },
        module: "./jsx-runtime/dist/emotion-react-jsx-runtime.development.esm.js",
        "import": "./jsx-runtime/dist/emotion-react-jsx-runtime.development.cjs.mjs",
        "default": "./jsx-runtime/dist/emotion-react-jsx-runtime.development.cjs.js"
      },
      "edge-light": {
        module: "./jsx-runtime/dist/emotion-react-jsx-runtime.edge-light.esm.js",
        "import": "./jsx-runtime/dist/emotion-react-jsx-runtime.edge-light.cjs.mjs",
        "default": "./jsx-runtime/dist/emotion-react-jsx-runtime.edge-light.cjs.js"
      },
      worker: {
        module: "./jsx-runtime/dist/emotion-react-jsx-runtime.edge-light.esm.js",
        "import": "./jsx-runtime/dist/emotion-react-jsx-runtime.edge-light.cjs.mjs",
        "default": "./jsx-runtime/dist/emotion-react-jsx-runtime.edge-light.cjs.js"
      },
      workerd: {
        module: "./jsx-runtime/dist/emotion-react-jsx-runtime.edge-light.esm.js",
        "import": "./jsx-runtime/dist/emotion-react-jsx-runtime.edge-light.cjs.mjs",
        "default": "./jsx-runtime/dist/emotion-react-jsx-runtime.edge-light.cjs.js"
      },
      browser: {
        module: "./jsx-runtime/dist/emotion-react-jsx-runtime.browser.esm.js",
        "import": "./jsx-runtime/dist/emotion-react-jsx-runtime.browser.cjs.mjs",
        "default": "./jsx-runtime/dist/emotion-react-jsx-runtime.browser.cjs.js"
      },
      module: "./jsx-runtime/dist/emotion-react-jsx-runtime.esm.js",
      "import": "./jsx-runtime/dist/emotion-react-jsx-runtime.cjs.mjs",
      "default": "./jsx-runtime/dist/emotion-react-jsx-runtime.cjs.js"
    },
    "./_isolated-hnrs": {
      types: {
        "import": "./_isolated-hnrs/dist/emotion-react-_isolated-hnrs.cjs.mjs",
        "default": "./_isolated-hnrs/dist/emotion-react-_isolated-hnrs.cjs.js"
      },
      development: {
        "edge-light": {
          module: "./_isolated-hnrs/dist/emotion-react-_isolated-hnrs.development.edge-light.esm.js",
          "import": "./_isolated-hnrs/dist/emotion-react-_isolated-hnrs.development.edge-light.cjs.mjs",
          "default": "./_isolated-hnrs/dist/emotion-react-_isolated-hnrs.development.edge-light.cjs.js"
        },
        worker: {
          module: "./_isolated-hnrs/dist/emotion-react-_isolated-hnrs.development.edge-light.esm.js",
          "import": "./_isolated-hnrs/dist/emotion-react-_isolated-hnrs.development.edge-light.cjs.mjs",
          "default": "./_isolated-hnrs/dist/emotion-react-_isolated-hnrs.development.edge-light.cjs.js"
        },
        workerd: {
          module: "./_isolated-hnrs/dist/emotion-react-_isolated-hnrs.development.edge-light.esm.js",
          "import": "./_isolated-hnrs/dist/emotion-react-_isolated-hnrs.development.edge-light.cjs.mjs",
          "default": "./_isolated-hnrs/dist/emotion-react-_isolated-hnrs.development.edge-light.cjs.js"
        },
        browser: {
          module: "./_isolated-hnrs/dist/emotion-react-_isolated-hnrs.browser.development.esm.js",
          "import": "./_isolated-hnrs/dist/emotion-react-_isolated-hnrs.browser.development.cjs.mjs",
          "default": "./_isolated-hnrs/dist/emotion-react-_isolated-hnrs.browser.development.cjs.js"
        },
        module: "./_isolated-hnrs/dist/emotion-react-_isolated-hnrs.development.esm.js",
        "import": "./_isolated-hnrs/dist/emotion-react-_isolated-hnrs.development.cjs.mjs",
        "default": "./_isolated-hnrs/dist/emotion-react-_isolated-hnrs.development.cjs.js"
      },
      "edge-light": {
        module: "./_isolated-hnrs/dist/emotion-react-_isolated-hnrs.edge-light.esm.js",
        "import": "./_isolated-hnrs/dist/emotion-react-_isolated-hnrs.edge-light.cjs.mjs",
        "default": "./_isolated-hnrs/dist/emotion-react-_isolated-hnrs.edge-light.cjs.js"
      },
      worker: {
        module: "./_isolated-hnrs/dist/emotion-react-_isolated-hnrs.edge-light.esm.js",
        "import": "./_isolated-hnrs/dist/emotion-react-_isolated-hnrs.edge-light.cjs.mjs",
        "default": "./_isolated-hnrs/dist/emotion-react-_isolated-hnrs.edge-light.cjs.js"
      },
      workerd: {
        module: "./_isolated-hnrs/dist/emotion-react-_isolated-hnrs.edge-light.esm.js",
        "import": "./_isolated-hnrs/dist/emotion-react-_isolated-hnrs.edge-light.cjs.mjs",
        "default": "./_isolated-hnrs/dist/emotion-react-_isolated-hnrs.edge-light.cjs.js"
      },
      browser: {
        module: "./_isolated-hnrs/dist/emotion-react-_isolated-hnrs.browser.esm.js",
        "import": "./_isolated-hnrs/dist/emotion-react-_isolated-hnrs.browser.cjs.mjs",
        "default": "./_isolated-hnrs/dist/emotion-react-_isolated-hnrs.browser.cjs.js"
      },
      module: "./_isolated-hnrs/dist/emotion-react-_isolated-hnrs.esm.js",
      "import": "./_isolated-hnrs/dist/emotion-react-_isolated-hnrs.cjs.mjs",
      "default": "./_isolated-hnrs/dist/emotion-react-_isolated-hnrs.cjs.js"
    },
    "./jsx-dev-runtime": {
      types: {
        "import": "./jsx-dev-runtime/dist/emotion-react-jsx-dev-runtime.cjs.mjs",
        "default": "./jsx-dev-runtime/dist/emotion-react-jsx-dev-runtime.cjs.js"
      },
      development: {
        "edge-light": {
          module: "./jsx-dev-runtime/dist/emotion-react-jsx-dev-runtime.development.edge-light.esm.js",
          "import": "./jsx-dev-runtime/dist/emotion-react-jsx-dev-runtime.development.edge-light.cjs.mjs",
          "default": "./jsx-dev-runtime/dist/emotion-react-jsx-dev-runtime.development.edge-light.cjs.js"
        },
        worker: {
          module: "./jsx-dev-runtime/dist/emotion-react-jsx-dev-runtime.development.edge-light.esm.js",
          "import": "./jsx-dev-runtime/dist/emotion-react-jsx-dev-runtime.development.edge-light.cjs.mjs",
          "default": "./jsx-dev-runtime/dist/emotion-react-jsx-dev-runtime.development.edge-light.cjs.js"
        },
        workerd: {
          module: "./jsx-dev-runtime/dist/emotion-react-jsx-dev-runtime.development.edge-light.esm.js",
          "import": "./jsx-dev-runtime/dist/emotion-react-jsx-dev-runtime.development.edge-light.cjs.mjs",
          "default": "./jsx-dev-runtime/dist/emotion-react-jsx-dev-runtime.development.edge-light.cjs.js"
        },
        browser: {
          module: "./jsx-dev-runtime/dist/emotion-react-jsx-dev-runtime.browser.development.esm.js",
          "import": "./jsx-dev-runtime/dist/emotion-react-jsx-dev-runtime.browser.development.cjs.mjs",
          "default": "./jsx-dev-runtime/dist/emotion-react-jsx-dev-runtime.browser.development.cjs.js"
        },
        module: "./jsx-dev-runtime/dist/emotion-react-jsx-dev-runtime.development.esm.js",
        "import": "./jsx-dev-runtime/dist/emotion-react-jsx-dev-runtime.development.cjs.mjs",
        "default": "./jsx-dev-runtime/dist/emotion-react-jsx-dev-runtime.development.cjs.js"
      },
      "edge-light": {
        module: "./jsx-dev-runtime/dist/emotion-react-jsx-dev-runtime.edge-light.esm.js",
        "import": "./jsx-dev-runtime/dist/emotion-react-jsx-dev-runtime.edge-light.cjs.mjs",
        "default": "./jsx-dev-runtime/dist/emotion-react-jsx-dev-runtime.edge-light.cjs.js"
      },
      worker: {
        module: "./jsx-dev-runtime/dist/emotion-react-jsx-dev-runtime.edge-light.esm.js",
        "import": "./jsx-dev-runtime/dist/emotion-react-jsx-dev-runtime.edge-light.cjs.mjs",
        "default": "./jsx-dev-runtime/dist/emotion-react-jsx-dev-runtime.edge-light.cjs.js"
      },
      workerd: {
        module: "./jsx-dev-runtime/dist/emotion-react-jsx-dev-runtime.edge-light.esm.js",
        "import": "./jsx-dev-runtime/dist/emotion-react-jsx-dev-runtime.edge-light.cjs.mjs",
        "default": "./jsx-dev-runtime/dist/emotion-react-jsx-dev-runtime.edge-light.cjs.js"
      },
      browser: {
        module: "./jsx-dev-runtime/dist/emotion-react-jsx-dev-runtime.browser.esm.js",
        "import": "./jsx-dev-runtime/dist/emotion-react-jsx-dev-runtime.browser.cjs.mjs",
        "default": "./jsx-dev-runtime/dist/emotion-react-jsx-dev-runtime.browser.cjs.js"
      },
      module: "./jsx-dev-runtime/dist/emotion-react-jsx-dev-runtime.esm.js",
      "import": "./jsx-dev-runtime/dist/emotion-react-jsx-dev-runtime.cjs.mjs",
      "default": "./jsx-dev-runtime/dist/emotion-react-jsx-dev-runtime.cjs.js"
    },
    "./package.json": "./package.json",
    "./types/css-prop": "./types/css-prop.d.ts",
    "./macro": {
      types: {
        "import": "./macro.d.mts",
        "default": "./macro.d.ts"
      },
      "default": "./macro.js"
    }
  },
  imports: {
    "#is-development": {
      development: "./src/conditions/true.ts",
      "default": "./src/conditions/false.ts"
    },
    "#is-browser": {
      "edge-light": "./src/conditions/false.ts",
      workerd: "./src/conditions/false.ts",
      worker: "./src/conditions/false.ts",
      browser: "./src/conditions/true.ts",
      "default": "./src/conditions/is-browser.ts"
    }
  },
  files: [
    "src",
    "dist",
    "jsx-runtime",
    "jsx-dev-runtime",
    "_isolated-hnrs",
    "types/css-prop.d.ts",
    "macro.*"
  ],
  sideEffects: false,
  author: "Emotion Contributors",
  license: "MIT",
  scripts: {
    "test:typescript": "dtslint types"
  },
  dependencies: {
    "@babel/runtime": "^7.18.3",
    "@emotion/babel-plugin": "^11.13.5",
    "@emotion/cache": "^11.14.0",
    "@emotion/serialize": "^1.3.3",
    "@emotion/use-insertion-effect-with-fallbacks": "^1.2.0",
    "@emotion/utils": "^1.4.2",
    "@emotion/weak-memoize": "^0.4.0",
    "hoist-non-react-statics": "^3.3.1"
  },
  peerDependencies: {
    react: ">=16.8.0"
  },
  peerDependenciesMeta: {
    "@types/react": {
      optional: true
    }
  },
  devDependencies: {
    "@definitelytyped/dtslint": "0.0.112",
    "@emotion/css": "11.13.5",
    "@emotion/css-prettifier": "1.2.0",
    "@emotion/server": "11.11.0",
    "@emotion/styled": "11.14.0",
    "@types/hoist-non-react-statics": "^3.3.5",
    "html-tag-names": "^1.1.2",
    react: "16.14.0",
    "svg-tag-names": "^1.1.1",
    typescript: "^5.4.5"
  },
  repository: "https://github.com/emotion-js/emotion/tree/main/packages/react",
  publishConfig: {
    access: "public"
  },
  "umd:main": "dist/emotion-react.umd.min.js",
  preconstruct: {
    entrypoints: [
      "./index.ts",
      "./jsx-runtime.ts",
      "./jsx-dev-runtime.ts",
      "./_isolated-hnrs.ts"
    ],
    umdName: "emotionReact",
    exports: {
      extra: {
        "./types/css-prop": "./types/css-prop.d.ts",
        "./macro": {
          types: {
            "import": "./macro.d.mts",
            "default": "./macro.d.ts"
          },
          "default": "./macro.js"
        }
      }
    }
  }
};
var jsx = function jsx2(type, props) {
  var args = arguments;
  if (props == null || !hasOwn.call(props, "css")) {
    return React4.createElement.apply(void 0, args);
  }
  var argsLength = args.length;
  var createElementArgArray = new Array(argsLength);
  createElementArgArray[0] = Emotion$1;
  createElementArgArray[1] = createEmotionProps(type, props);
  for (var i11 = 2; i11 < argsLength; i11++) {
    createElementArgArray[i11] = args[i11];
  }
  return React4.createElement.apply(null, createElementArgArray);
};
(function(_jsx) {
  var JSX;
  /* @__PURE__ */ (function(_JSX) {
  })(JSX || (JSX = _jsx.JSX || (_jsx.JSX = {})));
})(jsx || (jsx = {}));
var warnedAboutCssPropForGlobal = false;
var Global = withEmotionCache(function(props, cache) {
  if (!warnedAboutCssPropForGlobal && // check for className as well since the user is
  // probably using the custom createElement which
  // means it will be turned into a className prop
  // I don't really want to add it to the type since it shouldn't be used
  ("className" in props && props.className || "css" in props && props.css)) {
    console.error("It looks like you're using the css prop on Global, did you mean to use the styles prop instead?");
    warnedAboutCssPropForGlobal = true;
  }
  var styles = props.styles;
  var serialized = serializeStyles([styles], void 0, React4.useContext(ThemeContext));
  var sheetRef = React4.useRef();
  useInsertionEffectWithLayoutFallback(function() {
    var key = cache.key + "-global";
    var sheet = new cache.sheet.constructor({
      key,
      nonce: cache.sheet.nonce,
      container: cache.sheet.container,
      speedy: cache.sheet.isSpeedy
    });
    var rehydrating = false;
    var node2 = document.querySelector('style[data-emotion="' + key + " " + serialized.name + '"]');
    if (cache.sheet.tags.length) {
      sheet.before = cache.sheet.tags[0];
    }
    if (node2 !== null) {
      rehydrating = true;
      node2.setAttribute("data-emotion", key);
      sheet.hydrate([node2]);
    }
    sheetRef.current = [sheet, rehydrating];
    return function() {
      sheet.flush();
    };
  }, [cache]);
  useInsertionEffectWithLayoutFallback(function() {
    var sheetRefCurrent = sheetRef.current;
    var sheet = sheetRefCurrent[0], rehydrating = sheetRefCurrent[1];
    if (rehydrating) {
      sheetRefCurrent[1] = false;
      return;
    }
    if (serialized.next !== void 0) {
      insertStyles(cache, serialized.next, true);
    }
    if (sheet.tags.length) {
      var element = sheet.tags[sheet.tags.length - 1].nextElementSibling;
      sheet.before = element;
      sheet.flush();
    }
    cache.insert("", serialized, sheet, false);
  }, [cache, serialized.name]);
  return null;
});
{
  Global.displayName = "EmotionGlobal";
}
function css() {
  for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
    args[_key] = arguments[_key];
  }
  return serializeStyles(args);
}
function keyframes() {
  var insertable = css.apply(void 0, arguments);
  var name = "animation-" + insertable.name;
  return {
    name,
    styles: "@keyframes " + name + "{" + insertable.styles + "}",
    anim: 1,
    toString: function toString() {
      return "_EMO_" + this.name + "_" + this.styles + "_EMO_";
    }
  };
}
var classnames = function classnames2(args) {
  var len = args.length;
  var i11 = 0;
  var cls = "";
  for (; i11 < len; i11++) {
    var arg = args[i11];
    if (arg == null) continue;
    var toAdd = void 0;
    switch (typeof arg) {
      case "boolean":
        break;
      case "object": {
        if (Array.isArray(arg)) {
          toAdd = classnames2(arg);
        } else {
          if (arg.styles !== void 0 && arg.name !== void 0) {
            console.error("You have passed styles created with `css` from `@emotion/react` package to the `cx`.\n`cx` is meant to compose class names (strings) so you should convert those styles to a class name by passing them to the `css` received from <ClassNames/> component.");
          }
          toAdd = "";
          for (var k8 in arg) {
            if (arg[k8] && k8) {
              toAdd && (toAdd += " ");
              toAdd += k8;
            }
          }
        }
        break;
      }
      default: {
        toAdd = arg;
      }
    }
    if (toAdd) {
      cls && (cls += " ");
      cls += toAdd;
    }
  }
  return cls;
};
function merge(registered, css2, className) {
  var registeredStyles = [];
  var rawClassName = getRegisteredStyles(registered, registeredStyles, className);
  if (registeredStyles.length < 2) {
    return className;
  }
  return rawClassName + css2(registeredStyles);
}
var Insertion3 = function Insertion4(_ref) {
  var cache = _ref.cache, serializedArr = _ref.serializedArr;
  useInsertionEffectAlwaysWithSyncFallback(function() {
    for (var i11 = 0; i11 < serializedArr.length; i11++) {
      insertStyles(cache, serializedArr[i11], false);
    }
  });
  return null;
};
var ClassNames = withEmotionCache(function(props, cache) {
  var hasRendered = false;
  var serializedArr = [];
  var css2 = function css3() {
    if (hasRendered && isDevelopment3) {
      throw new Error("css can only be used during render");
    }
    for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
      args[_key] = arguments[_key];
    }
    var serialized = serializeStyles(args, cache.registered);
    serializedArr.push(serialized);
    registerStyles(cache, serialized, false);
    return cache.key + "-" + serialized.name;
  };
  var cx = function cx2() {
    if (hasRendered && isDevelopment3) {
      throw new Error("cx can only be used during render");
    }
    for (var _len2 = arguments.length, args = new Array(_len2), _key2 = 0; _key2 < _len2; _key2++) {
      args[_key2] = arguments[_key2];
    }
    return merge(cache.registered, css2, classnames(args));
  };
  var content = {
    css: css2,
    cx,
    theme: React4.useContext(ThemeContext)
  };
  var ele = props.children(content);
  hasRendered = true;
  return React4.createElement(React4.Fragment, null, React4.createElement(Insertion3, {
    cache,
    serializedArr
  }), ele);
});
{
  ClassNames.displayName = "EmotionClassNames";
}
{
  isBrowser2 = typeof document !== "undefined";
  isTestEnv = typeof jest !== "undefined" || typeof vi !== "undefined";
  if (isBrowser2 && !isTestEnv) {
    globalContext = typeof globalThis !== "undefined" ? globalThis : isBrowser2 ? window : global;
    globalKey = "__EMOTION_REACT_" + pkg.version.split(".")[0] + "__";
    if (globalContext[globalKey]) {
      console.warn("You are loading @emotion/react when it is already loaded. Running multiple instances may cause problems. This can happen if multiple versions are used, or if multiple builds of the same version are used.");
    }
    globalContext[globalKey] = true;
  }
}
var isBrowser2;
var isTestEnv;
var globalContext;
var globalKey;

// node_modules/@emotion/styled/base/dist/emotion-styled-base.browser.development.esm.js
var React5 = __toESM(require_react());

// node_modules/@emotion/is-prop-valid/dist/emotion-is-prop-valid.esm.js
var reactPropsRegex = /^((children|dangerouslySetInnerHTML|key|ref|autoFocus|defaultValue|defaultChecked|innerHTML|suppressContentEditableWarning|suppressHydrationWarning|valueLink|abbr|accept|acceptCharset|accessKey|action|allow|allowUserMedia|allowPaymentRequest|allowFullScreen|allowTransparency|alt|async|autoComplete|autoPlay|capture|cellPadding|cellSpacing|challenge|charSet|checked|cite|classID|className|cols|colSpan|content|contentEditable|contextMenu|controls|controlsList|coords|crossOrigin|data|dateTime|decoding|default|defer|dir|disabled|disablePictureInPicture|disableRemotePlayback|download|draggable|encType|enterKeyHint|fetchpriority|fetchPriority|form|formAction|formEncType|formMethod|formNoValidate|formTarget|frameBorder|headers|height|hidden|high|href|hrefLang|htmlFor|httpEquiv|id|inputMode|integrity|is|keyParams|keyType|kind|label|lang|list|loading|loop|low|marginHeight|marginWidth|max|maxLength|media|mediaGroup|method|min|minLength|multiple|muted|name|nonce|noValidate|open|optimum|pattern|placeholder|playsInline|popover|popoverTarget|popoverTargetAction|poster|preload|profile|radioGroup|readOnly|referrerPolicy|rel|required|reversed|role|rows|rowSpan|sandbox|scope|scoped|scrolling|seamless|selected|shape|size|sizes|slot|span|spellCheck|src|srcDoc|srcLang|srcSet|start|step|style|summary|tabIndex|target|title|translate|type|useMap|value|width|wmode|wrap|about|datatype|inlist|prefix|property|resource|typeof|vocab|autoCapitalize|autoCorrect|autoSave|color|incremental|fallback|inert|itemProp|itemScope|itemType|itemID|itemRef|on|option|results|security|unselectable|accentHeight|accumulate|additive|alignmentBaseline|allowReorder|alphabetic|amplitude|arabicForm|ascent|attributeName|attributeType|autoReverse|azimuth|baseFrequency|baselineShift|baseProfile|bbox|begin|bias|by|calcMode|capHeight|clip|clipPathUnits|clipPath|clipRule|colorInterpolation|colorInterpolationFilters|colorProfile|colorRendering|contentScriptType|contentStyleType|cursor|cx|cy|d|decelerate|descent|diffuseConstant|direction|display|divisor|dominantBaseline|dur|dx|dy|edgeMode|elevation|enableBackground|end|exponent|externalResourcesRequired|fill|fillOpacity|fillRule|filter|filterRes|filterUnits|floodColor|floodOpacity|focusable|fontFamily|fontSize|fontSizeAdjust|fontStretch|fontStyle|fontVariant|fontWeight|format|from|fr|fx|fy|g1|g2|glyphName|glyphOrientationHorizontal|glyphOrientationVertical|glyphRef|gradientTransform|gradientUnits|hanging|horizAdvX|horizOriginX|ideographic|imageRendering|in|in2|intercept|k|k1|k2|k3|k4|kernelMatrix|kernelUnitLength|kerning|keyPoints|keySplines|keyTimes|lengthAdjust|letterSpacing|lightingColor|limitingConeAngle|local|markerEnd|markerMid|markerStart|markerHeight|markerUnits|markerWidth|mask|maskContentUnits|maskUnits|mathematical|mode|numOctaves|offset|opacity|operator|order|orient|orientation|origin|overflow|overlinePosition|overlineThickness|panose1|paintOrder|pathLength|patternContentUnits|patternTransform|patternUnits|pointerEvents|points|pointsAtX|pointsAtY|pointsAtZ|preserveAlpha|preserveAspectRatio|primitiveUnits|r|radius|refX|refY|renderingIntent|repeatCount|repeatDur|requiredExtensions|requiredFeatures|restart|result|rotate|rx|ry|scale|seed|shapeRendering|slope|spacing|specularConstant|specularExponent|speed|spreadMethod|startOffset|stdDeviation|stemh|stemv|stitchTiles|stopColor|stopOpacity|strikethroughPosition|strikethroughThickness|string|stroke|strokeDasharray|strokeDashoffset|strokeLinecap|strokeLinejoin|strokeMiterlimit|strokeOpacity|strokeWidth|surfaceScale|systemLanguage|tableValues|targetX|targetY|textAnchor|textDecoration|textRendering|textLength|to|transform|u1|u2|underlinePosition|underlineThickness|unicode|unicodeBidi|unicodeRange|unitsPerEm|vAlphabetic|vHanging|vIdeographic|vMathematical|values|vectorEffect|version|vertAdvY|vertOriginX|vertOriginY|viewBox|viewTarget|visibility|widths|wordSpacing|writingMode|x|xHeight|x1|x2|xChannelSelector|xlinkActuate|xlinkArcrole|xlinkHref|xlinkRole|xlinkShow|xlinkTitle|xlinkType|xmlBase|xmlns|xmlnsXlink|xmlLang|xmlSpace|y|y1|y2|yChannelSelector|z|zoomAndPan|for|class|autofocus)|(([Dd][Aa][Tt][Aa]|[Aa][Rr][Ii][Aa]|x)-.*))$/;
var isPropValid = memoize(
  function(prop) {
    return reactPropsRegex.test(prop) || prop.charCodeAt(0) === 111 && prop.charCodeAt(1) === 110 && prop.charCodeAt(2) < 91;
  }
  /* Z+1 */
);

// node_modules/@emotion/styled/base/dist/emotion-styled-base.browser.development.esm.js
var isDevelopment4 = true;
var testOmitPropsOnStringTag = isPropValid;
var testOmitPropsOnComponent = function testOmitPropsOnComponent2(key) {
  return key !== "theme";
};
var getDefaultShouldForwardProp = function getDefaultShouldForwardProp2(tag) {
  return typeof tag === "string" && // 96 is one less than the char code
  // for "a" so this is checking that
  // it's a lowercase character
  tag.charCodeAt(0) > 96 ? testOmitPropsOnStringTag : testOmitPropsOnComponent;
};
var composeShouldForwardProps = function composeShouldForwardProps2(tag, options, isReal) {
  var shouldForwardProp;
  if (options) {
    var optionsShouldForwardProp = options.shouldForwardProp;
    shouldForwardProp = tag.__emotion_forwardProp && optionsShouldForwardProp ? function(propName) {
      return tag.__emotion_forwardProp(propName) && optionsShouldForwardProp(propName);
    } : optionsShouldForwardProp;
  }
  if (typeof shouldForwardProp !== "function" && isReal) {
    shouldForwardProp = tag.__emotion_forwardProp;
  }
  return shouldForwardProp;
};
var ILLEGAL_ESCAPE_SEQUENCE_ERROR2 = `You have illegal escape sequence in your template literal, most likely inside content's property value.
Because you write your CSS inside a JavaScript string you actually have to do double escaping, so for example "content: '\\00d7';" should become "content: '\\\\00d7';".
You can read more about this here:
https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Template_literals#ES2018_revision_of_illegal_escape_sequences`;
var Insertion5 = function Insertion6(_ref) {
  var cache = _ref.cache, serialized = _ref.serialized, isStringTag = _ref.isStringTag;
  registerStyles(cache, serialized, isStringTag);
  useInsertionEffectAlwaysWithSyncFallback(function() {
    return insertStyles(cache, serialized, isStringTag);
  });
  return null;
};
var createStyled = function createStyled2(tag, options) {
  {
    if (tag === void 0) {
      throw new Error("You are trying to create a styled element with an undefined component.\nYou may have forgotten to import it.");
    }
  }
  var isReal = tag.__emotion_real === tag;
  var baseTag = isReal && tag.__emotion_base || tag;
  var identifierName;
  var targetClassName;
  if (options !== void 0) {
    identifierName = options.label;
    targetClassName = options.target;
  }
  var shouldForwardProp = composeShouldForwardProps(tag, options, isReal);
  var defaultShouldForwardProp = shouldForwardProp || getDefaultShouldForwardProp(baseTag);
  var shouldUseAs = !defaultShouldForwardProp("as");
  return function() {
    var args = arguments;
    var styles = isReal && tag.__emotion_styles !== void 0 ? tag.__emotion_styles.slice(0) : [];
    if (identifierName !== void 0) {
      styles.push("label:" + identifierName + ";");
    }
    if (args[0] == null || args[0].raw === void 0) {
      styles.push.apply(styles, args);
    } else {
      var templateStringsArr = args[0];
      if (templateStringsArr[0] === void 0) {
        console.error(ILLEGAL_ESCAPE_SEQUENCE_ERROR2);
      }
      styles.push(templateStringsArr[0]);
      var len = args.length;
      var i11 = 1;
      for (; i11 < len; i11++) {
        if (templateStringsArr[i11] === void 0) {
          console.error(ILLEGAL_ESCAPE_SEQUENCE_ERROR2);
        }
        styles.push(args[i11], templateStringsArr[i11]);
      }
    }
    var Styled = withEmotionCache(function(props, cache, ref) {
      var FinalTag = shouldUseAs && props.as || baseTag;
      var className = "";
      var classInterpolations = [];
      var mergedProps = props;
      if (props.theme == null) {
        mergedProps = {};
        for (var key in props) {
          mergedProps[key] = props[key];
        }
        mergedProps.theme = React5.useContext(ThemeContext);
      }
      if (typeof props.className === "string") {
        className = getRegisteredStyles(cache.registered, classInterpolations, props.className);
      } else if (props.className != null) {
        className = props.className + " ";
      }
      var serialized = serializeStyles(styles.concat(classInterpolations), cache.registered, mergedProps);
      className += cache.key + "-" + serialized.name;
      if (targetClassName !== void 0) {
        className += " " + targetClassName;
      }
      var finalShouldForwardProp = shouldUseAs && shouldForwardProp === void 0 ? getDefaultShouldForwardProp(FinalTag) : defaultShouldForwardProp;
      var newProps = {};
      for (var _key in props) {
        if (shouldUseAs && _key === "as") continue;
        if (finalShouldForwardProp(_key)) {
          newProps[_key] = props[_key];
        }
      }
      newProps.className = className;
      if (ref) {
        newProps.ref = ref;
      }
      return React5.createElement(React5.Fragment, null, React5.createElement(Insertion5, {
        cache,
        serialized,
        isStringTag: typeof FinalTag === "string"
      }), React5.createElement(FinalTag, newProps));
    });
    Styled.displayName = identifierName !== void 0 ? identifierName : "Styled(" + (typeof baseTag === "string" ? baseTag : baseTag.displayName || baseTag.name || "Component") + ")";
    Styled.defaultProps = tag.defaultProps;
    Styled.__emotion_real = Styled;
    Styled.__emotion_base = baseTag;
    Styled.__emotion_styles = styles;
    Styled.__emotion_forwardProp = shouldForwardProp;
    Object.defineProperty(Styled, "toString", {
      value: function value() {
        if (targetClassName === void 0 && isDevelopment4) {
          return "NO_COMPONENT_SELECTOR";
        }
        return "." + targetClassName;
      }
    });
    Styled.withComponent = function(nextTag, nextOptions) {
      var newStyled = createStyled2(nextTag, _extends({}, options, nextOptions, {
        shouldForwardProp: composeShouldForwardProps(Styled, nextOptions, true)
      }));
      return newStyled.apply(void 0, styles);
    };
    return Styled;
  };
};

// node_modules/@emotion/styled/dist/emotion-styled.browser.development.esm.js
var import_react7 = __toESM(require_react());
var tags = [
  "a",
  "abbr",
  "address",
  "area",
  "article",
  "aside",
  "audio",
  "b",
  "base",
  "bdi",
  "bdo",
  "big",
  "blockquote",
  "body",
  "br",
  "button",
  "canvas",
  "caption",
  "cite",
  "code",
  "col",
  "colgroup",
  "data",
  "datalist",
  "dd",
  "del",
  "details",
  "dfn",
  "dialog",
  "div",
  "dl",
  "dt",
  "em",
  "embed",
  "fieldset",
  "figcaption",
  "figure",
  "footer",
  "form",
  "h1",
  "h2",
  "h3",
  "h4",
  "h5",
  "h6",
  "head",
  "header",
  "hgroup",
  "hr",
  "html",
  "i",
  "iframe",
  "img",
  "input",
  "ins",
  "kbd",
  "keygen",
  "label",
  "legend",
  "li",
  "link",
  "main",
  "map",
  "mark",
  "marquee",
  "menu",
  "menuitem",
  "meta",
  "meter",
  "nav",
  "noscript",
  "object",
  "ol",
  "optgroup",
  "option",
  "output",
  "p",
  "param",
  "picture",
  "pre",
  "progress",
  "q",
  "rp",
  "rt",
  "ruby",
  "s",
  "samp",
  "script",
  "section",
  "select",
  "small",
  "source",
  "span",
  "strong",
  "style",
  "sub",
  "summary",
  "sup",
  "table",
  "tbody",
  "td",
  "textarea",
  "tfoot",
  "th",
  "thead",
  "time",
  "title",
  "tr",
  "track",
  "u",
  "ul",
  "var",
  "video",
  "wbr",
  // SVG
  "circle",
  "clipPath",
  "defs",
  "ellipse",
  "foreignObject",
  "g",
  "image",
  "line",
  "linearGradient",
  "mask",
  "path",
  "pattern",
  "polygon",
  "polyline",
  "radialGradient",
  "rect",
  "stop",
  "svg",
  "text",
  "tspan"
];
var styled = createStyled.bind(null);
tags.forEach(function(tagName) {
  styled[tagName] = styled(tagName);
});

// node_modules/@hakit/core/dist/es/types/autogenerated-types-by-domain.js
var c4 = {
  UNKNOWN: "unknown",
  ONOFF: "onoff",
  BRIGHTNESS: "brightness",
  COLOR_TEMP: "color_temp",
  HS: "hs",
  XY: "xy",
  RGB: "rgb",
  RGBW: "rgbw",
  RGBWW: "rgbww",
  WHITE: "white"
};

// node_modules/@hakit/core/dist/es/utils/computeDomain.js
var import_lodash2 = __toESM(require_lodash(), 1);
var m5 = (o13) => (0, import_lodash2.snakeCase)(o13.substring(0, o13.indexOf(".")));

// node_modules/@hakit/core/dist/es/utils/number.js
var u6 = (i11, t9) => {
  const r13 = t9?.display_precision;
  if (r13 != null)
    return {
      maximumFractionDigits: r13,
      minimumFractionDigits: r13
    };
  if (Number.isInteger(Number(i11?.attributes?.step)) && Number.isInteger(Number(i11?.state)))
    return { maximumFractionDigits: 0 };
};
var a4 = (i11) => m6(i11.attributes);
var m6 = (i11, t9) => !!i11.unit_of_measurement || !!i11.state_class || (t9 || []).includes(i11.device_class || "");
var s4 = (i11, t9) => {
  const r13 = {
    maximumFractionDigits: 2,
    ...t9
  };
  if (typeof i11 != "string")
    return r13;
  if (!t9 || t9.minimumFractionDigits === void 0 && t9.maximumFractionDigits === void 0) {
    const e6 = i11.indexOf(".") > -1 ? i11.split(".")[1].length : 0;
    r13.minimumFractionDigits = e6, r13.maximumFractionDigits = e6;
  }
  return r13;
};
var n3 = (i11, t9 = 2) => Math.round(i11 * 10 ** t9) / 10 ** t9;
var c5 = (i11, t9) => (Number.isNaN = Number.isNaN || function r13(e6) {
  return typeof e6 == "number" && r13(e6);
}, !Number.isNaN(Number(i11)) && i11 !== "" ? new Intl.NumberFormat(
  "en-US",
  s4(i11, {
    ...t9,
    useGrouping: false
  })
).format(Number(i11)) : typeof i11 == "string" ? i11 : `${n3(i11, t9?.maximumFractionDigits).toString()}${t9?.style === "currency" ? ` ${t9.currency}` : ""}`);

// node_modules/@hakit/core/dist/es/utils/blankBeforeUnit.js
var s5 = (e6) => {
  switch (e6.language) {
    case "cs":
    case "de":
    case "fi":
    case "fr":
    case "sk":
    case "sv":
      return " ";
    default:
      return "";
  }
};
var a5 = (e6, r13) => e6 === "°" ? "" : r13 && e6 === "%" ? s5(r13) : " ";

// node_modules/@hakit/core/dist/es/data/entity.js
var n4 = "unavailable";
var t5 = "unknown";
var F2 = "on";
var N3 = "off";
var T3 = "°C";
var S3 = "°F";
var I4 = /* @__PURE__ */ new Set(["camera", "image", "media_player"]);
var O2 = ["on", "off", n4, t5];
var o3 = (s9) => (a13, c15) => s9.includes(a13, c15);
var e3 = [n4, t5];
var A3 = [n4, t5, "off"];
var _2 = o3(e3);
var i4 = o3(A3);

// node_modules/@hakit/core/dist/es/HassConnect/handleSuspendResume.js
function v3(s9, a13 = {}) {
  const {
    suspendWhenHidden: g10 = true,
    hiddenDelayMs: f13 = 3e5,
    // default 5 minutes
    debug: e6 = false,
    onStatusChange: o13
  } = a13, u15 = f13;
  let n15 = null, i11 = null, l10 = false;
  s9.connected ? (e6 && console.log("[SR] Connection is already active → handleSuspendResume will manage suspension"), o13?.("connected")) : (e6 && console.log("[SR] Connection is not active"), o13?.("disconnected"));
  function p8() {
    if (e6 && console.log("[SR] onHidden() triggered"), !g10) {
      e6 && console.log("[SR] suspendWhenHidden is false → skipping suspension");
      return;
    }
    if (l10) {
      e6 && console.log("[SR] Already suspended → skipping duplicate suspension");
      return;
    }
    l10 = true;
    const m15 = new Promise((R5) => {
      n15 = () => {
        e6 && console.log("[SR] pendingResolve() called → lifting suspension"), l10 = false, n15 = null, R5(), o13?.("connected");
      };
    });
    e6 && console.log("[SR] Calling connection.suspendReconnectUntil(...)"), o13?.("pending-suspension"), s9.suspendReconnectUntil(m15), e6 && console.log(`[SR] Starting hidden delay of ${u15}ms before actual suspend()`), i11 = window.setTimeout(() => {
      i11 = null, document.hidden ? (e6 && console.log("[SR] Hidden timeout elapsed → calling suspend()"), t9()) : n15 && (e6 && console.log("[SR] Hidden timeout elapsed but page is visible → resolving pendingResolve()"), n15());
    }, u15), window.addEventListener("focus", d12, { once: true });
  }
  function d12() {
    e6 && console.log("[SR] onVisibleOrResume() fired (page became visible)"), i11 !== null && (clearTimeout(i11), i11 = null, e6 && console.log("[SR] Cleared hiddenTimeoutId (user returned before allotted time)")), n15 && (e6 && console.log("[SR] Resolving pendingResolve() on actual resume"), n15());
  }
  function c15() {
    document.hidden ? (e6 && console.log("[SR] visibilitychange → HIDDEN"), p8()) : (e6 && console.log("[SR] visibilitychange → VISIBLE"), d12());
  }
  function r13() {
    e6 && console.log("[SR] resume event fired"), d12();
  }
  function t9() {
    if (!s9.connected) {
      e6 && console.log("[SR] Connection already suspended → skipping suspend()");
      return;
    }
    o13?.("suspended"), e6 && console.log("[SR] suspend() called → suspending connection"), window.stop(), s9.suspend();
  }
  return document.addEventListener("visibilitychange", c15, false), document.addEventListener("freeze", t9), document.addEventListener("resume", r13), e6 && console.log("[SR] handleSuspendResume() initialized; debugging is ON"), () => {
    e6 && console.log("[SR] cleanup() called → removing listeners & clearing timeouts"), document.removeEventListener("visibilitychange", c15, false), document.removeEventListener("freeze", t9), document.removeEventListener("resume", r13), window.removeEventListener("focus", d12), i11 !== null && (e6 && console.log("[SR] cleanup: Clearing hiddenTimeoutId"), clearTimeout(i11), i11 = null), n15 && (e6 && console.log("[SR] cleanup: Resolving pendingResolve() to let reconnection proceed"), n15(), n15 = null, l10 = false);
  };
}

// node_modules/@hakit/core/dist/es/HassConnect/tryConnection.js
function i5(t9, r13) {
  const n15 = (() => {
    switch (t9) {
      case ERR_INVALID_AUTH:
        return `ERR_INVALID_AUTH: Invalid authentication. ${r13 ? 'Check your "Long-Lived Access Token".' : ""}`;
      case ERR_CANNOT_CONNECT:
        return "ERR_CANNOT_CONNECT: Unable to connect";
      case ERR_CONNECTION_LOST:
        return "ERR_CONNECTION_LOST: Lost connection to home assistant.";
      case ERR_HASS_HOST_REQUIRED:
        return "ERR_HASS_HOST_REQUIRED: Please enter a Home Assistant URL.";
      case ERR_INVALID_HTTPS_TO_HTTP:
        return 'ERR_INVALID_HTTPS_TO_HTTP: Cannot connect to Home Assistant instances over "http://".';
      default:
        return null;
    }
  })();
  return n15 !== null ? n15 : t9?.error || t9?.message || `Unknown Error (${t9})`;
}
function _3() {
  try {
    return window.top?.hassConnection;
  } catch (t9) {
    console.error("Error getting inherited connection", t9);
    return;
  }
}
function g3(t9, r13) {
  const o13 = location && location.search.includes("auth_callback=1"), n15 = !!_3(), c15 = !!r13, a13 = !!c2(t9, false);
  switch (true) {
    case o13:
      return "auth-callback";
    case n15:
      return "inherited-auth";
    case c15:
      return "provided-token";
    case a13:
      return "saved-tokens";
    default:
      return "user-request";
  }
}
var N4 = async (t9, r13) => {
  const o13 = g3(t9, r13);
  if (o13 === "inherited-auth")
    try {
      const { auth: e6, conn: s9 } = await _3();
      return {
        type: "success",
        connection: s9,
        auth: e6
      };
    } catch (e6) {
      return {
        type: "error",
        error: i5(e6, r13)
      };
    }
  if (o13 === "provided-token" && r13)
    try {
      const e6 = await createLongLivedTokenAuth(t9, r13);
      return {
        type: "success",
        connection: await createConnection({ auth: e6 }),
        auth: e6
      };
    } catch (e6) {
      return {
        type: "error",
        error: i5(e6, r13)
      };
    }
  const n15 = {
    saveTokens: a3,
    loadTokens: () => Promise.resolve(c2(t9))
  };
  if (t9 && o13 === "user-request") {
    if (n15.hassUrl = t9, n15.hassUrl === "")
      return {
        type: "error",
        error: "Please enter a Home Assistant URL."
      };
    if (n15.hassUrl.indexOf("://") === -1)
      return {
        type: "error",
        error: "Please enter your full URL, including the protocol part (https://)."
      };
    try {
      new URL(n15.hassUrl);
    } catch (e6) {
      return console.error("Error:", e6), {
        type: "error",
        error: "Invalid URL"
      };
    }
  }
  let c15;
  try {
    c15 = await getAuth(n15);
  } catch (e6) {
    return e6?.error === "invalid_grant" ? (l2(), N4(t9, r13)) : o13 === "saved-tokens" && e6 === ERR_CANNOT_CONNECT ? {
      type: "failed",
      cannotConnect: true
    } : {
      type: "error",
      error: i5(e6, r13)
    };
  } finally {
    location && location.search.includes("auth_callback=1") && history.replaceState(null, "", location.pathname);
  }
  let a13;
  try {
    a13 = await createConnection({ auth: c15 });
  } catch (e6) {
    if (o13 === "saved-tokens") {
      if (e6 === ERR_CANNOT_CONNECT)
        return {
          type: "failed",
          cannotConnect: true
        };
      e6 === ERR_INVALID_AUTH && a3(null);
    }
    return {
      type: "error",
      error: i5(e6, r13)
    };
  }
  return {
    type: "success",
    connection: a13,
    auth: c15
  };
};

// node_modules/@hakit/core/dist/es/utils/string.js
var import_lodash3 = __toESM(require_lodash(), 1);
function m7(t9) {
  return t9 ? (0, import_lodash3.join)((0, import_lodash3.map)((0, import_lodash3.split)(String(t9), "_"), import_lodash3.capitalize), " ") : "";
}
var c6 = (t9) => new Intl.Collator(t9, { sensitivity: "accent", numeric: true });
var l4 = (t9) => new Intl.Collator(t9, { numeric: true });
var p3 = (t9, n15, e6 = void 0) => l4(e6).compare(t9, n15);
var u7 = (t9, n15, e6 = void 0) => c6(e6).compare(t9, n15);

// node_modules/@hakit/core/dist/es/utils/stripPrefixFromEntityName.js
var a7 = [" ", ": ", " - "];
var c7 = (s9, o13) => {
  const r13 = s9.toLowerCase(), n15 = o13.toLowerCase();
  for (const i11 of a7) {
    const t9 = `${n15}${i11}`;
    if (r13.startsWith(t9)) {
      const e6 = s9.substring(t9.length);
      if (e6.length)
        return f5(e6.substr(0, e6.indexOf(" "))) ? e6 : e6[0].toUpperCase() + e6.slice(1);
    }
  }
};
var f5 = (s9) => s9.toLowerCase() !== s9;

// node_modules/@hakit/core/dist/es/utils/subscribe/areas.js
var import_lodash4 = __toESM(require_lodash(), 1);
var n5 = (r13) => r13.sendMessagePromise({
  type: "config/area_registry/list"
});
var y2 = (r13, e6) => r13.subscribeEvents(
  (0, import_lodash4.debounce)(() => n5(r13).then((t9) => e6.setState(t9, true)), 500, {
    leading: true,
    trailing: true
  }),
  "area_registry_updated"
);
var m8 = (r13, e6) => createCollection("_areaRegistry", n5, y2, r13, e6);
var A4 = (r13, e6) => r13.sendMessagePromise({
  type: "config/area_registry/create",
  ...e6
});
var l5 = (r13, e6, t9) => r13.sendMessagePromise({
  type: "config/area_registry/update",
  area_id: e6,
  ...t9
});
var R3 = (r13, e6) => r13.sendMessagePromise({
  type: "config/area_registry/delete",
  area_id: e6
});
var E2 = (r13) => {
  const e6 = {};
  for (const t9 of r13)
    t9.area_id && (t9.area_id in e6 || (e6[t9.area_id] = []), e6[t9.area_id].push(t9));
  return e6;
};
var b3 = (r13) => {
  const e6 = {};
  for (const t9 of r13)
    t9.area_id && (t9.area_id in e6 || (e6[t9.area_id] = []), e6[t9.area_id].push(t9));
  return e6;
};
var x3 = (r13, e6) => (t9, a13) => {
  const i11 = e6 ? e6.indexOf(t9) : -1, s9 = e6 ? e6.indexOf(a13) : -1;
  if (i11 === -1 && s9 === -1) {
    const o13 = r13?.[t9]?.name ?? t9, c15 = r13?.[a13]?.name ?? a13;
    return p3(o13, c15);
  }
  return i11 === -1 ? 1 : s9 === -1 ? -1 : i11 - s9;
};

// node_modules/@hakit/core/dist/es/utils/subscribe/floors.js
var import_lodash5 = __toESM(require_lodash(), 1);
var t6 = (e6) => e6.sendMessagePromise({
  type: "config/floor_registry/list"
});
var l6 = (e6, r13) => e6.subscribeEvents(
  (0, import_lodash5.debounce)(() => t6(e6).then((o13) => r13.setState(o13, true)), 500, {
    leading: true,
    trailing: true
  }),
  "floor_registry_updated"
);
var n6 = (e6, r13) => createCollection("_floorRegistry", t6, l6, e6, r13);

// node_modules/@hakit/core/dist/es/utils/subscribe/user.js
var n7 = async (e6, { includeSystemGenerated: s9 = false, includeInactiveUsers: r13 = false } = {}) => (await e6.sendMessagePromise({ type: "config/auth/list" })).filter((c15) => !(!s9 && c15.system_generated || !r13 && !c15.is_active));
var f6 = (e6, s9, r13) => (n7(e6, r13).then(s9).catch((t9) => {
  console.warn("subscribeUsers: failed to fetch users", t9), s9([]);
}), () => {
});
var u8 = (e6) => getCollection(e6, "_usr", () => getUser(e6), void 0);
var l7 = (e6, s9) => u8(e6).subscribe(s9);

// node_modules/@hakit/core/dist/index-1tDtVGr2.js
var ge = [
  c4.HS,
  c4.XY,
  c4.RGB,
  c4.RGBW,
  c4.RGBWW
];
var pr = [
  ...ge,
  c4.COLOR_TEMP,
  c4.BRIGHTNESS,
  c4.WHITE
];
var Ge = (e6, r13) => e6.attributes.supported_color_modes?.includes(r13) || false;
var In = (e6) => e6.attributes.color_mode && ge.includes(e6.attributes.color_mode) || false;
var Ke = (e6) => e6.attributes.supported_color_modes?.some((r13) => ge.includes(r13)) || false;
var kn = (e6) => e6.attributes.supported_color_modes?.some((r13) => pr.includes(r13)) || false;
var Fn = (e6) => Ke(e6) || Ge(e6, c4.COLOR_TEMP);
var Un = (e6) => e6.attributes.color_mode === c4.RGBWW ? e6.attributes.rgbww_color : e6.attributes.color_mode === c4.RGBW ? e6.attributes.rgbw_color : e6.attributes.rgb_color;
var Q2 = 4;
var yr = [
  { rgb_color: [127, 172, 255] },
  // blue #7FACFF
  { rgb_color: [215, 150, 255] },
  // purple #D796FF
  { rgb_color: [255, 158, 243] },
  // pink #FF9EF3
  { rgb_color: [255, 110, 84] }
  // red #FF6E54
];
var jn = (e6) => {
  const r13 = [], t9 = Ge(e6, c4.COLOR_TEMP), n15 = Ke(e6);
  if (t9) {
    const o13 = e6.attributes.min_color_temp_kelvin, i11 = (e6.attributes.max_color_temp_kelvin - o13) / (Q2 - 1);
    for (let a13 = 0; a13 < Q2; a13++)
      r13.push({
        color_temp_kelvin: Math.round(o13 + i11 * a13)
      });
  } else if (n15) {
    const i11 = 4500 / (Q2 - 1);
    for (let a13 = 0; a13 < Q2; a13++)
      r13.push({
        rgb_color: I(Math.round(2e3 + i11 * a13))
      });
  }
  return n15 && r13.push(...yr), r13;
};
function N5(e6) {
  return typeof e6 == "string" ? new Date(e6) : e6;
}
function _r() {
  const e6 = (g10) => ht(g10), r13 = () => M2.getState();
  return {
    formatDate: (g10) => {
      const u15 = N5(g10), { locale: f13, config: l10 } = r13();
      return !f13 || !l10 ? e6(u15) : at(u15, l10, f13);
    },
    formatTime: (g10) => {
      const u15 = N5(g10), { locale: f13, config: l10 } = r13();
      return !f13 || !l10 ? e6(u15) : X2(u15, l10, f13);
    },
    formatTimeWithoutAmPm: (g10) => {
      const u15 = N5(g10), { locale: f13, config: l10 } = r13();
      return !f13 || !l10 ? `${u15.getHours().toString().padStart(2, "0")}:${u15.getMinutes().toString().padStart(2, "0")}` : it(u15, l10, f13);
    },
    formatHour: (g10) => {
      const u15 = N5(g10), { locale: f13, config: l10 } = r13();
      return !f13 || !l10 ? u15.getHours().toString().padStart(2, "0") : st(u15, l10, f13);
    },
    formatAmPmSuffix: (g10) => {
      const u15 = N5(g10), { locale: f13, config: l10 } = r13();
      return !f13 || !l10 ? u15.getHours() >= 12 ? "PM" : "AM" : pt(u15, f13, l10);
    },
    formatMinute: (g10) => {
      const u15 = N5(g10), { locale: f13, config: l10 } = r13();
      return !f13 || !l10 ? u15.getMinutes().toString().padStart(2, "0") : ut(u15, l10, f13);
    },
    formatSeconds: (g10) => {
      const u15 = N5(g10), { locale: f13, config: l10 } = r13();
      return !f13 || !l10 ? u15.getSeconds().toString().padStart(2, "0") : gt(u15, l10, f13);
    },
    formatDateTime: (g10) => {
      const u15 = N5(g10), { locale: f13, config: l10 } = r13();
      return !f13 || !l10 ? e6(u15) : ct(u15, l10, f13);
    },
    formatDateTimeWithSeconds: (g10) => {
      const u15 = N5(g10), { locale: f13, config: l10 } = r13();
      return !f13 || !l10 ? e6(u15) : ft(u15, f13, l10);
    },
    formatShortDateTime: (g10) => {
      const u15 = N5(g10), { locale: f13, config: l10 } = r13();
      return !f13 || !l10 ? e6(u15) : b2(u15, f13, l10);
    },
    formatShortDateTimeWithYear: (g10) => {
      const u15 = N5(g10), { locale: f13, config: l10 } = r13();
      return !f13 || !l10 ? e6(u15) : j2(u15, f13, l10);
    },
    formatShortDateTimeWithConditionalYear: (g10) => {
      const u15 = N5(g10), { locale: f13, config: l10 } = r13();
      return !f13 || !l10 ? e6(u15) : dt(u15, f13, l10);
    },
    formatDateTimeWithBrowserDefaults: (g10) => ht(N5(g10)),
    formatDateTimeNumeric: (g10) => {
      const u15 = N5(g10), { locale: f13, config: l10 } = r13();
      return !f13 || !l10 ? e6(u15) : yt(u15, f13, l10);
    },
    formatDateWeekdayDay: (g10) => {
      const u15 = N5(g10), { locale: f13, config: l10 } = r13();
      return !f13 || !l10 ? e6(u15) : _t(u15, f13, l10);
    },
    formatDateShort: (g10) => {
      const u15 = N5(g10), { locale: f13, config: l10 } = r13();
      return !f13 || !l10 ? e6(u15) : Dt(u15, f13, l10);
    },
    formatDateVeryShort: (g10) => {
      const u15 = N5(g10), { locale: f13, config: l10 } = r13();
      return !f13 || !l10 ? e6(u15) : lt(u15, f13, l10);
    },
    formatDateMonthYear: (g10) => {
      const u15 = N5(g10), { locale: f13, config: l10 } = r13();
      return !f13 || !l10 ? e6(u15) : Tt(u15, f13, l10);
    },
    formatDateMonth: (g10) => {
      const u15 = N5(g10), { locale: f13, config: l10 } = r13();
      return !f13 || !l10 ? e6(u15) : Mt(u15, f13, l10);
    },
    formatDateYear: (g10) => {
      const u15 = N5(g10), { locale: f13, config: l10 } = r13();
      return !f13 || !l10 ? e6(u15) : zt(u15, f13, l10);
    },
    formatDateWeekday: (g10) => {
      const u15 = N5(g10), { locale: f13, config: l10 } = r13();
      return !f13 || !l10 ? e6(u15) : It(u15, f13, l10);
    },
    formatDateWeekdayShort: (g10) => {
      const u15 = N5(g10), { locale: f13, config: l10 } = r13();
      return !f13 || !l10 ? e6(u15) : Ft(u15, f13, l10);
    },
    formatDateNumeric: (g10) => {
      const u15 = N5(g10), { locale: f13, config: l10 } = r13();
      return !f13 || !l10 ? e6(u15) : wt(u15, f13, l10);
    }
  };
}
async function gr(e6, r13) {
  try {
    const { connection: t9, hassUrl: n15 } = M2.getState(), o13 = await fetch(`${n15}/api${e6}`, {
      method: "GET",
      ...r13 ?? {},
      headers: {
        Authorization: "Bearer " + t9?.options.auth?.accessToken,
        "Content-type": "application/json;charset=UTF-8",
        ...r13?.headers ?? {}
      }
    });
    return o13.status === 200 ? {
      status: "success",
      data: await o13.json()
    } : {
      status: "error",
      data: o13.statusText
    };
  } catch (t9) {
    return console.error("API Error:", t9), {
      status: "error",
      data: `API Request failed for endpoint "${e6}", follow instructions here: https://shannonhochkins.github.io/ha-component-kit/?path=/docs/core-hooks-usehass-hass-callapi--docs.`
    };
  }
}
var hr = (e6, r13) => {
  const { last_changed: t9, last_updated: n15, context: o13, ...s9 } = e6, { last_changed: i11, last_updated: a13, context: p8, ...d12 } = r13;
  return JSON.stringify(s9) === JSON.stringify(d12);
};
var M2 = create((e6, r13) => ({
  sensorNumericDeviceClasses: [],
  setSensorNumericDeviceClasses: (t9) => e6({ sensorNumericDeviceClasses: t9 }),
  locale: null,
  setLocale: (t9) => e6({ locale: t9 }),
  routes: [],
  setRoutes: (t9) => e6(() => ({ routes: t9 })),
  entities: {},
  devices: {},
  setDevices: (t9) => e6(() => ({ devices: t9 })),
  entitiesRegistryDisplay: {},
  setEntitiesRegistryDisplay: (t9) => e6(() => ({ entitiesRegistryDisplay: t9 })),
  areas: {},
  setAreas: (t9) => e6(() => ({ areas: t9 })),
  floors: {},
  services: {},
  setServices: (t9) => e6(() => ({ services: t9 })),
  setFloors: (t9) => e6(() => ({ floors: t9 })),
  setHassUrl: (t9) => e6({ hassUrl: t9 }),
  hassUrl: null,
  hash: "",
  locales: null,
  setLocales: (t9) => e6({ locales: t9 }),
  setHash: (t9) => e6({ hash: t9 }),
  setPortalRoot: (t9) => e6({ portalRoot: t9 }),
  windowContext: window,
  setWindowContext: (t9) => e6({ windowContext: t9 }),
  setEntities: (t9) => e6((n15) => {
    let o13 = false;
    const s9 = { ...n15.entities };
    for (const [i11, a13] of Object.entries(t9)) {
      const p8 = n15.entities[i11];
      if (!p8) {
        s9[i11] = a13, o13 = true;
        continue;
      }
      hr(p8, a13) || (s9[i11] = a13, o13 = true);
    }
    return o13 ? { entities: s9, lastUpdated: Date.now(), ready: true } : n15;
  }),
  connectionStatus: "pending",
  setConnectionStatus: (t9) => e6({ connectionStatus: t9 }),
  connection: null,
  setConnection: (t9) => e6({ connection: t9 }),
  cannotConnect: false,
  setCannotConnect: (t9) => e6({ cannotConnect: t9 }),
  ready: false,
  setReady: (t9) => e6({ ready: t9 }),
  auth: null,
  setAuth: (t9) => e6({ auth: t9 }),
  config: null,
  setConfig: (t9) => e6({ config: t9 }),
  user: null,
  setUser: (t9) => e6({ user: t9 }),
  users: [],
  setUsers: (t9) => e6({ users: t9 }),
  error: null,
  setError: (t9) => e6({ error: t9 }),
  globalComponentStyles: {},
  setGlobalComponentStyles: (t9) => e6(() => ({ globalComponentStyles: t9 })),
  disconnectCallbacks: [],
  onDisconnect: (t9) => e6((n15) => ({ disconnectCallbacks: [...n15.disconnectCallbacks, t9] })),
  triggerOnDisconnect: () => e6((t9) => (t9.disconnectCallbacks.forEach((n15) => n15()), { disconnectCallbacks: [] })),
  helpers: {
    logout() {
      const { reset: t9 } = B3.getState(), { setError: n15 } = r13();
      try {
        t9(), l2(), location && location.reload();
      } catch (o13) {
        console.error("Error:", o13), n15("Unable to log out!");
      }
    },
    callService: ((t9) => {
      const { domain: n15, service: o13, serviceData: s9, target: i11, returnResponse: a13 } = t9, { connection: p8, ready: d12 } = r13(), y6 = typeof i11 == "string" || (0, import_lodash6.isArray)(i11) ? { entity_id: i11 } : i11;
      if (!p8 || !d12)
        return a13 ? Promise.reject(new Error("callService: connection not established or not ready")) : void 0;
      try {
        const _8 = callService2(p8, (0, import_lodash6.snakeCase)(n15), (0, import_lodash6.snakeCase)(o13), s9 ?? {}, y6, a13);
        return a13 ? _8 : void 0;
      } catch (_8) {
        return console.error("Error calling service:", _8), a13 ? Promise.reject(_8) : void 0;
      }
    }),
    addRoute(t9) {
      const { routes: n15, setRoutes: o13 } = r13();
      if (!n15.find((i11) => i11.hash === t9.hash)) {
        const i11 = typeof window < "u" ? window.location.hash.replace("#", "") : "", a13 = i11 !== "" && i11 === t9.hash;
        o13([...n15, { ...t9, active: a13 }]);
      }
    },
    getRoute(t9) {
      const { routes: n15 } = r13();
      return n15.find((o13) => o13.hash === t9) || null;
    },
    getAllEntities() {
      return r13().entities;
    },
    joinHassUrl(t9) {
      const { connection: n15 } = r13();
      return n15 ? new URL(t9, n15.options.auth?.data.hassUrl).toString() : "";
    },
    callApi: gr,
    dateTime: {
      shouldUseAmPm: () => {
        const { locale: t9 } = r13();
        return t9 ? i3(t9) : true;
      },
      getTimeZone() {
        const { locale: t9, config: n15 } = r13();
        return !n15 || !t9 ? "UTC" : r4(t9.time_zone, n15.time_zone);
      }
    }
  },
  formatter: {
    stateValue: (t9) => {
      const { config: n15, entitiesRegistryDisplay: o13, locale: s9, sensorNumericDeviceClasses: i11 } = r13();
      return !n15 || !s9 ? "" : rn(t9, n15, o13, s9, i11, t9.state);
    },
    attributeValue: (t9, n15) => {
      const { config: o13, entitiesRegistryDisplay: s9, locale: i11 } = r13();
      return !o13 || !i11 ? "" : ot2(t9, i11, o13, s9, n15);
    },
    ..._r()
  }
}));
var B3 = create((e6, r13) => ({
  authenticated: false,
  setAuthenticated: (t9) => e6({ authenticated: t9 }),
  subscriptions: {},
  addSubscription: (t9, n15) => {
    if (!n15) return;
    const o13 = r13().subscriptions;
    if (o13[t9])
      try {
        o13[t9]();
      } catch (s9) {
        console.warn(`Failed to unsubscribe previous subscription for key '${t9}'`, s9);
      }
    e6({ subscriptions: { ...o13, [t9]: n15 } });
  },
  removeSubscription: (t9) => {
    const n15 = r13().subscriptions;
    if (!n15[t9]) return;
    try {
      n15[t9]();
    } catch (s9) {
      console.warn(`Failed to unsubscribe subscription for key '${t9}'`, s9);
    }
    const o13 = { ...n15 };
    delete o13[t9], e6({ subscriptions: o13 });
  },
  unsubscribeAll: () => {
    const t9 = r13().subscriptions;
    for (const n15 of Object.keys(t9))
      try {
        t9[n15]();
      } catch (o13) {
        console.warn(`Failed during mass unsubscribe for key '${n15}'`, o13);
      }
    e6({ subscriptions: {} });
  },
  reset() {
    const { unsubscribeAll: t9, setAuthenticated: n15 } = r13(), {
      setAuth: o13,
      setUser: s9,
      setCannotConnect: i11,
      setConfig: a13,
      setConnection: p8,
      setEntities: d12,
      setError: y6,
      setReady: _8,
      setRoutes: P3,
      setConnectionStatus: x6
    } = M2.getState();
    o13(null), P3([]), _8(false), p8(null), d12({}), a13(null), y6(null), i11(false), s9(null), x6("pending"), n15(false), t9();
  }
}));
var Hn = M2;
var ne = M2;
var V2 = {};
function vr(e6) {
  Object.assign(V2, e6);
}
function j3(e6, r13) {
  const { search: t9, replace: n15, fallback: o13 } = r13 ?? {};
  return V2[e6] ? typeof t9 == "string" && typeof n15 == "string" ? V2[e6].replace(`${t9}`, n15).trim() : V2[e6] : o13 || e6;
}
function zn() {
  return V2;
}
var Bn = (e6, r13) => {
  const { fallback: t9 = j3("unknown") } = r13 ?? {}, [n15, o13] = (0, import_react8.useState)(t9), s9 = ne((a13) => a13.config), i11 = ne((a13) => a13.locale);
  return (0, import_react8.useEffect)(() => {
    (async () => {
      const p8 = s9?.language, d12 = m3.find((y6) => y6.code === i11?.language || y6.code === p8);
      if (d12) {
        const y6 = await d12.fetch();
        o13(y6[e6] ?? t9);
      }
    })();
  }, [e6, t9, i11, s9]), n15;
};
var Sr = [{ type: "device" }, { type: "entity" }];
var br = " ";
function Vn(e6) {
  const r13 = m5(e6.entity_id), t9 = e6.state;
  if (["button", "event", "input_button", "scene"].includes(r13))
    return t9 !== n4;
  if (_2(t9) || t9 === N3 && r13 !== "alert")
    return false;
  switch (r13) {
    case "alarm_control_panel":
      return t9 !== "disarmed";
    case "alert":
      return t9 !== "idle";
    case "cover":
      return t9 !== "closed";
    case "device_tracker":
    case "person":
      return t9 !== "not_home";
    case "lawn_mower":
      return ["mowing", "error"].includes(t9);
    case "lock":
      return t9 !== "locked";
    case "media_player":
      return t9 !== "standby";
    case "vacuum":
      return !["idle", "docked", "paused"].includes(t9);
    case "plant":
      return t9 === "problem";
    case "group":
      return ["on", "home", "open", "locked", "problem"].includes(t9);
    case "timer":
      return t9 === "active";
    case "camera":
      return t9 === "streaming";
  }
  return true;
}
var Er = (e6) => e6.substr(e6.indexOf(".") + 1);
var Dr = (e6, r13) => r13?.friendly_name === void 0 ? Er(e6).replace(/_/g, " ") : r13.friendly_name || "";
var G2 = (e6) => Dr(e6.entity_id, e6.attributes);
var Tr = (e6, r13, t9, n15, o13) => {
  const s9 = r13[e6.entity_id];
  return s9 ? Cr(s9, r13, t9, n15, o13) : {
    entity: null,
    device: null,
    area: null,
    floor: null
  };
};
var Cr = (e6, r13, t9, n15, o13) => {
  const s9 = r13[e6.entity_id], i11 = e6?.device_id, a13 = i11 ? t9[i11] : void 0, p8 = e6?.area_id || a13?.area_id, d12 = p8 ? n15[p8] : void 0, y6 = d12?.floor_id, _8 = y6 ? o13[y6] : void 0;
  return {
    entity: s9,
    device: a13 || null,
    area: d12 || null,
    floor: _8 || null
  };
};
var Rr = (e6, r13, t9) => he(e6) || t9 && qr(r13, t9) || j3("unnamed_device");
var he = (e6) => (e6.name_by_user || e6.name)?.trim();
var Je = (e6, r13, t9) => {
  const n15 = r13[e6.entity_id];
  return n15 ? wr(n15, t9) : G2(e6);
};
var wr = (e6, r13, t9) => {
  const n15 = e6.name || ("original_name" in e6 && e6.original_name != null ? String(e6.original_name) : void 0), o13 = e6.device_id ? r13[e6.device_id] : void 0;
  if (!o13)
    return n15 || (t9 ? G2(t9) : void 0);
  const s9 = he(o13);
  if (s9 !== n15)
    return s9 && n15 && c7(n15, s9) || n15;
};
var Pr = (e6) => e6.name?.trim();
var Xe = (e6) => e6.name?.trim();
var Ze = (e6, r13, t9, n15, o13, s9) => {
  const { device: i11, area: a13, floor: p8 } = Tr(e6, t9, n15, o13, s9);
  return r13.map((y6) => {
    switch (y6.type) {
      case "entity":
        return Je(e6, t9, n15);
      case "device":
        return i11 ? he(i11) : void 0;
      case "area":
        return a13 ? Xe(a13) : void 0;
      case "floor":
        return p8 ? Pr(p8) : void 0;
      case "text":
        return y6.text;
      default:
        return "";
    }
  });
};
var Ar = (e6, r13, t9) => !Je(e6, r13, t9);
var qn = (e6, r13, t9, n15, o13, s9, i11) => {
  let a13 = Array.isArray(r13) ? r13 : r13 ? [r13] : Sr;
  const p8 = i11?.separator ?? br;
  if (a13.every((_8) => _8.type === "text"))
    return a13.map((_8) => _8.text).join(p8);
  Ar(e6, t9, n15) && (a13.some((P3) => P3.type === "device") || (a13 = a13.map((P3) => P3.type === "entity" ? { type: "device" } : P3)));
  const y6 = Ze(e6, a13, t9, n15, o13, s9);
  return y6.length === 1 ? y6[0] || "" : y6.filter((_8) => _8).join(p8);
};
var ee = { exports: {} };
var C2 = {};
var we;
function xr() {
  return we || (we = 1, (function() {
    var e6 = typeof Symbol == "function" && Symbol.for, r13 = e6 ? Symbol.for("react.element") : 60103, t9 = e6 ? Symbol.for("react.portal") : 60106, n15 = e6 ? Symbol.for("react.fragment") : 60107, o13 = e6 ? Symbol.for("react.strict_mode") : 60108, s9 = e6 ? Symbol.for("react.profiler") : 60114, i11 = e6 ? Symbol.for("react.provider") : 60109, a13 = e6 ? Symbol.for("react.context") : 60110, p8 = e6 ? Symbol.for("react.async_mode") : 60111, d12 = e6 ? Symbol.for("react.concurrent_mode") : 60111, y6 = e6 ? Symbol.for("react.forward_ref") : 60112, _8 = e6 ? Symbol.for("react.suspense") : 60113, P3 = e6 ? Symbol.for("react.suspense_list") : 60120, x6 = e6 ? Symbol.for("react.memo") : 60115, b11 = e6 ? Symbol.for("react.lazy") : 60116, w3 = e6 ? Symbol.for("react.block") : 60121, m15 = e6 ? Symbol.for("react.fundamental") : 60117, E7 = e6 ? Symbol.for("react.responder") : 60118, $4 = e6 ? Symbol.for("react.scope") : 60119;
    function v10(h5) {
      return typeof h5 == "string" || typeof h5 == "function" || // Note: its typeof might be other than 'symbol' or 'number' if it's a polyfill.
      h5 === n15 || h5 === d12 || h5 === s9 || h5 === o13 || h5 === _8 || h5 === P3 || typeof h5 == "object" && h5 !== null && (h5.$$typeof === b11 || h5.$$typeof === x6 || h5.$$typeof === i11 || h5.$$typeof === a13 || h5.$$typeof === y6 || h5.$$typeof === m15 || h5.$$typeof === E7 || h5.$$typeof === $4 || h5.$$typeof === w3);
    }
    function S6(h5) {
      if (typeof h5 == "object" && h5 !== null) {
        var ue = h5.$$typeof;
        switch (ue) {
          case r13:
            var Z3 = h5.type;
            switch (Z3) {
              case p8:
              case d12:
              case n15:
              case s9:
              case o13:
              case _8:
                return Z3;
              default:
                var Se = Z3 && Z3.$$typeof;
                switch (Se) {
                  case a13:
                  case y6:
                  case b11:
                  case x6:
                  case i11:
                    return Se;
                  default:
                    return ue;
                }
            }
          case t9:
            return ue;
        }
      }
    }
    var c15 = p8, R5 = d12, L3 = a13, I7 = i11, g10 = r13, u15 = y6, f13 = n15, l10 = b11, ie2 = x6, K4 = t9, se2 = s9, z5 = o13, W3 = _8, J2 = false;
    function ae2(h5) {
      return J2 || (J2 = true, console.warn("The ReactIs.isAsyncMode() alias has been deprecated, and will be removed in React 17+. Update your code to use ReactIs.isConcurrentMode() instead. It has the exact same API.")), X3(h5) || S6(h5) === p8;
    }
    function X3(h5) {
      return S6(h5) === d12;
    }
    function ce(h5) {
      return S6(h5) === a13;
    }
    function D6(h5) {
      return S6(h5) === i11;
    }
    function O5(h5) {
      return typeof h5 == "object" && h5 !== null && h5.$$typeof === r13;
    }
    function A7(h5) {
      return S6(h5) === y6;
    }
    function it2(h5) {
      return S6(h5) === n15;
    }
    function st2(h5) {
      return S6(h5) === b11;
    }
    function at2(h5) {
      return S6(h5) === x6;
    }
    function ct2(h5) {
      return S6(h5) === t9;
    }
    function ut2(h5) {
      return S6(h5) === s9;
    }
    function ft2(h5) {
      return S6(h5) === o13;
    }
    function lt2(h5) {
      return S6(h5) === _8;
    }
    C2.AsyncMode = c15, C2.ConcurrentMode = R5, C2.ContextConsumer = L3, C2.ContextProvider = I7, C2.Element = g10, C2.ForwardRef = u15, C2.Fragment = f13, C2.Lazy = l10, C2.Memo = ie2, C2.Portal = K4, C2.Profiler = se2, C2.StrictMode = z5, C2.Suspense = W3, C2.isAsyncMode = ae2, C2.isConcurrentMode = X3, C2.isContextConsumer = ce, C2.isContextProvider = D6, C2.isElement = O5, C2.isForwardRef = A7, C2.isFragment = it2, C2.isLazy = st2, C2.isMemo = at2, C2.isPortal = ct2, C2.isProfiler = ut2, C2.isStrictMode = ft2, C2.isSuspense = lt2, C2.isValidElementType = v10, C2.typeOf = S6;
  })()), C2;
}
var Pe;
function Wr() {
  return Pe || (Pe = 1, false ? ee.exports = Nr() : ee.exports = xr()), ee.exports;
}
var fe;
var Ae;
function Or() {
  if (Ae) return fe;
  Ae = 1;
  var e6 = Wr(), r13 = {
    childContextTypes: true,
    contextType: true,
    contextTypes: true,
    defaultProps: true,
    displayName: true,
    getDefaultProps: true,
    getDerivedStateFromError: true,
    getDerivedStateFromProps: true,
    mixins: true,
    propTypes: true,
    type: true
  }, t9 = {
    name: true,
    length: true,
    prototype: true,
    caller: true,
    callee: true,
    arguments: true,
    arity: true
  }, n15 = {
    $$typeof: true,
    render: true,
    defaultProps: true,
    displayName: true,
    propTypes: true
  }, o13 = {
    $$typeof: true,
    compare: true,
    defaultProps: true,
    displayName: true,
    propTypes: true,
    type: true
  }, s9 = {};
  s9[e6.ForwardRef] = n15, s9[e6.Memo] = o13;
  function i11(b11) {
    return e6.isMemo(b11) ? o13 : s9[b11.$$typeof] || r13;
  }
  var a13 = Object.defineProperty, p8 = Object.getOwnPropertyNames, d12 = Object.getOwnPropertySymbols, y6 = Object.getOwnPropertyDescriptor, _8 = Object.getPrototypeOf, P3 = Object.prototype;
  function x6(b11, w3, m15) {
    if (typeof w3 != "string") {
      if (P3) {
        var E7 = _8(w3);
        E7 && E7 !== P3 && x6(b11, E7, m15);
      }
      var $4 = p8(w3);
      d12 && ($4 = $4.concat(d12(w3)));
      for (var v10 = i11(b11), S6 = i11(w3), c15 = 0; c15 < $4.length; ++c15) {
        var R5 = $4[c15];
        if (!t9[R5] && !(m15 && m15[R5]) && !(S6 && S6[R5]) && !(v10 && v10[R5])) {
          var L3 = y6(w3, R5);
          try {
            a13(b11, R5, L3);
          } catch {
          }
        }
      }
    }
    return b11;
  }
  return fe = x6, fe;
}
Or();
var Mr = function(r13) {
  return r13();
};
var $r = H2.useInsertionEffect ? H2.useInsertionEffect : false;
var Lr = $r || Mr;
var Qe = H2.createContext(
  // we're doing this to avoid preconstruct's dead code elimination in this one case
  // because this module is primarily intended for the browser and node
  // but it's also required in react native and similar environments sometimes
  // and we could have a special build just for that
  // but this is much easier and the native packages
  // might use a different theme context in the future anyway
  typeof HTMLElement < "u" ? createCache({
    key: "css"
  }) : null
);
Qe.Provider;
var Ir = function(r13) {
  return (0, import_react8.forwardRef)(function(t9, n15) {
    var o13 = (0, import_react8.useContext)(Qe);
    return r13(t9, o13, n15);
  });
};
var kr = H2.createContext({});
var oe = {}.hasOwnProperty;
var ye = "__EMOTION_TYPE_PLEASE_DO_NOT_USE__";
var et = function(r13, t9) {
  var n15 = {};
  for (var o13 in t9)
    oe.call(t9, o13) && (n15[o13] = t9[o13]);
  return n15[ye] = r13, n15;
};
var Fr = function(r13) {
  var t9 = r13.cache, n15 = r13.serialized, o13 = r13.isStringTag;
  return registerStyles(t9, n15, o13), Lr(function() {
    return insertStyles(t9, n15, o13);
  }), null;
};
var Ur = Ir(function(e6, r13, t9) {
  var n15 = e6.css;
  typeof n15 == "string" && r13.registered[n15] !== void 0 && (n15 = r13.registered[n15]);
  var o13 = e6[ye], s9 = [n15], i11 = "";
  typeof e6.className == "string" ? i11 = getRegisteredStyles(r13.registered, s9, e6.className) : e6.className != null && (i11 = e6.className + " ");
  var a13 = serializeStyles(s9, void 0, H2.useContext(kr));
  i11 += r13.key + "-" + a13.name;
  var p8 = {};
  for (var d12 in e6)
    oe.call(e6, d12) && d12 !== "css" && d12 !== ye && (p8[d12] = e6[d12]);
  return p8.className = i11, t9 && (p8.ref = t9), H2.createElement(H2.Fragment, null, H2.createElement(Fr, {
    cache: r13,
    serialized: a13,
    isStringTag: typeof o13 == "string"
  }), H2.createElement(o13, p8));
});
var tt = Ur;
var Ne = q2.Fragment;
var k4 = function(r13, t9, n15) {
  return oe.call(t9, "css") ? q2.jsx(tt, et(r13, t9), n15) : q2.jsx(r13, t9, n15);
};
var ve = function(r13, t9, n15) {
  return oe.call(t9, "css") ? q2.jsxs(tt, et(r13, t9), n15) : q2.jsxs(r13, t9, n15);
};
var xe = (e6) => Symbol.iterator in e6;
var We = (e6) => (
  // HACK: avoid checking entries type
  "entries" in e6
);
var Oe = (e6, r13) => {
  const t9 = e6 instanceof Map ? e6 : new Map(e6.entries()), n15 = r13 instanceof Map ? r13 : new Map(r13.entries());
  if (t9.size !== n15.size)
    return false;
  for (const [o13, s9] of t9)
    if (!n15.has(o13) || !Object.is(s9, n15.get(o13)))
      return false;
  return true;
};
var jr = (e6, r13) => {
  const t9 = e6[Symbol.iterator](), n15 = r13[Symbol.iterator]();
  let o13 = t9.next(), s9 = n15.next();
  for (; !o13.done && !s9.done; ) {
    if (!Object.is(o13.value, s9.value))
      return false;
    o13 = t9.next(), s9 = n15.next();
  }
  return !!o13.done && !!s9.done;
};
function Yr(e6, r13) {
  return Object.is(e6, r13) ? true : typeof e6 != "object" || e6 === null || typeof r13 != "object" || r13 === null || Object.getPrototypeOf(e6) !== Object.getPrototypeOf(r13) ? false : xe(e6) && xe(r13) ? We(e6) && We(r13) ? Oe(e6, r13) : jr(e6, r13) : Oe(
    { entries: () => Object.entries(e6) },
    { entries: () => Object.entries(r13) }
  );
}
function Hr(e6) {
  const r13 = import_react8.default.useRef(void 0);
  return (t9) => {
    const n15 = e6(t9);
    return Yr(r13.current, n15) ? r13.current : r13.current = n15;
  };
}
function zr() {
  const e6 = ne((r13) => r13.config);
  return (0, import_react8.useMemo)(() => e6, [e6]);
}
var rt2 = (e6) => e6.sendMessagePromise({ type: "config/device_registry/list" });
var Br = (e6, r13) => e6.subscribeEvents(
  (0, import_lodash6.debounce)(() => rt2(e6).then((t9) => r13.setState(t9, true)), 500, { leading: true, trailing: true }),
  "device_registry_updated"
);
var Vr = (e6, r13) => createCollection("_dr", rt2, Br, e6, r13);
var qr = (e6, r13) => {
  for (const t9 of r13 || []) {
    const n15 = typeof t9 == "string" ? t9 : t9.entity_id, o13 = e6[n15];
    if (o13)
      return G2(o13);
  }
};
var Gn = (e6, r13) => e6.filter((t9) => t9.area_id === r13);
var Kn = (e6, r13, t9) => e6.sendMessagePromise({ type: "config/device_registry/update", device_id: r13, ...t9 });
var Jn = (e6, r13, t9) => e6.sendMessagePromise({
  type: "config/device_registry/remove_config_entry",
  device_id: r13,
  config_entry_id: t9
});
var Xn = (e6, r13) => e6.sort((t9, n15) => u7(t9.name || "", n15.name || "", r13));
var Zn = (e6) => {
  const r13 = {};
  for (const t9 of e6)
    t9.device_id && (t9.device_id in r13 || (r13[t9.device_id] = []), r13[t9.device_id].push(t9));
  return r13;
};
var Gr = (e6) => {
  const r13 = {};
  for (const t9 of e6)
    t9.device_id && (t9.device_id in r13 || (r13[t9.device_id] = []), r13[t9.device_id].push(t9));
  return r13;
};
var Qn = (e6, r13, t9, n15) => {
  const o13 = {};
  for (const s9 of r13) {
    const i11 = e6[s9.entity_id];
    !i11?.domain || s9.device_id === null || (o13[s9.device_id] = o13[s9.device_id] || /* @__PURE__ */ new Set(), o13[s9.device_id].add(i11.domain));
  }
  if (t9 && n15)
    for (const s9 of t9)
      for (const i11 of s9.config_entries) {
        const a13 = n15.find((p8) => p8.entry_id === i11);
        a13?.domain && (o13[s9.id] = o13[s9.id] || /* @__PURE__ */ new Set(), o13[s9.id].add(a13.domain));
      }
  return o13;
};
var Kr = (e6, r13, t9) => {
  const n15 = e6.area_id, o13 = n15 ? r13[n15] : void 0, s9 = o13?.floor_id, i11 = s9 ? t9[s9] : void 0;
  return {
    device: e6,
    area: o13 || null,
    floor: i11 || null
  };
};
var eo = (e6, r13, t9, n15, o13, s9, i11, a13, p8, d12, y6, _8, P3, x6 = "") => {
  const b11 = Object.values(o13), w3 = Object.values(n15);
  let m15 = {};
  (i11 || a13 || p8 || y6) && (m15 = Gr(w3));
  let E7 = b11.filter((v10) => v10.id === P3 || !v10.disabled_by);
  return i11 && (E7 = E7.filter((v10) => {
    const S6 = m15[v10.id];
    return !S6 || !S6.length ? false : m15[v10.id].some((c15) => i11.includes(m5(c15.entity_id)));
  })), a13 && (E7 = E7.filter((v10) => {
    const S6 = m15[v10.id];
    return !S6 || !S6.length ? true : w3.every((c15) => !a13.includes(m5(c15.entity_id)));
  })), _8 && (E7 = E7.filter((v10) => !_8.includes(v10.id))), p8 && (E7 = E7.filter((v10) => {
    const S6 = m15[v10.id];
    return !S6 || !S6.length ? false : m15[v10.id].some((c15) => {
      const R5 = e6[c15.entity_id];
      return R5 ? R5.attributes.device_class && p8.includes(R5.attributes.device_class) : false;
    });
  })), y6 && (E7 = E7.filter((v10) => {
    const S6 = m15[v10.id];
    return !S6 || !S6.length ? false : S6.some((c15) => {
      const R5 = e6[c15.entity_id];
      return R5 ? y6(R5) : false;
    });
  })), d12 && (E7 = E7.filter(
    (v10) => (
      // We always want to include the device of the current value
      v10.id === P3 || d12(v10)
    )
  )), E7.map((v10) => {
    const S6 = Rr(v10, e6, m15[v10.id]), { area: c15 } = Kr(v10, r13, t9), R5 = c15 ? Xe(c15) : void 0, L3 = v10.primary_config_entry ? s9?.[v10.primary_config_entry] : void 0, I7 = L3?.domain, g10 = I7 ? j3(`${I7}.title`, {
      fallback: I7
    }) : void 0;
    return {
      id: `${x6}${v10.id}`,
      label: "",
      primary: S6 || j3("unnamed_device"),
      secondary: R5,
      domain: L3?.domain,
      domain_name: g10,
      search_labels: [S6, R5, I7, g10].filter(Boolean),
      sorting_label: S6 || "zzz"
    };
  });
};
var le = ["sensor", "binary_sensor"];
var to = (e6, r13) => {
  const t9 = r13.filter(
    (n15) => e6[n15.entity_id] && e6[n15.entity_id].attributes.device_class === "battery" && le.includes(m5(n15.entity_id))
  ).sort(
    (n15, o13) => le.indexOf(m5(n15.entity_id)) - le.indexOf(m5(o13.entity_id))
  );
  if (t9.length > 0)
    return t9[0];
};
var ro = (e6, r13) => r13.find(
  (t9) => e6[t9.entity_id] && e6[t9.entity_id].attributes.device_class === "battery_charging"
);
var no = (e6, r13) => {
  if (r13.name)
    return r13.name;
  const t9 = e6[r13.entity_id];
  return t9 ? G2(t9) : r13.original_name ? r13.original_name : r13.entity_id;
};
var oo = (e6, r13) => e6.sendMessagePromise({
  type: "config/entity_registry/get",
  entity_id: r13
});
var io = (e6, r13) => e6.sendMessagePromise({
  type: "config/entity_registry/get_entries",
  entity_ids: r13
});
var so = (e6, r13, t9) => e6.sendMessagePromise({
  type: "config/entity_registry/update",
  entity_id: r13,
  ...t9
});
var ao = (e6, r13) => e6.sendMessagePromise({
  type: "config/entity_registry/remove",
  entity_id: r13
});
var nt2 = (e6) => e6.sendMessagePromise({
  type: "config/entity_registry/list_for_display"
});
var Jr = (e6, r13) => e6.subscribeEvents(
  (0, import_lodash6.debounce)(() => nt2(e6).then((t9) => r13.setState(t9, true)), 500, {
    leading: true,
    trailing: true
  }),
  "entity_registry_updated"
);
var Xr = (e6, r13) => createCollection(
  "_entityRegistryDisplay",
  nt2,
  Jr,
  e6,
  r13
);
var co = (e6, r13) => e6.sort((t9, n15) => u7(t9.name || "", n15.name || "", r13));
var uo = (e6) => {
  const r13 = {};
  for (const t9 of e6)
    r13[t9.entity_id] = t9;
  return r13;
};
var fo = (e6) => {
  const r13 = {};
  for (const t9 of e6)
    r13[t9.id] = t9;
  return r13;
};
var lo = (e6) => {
  const r13 = {};
  for (const t9 of e6)
    t9.platform && (r13[t9.entity_id] = t9.platform);
  return r13;
};
var mo = (e6, r13) => e6.sendMessagePromise({
  type: "config/entity_registry/get_automatic_entity_ids",
  entity_ids: r13
});
var po = (e6, r13, t9, n15, o13, s9, i11, a13, p8, d12, y6, _8, P3, x6 = "") => {
  let b11 = [], w3 = Object.keys(e6);
  return y6 && (w3 = w3.filter((m15) => y6.includes(m15))), _8 && (w3 = w3.filter((m15) => !_8.includes(m15))), s9 && (w3 = w3.filter((m15) => s9.includes(m5(m15)))), i11 && (w3 = w3.filter((m15) => !i11.includes(m5(m15)))), b11 = w3.map((m15) => {
    const E7 = e6[m15], $4 = G2(E7), [v10, S6, c15] = Ze(
      E7,
      [{ type: "entity" }, { type: "device" }, { type: "area" }],
      r13,
      t9,
      n15,
      o13
    ), R5 = m5(m15), L3 = j3(`${R5}.title`, {
      fallback: j3(R5)
    }), I7 = v10 || S6 || m15, g10 = [c15, v10 ? S6 : void 0].filter(Boolean).join(" ▸ ");
    return {
      id: `${x6}${m15}`,
      primary: I7,
      secondary: g10,
      domain_name: L3,
      sorting_label: [S6, v10].filter(Boolean).join("_"),
      search_labels: [v10, S6, c15, L3, $4, m15].filter(Boolean),
      stateObj: E7
    };
  }), p8 && (b11 = b11.filter(
    (m15) => (
      // We always want to include the entity of the current value
      m15.id === P3 || m15.stateObj?.attributes.device_class && p8.includes(m15.stateObj.attributes.device_class)
    )
  )), d12 && (b11 = b11.filter(
    (m15) => (
      // We always want to include the entity of the current value
      m15.id === P3 || m15.stateObj?.attributes.unit_of_measurement && d12.includes(m15.stateObj.attributes.unit_of_measurement)
    )
  )), a13 && (b11 = b11.filter(
    (m15) => (
      // We always want to include the entity of the current value
      m15.id === P3 || m15.stateObj && a13(m15.stateObj)
    )
  )), b11;
};
var Zr = /* @__PURE__ */ new Set([
  "temperature",
  "current_temperature",
  "target_temperature",
  "target_temp_temp",
  "target_temp_high",
  "target_temp_low",
  "target_temp_step",
  "min_temp",
  "max_temp"
]);
var Qr = {
  light: {
    brightness: (e6) => Math.round(e6 / 255 * 100).toString()
  },
  media_player: {
    volume_level: (e6) => Math.round(e6 * 100).toString(),
    media_duration: (e6) => nt(e6.toString(), "s")
  }
};
var en = {
  climate: {
    humidity: "%",
    current_humidity: "%",
    target_humidity_low: "%",
    target_humidity_high: "%",
    target_humidity_step: "%",
    min_humidity: "%",
    max_humidity: "%"
  },
  cover: {
    current_position: "%",
    current_tilt_position: "%"
  },
  fan: {
    percentage: "%"
  },
  humidifier: {
    humidity: "%",
    current_humidity: "%",
    min_humidity: "%",
    max_humidity: "%"
  },
  light: {
    color_temp: "mired",
    max_mireds: "mired",
    min_mireds: "mired",
    color_temp_kelvin: "K",
    min_color_temp_kelvin: "K",
    max_color_temp_kelvin: "K",
    brightness: "%"
  },
  sun: {
    azimuth: "°",
    elevation: "°"
  },
  vacuum: {
    battery_level: "%"
  },
  valve: {
    current_position: "%"
  },
  sensor: {
    battery_level: "%"
  },
  media_player: {
    volume_level: "%"
  }
};
var tn = (e6, r13, t9) => {
  const n15 = e6.unit_system.length || "";
  switch (t9) {
    case "visibility":
      return r13.attributes.visibility_unit || n15;
    case "precipitation":
      return r13.attributes.precipitation_unit || (n15 === "km" ? "mm" : "in");
    case "pressure":
      return r13.attributes.pressure_unit || (n15 === "km" ? "hPa" : "inHg");
    case "temperature":
    case "templow":
      return r13.attributes.temperature_unit || e6.unit_system.temperature;
    case "wind_speed":
      return r13.attributes.wind_speed_unit || `${n15}/h`;
    case "humidity":
    case "precipitation_probability":
      return "%";
    default: {
      const o13 = e6.unit_system;
      return t9 in o13 ? o13[t9] : "";
    }
  }
};
var ot2 = (e6, r13, t9, n15, o13, s9) => {
  const i11 = s9 !== void 0 ? s9 : e6.attributes[o13];
  if (i11 == null)
    return j3("unknown");
  if (typeof i11 == "number") {
    const a13 = m5(e6.entity_id), p8 = Qr[a13]?.[o13], d12 = p8 ? p8(i11) : c5(i11);
    let _8 = en[a13]?.[o13];
    return a13 === "weather" ? _8 = tn(t9, e6, o13) : Zr.has(o13) && (_8 = t9.unit_system.temperature), _8 ? `${d12}${a5(_8, r13)}${_8}` : d12;
  }
  if (typeof i11 == "string" && rt(i11, true)) {
    if (ot(i11)) {
      const p8 = new Date(i11);
      if (mt(p8))
        return ft(p8, r13, t9);
    }
    const a13 = new Date(i11);
    if (mt(a13))
      return at(a13, t9, r13);
  }
  return Array.isArray(i11) && i11.some((a13) => a13 instanceof Object) || !Array.isArray(i11) && i11 instanceof Object ? JSON.stringify(i11) : Array.isArray(i11) ? i11.map((a13) => ot2(e6, r13, t9, n15, o13, a13)).join(", ") : j3(i11);
};
var rn = (e6, r13, t9, n15, o13, s9) => {
  const i11 = t9?.[e6.entity_id];
  return nn(
    n15,
    o13,
    r13,
    i11,
    e6.entity_id,
    e6.attributes,
    s9 !== void 0 ? s9 : e6.state
  );
};
var nn = (e6, r13, t9, n15, o13, s9, i11) => {
  if (i11 === t5 || i11 === n4)
    return j3(i11);
  const a13 = m5(o13), p8 = a13 === "counter" || a13 === "number" || a13 === "input_number";
  if (m6(s9, a13 === "sensor" ? r13 : []) || p8) {
    const d12 = s9.unit_of_measurement;
    if (s9.device_class === "duration" && s9.unit_of_measurement && $2[d12] && n15?.display_precision === void 0)
      try {
        return nt(i11, d12);
      } catch {
      }
    if (s9.device_class === "monetary")
      try {
        return c5(i11, {
          style: "currency",
          currency: s9.unit_of_measurement,
          minimumFractionDigits: 2,
          // Override monetary options with number format
          ...u6({ state: i11, attributes: s9 }, n15)
        });
      } catch {
      }
    const y6 = c5(i11, u6({ state: i11, attributes: s9 }, n15)), _8 = s9.unit_of_measurement;
    return _8 ? `${y6}${_8}` : y6;
  }
  if (["date", "input_datetime", "time"].includes(a13))
    try {
      const d12 = i11.split(" ");
      if (d12.length === 2)
        return e6 ? ct(new Date(d12.join("T")), t9, e6) : new Date(d12.join("T")).toLocaleString();
      if (d12.length === 1) {
        if (i11.includes("-"))
          return e6 ? at(/* @__PURE__ */ new Date(`${i11}T00:00`), t9, e6) : (/* @__PURE__ */ new Date(`${i11}T00:00`)).toLocaleDateString();
        if (i11.includes(":")) {
          const y6 = /* @__PURE__ */ new Date();
          return e6 ? X2(/* @__PURE__ */ new Date(`${y6.toISOString().split("T")[0]}T${i11}`), t9, e6) : (/* @__PURE__ */ new Date(`${y6.toISOString().split("T")[0]}T${i11}`)).toLocaleTimeString();
        }
      }
      return i11;
    } catch {
      return i11;
    }
  if ([
    "ai_task",
    "button",
    "conversation",
    "event",
    "image",
    "input_button",
    "notify",
    "scene",
    "stt",
    "tag",
    "tts",
    "wake_word",
    "datetime"
  ].includes(a13) || a13 === "sensor" && s9.device_class === "timestamp")
    try {
      return e6 ? ct(new Date(i11), t9, e6) : new Date(i11).toLocaleString();
    } catch {
      return i11;
    }
  if (["button", "conversation", "event", "image", "input_button", "notify", "scene", "stt", "tag", "tts", "wake_word"].includes(a13) || a13 === "sensor" && s9.device_class === "timestamp")
    try {
      return e6 ? ct(new Date(i11), t9, e6) : new Date(i11).toLocaleString();
    } catch {
      return i11;
    }
  return j3(i11);
};
var Me = /* @__PURE__ */ new Set();
function on({
  children: e6,
  hassUrl: r13,
  hassToken: t9,
  portalRoot: n15,
  windowContext: o13,
  renderError: s9 = (a13) => a13,
  handleResumeOptions: i11
}) {
  const a13 = B3((m15) => m15.addSubscription), p8 = B3((m15) => m15.setAuthenticated), {
    hash: d12,
    ready: y6,
    error: _8,
    cannotConnect: P3,
    setError: x6
  } = M2(
    Hr((m15) => ({
      hash: m15.hash,
      routes: m15.routes,
      ready: m15.ready,
      // ready is set internally in the store when we have entities (setEntities does this)
      error: m15.error,
      cannotConnect: m15.cannotConnect,
      auth: m15.auth,
      setError: m15.setError
    }))
  );
  (0, import_react8.useEffect)(() => {
    const { setPortalRoot: m15 } = M2.getState();
    n15 && m15(n15);
  }, [n15]), (0, import_react8.useEffect)(() => {
    const { setWindowContext: m15 } = M2.getState();
    o13 && m15(o13);
  }, [o13]);
  const b11 = (0, import_react8.useCallback)(async () => {
    const {
      setError: m15,
      setUser: E7,
      setCannotConnect: $4,
      setAuth: v10,
      setConnection: S6,
      setEntities: c15,
      setConfig: R5,
      setConnectionStatus: L3,
      setAreas: I7,
      setDevices: g10,
      setFloors: u15,
      setEntitiesRegistryDisplay: f13,
      setServices: l10,
      setUsers: ie2,
      setLocale: K4,
      setSensorNumericDeviceClasses: se2
    } = M2.getState(), z5 = await N4(r13, t9);
    if (z5.type === "error")
      p8(false), m15(z5.error);
    else if (z5.type === "failed")
      p8(false), $4(true);
    else if (z5.type === "success") {
      const { connection: W3, auth: J2 } = z5;
      v10(J2), S6(W3), a13(
        "entities",
        subscribeEntities(W3, (D6) => {
          c15(D6);
        })
      ), a13(
        "entity_registry_display",
        Xr(W3, (D6) => {
          const O5 = {};
          for (const A7 of D6.entities)
            O5[A7.ei] = {
              entity_id: A7.ei,
              device_id: A7.di,
              area_id: A7.ai,
              labels: A7.lb,
              translation_key: A7.tk,
              platform: A7.pl,
              entity_category: A7.ec !== void 0 ? D6.entity_categories[A7.ec] : void 0,
              has_entity_name: A7.hn,
              name: A7.en,
              icon: A7.ic,
              hidden: A7.hb,
              display_precision: A7.dp
            };
          f13(O5);
        })
      ), a13(
        "areas",
        m8(W3, (D6) => {
          const O5 = {};
          for (const A7 of D6)
            O5[A7.area_id] = A7;
          I7(O5);
        })
      ), a13(
        "devices",
        Vr(W3, (D6) => {
          const O5 = {};
          for (const A7 of D6)
            O5[A7.id] = A7;
          g10(O5);
        })
      ), a13(
        "floors",
        n6(W3, (D6) => {
          const O5 = {};
          for (const A7 of D6)
            O5[A7.floor_id] = A7;
          u15(O5);
        })
      ), a13(
        "config",
        subscribeConfig(W3, (D6) => {
          R5(D6);
        })
      ), a13(
        "current_user",
        l7(W3, (D6) => {
          E7(D6);
        })
      ), a13(
        "services",
        subscribeServices(W3, (D6) => {
          l10(D6);
        })
      ), a13(
        "users",
        f6(W3, (D6) => {
          ie2(D6);
        })
      ), a13(
        "language",
        await r3(W3, "language", (D6) => {
          if (D6.value) {
            const O5 = g2(D6.value);
            K4({
              ...D6.value,
              language: O5
            });
          } else
            K4(D6.value);
        })
      ), W3.sendMessagePromise({
        type: "sensor/numeric_device_classes"
      }).then((D6) => {
        se2(D6.numeric_device_classes);
      });
      const { onStatusChange: ae2, ...X3 } = i11 || {}, ce = v3(W3, {
        suspendWhenHidden: true,
        hiddenDelayMs: 3e5,
        // 5 minutes
        debug: false,
        onStatusChange: (D6) => {
          L3(D6), ae2?.(D6);
        },
        ...X3
      });
      a13("resume", ce);
    }
  }, [r13, t9, i11, a13, p8]);
  (0, import_react8.useEffect)(() => {
    const { setHassUrl: m15 } = M2.getState();
    m15(r13);
  }, [r13]), (0, import_react8.useEffect)(() => {
    const { setHash: m15 } = M2.getState();
    location.hash !== "" && location.hash.replace("#", "") !== d12 && m15(location.hash);
  }, [d12]), (0, import_react8.useEffect)(() => (window.addEventListener("hashchange", $e), () => {
    window.removeEventListener("hashchange", $e);
  }), []), (0, import_react8.useEffect)(() => () => B3.getState().reset(), []);
  const w3 = (0, import_react8.useCallback)(async () => {
    if (!Me.has(r13)) {
      Me.add(r13);
      try {
        B3.getState().authenticated && M2.getState().hassUrl !== r13 && B3.getState().reset(), p8(true), i11?.onStatusChange?.("pending"), await b11();
      } catch (m15) {
        const E7 = i5(m15);
        x6(`Unable to connect to Home Assistant, please check the URL: "${E7}"`);
      }
    }
  }, [b11, x6, i11, r13, p8]);
  return (0, import_react8.useEffect)(() => {
    w3();
  }, [w3]), P3 ? s9(
    ve("p", { children: [
      "Unable to connect to ",
      c2(r13)?.hassUrl,
      ", refresh the page and try again, or",
      " ",
      k4("a", { onClick: ne.getState().helpers.logout, children: "Logout" }),
      "."
    ] })
  ) : _8 === null ? e6(y6) : s9(_8);
}
function $e() {
  const { routes: e6, setRoutes: r13, setHash: t9 } = M2.getState();
  r13(
    e6.map((n15) => n15.hash === location.hash.replace("#", "") ? {
      ...n15,
      active: true
    } : {
      ...n15,
      active: false
    })
  ), t9(location.hash);
}
function sn({ locale: e6, children: r13 }) {
  const t9 = zr(), [n15, o13] = (0, import_react8.useState)(false), s9 = (0, import_react8.useRef)(false), i11 = (0, import_react8.useRef)(null), a13 = M2((d12) => d12.setError), p8 = M2((d12) => d12.setLocales);
  return (0, import_react8.useEffect)(() => {
    if (!(e6 ?? t9?.language))
      return;
    const y6 = m3.find(({ code: _8 }) => _8 === (e6 ?? t9?.language));
    if (i11.current !== y6?.code && (o13(false), s9.current = false, a13(null)), !y6)
      s9.current = false, a13(
        `Locale "${e6 ?? t9?.language}" not found, available options are "${m3.map(({ code: _8 }) => `${_8}`).join(", ")}"`
      );
    else {
      if (s9.current) return;
      s9.current = true, i11.current = y6.code, y6.fetch().then((_8) => {
        s9.current = false, o13(true), vr(_8), p8(_8);
      }).catch((_8) => {
        s9.current = false, o13(true), a13(`Error retrieving translations from Home Assistant: ${_8?.message ?? _8}`);
      });
    }
  }, [t9, n15, p8, a13, e6]), n15 ? r13 : null;
}
var te = keyframes`
  0% {stroke-width:0; opacity:0;}
  50% {stroke-width:5; opacity:1;}
  100% {stroke-width:0; opacity:0;}
`;
function an({ className: e6 }) {
  return k4("div", { className: e6, children: ve("svg", { children: [
    k4("path", { d: "m 12.5,20 15,0 0,0 -15,0 z" }),
    k4("path", { d: "m 32.5,20 15,0 0,0 -15,0 z" }),
    k4("path", { d: "m 52.5,20 15,0 0,0 -15,0 z" }),
    k4("path", { d: "m 72.5,20 15,0 0,0 -15,0 z" })
  ] }) });
}
var cn = styled(an)`
  position: fixed;
  inset: 0;
  background-color: #1a1a1a;
  svg {
    position: absolute;
    top: 50%;
    left: 50%;
    width: 6.25em;
    height: 3.125em;
    margin: -1.562em 0 0 -3.125em;
    path {
      fill: none;
      stroke: #f0c039;
      opacity: 0;
    }
    path:nth-of-type(1) {
      animation: ${te} 1s ease-in-out 0s infinite alternate;
    }
    path:nth-of-type(2) {
      animation: ${te} 1s ease-in-out 0.1s infinite alternate;
    }
    path:nth-of-type(3) {
      animation: ${te} 1s ease-in-out 0.2s infinite alternate;
    }
    path:nth-of-type(4) {
      animation: ${te} 1s ease-in-out 0.3s infinite alternate;
    }
  }
`;
var Le = styled.div`
  width: 100%;
  height: 100%;
`;
var yo = (0, import_react8.memo)(function({
  children: r13,
  hassUrl: t9,
  hassToken: n15,
  loading: o13 = k4(cn, {}),
  onReady: s9,
  options: i11 = {},
  wrapperProps: a13
}) {
  const p8 = (0, import_react8.useRef)(false), d12 = (0, import_react8.useMemo)(() => {
    try {
      return new URL(t9).origin;
    } catch (y6) {
      return console.log("Error:", y6), null;
    }
  }, [t9]);
  return !d12 || d12 === "null" || d12 === null ? k4(Ne, { children: "Provide the hassUrl prop with a valid url to your home assistant instance." }) : k4(on, { hassUrl: d12, hassToken: n15, ...i11, children: (y6) => k4(Ne, { children: y6 ? k4(Le, { ...a13, children: ve(sn, { locale: i11.locale, children: [
    s9 && !p8.current && (s9(), p8.current = true, null),
    r13
  ] }) }) : k4(Le, { ...a13, children: o13 }) }) });
});

// node_modules/@hakit/core/dist/es/utils/colors/index.js
var import_react11 = __toESM(require_react(), 1);
var c8 = (t9) => {
  if (t9 && t9.attributes && t9.attributes.brightness && !t9.entity_id.startsWith("plant")) {
    const r13 = t9.attributes.brightness, s9 = t9.state === F2 ? Math.max(Math.round((t9.attributes.brightness ?? 0) * 100 / 255), 1) : 0, i11 = Number(Number((r13 + 245) / 5).toFixed(0));
    return {
      raw: s9,
      css: `brightness(${i11}%)`
    };
  }
  return {
    css: "brightness(100%)",
    raw: 100
  };
};

// node_modules/@hakit/core/dist/es/utils/colors/lab.js
var e4 = (n15, t9 = 1) => [n15[0] - 18 * t9, n15[1], n15[2]];
var o5 = (n15, t9 = 1) => e4(n15, -t9);

// node_modules/@hakit/core/dist/es/utils/colors.js
function p4(r13) {
  if (r13.attributes) {
    if ("hs_color" in r13.attributes && r13.attributes.hs_color !== null)
      return j([r13.attributes.hs_color[0], r13.attributes.hs_color[1] / 100]);
    if ("color_temp_kelvin" in r13.attributes && r13.attributes.color_temp_kelvin !== null)
      return I(r13.attributes.color_temp_kelvin);
    if ("rgb_color" in r13.attributes && r13.attributes.rgb_color !== null)
      return r13.attributes.rgb_color;
    if ("rgbw_color" in r13.attributes && r13.attributes.rgbw_color !== null)
      return B(r13.attributes.rgbw_color);
    if ("rgbww_color" in r13.attributes && r13.attributes.rgbww_color !== null)
      return k(r13.attributes.rgbww_color, r13.attributes.min_color_temp_kelvin, r13.attributes.max_color_temp_kelvin);
    if (kn(r13))
      return I(2e3);
  }
  return null;
}
function k5(r13) {
  const o13 = r13 ? p4(r13) : null, t9 = o13 ? o13.map((c15) => c15.toFixed(0)).map(Number) : null, s9 = o13 ? f2(o13) : "var(--ha-A400)", u15 = t9 ? `rgba(${t9.join(", ")})` : "var(--ha-S500-contrast)", b11 = t9 ? `rgba(${[...t9, 0.35].join(", ")})` : "var(--ha-A200)", { css: a13, raw: e6 } = c8(r13);
  return {
    color: o13 || [33, 33, 33],
    hexColor: s9,
    rgbColor: u15,
    rgbaColor: b11,
    brightness: a13,
    brightnessValue: e6
  };
}

// node_modules/@hakit/core/dist/es/utils/computeDomainTitle.js
var import_lodash8 = __toESM(require_lodash(), 1);
var u9 = (o13, t9) => {
  const a13 = m5(o13);
  switch (a13) {
    case "plant":
      return j3("plant_status");
    case "switch":
      return t9 && t9 === "outlet" ? j3("outlet") : j3("switch");
    case "alarm_control_panel":
      return j3("alarm_panel");
    case "lawn_mower":
      return j3("lawn_mower_commands");
    case "datetime":
      return j3("date_time");
    case "alert":
      return j3("alert_classes");
    case "water_heater":
      return `${j3("water")} ${j3("heat")}`;
    case "logbook":
      return j3("activity");
    case "homeassistant":
      return j3("home_assistant");
    // exact matches
    case "weather":
    case "sun":
    case "binary_sensor":
    case "timer":
    case "counter":
    case "automation":
    case "input_select":
    case "device_tracker":
    case "media_player":
    case "input_number":
      return j3(a13);
    case "stt":
    case "google":
    case "reolink":
    case "notify":
    case "zha":
    case "vacuum":
      return (0, import_lodash8.startCase)((0, import_lodash8.lowerCase)(a13));
    case "frontend":
    case "conversation":
    case "hassio":
    case "command_line":
    case "onvif":
    case "rest_command":
    case "system_log":
    case "media_extractor":
    case "file":
    case "persistent_notification":
    case "cloud":
    case "profiler":
    case "recorder":
    case "logger":
    case "tts":
    case "backup":
    case "climate":
      return j3(`${a13}.title`);
    default: {
      const s9 = j3(a13, {
        // just try to process with the title suffix
        fallback: j3(`${a13}.title`, {
          fallback: a13
        })
      });
      return s9 === a13 ? (0, import_lodash8.startCase)((0, import_lodash8.lowerCase)(a13)) : s9;
    }
  }
};

// node_modules/@hakit/core/dist/es/utils/supports-feature.js
var r7 = (t9, e6) => (t9.supported_features & e6) !== 0;

// node_modules/@hakit/core/dist/es/utils/time/time-ago.js
function f7(a13, u15) {
  const o13 = a13.getTime();
  if (isNaN(o13))
    return console.error("Invalid timestamp"), "unknown";
  const m15 = new Intl.RelativeTimeFormat(u15, { numeric: "auto" });
  let t9;
  const n15 = [
    ["second", 60],
    ["minute", 60],
    ["hour", 24],
    ["day", 30],
    ["month", 12],
    ["year", 1 / 0]
  ], i11 = (o13 - (/* @__PURE__ */ new Date()).getTime()) / 1e3;
  let e6 = Math.abs(i11);
  for (t9 = 0; t9 < n15.length && !(e6 < n15[t9][1]); t9++)
    e6 /= n15[t9][1];
  const r13 = m15.format(Math.round(e6) * (i11 < 0 ? -1 : 1), n15[t9][0]);
  return r13 === "now" ? "just now" : r13;
}

// node_modules/@hakit/core/dist/index-BiRhtKmJ.js
var import_react22 = __toESM(require_react(), 1);
var import_lodash13 = __toESM(require_lodash(), 1);

// node_modules/@hakit/core/dist/es/hooks/useService/index.js
var import_react13 = __toESM(require_react(), 1);
var import_lodash9 = __toESM(require_lodash(), 1);
function a8(e6, r13, t9) {
  return new Proxy(
    // @ts-expect-error - purposely not defining the target object here
    {},
    {
      get: (s9, n15) => {
        const i11 = n15;
        if (i11 !== "toJSON")
          return function(p8) {
            const { target: f13, serviceData: o13, returnResponse: u15 } = p8 || {};
            let c15 = t9 ?? f13;
            return Array.isArray(c15) && (c15 = [...(0, import_lodash9.uniq)(c15)]), console.info(
              `${j3("perform_action", {
                search: "{name}",
                replace: `${e6}.${i11}`
              })} ${e6}.${i11}:`,
              {
                target: c15,
                serviceData: o13,
                returnResponse: u15
              }
            ), r13({
              domain: e6,
              service: i11,
              serviceData: o13,
              target: c15,
              // necessary cast here as the overloads expect true | false | undefined not a boolean
              returnResponse: u15
            });
          };
      }
    }
  );
}
function b4(e6, r13) {
  const { callService: t9 } = ne.getState().helpers, s9 = (0, import_react13.useMemo)(() => e6 ? a8(e6, t9, r13) : void 0, [e6, t9, r13]), n15 = (0, import_react13.useCallback)(
    (i11) => a8(i11, t9, r13),
    [t9, r13]
  );
  return e6 ? s9 : n15;
}

// node_modules/@hakit/core/dist/es/hooks/useIcon/domains/binary_sensor.js
var t7 = (r13) => {
  const e6 = r13.state === "off";
  switch (r13.attributes.device_class) {
    case "battery":
      return e6 ? "mdi:battery" : "mdi:battery-outline";
    case "battery_charging":
      return e6 ? "mdi:battery" : "mdi:battery-charging";
    case "carbon_monoxide":
      return e6 ? "mdi:smoke-detector" : "mdi:smoke-detector-alert";
    case "cold":
      return e6 ? "mdi:thermometer" : "mdi:snowflake";
    case "connectivity":
      return e6 ? "mdi:close-network-outline" : "mdi:check-network-outline";
    case "door":
      return e6 ? "mdi:door-closed" : "mdi:door-open";
    case "garage_door":
      return e6 ? "mdi:garage" : "mdi:garage-open";
    case "power":
      return e6 ? "mdi:power-plug-off" : "mdi:power-plug";
    case "gas":
    case "problem":
    case "safety":
    case "tamper":
      return e6 ? "mdi:check-circle" : "mdi:alert-circle";
    case "smoke":
      return e6 ? "mdi:smoke-detector-variant" : "mdi:smoke-detector-variant-alert";
    case "heat":
      return e6 ? "mdi:thermometer" : "mdi:fire";
    case "light":
      return e6 ? "mdi:brightness-5" : "mdi:brightness-7";
    case "lock":
      return e6 ? "mdi:lock" : "mdi:lock-open";
    case "moisture":
      return e6 ? "mdi:water-off" : "mdi:water";
    case "motion":
      return e6 ? "mdi:motion-sensor-off" : "mdi:motion-sensor";
    case "occupancy":
      return e6 ? "mdi:home-outline" : "mdi:home";
    case "opening":
      return e6 ? "mdi:square" : "mdi:square-outline";
    case "plug":
      return e6 ? "mdi:power-plug-off" : "mdi:power-plug";
    case "presence":
      return e6 ? "mdi:home-outline" : "mdi:home";
    case "running":
      return e6 ? "mdi:stop" : "mdi:play";
    case "sound":
      return e6 ? "mdi:music-note-off" : "mdi:music-note";
    case "update":
      return e6 ? "mdi:package" : "mdi:package-up";
    case "vibration":
      return e6 ? "mdi:crop-portrait" : "mdi:vibrate";
    case "window":
      return e6 ? "mdi:window-closed" : "mdi:window-open";
    default:
      return e6 ? "mdi:radiobox-blank" : "mdi:checkbox-marked-circle";
  }
};

// node_modules/@hakit/core/dist/es/hooks/useIcon/domains/sensor.js
var import_lodash10 = __toESM(require_lodash(), 1);
var import_react15 = __toESM(require_react(), 1);

// node_modules/@hakit/core/dist/es/hooks/useIcon/domains/constants.js
var e5 = {
  apparent_power: "mdi:flash",
  aqi: "mdi:air-filter",
  atmospheric_pressure: "mdi:thermometer-lines",
  // battery: mdiBattery, => not included by design since `sensorIcon()` will dynamically determine the icon
  carbon_dioxide: "mdi:molecule-co2",
  carbon_monoxide: "mdi:molecule-co",
  current: "mdi:current-ac",
  data_rate: "mdi:transmission-tower",
  data_size: "mdi:database",
  date: "mdi:calendar",
  distance: "mdi:arrow-left-right",
  duration: "mdi:progress-clock",
  energy: "mdi:lightning-bolt",
  frequency: "mdi:sine-wave",
  gas: "mdi:meter-gas",
  humidity: "mdi:water-percent",
  illuminance: "mdi:brightness-5",
  irradiance: "mdi:sun-wireless",
  moisture: "mdi:water-percent",
  monetary: "mdi:cash",
  nitrogen_dioxide: "mdi:molecule",
  nitrogen_monoxide: "mdi:molecule",
  nitrous_oxide: "mdi:molecule",
  ozone: "mdi:molecule",
  ph: "mdi:ph",
  pm1: "mdi:molecule",
  pm10: "mdi:molecule",
  pm25: "mdi:molecule",
  power: "mdi:flash",
  power_factor: "mdi:angle-acute",
  precipitation: "mdi:weather-rainy",
  precipitation_intensity: "mdi:weather-pouring",
  pressure: "mdi:gauge",
  reactive_power: "mdi:flash",
  signal_strength: "mdi:wifi",
  sound_pressure: "mdi:ear-hearing",
  speed: "mdi:speedometer",
  sulphur_dioxide: "mdi:molecule",
  temperature: "mdi:thermometer",
  timestamp: "mdi:clock",
  volatile_organic_compounds: "mdi:molecule",
  volatile_organic_compounds_parts: "mdi:molecule",
  voltage: "mdi:sine-wave",
  volume: "mdi:car-coolant-level",
  water: "mdi:water",
  weight: "mdi:weight",
  wind_speed: "mdi:weather-windy"
};
var i8 = {
  air_quality: "mdi:air-filter",
  alert: "mdi:alert",
  calendar: "mdi:calendar",
  climate: "mdi:thermostat",
  configurator: "mdi:cog",
  conversation: "mdi:microphone-message",
  counter: "mdi:counter",
  datetime: "mdi:calendar-clock",
  date: "mdi:calendar",
  demo: "mdi:home-assistant",
  google_assistant: "mdi:google-assistant",
  group: "mdi:google-circles-communities",
  homeassistant: "mdi:home-assistant",
  homekit: "mdi:home-automation",
  image: "mdi:image",
  image_processing: "mdi:image-filter-frames",
  input_button: "mdi:button-pointer",
  input_datetime: "mdi:calendar-clock",
  input_number: "mdi:ray-vertex",
  input_select: "mdi:format-list-bulleted",
  input_text: "mdi:form-textbox",
  lawn_mower: "mdi:robot-mower",
  light: "mdi:lightbulb",
  mailbox: "mdi:mailbox",
  notify: "mdi:comment-alert",
  number: "mdi:ray-vertex",
  persistent_notification: "mdi:bell",
  plant: "mdi:flower",
  proximity: "mdi:apple-safari",
  remote: "mdi:remote",
  scene: "mdi:palette",
  media_player: "mdi:music-box",
  schedule: "mdi:calendar-clock",
  script: "mdi:script-text",
  select: "mdi:format-list-bulleted",
  sensor: "mdi:eye",
  simple_alarm: "mdi:bell",
  siren: "mdi:bullhorn",
  stt: "mdi:microphone-message",
  text: "mdi:form-textbox",
  todo: "mdi:clipboard-list",
  time: "mdi:clock",
  timer: "mdi:timer-outline",
  tts: "mdi:speaker-message",
  updater: "mdi:cloud-upload",
  vacuum: "mdi:robot-vacuum",
  wake_word: "mdi:chat-sleep",
  zone: "mdi:map-marker-radius"
};

// node_modules/@hakit/core/dist/es/hooks/useIcon/domains/battery.js
var a9 = {
  10: "mdi:battery-10",
  20: "mdi:battery-20",
  30: "mdi:battery-30",
  40: "mdi:battery-40",
  50: "mdi:battery-50",
  60: "mdi:battery-60",
  70: "mdi:battery-70",
  80: "mdi:battery-80",
  90: "mdi:battery-90",
  100: "mdi:battery"
};
var n8 = {
  10: "mdi:battery-charging-10",
  20: "mdi:battery-charging-20",
  30: "mdi:battery-charging-30",
  40: "mdi:battery-charging-40",
  50: "mdi:battery-charging-50",
  60: "mdi:battery-charging-60",
  70: "mdi:battery-charging-70",
  80: "mdi:battery-charging-80",
  90: "mdi:battery-charging-90",
  100: "mdi:battery-charging"
};
var b5 = (t9, r13 = false) => {
  const i11 = Math.round(t9 / 10) * 10;
  return r13 && t9 >= 10 ? n8[i11] : r13 ? "mdi:battery-charging-outline" : t9 <= 5 ? "mdi:battery-alert-variant-outline" : a9[i11];
};
var y3 = (t9, r13) => {
  const i11 = t9.state, e6 = r13 && r13.state === "on";
  return d5(i11, e6);
};
var d5 = (t9, r13) => {
  const i11 = Number(t9);
  return isNaN(i11) ? t9 === "off" ? "mdi:battery" : t9 === "on" ? "mdi:battery-alert" : "mdi:battery-unknown" : b5(i11, r13);
};

// node_modules/@hakit/core/dist/es/hooks/useIcon/domains/sensor.js
var n9 = "battery";
var d6 = (t9) => {
  const r13 = t9?.attributes.device_class;
  if (r13 && r13 in e5)
    return e5[r13];
  if (r13 === n9)
    return t9 ? y3(t9) : "mdi:battery";
  const o13 = t9?.attributes.unit_of_measurement;
  if (o13 === T3 || o13 === S3)
    return "mdi:thermometer";
};

// node_modules/@hakit/core/dist/es/hooks/useIcon/domains/number.js
var o6 = (e6) => {
  const r13 = e6.attributes.device_class;
  if (r13 && r13 in e5)
    return e5[r13];
};

// node_modules/@hakit/core/dist/es/hooks/useIcon/domains/cover.js
var o7 = (e6) => {
  const r13 = e6.state !== "closed";
  switch (e6.attributes.device_class) {
    case "garage":
      switch (e6.state) {
        case "opening":
          return "mdi:arrow-up-box";
        case "closing":
          return "mdi:arrow-down-box";
        case "closed":
          return "mdi:garage";
        default:
          return "mdi:garage-open";
      }
    case "gate":
      switch (e6.state) {
        case "opening":
        case "closing":
          return "mdi:gate-arrow-right";
        case "closed":
          return "mdi:gate";
        default:
          return "mdi:gate-open";
      }
    case "door":
      return r13 ? "mdi:door-open" : "mdi:door-closed";
    case "damper":
      return r13 ? "mdi:circle" : "mdi:circle-slice-8";
    case "shutter":
      switch (e6.state) {
        case "opening":
          return "mdi:arrow-up-box";
        case "closing":
          return "mdi:arrow-down-box";
        case "closed":
          return "mdi:window-shutter";
        default:
          return "mdi:window-shutter-open";
      }
    case "curtain":
      switch (e6.state) {
        case "opening":
          return "mdi:arrow-split-vertical";
        case "closing":
          return "mdi:arrow-collapse-horizontal";
        case "closed":
          return "mdi:curtains-closed";
        default:
          return "mdi:curtains";
      }
    case "blind":
      switch (e6.state) {
        case "opening":
          return "mdi:arrow-up-box";
        case "closing":
          return "mdi:arrow-down-box";
        case "closed":
          return "mdi:blinds-horizontal-closed";
        default:
          return "mdi:blinds-horizontal";
      }
    case "shade":
      switch (e6.state) {
        case "opening":
          return "mdi:arrow-up-box";
        case "closing":
          return "mdi:arrow-down-box";
        case "closed":
          return "mdi:roller-shade-closed";
        default:
          return "mdi:roller-shade";
      }
    case "window":
      switch (e6.state) {
        case "opening":
          return "mdi:arrow-up-box";
        case "closing":
          return "mdi:arrow-down-box";
        case "closed":
          return "mdi:window-closed";
        default:
          return "mdi:window-open";
      }
  }
  switch (e6.state) {
    case "opening":
      return "mdi:arrow-up-box";
    case "closing":
      return "mdi:arrow-down-box";
    case "closed":
      return "mdi:window-closed";
    default:
      return "mdi:window-open";
  }
};

// node_modules/@hakit/core/dist/es/hooks/useIcon/domains/alarm.js
var r8 = (e6) => {
  switch (e6) {
    case "armed_away":
      return "mdi:shield-lock";
    case "armed_vacation":
      return "mdi:shield-airplane";
    case "armed_home":
      return "mdi:shield-home";
    case "armed_night":
      return "mdi:shield-moon";
    case "armed_custom_bypass":
      return "mdi:security";
    case "pending":
      return "mdi:shield-outline";
    case "triggered":
      return "mdi:bell-ring";
    case "disarmed":
      return "mdi:shield-off";
    default:
      return "mdi:shield";
  }
};

// node_modules/@hakit/core/dist/es/hooks/useIcon/domains/weather.js
function r9(e6) {
  switch (e6) {
    case "clear-night":
      return "mdi:weather-night";
    case "cloudy":
      return "mdi:weather-cloudy";
    case "fog":
      return "mdi:weather-fog";
    case "hail":
      return "mdi:weather-hail";
    case "lightning":
      return "mdi:weather-lightning";
    case "lightning-rainy":
      return "mdi:weather-lightning-rainy";
    case "partlycloudy":
      return "mdi:weather-partly-cloudy";
    case "pouring":
      return "mdi:weather-pouring";
    case "rainy":
      return "mdi:weather-rainy";
    case "snowy":
      return "mdi:weather-snowy";
    case "snowy-rainy":
      return "mdi:weather-snowy-rainy";
    case "sunny":
      return "mdi:weather-sunny";
    case "windy":
      return "mdi:weather-windy";
    case "windy-variant":
      return "mdi:weather-windy-variant";
    case "exceptional":
    default:
      return "mdi:alert-circle-outline";
  }
}

// node_modules/@hakit/core/dist/es/hooks/useHistory/index.js
var import_react21 = __toESM(require_react(), 1);

// node_modules/@hakit/core/dist/es/hooks/useSubscribeEntity/index.js
var import_react17 = __toESM(require_react(), 1);
var import_lodash11 = __toESM(require_lodash(), 1);
function b6(t9) {
  const r13 = ne((o13) => o13.entities[t9]);
  return (0, import_react17.useCallback)(
    (o13) => {
      if (t9 === "unknown")
        return null;
      if (!r13) {
        if (o13)
          return null;
        throw new Error(`${j3("entity_not_found")} - "${t9}"`);
      }
      return r13;
    },
    [r13, t9]
  );
}

// node_modules/@hakit/core/dist/es/hooks/useHistory/history.js
var import_lodash12 = __toESM(require_lodash(), 1);
var import_react19 = __toESM(require_react(), 1);
var k6 = ["climate", "humidifier", "water_heater"];
var C3 = ["climate", "humidifier", "input_datetime", "water_heater", "person", "device_tracker"];
var T4 = [
  "temperature",
  "current_temperature",
  "target_temp_low",
  "target_temp_high",
  "hvac_action",
  "humidity",
  "mode",
  "action",
  "current_humidity"
];
var j4 = (e6) => C3.includes(m5(e6));
var Q3 = ({
  connection: e6,
  entityIds: i11,
  callbackFunction: n15,
  hoursToShow: s9 = 24,
  minimalResponse: t9 = true,
  significantChangesOnly: o13 = true,
  noAttributes: u15
}) => {
  const r13 = {
    type: "history/stream",
    entity_ids: i11,
    start_time: new Date((/* @__PURE__ */ new Date()).getTime() - 3600 * s9 * 1e3).toISOString(),
    minimal_response: t9,
    significant_changes_only: o13,
    no_attributes: u15 ?? !i11.some((c15) => j4(c15))
  }, f13 = new U2(s9);
  return e6.subscribeMessage((c15) => n15(f13.processMessage(c15)), r13);
};
var U2 = class {
  hoursToShow;
  combinedHistory;
  constructor(i11) {
    this.hoursToShow = i11, this.combinedHistory = {};
  }
  processMessage(i11) {
    if (!this.combinedHistory || !Object.keys(this.combinedHistory).length)
      return this.combinedHistory = i11.states, this.combinedHistory;
    if (!Object.keys(i11.states).length)
      return this.combinedHistory;
    const n15 = this.hoursToShow ? ((/* @__PURE__ */ new Date()).getTime() - 3600 * this.hoursToShow * 1e3) / 1e3 : void 0, s9 = {};
    for (const t9 of Object.keys(this.combinedHistory))
      s9[t9] = [];
    for (const t9 of Object.keys(i11.states))
      s9[t9] = [];
    for (const t9 of Object.keys(s9)) {
      if (t9 in this.combinedHistory && t9 in i11.states) {
        const o13 = this.combinedHistory[t9], u15 = o13[o13.length - 1];
        s9[t9] = o13.concat(i11.states[t9]), i11.states[t9][0].lu < u15.lu && (s9[t9] = s9[t9].sort((r13, f13) => r13.lu - f13.lu));
      } else t9 in this.combinedHistory ? s9[t9] = this.combinedHistory[t9] : s9[t9] = i11.states[t9];
      if (n15 && t9 in this.combinedHistory) {
        const o13 = s9[t9].filter((r13) => r13.lu < n15);
        if (!o13.length || (s9[t9] = s9[t9].filter((r13) => r13.lu >= n15), s9[t9].length && s9[t9][0].lu === n15))
          continue;
        const u15 = o13[o13.length - 1];
        u15.lu = n15, s9[t9].unshift(u15);
      }
    }
    return this.combinedHistory = s9, this.combinedHistory;
  }
};
var v5 = (e6, i11) => e6.state === i11.state && // Only compare attributes if both states have an attributes object.
// When `minimal_response` is sent, only the first and last state
// will have attributes except for domains in DOMAINS_USE_LAST_UPDATED.
(!e6.attributes || !i11.attributes || T4.every((n15) => e6.attributes[n15] === i11.attributes[n15]));
var L2 = (e6, i11, n15) => {
  const s9 = ne.getState().entities, t9 = [];
  return Object.keys(n15).forEach((o13) => {
    const u15 = n15[o13], r13 = u15[0], f13 = m5(o13), c15 = [];
    for (const a13 of u15) {
      let l10;
      if (k6.includes(f13)) {
        l10 = {
          state: a13.s,
          last_changed: a13.lu * 1e3,
          attributes: {}
        };
        for (const m15 of T4)
          m15 in a13.a && (l10.attributes[m15] = a13.a[m15]);
      } else
        l10 = {
          state: a13.s,
          // lc (last_changed) may be omitted if its the same
          // as lu (last_updated).
          last_changed: (a13.lc ? a13.lc : a13.lu) * 1e3,
          attributes: {}
        };
      c15.length > 1 && v5(l10, c15[c15.length - 1]) && v5(l10, c15[c15.length - 2]) || c15.push(l10);
    }
    const S6 = o13 in s9 ? s9[o13].attributes : "friendly_name" in r13.a ? r13.a : void 0;
    t9.push({
      domain: f13,
      name: Dr(o13, S6 || {}),
      entity_id: o13,
      states: c15
    });
  }), {
    unit: e6,
    device_class: i11,
    identifier: Object.keys(n15).join(""),
    data: t9
  };
};
var z2 = (e6, i11, n15) => {
  const s9 = [], t9 = i11[0], o13 = ne.getState().formatter, u15 = ne.getState().entities;
  for (const r13 of i11) {
    if (s9.length > 0 && r13.s === s9[s9.length - 1].state)
      continue;
    const f13 = {};
    n15?.attributes.device_class && (f13.device_class = n15?.attributes.device_class);
    const c15 = u15[e6];
    s9.push({
      state_localize: o13.attributeValue(
        {
          ...c15,
          attributes: {
            ...r13.a || t9.a,
            ...f13
          }
        },
        r13.s
      ),
      state: r13.s,
      // lc (last_changed) may be omitted if its the same
      // as lu (last_updated).
      last_changed: (r13.lc ? r13.lc : r13.lu) * 1e3
    });
  }
  return {
    name: Dr(e6, n15?.attributes || t9.a),
    entity_id: e6,
    data: s9
  };
};
var B4 = ["counter", "input_number", "number"];
var N6 = (e6) => B4.includes(e6);
var D2 = (e6) => "unit_of_measurement" in e6 || "state_class" in e6;
var F3 = (e6, i11) => e6.attributes.device_class != null && i11.includes(e6.attributes.device_class);
var K3 = (e6, i11, n15, s9, t9 = false) => t9 || N6(e6) || i11 != null && D2(i11.attributes) || i11 != null && e6 === "sensor" && F3(i11, s9) || n15 != null;
var x4 = " ";
var P2 = (e6, i11, n15) => n15 ? `${e6}_${i11 || ""}` : e6;
var Y4 = (e6, i11, n15 = false, s9 = false) => {
  const t9 = {}, o13 = [], u15 = {}, r13 = ne.getState().sensorNumericDeviceClasses, f13 = ne.getState().config, c15 = ne.getState().entities;
  return e6.entity_id in i11 ? u15[e6.entity_id] = i11[e6.entity_id] : c15[e6.entity_id] && (u15[e6.entity_id] = [
    {
      s: c15[e6.entity_id].state,
      a: c15[e6.entity_id].attributes,
      lu: new Date(c15[e6.entity_id].last_updated).getTime() / 1e3
    }
  ]), u15 ? (Object.keys(i11).forEach((a13) => {
    const l10 = u15[a13];
    if (l10.length === 0)
      return;
    const m15 = m5(a13), _8 = a13 in c15 ? c15[a13] : void 0, y6 = _8 || N6(m15) ? void 0 : l10.find((H4) => H4.a && D2(H4.a)), w3 = K3(m15, _8, y6, r13, n15);
    let d12;
    w3 ? d12 = _8?.attributes.unit_of_measurement || y6?.a.unit_of_measurement || x4 : m15 === "zone" ? d12 = j3("people_in_zone") : m15 === "climate" || m15 === "water_heater" ? d12 = f13?.unit_system.temperature : m15 === "humidifier" && (d12 = "%");
    let p8;
    m15 === "climate" ? p8 = "temperature" : m15 === "humidifier" ? p8 = "humidity" : m15 === "water_heater" && (p8 = "temperature");
    const A7 = p8 || (_8?.attributes || y6?.a)?.device_class, h5 = P2(d12, A7, s9);
    d12 ? h5 && h5 in t9 && a13 in t9[h5] ? t9[h5][a13].push(...l10) : h5 && (h5 in t9 || (t9[h5] = {}), t9[h5][a13] = l10) : o13.push(z2(a13, l10, _8));
  }), { line: Object.keys(t9).map((a13) => {
    const l10 = a13.split("_"), m15 = l10[0], _8 = l10.slice(1).join("_") || void 0;
    return L2(m15, _8, t9[a13]);
  }), timeline: o13 }) : { line: [], timeline: [] };
};

// node_modules/@hakit/core/dist/es/hooks/useHistory/coordinates.js
var m10 = (t9) => t9.reduce((c15, s9) => c15 + parseFloat(s9.state), 0) / t9.length;
var x5 = (t9) => parseFloat(t9[t9.length - 1].state) || 0;
var _4 = (t9, c15, s9, u15, r13, g10) => {
  const n15 = [];
  let l10 = (g10 - r13) / 80;
  l10 = l10 !== 0 ? l10 : 80;
  let e6 = s9 / (c15 - (u15 === 1 ? 1 : 0));
  e6 = isFinite(e6) ? e6 : s9;
  const o13 = t9.filter(Boolean)[0];
  let f13 = [m10(o13), x5(o13)];
  const p8 = (a13, d12, M4 = 0, b11 = 1) => {
    if (b11 > 1 && a13)
      return a13.forEach((T7, R5) => p8(T7, d12, R5, b11 - 1));
    const h5 = e6 * (d12 + M4 / 6);
    a13 && (f13 = [m10(a13), x5(a13)]);
    const v10 = 80 + 5 / 2 - ((a13 ? f13[0] : f13[1]) - r13) / l10;
    return n15.push([h5, v10]);
  };
  for (let a13 = 0; a13 < t9.length; a13 += 1)
    p8(t9[a13], a13, 0, u15);
  return n15.length === 1 && (n15[1] = [s9, n15[0][1]]), n15.push([s9, n15[n15.length - 1][1]]), n15;
};
var D3 = (t9, c15, s9, u15, r13) => {
  t9.forEach((e6) => {
    e6.state = Number(e6.state);
  }), t9.every((e6) => Number.isNaN(e6.state)) && (t9 = t9.map((e6, o13) => Number.isNaN(e6.state) ? {
    ...e6,
    state: o13
  } : e6)), t9 = t9.filter((e6) => !Number.isNaN(e6.state));
  const g10 = r13?.min !== void 0 ? r13.min : Math.min(...t9.map((e6) => e6.state)), n15 = r13?.max !== void 0 ? r13.max : Math.max(...t9.map((e6) => e6.state)), N7 = (/* @__PURE__ */ new Date()).getTime(), l10 = (e6, o13, f13) => {
    const p8 = N7 - new Date(o13.last_changed).getTime();
    let a13 = Math.abs(p8 / (1e3 * 3600) - c15);
    return f13 ? (a13 = (a13 - Math.floor(a13)) * 60, a13 = Number((Math.round(a13 / 10) * 10).toString()[0])) : a13 = Math.floor(a13), e6[a13] || (e6[a13] = []), e6[a13].push(o13), e6;
  };
  if (t9 = t9.reduce(
    // @ts-expect-error - bad typing, fix later
    (e6, o13) => l10(e6, o13, false),
    []
  ), u15 > 1 && (t9 = t9.map(
    (e6) => (
      // @ts-expect-error - bad typing, fix later
      e6.reduce((o13, f13) => l10(o13, f13, true), [])
    )
  )), !!t9.length)
    return _4(t9, c15, s9, u15, g10, n15);
};
var E3 = (t9, c15, s9, u15, r13) => {
  if (!t9)
    return;
  const g10 = t9.map((n15) => ({
    _state: n15.s,
    state: Number(n15.s),
    // With minimal response and compressed state, we don't have last_changed,
    // so we use last_updated since its always the same as last_changed since
    // we already filtered out states that are the same.
    last_changed: n15.lu * 1e3
  }));
  return D3(g10, c15, s9, u15, r13);
};

// node_modules/@hakit/core/dist/es/hooks/useHistory/index.js
var E4 = (i11, e6) => {
  const a13 = ne((t9) => t9.connection), n15 = (0, import_react21.useRef)(false), l10 = b6(i11), [o13, y6] = (0, import_react21.useState)({}), [u15, g10] = (0, import_react21.useState)({
    loading: !e6?.disable,
    timeline: [],
    entityHistory: [],
    coordinates: []
  }), s9 = (0, import_react21.useMemo)(() => ({
    disable: e6?.disable,
    significantChangesOnly: e6?.significantChangesOnly,
    minimalResponse: e6?.minimalResponse,
    hoursToShow: e6?.hoursToShow,
    limits: e6?.limits,
    forceNumeric: e6?.forceNumeric,
    splitDeviceClasses: e6?.splitDeviceClasses
  }), [
    e6?.disable,
    e6?.significantChangesOnly,
    e6?.minimalResponse,
    e6?.hoursToShow,
    e6?.limits,
    e6?.forceNumeric,
    e6?.splitDeviceClasses
  ]);
  return (0, import_react21.useEffect)(() => {
    if (!a13 || s9?.disable) return;
    let t9 = true, c15 = null;
    return Q3({
      connection: a13,
      entityIds: [i11],
      significantChangesOnly: s9?.significantChangesOnly,
      minimalResponse: s9?.minimalResponse,
      hoursToShow: s9?.hoursToShow,
      callbackFunction: (r13) => {
        t9 && (n15.current = true, y6(r13));
      }
    }).then((r13) => {
      c15 = r13;
    }).catch(() => {
      c15?.(), n15.current = false;
    }), () => {
      t9 = false, c15?.(), n15.current = false;
    };
  }, [i11, s9, a13]), (0, import_react21.useEffect)(() => {
    if (!s9?.disable && n15.current) {
      const t9 = l10(true);
      if (!t9) return;
      const r13 = Y4(t9, o13, s9.forceNumeric, s9?.splitDeviceClasses).timeline.filter(({ entity_id: b11 }) => b11 === i11), d12 = E3(
        o13[i11],
        s9?.hoursToShow ?? 24,
        500,
        // viewbox of the svgGraph
        typeof s9?.significantChangesOnly > "u" || s9?.significantChangesOnly === true ? 1 : 2,
        s9?.limits
      ) ?? [];
      g10({
        loading: false,
        timeline: r13.length > 0 ? r13[0].data : [],
        entityHistory: o13[i11],
        coordinates: d12
      });
    }
  }, [i11, s9, l10, o13]), (0, import_react21.useMemo)(() => u15, [u15]);
};

// node_modules/@hakit/core/dist/index-BiRhtKmJ.js
var p5 = {
  returnNullIfNotFound: false,
  historyOptions: {
    hoursToShow: 24,
    significantChangesOnly: true,
    minimalResponse: true,
    disable: true
  }
};
function H3(r13, t9) {
  const e6 = /* @__PURE__ */ new Date(), o13 = new Date(r13.attributes.last_triggered ?? r13.last_updated), n15 = f7(o13, t9), i11 = Math.abs(e6.getTime() - o13.getTime()), f13 = n15 === "just now", { hexColor: c15, rgbColor: u15, brightness: m15, brightnessValue: s9, rgbaColor: w3, color: d12 } = k5(r13), k8 = v6(m5(r13.entity_id), r13);
  return {
    ...r13,
    attributes: {
      ...r13.attributes,
      icon: r13.attributes?.icon || k8
    },
    custom: {
      color: d12,
      relativeTime: n15,
      timeDiff: i11,
      active: f13,
      hexColor: c15,
      rgbColor: u15,
      brightness: m15,
      brightnessValue: s9,
      rgbaColor: w3
    }
  };
}
function $3(r13, t9 = p5) {
  const { returnNullIfNotFound: e6, historyOptions: o13 } = {
    ...p5,
    ...t9,
    historyOptions: {
      ...p5.historyOptions,
      ...t9.historyOptions
    }
  }, i11 = b6(r13)(e6), f13 = m5(r13), c15 = b4(f13, r13), u15 = E4(r13, o13), m15 = ne((d12) => d12.config?.language), s9 = (0, import_react22.useMemo)(() => i11 ? H3(i11, m15) : null, [i11, m15]);
  return (0, import_react22.useMemo)(() => s9 == null ? null : { ...s9, service: c15, history: u15 }, [s9, c15, u15]);
}
function ne2(r13, t9) {
  const e6 = (0, import_react22.useMemo)(() => {
    const o13 = (0, import_lodash13.snakeCase)(r13);
    return i8[o13] ? i8[o13] : "mdi:information-outline";
  }, [r13]);
  return e6 === null ? null : k4(
    Icon,
    {
      style: {
        fontSize: t9?.fontSize ?? "24px"
      },
      icon: e6,
      ...t9
    }
  );
}
function ie(r13, t9) {
  return (0, import_react22.useMemo)(() => r13 === null ? null : k4(
    Icon,
    {
      style: {
        fontSize: t9?.fontSize ?? "24px"
      },
      icon: r13 || "octicon:info-24",
      ...t9
    }
  ), [r13, t9]);
}
function v6(r13, t9) {
  const e6 = t9.state;
  switch (r13) {
    case "alarm_control_panel":
      return r8(e6);
    case "automation":
      return e6 === "unavailable" ? "mdi:robot-confused" : e6 === "off" ? "mdi:robot-off" : "mdi:robot";
    case "binary_sensor":
      return t7(t9);
    case "button":
      switch (t9.attributes.device_class) {
        case "identify":
          return "mdi:crosshairs-question";
        case "restart":
          return "mdi:restart";
        case "update":
          return "mdi:package-up";
        default:
          return "mdi:button-pointer";
      }
    case "camera":
      return e6 === "off" ? "mdi:video-off" : "mdi:video";
    case "cover":
      return o7(t9);
    case "device_tracker":
      return t9.attributes.source_type === "router" ? e6 === "home" ? "mdi:lan-connect" : "mdi:lan-disconnect" : ["bluetooth", "bluetooth_le"].includes(t9.attributes.source_type) ? e6 === "home" ? "mdi:bluetooth-connect" : "mdi:bluetooth" : e6 === "not_home" ? "mdi:account-arrow-right" : "mdi:account";
    case "event":
      switch (t9.attributes.device_class) {
        case "doorbell":
          return "mdi:doorbell";
        case "button":
          return "mdi:gesture-tap-button";
        case "motion":
          return "mdi:motion-sensor";
        default:
          return "mdi:eye-check";
      }
    case "fan":
      return e6 === "off" ? "mdi:fan-off" : "mdi:fan";
    case "humidifier":
      return e6 === "off" ? "mdi:air-humidifier-off" : "mdi:air-humidifier";
    case "input_boolean":
      return e6 === "on" ? "mdi:check-circle-outline" : "mdi:close-circle-outline";
    case "input_datetime":
      if (!t9.attributes.has_date)
        return "mdi:clock";
      if (!t9.attributes.has_time)
        return "mdi:calendar";
      break;
    case "lock":
      switch (e6) {
        case "unlocked":
          return "mdi:lock-open";
        case "jammed":
          return "mdi:lock-alert";
        case "locking":
        case "unlocking":
          return "mdi:lock-clock";
        default:
          return "mdi:lock";
      }
    case "media_player":
      switch (t9.attributes.device_class) {
        case "speaker":
          switch (e6) {
            case "playing":
              return "mdi:speaker-play";
            case "paused":
              return "mdi:speaker-pause";
            case "off":
              return "mdi:speaker-off";
            default:
              return "mdi:speaker";
          }
        case "tv":
          switch (e6) {
            case "playing":
              return "mdi:television-play";
            case "paused":
              return "mdi:television-pause";
            case "off":
              return "mdi:television-off";
            default:
              return "mdi:television";
          }
        case "receiver":
          switch (e6) {
            case "off":
              return "mdi:audio-video-off";
            default:
              return "mdi:audio-video";
          }
        default:
          switch (e6) {
            case "playing":
            case "paused":
              return "mdi:cast-connected";
            case "off":
              return "mdi:cast-off";
            default:
              return "mdi:cast";
          }
      }
    case "number": {
      const o13 = o6(t9);
      if (o13)
        return o13;
      break;
    }
    case "person":
      return e6 === "not_home" ? "mdi:account-arrow-right" : "mdi:account";
    case "switch":
      switch (t9.attributes.device_class) {
        case "outlet":
          return e6 === "on" ? "mdi:power-plug" : "mdi:power-plug-off";
        case "switch":
          return e6 === "on" ? "mdi:toggle-switch-variant" : "mdi:toggle-switch-variant-off";
        default:
          return "mdi:toggle-switch-variant";
      }
    case "sensor": {
      const o13 = d6(t9);
      if (o13)
        return o13;
      break;
    }
    case "sun":
      return t9.state === "above_horizon" ? "mdi:white-balance-sunny" : "mdi:weather-night";
    case "switch_as_x":
      return "mdi:swap-horizontal";
    case "threshold":
      return "mdi:chart-sankey";
    case "update":
      return "mdi:package";
    case "water_heater":
      return e6 === "off" ? "mdi:water-boiler-off" : "mdi:water-boiler";
    case "weather":
      return r9(t9.state);
  }
  if (r13 in i8)
    return i8[r13];
}
function se(r13, t9) {
  const e6 = $3(r13 || "unknown", {
    returnNullIfNotFound: true
  });
  return (0, import_react22.useMemo)(() => {
    if (e6 === null) return null;
    const n15 = e6.attributes.icon ?? v6(m5(r13), e6);
    return n15 ? k4(
      Icon,
      {
        style: {
          fontSize: t9?.fontSize ?? "24px"
        },
        icon: n15,
        ...t9
      }
    ) : null;
  }, [e6, r13, t9]);
}

// node_modules/@hakit/core/dist/es/hooks/useEntities/index.js
var import_lodash14 = __toESM(require_lodash(), 1);
var import_react24 = __toESM(require_react(), 1);
function c10(t9, i11) {
  return t9.map((r13) => $3(r13, i11));
}

// node_modules/@hakit/core/dist/es/hooks/useLightBrightness/index.js
var import_react26 = __toESM(require_react(), 1);
var import_lodash15 = __toESM(require_lodash(), 1);
var g5 = (t9) => (0, import_react26.useMemo)(
  () => t9.state === F2 ? Math.max(Math.round((t9.attributes.brightness ?? 0) * 100 / 255), 1) : 0,
  [t9.attributes.brightness, t9.state]
);

// node_modules/@hakit/core/dist/es/hooks/useLightTemperature/index.js
var import_react28 = __toESM(require_react(), 1);
var import_lodash16 = __toESM(require_lodash(), 1);
var b7 = (r13) => (0, import_react28.useMemo)(() => {
  if (r13.state === F2)
    return r13.attributes.color_mode === c4.COLOR_TEMP ? r13.attributes.color_temp_kelvin : void 0;
}, [r13.state, r13.attributes.color_mode, r13.attributes.color_temp_kelvin]);

// node_modules/@hakit/core/dist/es/hooks/useLightColor/index.js
var import_react30 = __toESM(require_react(), 1);
var import_lodash17 = __toESM(require_lodash(), 1);
var C5 = (o13) => {
  const c15 = (0, import_react30.useMemo)(() => {
    if (o13.state === F2) {
      if (o13.attributes.color_mode === c4.RGB && o13.attributes.rgb_color && !Ge(o13, c4.RGBWW) && !Ge(o13, c4.RGBW)) {
        const r13 = Math.max(...o13.attributes.rgb_color);
        if (r13 < 255)
          return r13;
      }
      return;
    }
  }, [o13]), d12 = (0, import_react30.useMemo)(() => {
    const r13 = o13.state !== F2 ? void 0 : o13.attributes.color_mode === c4.RGBW && o13.attributes.rgbw_color ? Math.round(o13.attributes.rgbw_color[3] * 100 / 255) : void 0;
    return r13 != null ? r13 * 255 / 100 : void 0;
  }, [o13]), e6 = (0, import_react30.useMemo)(() => {
    const r13 = o13.state !== F2 ? void 0 : o13.attributes.color_mode === c4.RGBWW && o13.attributes.rgbww_color ? Math.round(o13.attributes.rgbww_color[3] * 100 / 255) : void 0;
    return r13 != null ? r13 * 255 / 100 : void 0;
  }, [o13]), i11 = (0, import_react30.useMemo)(() => {
    const r13 = o13.state !== F2 ? void 0 : o13.attributes.color_mode === c4.RGBWW && o13.attributes.rgbww_color ? Math.round(o13.attributes.rgbww_color[4] * 100 / 255) : void 0;
    return r13 != null ? r13 * 255 / 100 : void 0;
  }, [o13]), t9 = Un(o13), m15 = (0, import_react30.useMemo)(() => {
    const r13 = o13.state !== F2 ? void 0 : t9 ? Math.round(Math.max(...t9.slice(0, 3)) * 100 / 255) : void 0;
    return r13 != null ? r13 * 255 / 100 : void 0;
  }, [o13, t9]), b11 = (0, import_react30.useMemo)(() => o13.state !== F2 ? void 0 : t9 ? Z(t9.slice(0, 3)) : void 0, [o13, t9]);
  return {
    brightnessAdjusted: c15,
    white: d12,
    coolWhite: e6,
    warmWhite: i11,
    colorBrightness: m15,
    hs: b11
  };
};

// node_modules/@hakit/core/dist/es/hooks/useLowDevices/index.js
var import_react32 = __toESM(require_react(), 1);
var import_lodash18 = __toESM(require_lodash(), 1);
var d7 = (t9) => t9.attributes.unit_of_measurement === "%" && t9.attributes.device_class === "battery";
var g6 = ({ blacklist: t9 = [], whitelist: s9 = [], min: o13 = 0, max: i11 = 20 } = {}) => {
  const m15 = ne(
    Hr((e6) => Object.values(e6.entities).filter(d7))
  );
  return (0, import_react32.useMemo)(
    () => m15.filter((e6) => {
      const u15 = Number(e6.state) <= i11 && Number(e6.state) >= o13, a13 = t9.some((r13) => e6.entity_id.includes(r13)), n15 = s9.length === 0 || s9.some((r13) => e6.entity_id.includes(r13));
      return u15 && n15 && !a13;
    }),
    [t9, m15, i11, o13, s9]
  );
};

// node_modules/@hakit/core/dist/es/hooks/useDevice/index.js
var import_react34 = __toESM(require_react(), 1);
var import_lodash19 = __toESM(require_lodash(), 1);
var d8 = (t9) => {
  const [i11, s9] = (0, import_react34.useState)(null), e6 = ne((o13) => o13.connection);
  return (0, import_react34.useEffect)(() => {
    (async () => {
      if (!e6)
        return;
      const n15 = await e6.sendMessagePromise({
        type: "config/entity_registry/get",
        entity_id: t9
      });
      s9(n15);
    })();
  }, [e6, t9]), i11;
};

// node_modules/@hakit/core/dist/es/hooks/useLogs/index.js
var import_react38 = __toESM(require_react(), 1);

// node_modules/@hakit/core/dist/es/hooks/useLogs/logbook.js
var import_lodash20 = __toESM(require_lodash(), 1);
var import_react36 = __toESM(require_react(), 1);
var S4 = ["counter", "proximity", "sensor", "zone"];
var z3 = (r13, a13) => ({
  entity_id: r13.entity_id,
  state: a13,
  attributes: {
    // Rebuild the historical state by copying static attributes only
    device_class: r13?.attributes.device_class,
    source_type: r13?.attributes.source_type,
    has_date: r13?.attributes.has_date,
    has_time: r13?.attributes.has_time,
    // We do not want to use dynamic entity pictures (e.g., from media player) for the log book rendering,
    // as they would present a false state in the log (played media right now vs actual historic data).
    entity_picture_local: I4.has(m5(r13.entity_id)) ? void 0 : r13?.attributes.entity_picture_local,
    entity_picture: I4.has(m5(r13.entity_id)) ? void 0 : r13?.attributes.entity_picture
  }
});
var M3 = (r13, a13, i11, s9, t9, c15) => {
  if ((t9 || c15) && (!t9 || t9.length === 0) && (!c15 || c15.length === 0))
    return Promise.reject(`${j3("no_matching_entities_found")}, ${j3("no_matching_devices_found")}`);
  const n15 = {
    type: "logbook/event_stream",
    start_time: i11,
    end_time: s9
  };
  return t9?.length && (n15.entity_ids = t9), c15?.length && (n15.device_ids = c15), r13.subscribeMessage((g10) => a13(g10), n15);
};
var l9 = {
  "numeric state of": "triggered_by_numeric_state_of",
  // number state trigger
  "state of": "triggered_by_state_of",
  // state trigger
  event: "triggered_by_event",
  // event trigger
  time: "triggered_by_time",
  // time trigger
  "time pattern": "triggered_by_time_pattern",
  // time trigger
  "Home Assistant stopping": "triggered_by_home_assistant_stopping",
  // stop event
  "Home Assistant starting": "triggered_by_home_assistant_starting"
  // start event
};
var U3 = (r13) => {
  for (const a13 in l9)
    if (r13.startsWith(a13))
      return r13.replace(
        a13,
        // @ts-expect-error - this is fine, it'll return undefined
        `${j3(`${l9[a13]}`)}`
      );
  return r13;
};
var D4 = (r13, a13, i11) => {
  switch (i11) {
    case "device_tracker":
    case "person":
      return r13 === "not_home" ? j3("was_detected_away") : r13 === "home" ? j3("was_detected_at_home") : j3("was_detected_at_state", {
        search: "{state}",
        replace: r13
      });
    case "sun":
      return r13 === "above_horizon" ? j3("rose") : j3("set");
    case "binary_sensor": {
      const s9 = r13 === F2, t9 = r13 === N3, c15 = a13.attributes.device_class;
      switch (c15) {
        case "battery":
          if (s9)
            return j3("was_low");
          if (t9)
            return j3("was_normal");
          break;
        case "connectivity":
          if (s9)
            return j3("was_connected");
          if (t9)
            return j3("was_disconnected");
          break;
        case "door":
        case "garage_door":
        case "opening":
        case "window":
          if (s9)
            return j3("was_opened");
          if (t9)
            return j3("was_closed");
          break;
        case "lock":
          if (s9)
            return j3("was_unlocked");
          if (t9)
            return j3("was_locked");
          break;
        case "plug":
          if (s9)
            return j3("was_plugged_in");
          if (t9)
            return j3("was_unplugged");
          break;
        case "presence":
          if (s9)
            return j3("was_detected_at_home");
          if (t9)
            return j3("was_detected_away");
          break;
        case "safety":
          if (s9)
            return j3("was_unsafe");
          if (t9)
            return j3("was_safe");
          break;
        case "cold":
        case "gas":
        case "heat":
        case "moisture":
        case "motion":
        case "occupancy":
        case "power":
        case "problem":
        case "smoke":
        case "sound":
        case "vibration":
          if (s9)
            return j3("detected_deviceclass", {
              search: "{device_class}",
              replace: c15
            });
          if (t9)
            return j3("cleared_no_deviceclass_detected", {
              search: "{device_class}",
              replace: c15
            });
          break;
        case "tamper":
          if (s9)
            return j3("detected_tampering");
          if (t9)
            return j3("cleared_tampering");
          break;
      }
      break;
    }
    case "cover":
      switch (r13) {
        case "open":
          return j3("was_opened");
        case "opening":
          return j3("is_opening");
        case "closing":
          return j3("is_closing");
        case "closed":
          return j3("was_closed");
      }
      break;
    case "event":
      return j3("detected_an_event");
    case "lock":
      switch (r13) {
        case "unlocked":
          return j3("was_unlocked");
        case "locking":
          return j3("is_locking");
        case "unlocking":
          return j3("is_unlocking");
        case "locked":
          return j3("was_locked");
        case "jammed":
          return j3("is_jammed");
      }
      break;
  }
  return r13 === F2 ? j3("turned_on") : r13 === N3 ? j3("turned_off") : r13 === t5 ? j3("became_unknown") : r13 === n4 ? j3("became_unavailable") : j3("changed_to_state", {
    search: "{state}",
    replace: a13 ? a13.state : r13
  });
};

// node_modules/@hakit/core/dist/es/hooks/useLogs/index.js
var import_lodash21 = __toESM(require_lodash(), 1);
var B5 = 24;
var b8 = (f13, s9) => new Date(f13.getTime() - s9 * 1e3).getTime() / 1e3;
function j5(f13, s9) {
  const [d12, k8] = (0, import_react38.useState)(void 0), l10 = ne((e6) => e6.connection), [w3, i11] = (0, import_react38.useState)([]), t9 = (0, import_react38.useRef)([]), a13 = (0, import_react38.useRef)(false), u15 = (0, import_react38.useRef)(void 0), c15 = (0, import_react38.useMemo)(() => {
    const e6 = {};
    return s9?.hoursToShow ? e6.recent = s9.hoursToShow * 60 * 60 : s9?.startTime && s9?.endTime ? e6.range = [s9.startTime, s9.endTime] : e6.recent = B5 * 60 * 60, e6;
  }, [s9]), h5 = (0, import_react38.useCallback)(() => {
    const e6 = /* @__PURE__ */ new Date();
    if ("range" in c15)
      return {
        now: e6,
        startTime: c15.range[0],
        endTime: c15.range[1],
        purgeBeforePythonTime: void 0
      };
    if ("recent" in c15) {
      const o13 = b8(e6, c15.recent);
      return {
        now: e6,
        startTime: new Date(o13 * 1e3),
        // end streaming one year from now
        endTime: new Date(e6.getTime() + 86400 * 365 * 1e3),
        purgeBeforePythonTime: b8(e6, c15.recent)
      };
    }
    throw new Error("Unexpected time specified");
  }, [c15]), T7 = (0, import_react38.useCallback)(
    (e6) => t9.current ? e6 ? t9.current.filter((o13) => o13.when > e6) : t9.current : [],
    []
  ), p8 = (0, import_react38.useCallback)(
    (e6) => {
      const o13 = c15 && "recent" in c15 ? b8(/* @__PURE__ */ new Date(), c15.recent) : void 0, r13 = [...e6.events].reverse();
      if (!t9.current.length) {
        t9.current = r13, i11(r13);
        return;
      }
      if (!r13.length)
        return;
      const n15 = T7(o13);
      n15.length ? r13[r13.length - 1].when > // oldest new entry
      n15[0].when ? (t9.current = r13.concat(n15), i11(t9.current)) : n15[n15.length - 1].when > // oldest old entry
      r13[0].when ? (t9.current = n15.concat(r13), i11(t9.current)) : (t9.current = n15.concat(r13).sort((L3, O5) => O5.when - L3.when), i11(t9.current)) : (t9.current = r13, i11(r13));
    },
    [c15, T7]
  );
  (0, import_react38.useEffect)(() => () => {
    a13.current = false, t9.current = [], i11([]);
  }, []);
  const S6 = (0, import_react38.useCallback)(
    (e6, o13) => M3(
      l10,
      (n15) => {
        a13.current && p8(n15);
      },
      o13.startTime.toISOString(),
      o13.endTime.toISOString(),
      [e6]
    ).catch((n15) => {
      a13.current = false, n15 instanceof Error && k8(n15.message);
    }),
    [l10, p8]
  ), E7 = c3(
    async (e6, o13) => {
      if (u15.current) {
        const r13 = await u15.current;
        r13 && await r13(), u15.current = void 0;
      }
      a13.current = true, u15.current = await S6(e6, o13);
    },
    100,
    {
      leading: true,
      trailing: true
    }
  );
  if ((0, import_react38.useEffect)(() => {
    const e6 = h5();
    return E7(f13, e6), () => {
      a13.current = false, u15.current && u15.current();
    };
  }, [h5, E7, S6, f13]), d12)
    throw new Error(d12);
  return (0, import_react38.useMemo)(() => w3, [w3]);
}

// node_modules/@hakit/core/dist/es/hooks/useHaStatus/index.js
var import_lodash22 = __toESM(require_lodash(), 1);
var import_react40 = __toESM(require_react(), 1);
function u10() {
  return zr()?.state ?? "LOADING";
}

// node_modules/@hakit/core/dist/es/hooks/useUser/index.js
function o9() {
  return ne((s9) => s9.user);
}

// node_modules/@hakit/core/dist/es/hooks/useUsers/index.js
var import_react42 = __toESM(require_react(), 1);
function i9({ includeSystemGenerated: s9 = false, includeInactiveUsers: e6 = false } = {}) {
  const t9 = ne((r13) => r13.users);
  return (0, import_react42.useMemo)(
    () => t9.filter((r13) => !(!s9 && r13.system_generated || !e6 && !r13.is_active)),
    [t9, s9, e6]
  );
}

// node_modules/@hakit/core/dist/es/hooks/useWeather/index.js
var import_react45 = __toESM(require_react(), 1);
var import_lodash24 = __toESM(require_lodash(), 1);

// node_modules/@hakit/core/dist/es/hooks/useWeather/helpers.js
var import_lodash23 = __toESM(require_lodash(), 1);
var import_react43 = __toESM(require_react(), 1);
var c13 = ((t9) => (t9[t9.FORECAST_DAILY = 1] = "FORECAST_DAILY", t9[t9.FORECAST_HOURLY = 2] = "FORECAST_HOURLY", t9[t9.FORECAST_TWICE_DAILY = 4] = "FORECAST_TWICE_DAILY", t9))(c13 || {});
var n11 = 288e5;
var u11 = 864e5;
var p6 = (t9) => {
  if (t9 && t9?.length && t9?.length > 2) {
    const i11 = new Date(t9[1].datetime);
    return new Date(t9[2].datetime).getTime() - i11.getTime() < n11;
  }
};
var d9 = (t9) => {
  if (t9 && t9?.length && t9?.length > 2) {
    const i11 = new Date(t9[1].datetime);
    return new Date(t9[2].datetime).getTime() - i11.getTime() < u11;
  }
};
var s7 = (t9) => {
  if (t9?.forecast && t9.forecast.length > 2)
    return p6(t9.forecast) ? {
      forecast: t9.forecast,
      type: "hourly"
    } : d9(t9.forecast) ? {
      forecast: t9.forecast,
      type: "twice_daily"
    } : { forecast: t9.forecast, type: "daily" };
};
var F4 = (t9, i11, r13) => {
  if (r13 === void 0)
    return i11?.type !== void 0 && i11?.forecast && i11?.forecast?.length > 2 ? { forecast: i11.forecast, type: i11?.type } : s7(t9);
  if (r13 === "legacy")
    return s7(t9);
  if (r13 === i11?.type && i11?.forecast && i11?.forecast?.length > 2)
    return { forecast: i11.forecast, type: r13 };
};
var I6 = (t9, i11, r13, e6) => t9.subscribeMessage(e6, {
  type: "weather/subscribe_forecast",
  forecast_type: r13,
  entity_id: i11
});
var O3 = (t9) => {
  const i11 = [];
  return r7(
    t9.attributes,
    1
    /* FORECAST_DAILY */
  ) && i11.push("daily"), r7(
    t9.attributes,
    4
    /* FORECAST_TWICE_DAILY */
  ) && i11.push("twice_daily"), r7(
    t9.attributes,
    2
    /* FORECAST_HOURLY */
  ) && i11.push("hourly"), i11;
};

// node_modules/@hakit/core/dist/es/hooks/useWeather/index.js
function B6(n15, v10) {
  const i11 = ne((r13) => r13.connection), c15 = $3(n15), [a13, d12] = (0, import_react45.useState)(null), [f13, h5] = (0, import_react45.useState)(null), e6 = (0, import_react45.useRef)(false), t9 = (0, import_react45.useRef)(void 0), { type: b11 = "daily" } = v10 || {}, m15 = (0, import_react45.useCallback)(
    (r13, o13) => I6(i11, r13, o13, (s9) => {
      e6.current && h5(s9);
    }).catch((s9) => {
      e6.current = false, s9 instanceof Error && d12(s9.message);
    }),
    [i11]
  ), l10 = c3(
    async (r13, o13) => {
      if (t9.current) {
        const u15 = await t9.current;
        u15 && u15(), t9.current = void 0;
      }
      e6.current = true, t9.current = await m15(r13, o13);
    },
    100,
    {
      trailing: true,
      leading: true
    }
  );
  if ((0, import_react45.useEffect)(() => (l10(n15, b11), () => {
    e6.current = false, t9.current && t9.current();
  }), [b11, l10, m15, n15]), a13)
    throw a13;
  return (0, import_react45.useMemo)(() => {
    const r13 = F4(c15.attributes, f13);
    return {
      ...c15,
      forecast: r13
    };
  }, [c15, f13]);
}

// node_modules/@hakit/core/dist/es/hooks/useAreas/index.js
var import_lodash25 = __toESM(require_lodash(), 1);
var import_react47 = __toESM(require_react(), 1);
function S5() {
  const { joinHassUrl: n15 } = ne.getState().helpers, r13 = ne((e6) => e6.areas), o13 = ne((e6) => e6.devices), c15 = ne((e6) => e6.floors), a13 = ne((e6) => e6.entitiesRegistryDisplay), d12 = ne((e6) => e6.entities);
  return (0, import_react47.useMemo)(() => {
    const e6 = Object.values(r13);
    return v8({
      areas: e6,
      devices: o13,
      entitiesRegistryDisplay: a13,
      floors: c15,
      joinHassUrl: n15,
      hassEntities: d12
    });
  }, [r13, o13, n15, a13, c15, d12]);
}
function v8({ areas: n15, devices: r13, entitiesRegistryDisplay: o13, floors: c15, joinHassUrl: a13, hassEntities: d12 }) {
  const e6 = (i11) => {
    const p8 = [];
    for (const t9 of Object.values(r13))
      t9.area_id === i11.area_id && t9.entry_type !== "service" && p8.push(t9);
    const u15 = /* @__PURE__ */ new Set(), l10 = [];
    for (const t9 of Object.values(o13)) {
      const _8 = t9.area_id === i11.area_id, m15 = !t9.area_id && t9.device_id && r13[t9.device_id]?.area_id === i11.area_id;
      if (_8 || m15) {
        const f13 = d12[t9.entity_id];
        f13 && !u15.has(t9.entity_id) && (u15.add(t9.entity_id), l10.push(f13));
      }
    }
    return {
      ...i11,
      picture: i11.picture ? a13(i11.picture) : null,
      devices: p8,
      entities: l10,
      floor: i11.floor_id && c15[i11.floor_id] || null
    };
  };
  return n15.map(e6);
}

// node_modules/@hakit/core/dist/es/utils/subscribe/entities.js
var import_lodash26 = __toESM(require_lodash(), 1);
var i10 = (t9) => t9.sendMessagePromise({
  type: "config/entity_registry/list"
});
var y5 = (t9, e6) => t9.subscribeEvents(
  (0, import_lodash26.debounce)(() => i10(t9).then((s9) => e6.setState(s9, true)), 500, {
    leading: true,
    trailing: true
  }),
  "entity_registry_updated"
);
var g9 = (t9, e6) => createCollection("_entityRegistry", i10, y5, t9, e6);

// node_modules/@hakit/core/dist/es/utils/subscribe/timeCacheFunctionPromise.js
var f9 = {};
function v9(t9) {
  return f9[t9];
}
function s8(t9, n15) {
  f9[t9] = n15;
}
var O4 = async (t9, n15, m15, r13, C6, i11) => {
  const u15 = v9(t9), l10 = (o13) => !r13 || r13(i11, o13.result) === o13.cacheKey ? o13.result : (f9[t9] = void 0, O4(t9, n15, m15, r13, C6, i11));
  if (u15)
    return u15 instanceof Promise ? u15.then(l10) : l10(u15);
  const d12 = m15(C6);
  return s8(t9, d12), d12.then(
    // When successful, set timer to clear cache
    (o13) => {
      s8(t9, {
        result: o13,
        cacheKey: r13?.(i11, o13)
      }), setTimeout(() => {
        s8(t9, void 0);
      }, n15);
    },
    // On failure, clear cache right away
    () => {
      s8(t9, void 0);
    }
  ), d12;
};

// node_modules/@hakit/core/dist/es/utils/subscribe/entity_sources.js
var o11 = (e6) => e6.sendMessagePromise({ type: "entity/source" });
var n13 = (e6, t9) => O4(
  "_entitySources",
  // cache key
  3e4,
  // 30s TTL
  o11,
  // underlying fetcher
  (s9) => Object.keys(s9.states).length,
  // invalidation heuristic based on entity count
  t9,
  e6
);

// node_modules/@hakit/core/dist/es/hooks/useTemplate/index.js
var import_react49 = __toESM(require_react(), 1);
var import_lodash27 = __toESM(require_lodash(), 1);
var z4 = (s9) => {
  const n15 = ne((o13) => o13.connection), [i11, e6] = (0, import_react49.useState)(null), t9 = (0, import_react49.useRef)(null), c15 = (0, import_react49.useMemo)(() => s9, [s9]);
  return (0, import_react49.useEffect)(() => {
    const o13 = () => {
      if (t9.current)
        try {
          t9.current();
        } catch {
        } finally {
          t9.current = null;
        }
    }, { enabled: u15 = true, ...m15 } = c15;
    if (!u15 || !n15) {
      o13(), u15 || e6(null);
      return;
    }
    const a13 = (r13) => {
      if ("error" in r13) {
        console.error("Error processing template:", r13.error), e6(r13.error);
        return;
      }
      e6((l10) => l10 === r13.result ? l10 : r13.result);
    }, p8 = (r13) => {
      console.error("Error processing template:", r13), e6(r13?.error || "Could not process template request.");
    };
    return n15.subscribeMessage(a13, {
      type: "render_template",
      ...m15
    }).then((r13) => {
      t9.current = r13;
    }).catch((r13) => {
      console.error("Error subscribing to template:", r13), p8(r13);
    }), o13;
  }, [n15, c15]), i11;
};

// node_modules/@hakit/core/dist/es/hooks/useCamera/index.js
var import_lodash28 = __toESM(require_lodash(), 1);
var import_react51 = __toESM(require_react(), 1);

// node_modules/@hakit/core/dist/es/hooks/useCamera/constants.js
var _6 = 640;
var A5 = 0.5625;
var T5 = [1, 2, 3, 4, 6, 8];
var E6 = 1;
var R4 = 2;
var t8 = "hls";
var c14 = "web_rtc";

// node_modules/@hakit/core/dist/es/hooks/useCamera/time-cache-entity-promise.js
var r12 = {};
var u12 = async (o13, m15, i11, a13, t9, ...p8) => {
  const c15 = Date.now(), s9 = `${o13}-${t9}`, e6 = r12[s9];
  if (e6 && c15 - e6.timestamp < m15)
    return e6.promise;
  const n15 = i11(a13, t9, ...p8);
  return r12[s9] = { timestamp: c15, promise: n15 }, n15;
};

// node_modules/@hakit/core/dist/es/hooks/useCamera/camera.js
var n14 = (t9, e6) => t9.sendMessagePromise({ type: "auth/sign_path", path: e6 });
var o12 = (t9, e6, a13) => `${t9}&width=${e6}&height=${a13}`;
var u13 = (t9) => `/api/camera_proxy_stream/${t9.entity_id}?token=${t9.attributes.access_token}`;
var d11 = async (t9, e6, a13, r13) => {
  const s9 = await u12("_cameraTmbUrl", 9e3, m13, t9, e6);
  return o12(s9, a13, r13);
};
var m13 = async (t9, e6) => {
  const { path: a13 } = await n14(t9, `/api/camera_proxy/${e6}`);
  return a13;
};
var _7 = async (t9, e6, a13) => {
  const r13 = {
    type: "camera/stream",
    entity_id: e6
  };
  a13 && (r13.format = a13);
  const { url: s9 } = await t9.sendMessagePromise(r13);
  return s9;
};

// node_modules/@hakit/core/dist/es/hooks/useCamera/index.js
function ae(W3, r13) {
  const e6 = $3(W3), { joinHassUrl: n15 } = ne.getState().helpers, s9 = ne((t9) => t9.connection), f13 = (0, import_react51.useRef)(false), l10 = (0, import_react51.useRef)(false), [_8, L3] = (0, import_react51.useState)(void 0), [y6, w3] = (0, import_react51.useState)(void 0), [i11, x6] = (0, import_react51.useState)(void 0), [h5, G3] = (0, import_react51.useState)(void 0), [E7, d12] = (0, import_react51.useState)(r13?.stream !== false), [g10, p8] = (0, import_react51.useState)(r13?.poster !== false), P3 = (0, import_react51.useMemo)(() => n15(u13(e6)), [e6, n15]), [o13, I7] = (0, import_react51.useState)({ frontend_stream_types: [] }), c15 = (0, import_react51.useCallback)(async () => {
    if (r13?.poster !== false && s9 && !_2(e6.state) && !l10.current) {
      l10.current = true, p8(true);
      try {
        const t9 = Math.ceil((r13?.imageWidth ?? _6) * devicePixelRatio), R5 = Math.ceil(t9 * (r13?.aspectRatio ?? A5)), C6 = await d11(s9, e6.entity_id, t9, R5);
        L3(n15(C6)), p8(false);
      } catch (t9) {
        p8(false), t9 instanceof Error && G3(t9);
      }
    }
  }, [e6.entity_id, n15, e6.state, s9, r13?.poster, r13?.aspectRatio, r13?.imageWidth]), S6 = (0, import_react51.useCallback)(
    async () => s9?.sendMessagePromise({
      type: "camera/capabilities",
      entity_id: e6.entity_id
    }),
    [s9, e6.entity_id]
  );
  (0, import_react51.useEffect)(() => {
    async function t9() {
      I7(await S6() || { frontend_stream_types: [] });
    }
    t9();
  }, [S6]);
  const u15 = (0, import_react51.useCallback)(async () => {
    if (r13?.stream !== false && s9 && !_2(e6.state) && !f13.current) {
      f13.current = true, d12(true);
      try {
        const t9 = await _7(s9, e6.entity_id);
        w3(n15(t9)), d12(false);
      } catch (t9) {
        d12(false), console.error(t9), t9 instanceof Error && x6(t9);
      }
    }
  }, [e6.entity_id, n15, e6.state, s9, r13?.stream]), U4 = (0, import_react51.useCallback)(() => i11 || !r7(e6.attributes, R4) ? true : o13.frontend_stream_types.includes(c14) ? typeof RTCPeerConnection > "u" : false, [e6.attributes, o13, i11]);
  return (0, import_react51.useEffect)(() => {
    u15(), c15();
  }, [u15, c15]), (0, import_react51.useMemo)(() => ({
    ...e6,
    stream: {
      url: y6,
      loading: E7,
      error: i11,
      refresh: async () => (f13.current = false, u15())
    },
    poster: {
      url: _8,
      loading: g10,
      error: h5,
      refresh: async () => (l10.current = false, c15())
    },
    mjpeg: {
      url: P3,
      shouldRenderMJPEG: U4()
    },
    ...o13
  }), [
    e6,
    y6,
    U4,
    E7,
    i11,
    u15,
    _8,
    g10,
    h5,
    P3,
    c15,
    o13
  ]);
}

// node_modules/@hakit/core/dist/es/hooks/useLocaleData/index.js
var import_lodash29 = __toESM(require_lodash(), 1);
var import_react53 = __toESM(require_react(), 1);
function u14() {
  return ne.getState().locale;
}

// node_modules/@hakit/core/dist/es/hooks/useRegistryData/index.js
var import_lodash30 = __toESM(require_lodash(), 1);
var import_react55 = __toESM(require_react(), 1);
function f11(r13) {
  return ne((t9) => t9[r13]);
}

// node_modules/@hakit/core/dist/es/hooks/useFloors/index.js
var import_lodash31 = __toESM(require_lodash(), 1);
var import_react57 = __toESM(require_react(), 1);
function k7() {
  const e6 = S5(), r13 = ne((o13) => o13.floors);
  return (0, import_react57.useMemo)(() => {
    const o13 = f12(e6), t9 = e6.filter((s9) => !s9.floor_id || !o13[s9.floor_id]), i11 = Object.values(r13).map((s9) => ({
      ...s9,
      type: "floor",
      areas: o13[s9.floor_id] || []
    }));
    return t9.length && i11.push({
      floor_id: "unassigned",
      name: "Unassigned",
      level: null,
      icon: null,
      aliases: [],
      created_at: Date.now(),
      modified_at: Date.now(),
      type: "unassigned",
      areas: t9
    }), i11;
  }, [e6, r13]);
}
var f12 = (e6) => {
  const r13 = {};
  for (const o13 of e6)
    o13.floor_id && (o13.floor_id in r13 || (r13[o13.floor_id] = []), r13[o13.floor_id].push(o13));
  return r13;
};
export {
  A5 as ASPECT_RATIO_DEFAULT,
  T5 as CAMERA_ORIENTATIONS,
  E6 as CAMERA_SUPPORT_ON_OFF,
  R4 as CAMERA_SUPPORT_STREAM,
  f3 as COLORS,
  S4 as CONTINUOUS_DOMAINS,
  Sr as DEFAULT_ENTITY_NAME,
  O2 as DEFAULT_STATES,
  I4 as DOMAINS_WITH_DYNAMIC_PICTURE,
  Qr as DOMAIN_ATTRIBUTES_FORMATTERS,
  en as DOMAIN_ATTRIBUTES_UNITS,
  d3 as DateFormat,
  i2 as FirstWeekday,
  yo as HassConnect,
  c4 as LIGHT_COLOR_MODES,
  T2 as LOCAL_TIME_ZONE,
  _6 as MAX_IMAGE_WIDTH,
  u5 as NumberFormat,
  N3 as OFF,
  A3 as OFF_STATES,
  F2 as ON,
  t8 as STREAM_TYPE_HLS,
  c14 as STREAM_TYPE_WEB_RTC,
  Zr as TEMPERATURE_ATTRIBUTES,
  r as THEME_COLORS,
  M as TimeFormat,
  z as TimeZone,
  n4 as UNAVAILABLE,
  e3 as UNAVAILABLE_STATES,
  T3 as UNIT_C,
  S3 as UNIT_F,
  $2 as UNIT_TO_MILLISECOND_CONVERT,
  t5 as UNKNOWN,
  x3 as areaCompare,
  b5 as batteryIconByLevel,
  u7 as caseInsensitiveStringCompare,
  mt as checkValidDate,
  Xe as computeAreaName,
  ot2 as computeAttributeValueDisplay,
  n as computeCssColor,
  jn as computeDefaultFavoriteColors,
  he as computeDeviceName,
  Rr as computeDeviceNameDisplay,
  m5 as computeDomain,
  u9 as computeDomainTitle,
  wr as computeEntityEntryName,
  Je as computeEntityName,
  qn as computeEntityNameDisplay,
  Ze as computeEntityNameList,
  no as computeEntityRegistryName,
  Pr as computeFloorName,
  Er as computeObjectId,
  rn as computeStateDisplay,
  nn as computeStateDisplayFromEntityAttributes,
  G2 as computeStateName,
  Dr as computeStateNameFromEntityAttributes,
  D3 as coordinates,
  A4 as createAreaRegistryEntry,
  z3 as createHistoricState,
  a8 as createService,
  R3 as deleteAreaRegistryEntry,
  Gn as devicesInArea,
  uo as entityRegistryByEntityId,
  fo as entityRegistryById,
  Ar as entityUseDeviceName,
  a as expandHex,
  qr as fallbackDeviceName,
  nt2 as fetchEntityRegistryDisplay,
  n13 as fetchEntitySourcesWithCache,
  t4 as findAvailableLanguage,
  ro as findBatteryChargingEntity,
  to as findBatteryEntity,
  pt as formatAmPmSuffix,
  at as formatDate,
  Mt as formatDateMonth,
  Tt as formatDateMonthYear,
  wt as formatDateNumeric,
  Dt as formatDateShort,
  ct as formatDateTime,
  yt as formatDateTimeNumeric,
  ht as formatDateTimeWithBrowserDefaults,
  ft as formatDateTimeWithSeconds,
  lt as formatDateVeryShort,
  It as formatDateWeekday,
  _t as formatDateWeekdayDay,
  Ft as formatDateWeekdayShort,
  zt as formatDateYear,
  nt as formatDuration,
  st as formatHour,
  ut as formatMinute,
  c5 as formatNumber,
  gt as formatSeconds,
  b2 as formatShortDateTime,
  dt as formatShortDateTimeWithConditionalYear,
  j2 as formatShortDateTimeWithYear,
  X2 as formatTime,
  it as formatTimeWithoutAmPm,
  b3 as getAreaDeviceLookup,
  E2 as getAreaEntityLookup,
  mo as getAutomaticEntityIds,
  e as getColorByIndex,
  k5 as getCssColorValue,
  s4 as getDefaultFormatOptions,
  Kr as getDeviceContext,
  Gr as getDeviceEntityDisplayLookup,
  Zn as getDeviceEntityLookup,
  Qn as getDeviceIntegrationLookup,
  eo as getDevices,
  po as getEntities,
  Tr as getEntityContext,
  Cr as getEntityEntryContext,
  lo as getEntityPlatformLookup,
  io as getExtendedEntityRegistryEntries,
  oo as getExtendedEntityRegistryEntry,
  v6 as getIconByEntity,
  Un as getLightCurrentModeRgbColor,
  u6 as getNumberFormatOptions,
  c as getRGBContrastRatio,
  O3 as getSupportedForecastTypes,
  g2 as getUserLocaleLanguage,
  tn as getWeatherUnit,
  S as hex2rgb,
  f as hexBlend,
  j as hs2rgb,
  k2 as hsv2rgb,
  rt as isDate,
  m6 as isNumericFromAttributes,
  a4 as isNumericState,
  i4 as isOffState,
  ot as isTimestamp,
  _2 as isUnavailableState,
  Y as lab2hex,
  I2 as lab2rgb,
  o5 as labBrighten,
  e4 as labDarken,
  In as lightIsInColorMode,
  kn as lightSupportsBrightness,
  Ke as lightSupportsColor,
  Ge as lightSupportsColorMode,
  Fn as lightSupportsFavoriteColors,
  c2 as loadTokens,
  m3 as locales,
  j3 as localize,
  D4 as localizeStateMessage,
  U3 as localizeTriggerSource,
  s as luminosity,
  S2 as millisecondsToDuration,
  Jn as removeConfigEntryFromDevice,
  ao as removeEntityRegistryEntry,
  r4 as resolveTimeZone,
  f2 as rgb2hex,
  Z as rgb2hs,
  d as rgb2hsv,
  X as rgb2lab,
  u3 as rgbContrast,
  B as rgbw2rgb,
  k as rgbww2rgb,
  n3 as round,
  a3 as saveTokens,
  i3 as shouldUseAmPm,
  Xn as sortDeviceRegistryByName,
  co as sortEntityRegistryByName,
  Vn as stateActive,
  c8 as stateColorBrightness,
  p3 as stringCompare,
  c7 as stripPrefixFromEntityName,
  m8 as subscribeAreaRegistry,
  Vr as subscribeDeviceRegistry,
  g9 as subscribeEntityRegistry,
  Xr as subscribeEntityRegistryDisplay,
  n6 as subscribeFloorRegistry,
  r3 as subscribeFrontendUserData,
  M3 as subscribeLogbook,
  l7 as subscribeUser,
  f6 as subscribeUsers,
  r7 as supportsFeatureFromAttributes,
  I as temperature2rgb,
  f7 as timeAgo,
  m7 as toReadableString,
  l5 as updateAreaRegistryEntry,
  Kn as updateDeviceRegistryEntry,
  so as updateEntityRegistryEntry,
  vr as updateLocales,
  S5 as useAreas,
  ae as useCamera,
  zr as useConfig,
  d8 as useDevice,
  c10 as useEntities,
  $3 as useEntity,
  k7 as useFloors,
  u10 as useHaStatus,
  ne as useHass,
  E4 as useHistory,
  ie as useIcon,
  ne2 as useIconByDomain,
  se as useIconByEntity,
  g5 as useLightBrightness,
  C5 as useLightColor,
  b7 as useLightTemperature,
  Bn as useLocale,
  u14 as useLocaleData,
  zn as useLocales,
  j5 as useLogs,
  g6 as useLowDevices,
  f11 as useRegistryData,
  b4 as useService,
  Hn as useStore,
  b6 as useSubscribeEntity,
  z4 as useTemplate,
  o9 as useUser,
  i9 as useUsers,
  B6 as useWeather,
  u8 as userCollection
};
//# sourceMappingURL=@hakit_core.js.map
